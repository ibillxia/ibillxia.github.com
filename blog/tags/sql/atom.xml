<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Tag: SQL | Bill's Blog]]></title>
  <link href="https://ibillxia.github.io/blog/tags/sql/atom.xml" rel="self"/>
  <link href="https://ibillxia.github.io/"/>
  <updated>2026-07-12T20:22:44+08:00</updated>
  <id>https://ibillxia.github.io/</id>
  <author>
    <name><![CDATA[Bill Xia]]></name>
    <email><![CDATA[ibillxia@gmail.com]]></email>
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[【简单之美】Hive JOIN使用详解]]></title>
    <link href="https://ibillxia.github.io/blog/2016/09/22/hive-join-usage-guide/"/>
    <updated>2016-09-22T15:55:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2016/09/22/hive-join-usage-guide</id>
    <content type="html"><![CDATA[<p>Hive是基于Hadoop平台的，它提供了类似SQL一样的查询语言HQL。有了Hive，如果使用过SQL语言，并且不理解Hadoop MapReduce运行原理，也就无法通过编程来实现MR，但是你仍然可以很容易地编写出特定查询分析的HQL语句，通过使用类似SQL的语法，将HQL查询语句提交Hive系统执行查询分析，最终Hive会帮你转换成底层Hadoop能够理解的MR Job。</p>

<p>对于最基本的HQL查询我们不再累述，这里主要说明Hive中进行统计分析时使用到的JOIN操作。在说明Hive JOIN之前，我们先简单说明一下，Hadoop执行MR Job的基本过程（运行机制），能更好的帮助我们理解HQL转换到底层的MR Job后是如何执行的。我们重点说明MapReduce执行过程中，从Map端到Reduce端这个过程（Shuffle）的执行情况，如图所示（来自《Hadoop: The Definitive Guide》）：</p>

<center><img src="/images/2016/IMAG2016092201.png"></center>




<!--more-->


<p>基本执行过程，描述如下：</p>

<ol>
<li><p>一个InputSplit输入到map，会运行我们实现的Mapper的处理逻辑，对数据进行映射操作。</p></li>
<li><p>map输出时，会首先将输出中间结果写入到map自带的buffer中（buffer默认大小为100M，可以通过io.sort.mb配置）。</p></li>
<li><p>map自带的buffer使用容量达到一定门限（默认0.80或80%，可以通过io.sort.spill.percent配置），一个后台线程会准备将buffer中的数据写入到磁盘。</p></li>
<li><p>这个后台线程在将buffer中数据写入磁盘之前，会首先将buffer中的数据进行partition（分区，partition数为Reducer的个数），对于每个的数据会基于Key进行一个in-memory排序。</p></li>
<li><p>排序后，会检查是否配置了Combiner，如果配置了则直接作用到已排序的每个partition的数据上，对map输出进行化简压缩（这样写入磁盘的数据量就会减少，降低I/O操作开销）。</p></li>
<li><p>现在可以将经过处理的buffer中的数据写入磁盘，生成一个文件（每次buffer容量达到设置的门限，都会对应着一个写入到磁盘的文件）。</p></li>
<li><p>map任务结束之前，会对输出的多个文件进行合并操作，合并成一个文件（若map输出至少3个文件，在多个文件合并后写入之前，如果配置了Combiner，则会运行来化简压缩输出的数据，文件个数可以通过min.num.splits.for.combine配置；如果指定了压缩map输出，这里会根据配置对数据进行压缩写入磁盘），这个文件仍然保持partition和排序的状态。</p></li>
<li><p>reduce阶段，每个reduce任务开始从多个map上拷贝属于自己partition（map阶段已经做好partition，而且每个reduce任务知道应该拷贝哪个partition；拷贝过程是在不同节点之间，Reducer上拷贝线程基于HTTP来通过网络传输数据）。</p></li>
<li><p>每个reduce任务拷贝的map任务结果的指定partition，也是先将数据放入到自带的一个buffer中（buffer默认大小为Heap内存的70%，可以通过mapred.job.shuffle.input.buffer.percent配置），如果配置了map结果进行压缩，则这时要先将数据解压缩后放入buffer中。</p></li>
<li><p>reduce自带的buffer使用容量达到一定门限（默认0.66或66%，可以通过mapred.job.shuffle.merge.percent配置），或者buffer中存放的map的输出的数量达到一定门限（默认1000，可以通过mapred.inmem.merge.threshold配置），buffer中的数据将会被写入到磁盘中。</p></li>
<li><p>在将buffer中多个map输出合并写入磁盘之前，如果设置了Combiner，则会化简压缩合并的map输出。</p></li>
<li><p>当属于该reducer的map输出全部拷贝完成，则会在reducer上生成多个文件，这时开始执行合并操作，并保持每个map输出数据中Key的有序性，将多个文件合并成一个文件（在reduce端可能存在buffer和磁盘上都有数据的情况，这样在buffer中的数据可以减少一定量的I/O写入操作开销）。</p></li>
<li><p>最后，执行reduce阶段，运行我们实现的Reducer中化简逻辑，最终将结果直接输出到HDFS中（因为Reducer运行在DataNode上，输出结果的第一个replica直接在存储在本地节点上）。</p></li>
</ol>


<p>通过上面的描述我们看到，在MR执行过程中，存在Shuffle过程的MR需要在网络中的节点之间（Mapper节点和Reducer节点）拷贝数据，如果传输的数据量很大会造成一定的网络开销。而且，Map端和Reduce端都会通过一个特定的buffer来在内存中临时缓存数据，如果无法根据实际应用场景中数据的规模来使用Hive，尤其是执行表的JOIN操作，有可能很浪费资源，降低了系统处理任务的效率，还可能因为内存不足造成OOME问题，导致计算任务失败。</p>

<p>下面，我们说明Hive中的JOIN操作，针对不同的JOIN方式，应该如何来实现和优化：</p>

<h2>生成一个MR Job</h2>

<p>多表连接，如果多个表中每个表都使用同一个列进行连接（出现在JOIN子句中），则只会生成一个MR Job，例如：</p>

<pre><code class="sql">SELECT a.val, b.val, c.val 
FROM a 
JOIN b ON (a.key = b.key1) 
JOIN c ON (c.key = b.key1)
</code></pre>

<p>三个表a、b、c都分别使用了同一个字段进行连接，亦即同一个字段同时出现在两个JOIN子句中，从而只生成一个MR Job。</p>

<h2>生成多个MR Job</h2>

<p>多表连接，如果多表中，其中存在一个表使用了至少2个字段进行连接（同一个表的至少2个列出现在JOIN子句中），则会至少生成2个MR Job，例如：</p>

<pre><code class="sql">SELECT a.val, b.val, c.val 
FROM a 
JOIN b ON (a.key = b.key1) 
JOIN c ON (c.key = b.key2)
</code></pre>

<p>三个表基于2个字段进行连接，这两个字段b.key1和b.key2同时出现在b表中。连接的过程是这样的：首先a和b表基于a.key和b.key1进行连接，对应着第一个MR Job；表a和b连接的结果，再和c进行连接，对应着第二个MR Job。</p>

<h2>表连接顺序优化</h2>

<p>多表连接，会转换成多个MR Job，每一个MR Job在Hive中称为JOIN阶段（Stage）。在每一个Stage，按照JOIN顺序中的最后一个表应该尽量是大表，因为JOIN前一阶段生成的数据会存在于Reducer的buffer中，通过stream最后面的表，直接从Reducer的buffer中读取已经缓冲的中间结果数据（这个中间结果数据可能是JOIN顺序中，前面表连接的结果的Key，数据量相对较小，内存开销就小），这样，与后面的大表进行连接时，只需要从buffer中读取缓存的Key，与大表中的指定Key进行连接，速度会更快，也可能避免内存缓冲区溢出。例如：</p>

<pre><code class="sql">SELECT a.val, b.val, c.val 
FROM a 
JOIN b ON (a.key = b.key1) 
JOIN c ON (c.key = b.key1)
</code></pre>

<p>这个JOIN语句，会生成一个MR Job，在选择JOIN顺序的时候，数据量相比应该是b &lt; c，表a和b基于a.key = b.key1进行连接，得到的结果（基于a和b进行连接的Key）会在Reducer上缓存在buffer中，在与c进行连接时，从buffer中读取Key（a.key=b.key1）来与表c的c.key进行连接。</p>

<p>另外，也可以通过给出一些Hint信息来启发JOIN操作，这指定了将哪个表作为大表，从而得到优化。例如：</p>

<pre><code class="sql">SELECT /*+ STREAMTABLE(a) */ a.val, b.val, c.val 
FROM a 
JOIN b ON (a.key = b.key1) 
JOIN c ON (c.key = b.key1)
</code></pre>

<p>上述JOIN语句中，a表被视为大表，则首先会对表b和c进行JOIN，然后再将得到的结果与表a进行JOIN。</p>

<h2>基于条件的LEFT OUTER JOIN优化</h2>

<p>左连接时，左表中出现的JOIN字段都保留，右表没有连接上的都为空。对于带WHERE条件的JOIN语句，例如：</p>

<pre><code class="sql">SELECT a.val, b.val 
FROM a 
LEFT OUTER JOIN b ON (a.key=b.key)
WHERE a.ds='2009-07-07' AND b.ds='2009-07-07'
</code></pre>

<p>执行顺序是，首先完成2表JOIN，然后再通过WHERE条件进行过滤，这样在JOIN过程中可能会输出大量结果，再对这些结果进行过滤，比较耗时。可以进行优化，将WHERE条件放在ON后，例如：</p>

<pre><code class="sql">SELECT a.val, b.val 
FROM a LEFT OUTER JOIN b
ON (a.key=b.key AND b.ds='2009-07-07' AND a.ds='2009-07-07')
</code></pre>

<p>这样，在JOIN的过程中，就对不满足条件的记录进行了预先过滤，可能会有更好的表现。</p>

<h2>左半连接（LEFT SEMI JOIN）</h2>

<p>左半连接实现了类似IN/EXISTS的查询语义，使用关系数据库子查询的方式实现查询SQL，例如：</p>

<pre><code class="sql">SELECT a.key, a.value 
FROM a WHERE a.key IN (SELECT b.key FROM b);
</code></pre>

<p>使用Hive对应于如下语句：</p>

<pre><code class="sql">SELECT a.key, a.val 
FROM a LEFT SEMI JOIN b ON (a.key = b.key)
</code></pre>

<p>需要注意的是，在LEFT SEMI JOIN中，表b只能出现在ON子句后面，不能够出现在SELECT和WHERE子句中。</p>

<p>关于子查询，这里提一下，Hive支持情况如下：</p>

<ul>
<li><p>在0.12版本，只支持FROM子句中的子查询；</p></li>
<li><p>在0.13版本，也支持WHERE子句中的子查询。</p></li>
</ul>


<h2>Map Side JOIN</h2>

<p>Map Side JOIN优化的出发点是，Map任务输出后，不需要将数据拷贝到Reducer节点，降低的数据在网络节点之间传输的开销。</p>

<p>多表连接，如果只有一个表比较大，其他表都很小，则JOIN操作会转换成一个只包含Map的Job，例如：</p>

<pre><code class="sql">SELECT /*+ MAPJOIN(b) */ a.key, a.value 
FROM a JOIN b ON a.key = b.key
</code></pre>

<p>对于表a数据的每一个Map，都能够完全读取表b的数据。这里，表a与b不允许执行FULL OUTER JOIN、RIGHT OUTER JOIN。</p>

<h2>BUCKET Map Side JOIN</h2>

<p>我们先看两个表a和b的DDL，表a为：</p>

<pre><code class="sql">CREATE TABLE a(key INT, othera STRING)
CLUSTERED BY(key) INTO 4 BUCKETS
ROW FORMAT DELIMITED
FIELDS TERMINATED BY '\001'
COLLECTION ITEMS TERMINATED BY '\002'
MAP KEYS TERMINATED BY '\003'
STORED AS SEQUENCEFILE;
</code></pre>

<p>表b为：</p>

<pre><code class="sql">CREATE TABLE b(key INT, otherb STRING)
CLUSTERED BY(key) INTO 32 BUCKETS
ROW FORMAT DELIMITED
FIELDS TERMINATED BY '\001'
COLLECTION ITEMS TERMINATED BY '\002'
MAP KEYS TERMINATED BY '\003'
STORED AS SEQUENCEFILE;
</code></pre>

<p>现在要基于a.key和b.key进行JOIN操作，此时JOIN列同时也是BUCKET列，JOIN语句如下：</p>

<pre><code class="sql">SELECT /*+ MAPJOIN(b) */ a.key, a.value 
FROM a JOIN b ON a.key = b.key
</code></pre>

<p>并且表a有4个BUCKET，表b有32个BUCKET，默认情况下，对于表a的每一个BUCKET，都会去获取表b中的每一个BUCKET来进行JOIN，这回造成一定的开销，因为只有表b中满足JOIN条件的BUCKET才会真正与表a的BUCKET进行连接。</p>

<p>这种默认行为可以进行优化，通过改变默认JOIN行为，只需要设置变量：</p>

<pre><code class="sql">set hive.optimize.bucketmapjoin = true
</code></pre>

<p>这样，JOIN的过程是，表a的BUCKET 1只会与表b中的BUCKET 1进行JOIN，而不再考虑表b中的其他BUCKET 2~32。</p>

<p>如果上述表具有相同的BUCKET，如都是32个，而且还是排序的，亦即，在表定义中在CLUSTERED BY(key)后面增加如下约束：</p>

<pre><code class="sql">SORTED BY(key)
</code></pre>

<p>则上述JOIN语句会执行一个Sort-Merge-Bucket (SMB) JOIN，同样需要设置如下参数来改变默认行为，优化JOIN时只遍历相关的BUCKET即可：</p>

<pre><code class="sql">set hive.input.format=org.apache.hadoop.hive.ql.io.BucketizedHiveInputFormat;
set hive.optimize.bucketmapjoin = true;
set hive.optimize.bucketmapjoin.sortedmerge = true;
</code></pre>

<p>关于更多的有关JOIN优化，可以参考后面的链接。</p>

<h2>参考资料</h2>

<p>[1]《Hadoop: The Definitive Guide》<br/>
[2]<a href="https://cwiki.apache.org/confluence/display/Hive/LanguageManual+DDL">Hive Language Manual - DDL</a><br/>
[3]<a href="https://cwiki.apache.org/confluence/display/Hive/LanguageManual+Joins">Hive Language Manual - Joins</a><br/>
[4]<a href="https://cwiki.apache.org/confluence/display/Hive/LanguageManual+JoinOptimization">Hive Language Manual - Join Optimization</a><br/>
[5]<a href="https://cwiki.apache.org/confluence/display/Hive/LanguageManual+SubQueries">Hive Language Manual - SubQueries</a><br/>
[6]<a href="https://cwiki.apache.org/confluence/display/Hive/OuterJoinBehavior">Hive Outer Join Behavior</a></p>

<p>原文链接：<a href="http://shiyanjun.cn/archives/588.html">http://shiyanjun.cn/archives/588.html</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[【美团】Hive SQL的编译过程详解]]></title>
    <link href="https://ibillxia.github.io/blog/2015/12/07/hive-sql-compilation-to-mapreduce/"/>
    <updated>2015-12-07T18:16:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2015/12/07/hive-sql-compilation-to-mapreduce</id>
    <content type="html"><![CDATA[<center><img src="/images/2015/IMAG2015120700.jpeg"></center>


<p>Hive是基于Hadoop的一个数据仓库系统，在各大公司都有广泛的应用。美团数据仓库也是基于Hive搭建，每天执行近万次的Hive ETL计算流程，负责每天数百GB的数据存储和分析。Hive的稳定性和性能对我们的数据分析非常关键。</p>

<p>在几次升级Hive的过程中，我们遇到了一些大大小小的问题。通过向社区的咨询和自己的努力，在解决这些问题的同时我们对Hive将SQL编译为MapReduce的过程有了比较深入的理解。对这一过程的理解不仅帮助我们解决了一些Hive的bug，也有利于我们优化Hive SQL，提升我们对Hive的掌控力，同时有能力去定制一些需要的功能。</p>

<!--more-->


<h1>MapReduce实现基本SQL操作的原理</h1>

<p>详细讲解SQL编译为MapReduce之前，我们先来看看MapReduce框架实现SQL基本操作的原理</p>

<h2>Join的实现原理</h2>

<pre><code class="sql">select u.name, o.orderid from order o join user u on o.uid = u.uid;
</code></pre>

<p>在map的输出value中为不同表的数据打上tag标记，在reduce阶段根据tag判断数据来源。MapReduce的过程如下（这里只是说明最基本的Join的实现，还有其他的实现方式）</p>

<center><img src="/images/2015/IMAG2015120701.png"></center>


<h2>Group By的实现原理</h2>

<pre><code class="sql">select rank, isonline, count(*) from city group by rank, isonline;
</code></pre>

<p>将GroupBy的字段组合为map的输出key值，利用MapReduce的排序，在reduce阶段保存LastKey区分不同的key。MapReduce的过程如下（当然这里只是说明Reduce端的非Hash聚合过程）</p>

<center><img src="/images/2015/IMAG2015120702.png"></center>


<h2>Distinct的实现原理</h2>

<pre><code class="sql">select dealid, count(distinct uid) num from order group by dealid;
</code></pre>

<p>当只有一个distinct字段时，如果不考虑Map阶段的Hash GroupBy，只需要将GroupBy字段和Distinct字段组合为map输出key，利用mapreduce的排序，同时将GroupBy字段作为reduce的key，在reduce阶段保存LastKey即可完成去重</p>

<center><img src="/images/2015/IMAG2015120703.png"></center>


<p>如果有多个distinct字段呢，如下面的SQL</p>

<pre><code class="sql">select dealid, count(distinct uid), count(distinct date) from order group by dealid;
</code></pre>

<p>实现方式有两种：</p>

<p>（1）如果仍然按照上面一个distinct字段的方法，即下图这种实现方式，无法跟据uid和date分别排序，也就无法通过LastKey去重，仍然需要在reduce阶段在内存中通过Hash去重</p>

<center><img src="/images/2015/IMAG2015120704.png"></center>


<p>（2）第二种实现方式，可以对所有的distinct字段编号，每行数据生成n行数据，那么相同字段就会分别排序，这时只需要在reduce阶段记录LastKey即可去重。</p>

<p>这种实现方式很好的利用了MapReduce的排序，节省了reduce阶段去重的内存消耗，但是缺点是增加了shuffle的数据量。</p>

<p>需要注意的是，在生成reduce value时，除第一个distinct字段所在行需要保留value值，其余distinct数据行value字段均可为空。</p>

<center><img src="/images/2015/IMAG2015120705.png"></center>


<h1>SQL转化为MapReduce的过程</h1>

<p>了解了MapReduce实现SQL基本操作之后，我们来看看Hive是如何将SQL转化为MapReduce任务的，整个编译过程分为六个阶段：</p>

<ol>
<li><p>Antlr定义SQL的语法规则，完成SQL词法，语法解析，将SQL转化为抽象语法树AST Tree</p></li>
<li><p>遍历AST Tree，抽象出查询的基本组成单元QueryBlock</p></li>
<li><p>遍历QueryBlock，翻译为执行操作树OperatorTree</p></li>
<li><p>逻辑层优化器进行OperatorTree变换，合并不必要的ReduceSinkOperator，减少shuffle数据量</p></li>
<li><p>遍历OperatorTree，翻译为MapReduce任务</p></li>
<li><p>物理层优化器进行MapReduce任务的变换，生成最终的执行计划</p></li>
</ol>


<p>下面分别对这六个阶段进行介绍</p>

<h2>Phase1 SQL词法，语法解析</h2>

<h3>Antlr</h3>

<p>Hive使用Antlr实现SQL的词法和语法解析。Antlr是一种语言识别的工具，可以用来构造领域语言。</p>

<p>这里不详细介绍Antlr，只需要了解使用Antlr构造特定的语言只需要编写一个语法文件，定义词法和语法替换规则即可，Antlr完成了词法分析、语法分析、语义分析、中间代码生成的过程。</p>

<p>Hive中语法规则的定义文件在0.10版本以前是Hive.g一个文件，随着语法规则越来越复杂，由语法规则生成的Java解析类可能超过Java类文件的最大上限，0.11版本将Hive.g拆成了5个文件，词法规则HiveLexer.g和语法规则的4个文件SelectClauseParser.g，FromClauseParser.g，IdentifiersParser.g，HiveParser.g。</p>

<h3>抽象语法树AST Tree</h3>

<p>经过词法和语法解析后，如果需要对表达式做进一步的处理，使用 Antlr 的抽象语法树语法Abstract Syntax Tree，在语法分析的同时将输入语句转换成抽象语法树，后续在遍历语法树时完成进一步的处理。</p>

<p>下面的一段语法是Hive SQL中SelectStatement的语法规则，从中可以看出，SelectStatement包含select, from, where, groupby, having, orderby等子句。</p>

<p>（在下面的语法规则中，箭头表示对于原语句的改写，改写后会加入一些特殊词标示特定语法，比如TOK_QUERY标示一个查询块）</p>

<pre><code>selectStatement
   :
   selectClause
   fromClause
   whereClause?
   groupByClause?
   havingClause?
   orderByClause?
   clusterByClause?
   distributeByClause?
   sortByClause?
   limitClause? -&gt; ^(TOK_QUERY fromClause ^(TOK_INSERT ^(TOK_DESTINATION ^(TOK_DIR TOK_TMP_FILE))
                     selectClause whereClause? groupByClause? havingClause? orderByClause? clusterByClause?
                     distributeByClause? sortByClause? limitClause?))
   ;
</code></pre>

<p>样例SQL</p>

<p>为了详细说明SQL翻译为MapReduce的过程，这里以一条简单的SQL为例，SQL中包含一个子查询，最终将数据写入到一张表中</p>

<pre><code>FROM
( 
  SELECT
    p.datekey datekey,
    p.userid userid,
    c.clienttype
  FROM
    detail.usersequence_client c
    JOIN fact.orderpayment p ON p.orderid = c.orderid
    JOIN default.user du ON du.userid = p.userid
  WHERE p.datekey = 20131118 
) base
INSERT OVERWRITE TABLE `test`.`customer_kpi`
SELECT
  base.datekey,
  base.clienttype,
  count(distinct base.userid) buyer_count
GROUP BY base.datekey, base.clienttype
</code></pre>

<h3>SQL生成AST Tree</h3>

<p>Antlr对Hive SQL解析的代码如下，HiveLexerX，HiveParser分别是Antlr对语法文件Hive.g编译后自动生成的词法解析和语法解析类，在这两个类中进行复杂的解析。</p>

<pre><code>HiveLexerX lexer = new HiveLexerX(new ANTLRNoCaseStringStream(command));    //词法解析，忽略关键词的大小写
TokenRewriteStream tokens = new TokenRewriteStream(lexer);
if (ctx != null) {
  ctx.setTokenRewriteStream(tokens);
}
HiveParser parser = new HiveParser(tokens);                                 //语法解析
parser.setTreeAdaptor(adaptor);
HiveParser.statement_return r = null;
try {
  r = parser.statement();                                                   //转化为AST Tree
} catch (RecognitionException e) {
  e.printStackTrace();
  throw new ParseException(parser.errors);
}
</code></pre>

<p>最终生成的AST Tree如下图右侧（使用Antlr Works生成，Antlr Works是Antlr提供的编写语法文件的编辑器），图中只是展开了骨架的几个节点，没有完全展开。</p>

<p>子查询1/2，分别对应右侧第1/2两个部分。</p>

<center><img src="/images/2015/IMAG2015120706.png"></center>


<p>这里注意一下内层子查询也会生成一个TOK_DESTINATION节点。请看上面SelectStatement的语法规则，这个节点是在语法改写中特意增加了的一个节点。原因是Hive中所有查询的数据均会保存在HDFS临时的文件中，无论是中间的子查询还是查询最终的结果，Insert语句最终会将数据写入表所在的HDFS目录下。</p>

<p>详细来看，将内存子查询的from子句展开后，得到如下AST Tree，每个表生成一个TOK_TABREF节点，Join条件生成一个“=”节点。其他SQL部分类似，不一一详述。</p>

<center><img src="/images/2015/IMAG2015120707.png"></center>


<h2>Phase2 SQL基本组成单元QueryBlock</h2>

<p>AST Tree仍然非常复杂，不够结构化，不方便直接翻译为MapReduce程序，AST Tree转化为QueryBlock就是将SQL进一部抽象和结构化。</p>

<h3>QueryBlock</h3>

<p>QueryBlock是一条SQL最基本的组成单元，包括三个部分：输入源，计算过程，输出。简单来讲一个QueryBlock就是一个子查询。</p>

<p>下图为Hive中QueryBlock相关对象的类图，解释图中几个重要的属性</p>

<ul>
<li><p>QB#aliasToSubq（表示QB类的aliasToSubq属性）保存子查询的QB对象，aliasToSubq key值是子查询的别名</p></li>
<li><p>QB#qbp即QBParseInfo保存一个基本SQL单元中的给个操作部分的AST Tree结构，QBParseInfo#nameToDest这个HashMap保存查询单元的输出，key的形式是inclause-i（由于Hive支持Multi Insert语句，所以可能有多个输出），value是对应的ASTNode节点，即TOK_DESTINATION节点。类QBParseInfo其余HashMap属性分别保存输出和各个操作的ASTNode节点的对应关系。</p></li>
<li><p>QBParseInfo#JoinExpr保存TOK_JOIN节点。QB#QBJoinTree是对Join语法树的结构化。</p></li>
<li><p>QB#qbm保存每个输入表的元信息，比如表在HDFS上的路径，保存表数据的文件格式等。</p></li>
<li><p>QBExpr这个对象是为了表示Union操作。</p></li>
</ul>


<center><img src="/images/2015/IMAG2015120708.png"></center>


<h3>AST Tree生成QueryBlock</h3>

<p>AST Tree生成QueryBlock的过程是一个递归的过程，先序遍历AST Tree，遇到不同的Token节点，保存到相应的属性中，主要包含以下几个过程</p>

<ul>
<li><p>TOK_QUERY => 创建QB对象，循环递归子节点</p></li>
<li><p>TOK_FROM => 将表名语法部分保存到QB对象的aliasToTabs等属性中</p></li>
<li><p>TOK_INSERT => 循环递归子节点</p></li>
<li><p>TOK_DESTINATION => 将输出目标的语法部分保存在QBParseInfo对象的nameToDest属性中</p></li>
<li><p>TOK_SELECT => 分别将查询表达式的语法部分保存在destToSelExpr、destToAggregationExprs、destToDistinctFuncExprs三个属性中</p></li>
<li><p>TOK_WHERE => 将Where部分的语法保存在QBParseInfo对象的destToWhereExpr属性中</p></li>
</ul>


<p>最终样例SQL生成两个QB对象，QB对象的关系如下，QB1是外层查询，QB2是子查询</p>

<pre><code>QB1

  \

   QB2
</code></pre>

<h2>Phase3 逻辑操作符Operator</h2>

<h3>Operator</h3>

<p>Hive最终生成的MapReduce任务，Map阶段和Reduce阶段均由OperatorTree组成。逻辑操作符，就是在Map阶段或者Reduce阶段完成单一特定的操作。</p>

<p>基本的操作符包括TableScanOperator，SelectOperator，FilterOperator，JoinOperator，GroupByOperator，ReduceSinkOperator</p>

<p>从名字就能猜出各个操作符完成的功能，TableScanOperator从MapReduce框架的Map接口原始输入表的数据，控制扫描表的数据行数，标记是从原表中取数据。JoinOperator完成Join操作。FilterOperator完成过滤操作</p>

<p>ReduceSinkOperator将Map端的字段组合序列化为Reduce Key/value, Partition Key，只可能出现在Map阶段，同时也标志着Hive生成的MapReduce程序中Map阶段的结束。</p>

<p>Operator在Map Reduce阶段之间的数据传递都是一个流式的过程。每一个Operator对一行数据完成操作后之后将数据传递给childOperator计算。</p>

<p>Operator类的主要属性和方法如下</p>

<ul>
<li><p>RowSchema表示Operator的输出字段</p></li>
<li><p>InputObjInspector outputObjInspector解析输入和输出字段</p></li>
<li><p>processOp接收父Operator传递的数据，forward将处理好的数据传递给子Operator处理</p></li>
<li><p>Hive每一行数据经过一个Operator处理之后，会对字段重新编号，colExprMap记录每个表达式经过当前Operator处理前后的名称对应关系，在下一个阶段逻辑优化阶段用来回溯字段名</p></li>
<li><p>由于Hive的MapReduce程序是一个动态的程序，即不确定一个MapReduce Job会进行什么运算，可能是Join，也可能是GroupBy，所以Operator将所有运行时需要的参数保存在OperatorDesc中，OperatorDesc在提交任务前序列化到HDFS上，在MapReduce任务执行前从HDFS读取并反序列化。Map阶段OperatorTree在HDFS上的位置在Job.getConf(“hive.exec.plan”) + “/map.xml”</p></li>
</ul>


<center><img src="/images/2015/IMAG2015120709.png"></center>


<h3>QueryBlock生成Operator Tree</h3>

<p>QueryBlock生成Operator Tree就是遍历上一个过程中生成的QB和QBParseInfo对象的保存语法的属性，包含如下几个步骤：</p>

<ul>
<li><p>QB#aliasToSubq => 有子查询，递归调用</p></li>
<li><p>QB#aliasToTabs => TableScanOperator</p></li>
<li><p>QBParseInfo#joinExpr => QBJoinTree => ReduceSinkOperator + JoinOperator</p></li>
<li><p>QBParseInfo#destToWhereExpr => FilterOperator</p></li>
<li><p>QBParseInfo#destToGroupby => ReduceSinkOperator + GroupByOperator</p></li>
<li><p>QBParseInfo#destToOrderby => ReduceSinkOperator + ExtractOperator</p></li>
</ul>


<p>由于Join/GroupBy/OrderBy均需要在Reduce阶段完成，所以在生成相应操作的Operator之前都会先生成一个ReduceSinkOperator，将字段组合并序列化为Reduce Key/value, Partition Key</p>

<p>接下来详细分析样例SQL生成OperatorTree的过程</p>

<p>先序遍历上一个阶段生成的QB对象</p>

<ol>
<li>首先根据子QueryBlock QB2#aliasToTabs {du=dim.user, c=detail.usersequence_client, p=fact.orderpayment}生成TableScanOperator</li>
</ol>


<pre><code>TableScanOperator(“dim.user”) TS[0]
TableScanOperator(“detail.usersequence_client”) TS[1]       TableScanOperator(“fact.orderpayment”) TS[2]
</code></pre>

<ol>
<li>先序遍历QBParseInfo#joinExpr生成QBJoinTree，类QBJoinTree也是一个树状结构，QBJoinTree保存左右表的ASTNode和这个查询的别名，最终生成的查询树如下</li>
</ol>


<pre><code>   base
   /  \
  p    du
 /      \
c        p
</code></pre>

<ol>
<li>前序遍历QBJoinTree，先生成detail.usersequence_client和fact.orderpayment的Join操作树</li>
</ol>


<center><img src="/images/2015/IMAG2015120710.png"></center>


<p>图中 TS=TableScanOperator RS=ReduceSinkOperator JOIN=JoinOperator</p>

<ol>
<li>生成中间表与dim.user的Join操作树</li>
</ol>


<center><img src="/images/2015/IMAG2015120711.png"></center>


<ol>
<li>根据QB2 QBParseInfo#destToWhereExpr 生成FilterOperator。此时QB2遍历完成。</li>
</ol>


<p>下图中SelectOperator在某些场景下会根据一些条件判断是否需要解析字段。</p>

<center><img src="/images/2015/IMAG2015120712.png"></center>


<p><strong> 图中 FIL= FilterOperator SEL= SelectOperator </strong></p>

<ol>
<li>根据QB1的QBParseInfo#destToGroupby生成ReduceSinkOperator + GroupByOperator</li>
</ol>


<center><img src="/images/2015/IMAG2015120713.png"></center>


<p>图中 GBY= GroupByOperator</p>

<p>GBY[12]是HASH聚合，即在内存中通过Hash进行聚合运算</p>

<ol>
<li>最终都解析完后，会生成一个FileSinkOperator，将数据写入HDFS</li>
</ol>


<center><img src="/images/2015/IMAG2015120714.png"></center>


<p><strong> 图中FS=FileSinkOperator </strong></p>

<h2>Phase4 逻辑层优化器</h2>

<p>大部分逻辑层优化器通过变换OperatorTree，合并操作符，达到减少MapReduce Job，减少shuffle数据量的目的。</p>

<table>
<thead>
<tr>
<th> 名称 </th>
<th> 作用 </th>
</tr>
</thead>
<tbody>
<tr>
<td> ② SimpleFetchOptimizer </td>
<td> 优化没有GroupBy表达式的聚合查询 </td>
</tr>
<tr>
<td> ② MapJoinProcessor </td>
<td> MapJoin，需要SQL中提供hint，0.11版本已不用 </td>
</tr>
<tr>
<td> ② BucketMapJoinOptimizer </td>
<td> BucketMapJoin </td>
</tr>
<tr>
<td> ② GroupByOptimizer </td>
<td> Map端聚合 </td>
</tr>
<tr>
<td> ① ReduceSinkDeDuplication </td>
<td> 合并线性的OperatorTree中partition/sort key相同的reduce </td>
</tr>
<tr>
<td> ① PredicatePushDown </td>
<td> 谓词前置 </td>
</tr>
<tr>
<td> ① CorrelationOptimizer </td>
<td> 利用查询中的相关性，合并有相关性的Job，HIVE-2206 </td>
</tr>
<tr>
<td> ColumnPruner </td>
<td> 字段剪枝 </td>
</tr>
</tbody>
</table>


<p>表格中①的优化器均是一个Job干尽可能多的事情/合并。②的都是减少shuffle数据量，甚至不做Reduce。</p>

<p>CorrelationOptimizer优化器非常复杂，都能利用查询中的相关性，合并有相关性的Job，参考 Hive Correlation Optimizer</p>

<p>对于样例SQL，有两个优化器对其进行优化。下面分别介绍这两个优化器的作用，并补充一个优化器ReduceSinkDeDuplication的作用</p>

<h3>PredicatePushDown优化器</h3>

<p>断言判断提前优化器将OperatorTree中的FilterOperator提前到TableScanOperator之后</p>

<center><img src="/images/2015/IMAG2015120715.png"></center>


<h3>NonBlockingOpDeDupProc优化器</h3>

<p>NonBlockingOpDeDupProc优化器合并SEL-SEL 或者 FIL-FIL 为一个Operator</p>

<center><img src="/images/2015/IMAG2015120716.png"></center>


<h3>ReduceSinkDeDuplication优化器</h3>

<p>ReduceSinkDeDuplication可以合并线性相连的两个RS。实际上CorrelationOptimizer是ReduceSinkDeDuplication的超集，能合并线性和非线性的操作RS，但是Hive先实现的ReduceSinkDeDuplication</p>

<p>譬如下面这条SQL语句</p>

<pre><code>from (select key, value from src group by key, value) s select s.key group by s.key;
</code></pre>

<p>经过前面几个阶段之后，会生成如下的OperatorTree，两个Tree是相连的，这里没有画到一起</p>

<center><img src="/images/2015/IMAG2015120717.png"></center>


<p>这时候遍历OperatorTree后能发现前前后两个RS输出的Key值和PartitionKey如下</p>

<table>
<thead>
<tr>
<th> RS </th>
<th> Key </th>
<th> PartitionKey </th>
</tr>
</thead>
<tbody>
<tr>
<td> childRS </td>
<td> key </td>
<td> key </td>
</tr>
<tr>
<td> parentRS </td>
<td> key,value </td>
<td> key,value </td>
</tr>
</tbody>
</table>


<p></br>
ReduceSinkDeDuplication优化器检测到：1. pRS Key完全包含cRS Key，且排序顺序一致；2. pRS PartitionKey完全包含cRS PartitionKey。符合优化条件，会对执行计划进行优化。</p>

<p>ReduceSinkDeDuplication将childRS和parentheRS与childRS之间的Operator删掉，保留的RS的Key为key,value字段，PartitionKey为key字段。合并后的OperatorTree如下：</p>

<center><img src="/images/2015/IMAG2015120718.png"></center>


<h2>Phase5 OperatorTree生成MapReduce Job的过程</h2>

<p>OperatorTree转化为MapReduce Job的过程分为下面几个阶段</p>

<ol>
<li><p>对输出表生成MoveTask</p></li>
<li><p>从OperatorTree的其中一个根节点向下深度优先遍历</p></li>
<li><p>ReduceSinkOperator标示Map/Reduce的界限，多个Job间的界限</p></li>
<li><p>遍历其他根节点，遇过碰到JoinOperator合并MapReduceTask</p></li>
<li><p>生成StatTask更新元数据</p></li>
<li><p>剪断Map与Reduce间的Operator的关系</p></li>
</ol>


<h3>对输出表生成MoveTask</h3>

<p>由上一步OperatorTree只生成了一个FileSinkOperator，直接生成一个MoveTask，完成将最终生成的HDFS临时文件移动到目标表目录下</p>

<pre><code>MoveTask[Stage-0]
Move Operator
</code></pre>

<h3>开始遍历</h3>

<p>将OperatorTree中的所有根节点保存在一个toWalk的数组中，循环取出数组中的元素（省略QB1，未画出）</p>

<center><img src="/images/2015/IMAG2015120719.png"></center>


<p>取出最后一个元素TS[p]放入栈 opStack{TS[p]}中</p>

<h3>Rule #1 TS% 生成MapReduceTask对象，确定MapWork</h3>

<p>发现栈中的元素符合下面规则R1（这里用python代码简单表示）</p>

<pre><code>"".join([t + "%" for t in opStack]) == "TS%"
</code></pre>

<p>生成一个MapReduceTask[Stage-1]对象，MapReduceTask[Stage-1]对象的MapWork属性保存Operator根节点的引用。由于OperatorTree之间之间的Parent Child关系，这个时候MapReduceTask[Stage-1]包含了以TS[p]为根的所有Operator</p>

<center><img src="/images/2015/IMAG2015120720.png"></center>


<h3>Rule #2 TS%.*RS% 确定ReduceWork</h3>

<p>继续遍历TS[p]的子Operator，将子Operator存入栈opStack中</p>

<p>当第一个RS进栈后，即栈opStack = {TS[p], FIL[18], RS[4]}时，就会满足下面的规则R2</p>

<pre><code>"".join([t + "%" for t in opStack]) == "TS%.*RS%"
</code></pre>

<p>这时候在MapReduceTask[Stage-1]对象的ReduceWork属性保存JOIN[5]的引用</p>

<center><img src="/images/2015/IMAG2015120721.png"></center>


<h3>Rule #3 RS%.*RS% 生成新MapReduceTask对象，切分MapReduceTask</h3>

<p>继续遍历JOIN[5]的子Operator，将子Operator存入栈opStack中</p>

<p>当第二个RS放入栈时，即当栈opStack = {TS[p], FIL[18], RS[4], JOIN[5], RS[6]}时，就会满足下面的规则R3</p>

<pre><code>"".join([t + "%" for t in opStack]) == “RS%.*RS%” //循环遍历opStack的每一个后缀数组
</code></pre>

<p>这时候创建一个新的MapReduceTask[Stage-2]对象，将OperatorTree从JOIN[5]和RS[6]之间剪开，并为JOIN[5]生成一个子Operator FS[19]，RS[6]生成一个TS[20]，MapReduceTask[Stage-2]对象的MapWork属性保存TS[20]的引用。</p>

<p>新生成的FS[19]将中间数据落地，存储在HDFS临时文件中。</p>

<center><img src="/images/2015/IMAG2015120722.png"></center>


<p>继续遍历RS[6]的子Operator，将子Operator存入栈opStack中</p>

<p>当opStack = {TS[p], FIL[18], RS[4], JOIN[5], RS[6], JOIN[8], SEL[10], GBY[12], RS[13]}时，又会满足R3规则</p>

<p>同理生成MapReduceTask[Stage-3]对象，并切开 Stage-2 和 Stage-3 的OperatorTree</p>

<center><img src="/images/2015/IMAG2015120723.png"></center>


<h3>R4 FS% 连接MapReduceTask与MoveTask</h3>

<p>最终将所有子Operator存入栈中之后，opStack = {TS[p], FIL[18], RS[4], JOIN[5], RS[6], JOIN[8], SEL[10], GBY[12], RS[13], GBY[14], SEL[15], FS[17]} 满足规则R4</p>

<pre><code>"".join([t + "%" for t in opStack]) == “FS%”
</code></pre>

<p>这时候将MoveTask与MapReduceTask[Stage-3]连接起来，并生成一个StatsTask，修改表的元信息</p>

<center><img src="/images/2015/IMAG2015120724.png"></center>


<h3>合并Stage</h3>

<p>此时并没有结束，还有两个根节点没有遍历。</p>

<p>将opStack栈清空，将toWalk的第二个元素加入栈。会发现opStack = {TS[du]}继续满足R1 TS%，生成MapReduceTask[Stage-5]</p>

<center><img src="/images/2015/IMAG2015120725.png"></center>


<p>继续从TS[du]向下遍历，当opStack={TS[du], RS[7]}时，满足规则R2 TS%.*RS%</p>

<p>此时将JOIN[8]保存为MapReduceTask[Stage-5]的ReduceWork时，发现在一个Map对象保存的Operator与MapReduceWork对象关系的Map 对象中发现，JOIN[8]已经存在。此时将MapReduceTask[Stage-2]和MapReduceTask[Stage-5]合并为一个MapReduceTask</p>

<center><img src="/images/2015/IMAG2015120726.png"></center>


<p>同理从最后一个根节点TS[c]开始遍历，也会对MapReduceTask进行合并</p>

<center><img src="/images/2015/IMAG2015120727.png"></center>


<h3>切分Map Reduce阶段</h3>

<p>最后一个阶段，将MapWork和ReduceWork中的OperatorTree以RS为界限剪开</p>

<center><img src="/images/2015/IMAG2015120728.png"></center>


<h3>OperatorTree生成MapReduceTask全貌</h3>

<p>最终共生成3个MapReduceTask，如下图</p>

<center><img src="/images/2015/IMAG2015120729.png"></center>


<h2>Phase6 物理层优化器</h2>

<p>这里不详细介绍每个优化器的原理，单独介绍一下MapJoin的优化器</p>

<table>
<thead>
<tr>
<th> 名称 </th>
<th> 作用 </th>
</tr>
</thead>
<tbody>
<tr>
<td> Vectorizer </td>
<td> HIVE-4160，将在0.13中发布 </td>
</tr>
<tr>
<td> SortMergeJoinResolver </td>
<td> 与bucket配合，类似于归并排序 </td>
</tr>
<tr>
<td> SamplingOptimizer </td>
<td> 并行order by优化器，在0.12中发布 </td>
</tr>
<tr>
<td> CommonJoinResolver + MapJoinResolver </td>
<td> MapJoin优化器 </td>
</tr>
</tbody>
</table>


<h3>MapJoin原理</h3>

<center><img src="/images/2015/IMAG2015120730.png"></center>


<p>MapJoin简单说就是在Map阶段将小表读入内存，顺序扫描大表完成Join。</p>

<p>上图是Hive MapJoin的原理图，出自Facebook工程师Liyin Tang的一篇介绍Join优化的slice，从图中可以看出MapJoin分为两个阶段：</p>

<ol>
<li><p>通过MapReduce Local Task，将小表读入内存，生成HashTableFiles上传至Distributed Cache中，这里会对HashTableFiles进行压缩。</p></li>
<li><p>MapReduce Job在Map阶段，每个Mapper从Distributed Cache读取HashTableFiles到内存中，顺序扫描大表，在Map阶段直接进行Join，将数据传递给下一个MapReduce任务。</p></li>
</ol>


<center><img src="/images/2015/IMAG2015120731.png"></center>


<p>如果Join的两张表一张表是临时表，就会生成一个ConditionalTask，在运行期间判断是否使用MapJoin</p>

<h3>CommonJoinResolver优化器</h3>

<p>CommonJoinResolver优化器就是将CommonJoin转化为MapJoin，转化过程如下</p>

<ol>
<li><p>深度优先遍历Task Tree</p></li>
<li><p>找到JoinOperator，判断左右表数据量大小</p></li>
<li><p>对与小表 + 大表 => MapJoinTask，对于小/大表 + 中间表 => ConditionalTask</p></li>
</ol>


<p>遍历上一个阶段生成的MapReduce任务，发现MapReduceTask[Stage-2]JOIN[8]中有一张表为临时表，先对Stage-2进行深度拷贝（由于需要保留原始执行计划为Backup Plan，所以这里将执行计划拷贝了一份），生成一个MapJoinOperator替代JoinOperator，然后生成一个MapReduceLocalWork读取小表生成HashTableFiles上传至DistributedCache中。</p>

<center><img src="/images/2015/IMAG2015120732.png"></center>


<p>MapReduceTask经过变换后的执行计划如下图所示</p>

<center><img src="/images/2015/IMAG2015120733.png"></center>


<h3>MapJoinResolver优化器</h3>

<p>MapJoinResolver优化器遍历Task Tree，将所有有local work的MapReduceTask拆成两个Task</p>

<center><img src="/images/2015/IMAG2015120734.png"></center>


<p>最终MapJoinResolver处理完之后，执行计划如下图所示</p>

<center><img src="/images/2015/IMAG2015120735.png"></center>


<h1>Hive SQL编译过程的设计</h1>

<p>从上述整个SQL编译的过程，可以看出编译过程的设计有几个优点值得学习和借鉴</p>

<ul>
<li><p>使用Antlr开源软件定义语法规则，大大简化了词法和语法的编译解析过程，仅仅需要维护一份语法文件即可。</p></li>
<li><p>整体思路很清晰，分阶段的设计使整个编译过程代码容易维护，使得后续各种优化器方便的以可插拔的方式开关，譬如Hive 0.13最新的特性Vectorization和对Tez引擎的支持都是可插拔的。</p></li>
<li><p>每个Operator只完成单一的功能，简化了整个MapReduce程序。</p></li>
</ul>


<h1>社区发展方向</h1>

<p>Hive依然在迅速的发展中，为了提升Hive的性能，hortonworks公司主导的Stinger计划提出了一系列对Hive的改进，比较重要的改进有：</p>

<ul>
<li><p>Vectorization - 使Hive从单行单行处理数据改为批量处理方式，大大提升了指令流水线和缓存的利用率</p></li>
<li><p>Hive on Tez - 将Hive底层的MapReduce计算框架替换为Tez计算框架。Tez不仅可以支持多Reduce阶段的任务MRR，还可以一次性提交执行计划，因而能更好的分配资源。</p></li>
<li><p>Cost Based Optimizer - 使Hive能够自动选择最优的Join顺序，提高查询速度</p></li>
<li><p>Implement insert, update, and delete in Hive with full ACID support - 支持表按主键的增量更新</p></li>
</ul>


<p>我们也将跟进社区的发展，结合自身的业务需要，提升Hive型ETL流程的性能</p>

<h1>参考资料</h1>

<p>[1]<a href="https://tech.meituan.com/2014/02/12/hive-sql-to-mapreduce.html">Hive SQL的编译过程 - 美团技术团队</a><br/>
[2]<a href="http://www.antlr.org/">Antlr</a><br/>
[3]<a href="https://cwiki.apache.org/confluence/display/Hive/Home">Hive Wiki</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Sql Server数据库常用的查询语句]]></title>
    <link href="https://ibillxia.github.io/blog/2009/07/23/useful-queries-methods-in-MS-SQL-Server/"/>
    <updated>2009-07-23T22:31:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2009/07/23/useful-queries-methods-in-MS-SQL-Server</id>
    <content type="html"><![CDATA[<h2>一、表操作</h2>

<p>例 1 对于表的教学管理数据库中的表 STUDENTS ，可以定义如下：
<code>
Create TABLE STUDENTS (SNO NUMERIC (6, 0) NOT NULL
SNAME CHAR (8) NOT NULL
AGE NUMERIC(3,0)
SEX CHAR(2)
BPLACE CHAR(20)
PRIMARY KEY(SNO))
</code></p>

<p>例 2 对于表的教学管理数据库中的表 ENROLLS ，可以定义如下：
<code>
Create TABLE ENROLLS(SNO NUMERIC(6,0) NOT NULL
CNO CHAR(4) NOT NULL
GRADE INT
PRIMARY KEY(SNO,CNO)
FOREIGN KEY(SNO) REFERENCES STUDENTS(SNO)
FOREIGN KEY(CNO) REFERENCES COURSES(CNO)
CHECK ((GRADE IS NULL) or (GRADE BETWEEN 0 AND 100)))
</code></p>

<!--more-->


<p>例 3 根据表的 STUDENTS 表，建立一个只包含学号、姓名、年龄的女学生表。
<code>
Create TABLE GIRL AS Select SNO, SNAME, AGE
FROM STUDENTS Where SEX=' 女 ';
</code></p>

<p>例 4 删除教师表 TEACHER 。
<code>
Drop TABLE TEACHER
</code></p>

<p>例 5 在教师表中增加住址列。
<code>
Alter TABLE TEACHERS ADD (ADDR CHAR(50))
</code></p>

<p>例 6 把 STUDENTS 表中的 BPLACE 列删除，并且把引用 BPLACE 列的所有视图和约束也一起删除。
<code>
Alter TABLE STUDENTS Drop BPLACE CASCADE
</code></p>

<p>例 7 补充定义 ENROLLS 表的主关键字。
<code>
Alter TABLE ENROLLS ADD PRIMARY KEY (SNO,CNO) ;
</code></p>

<h2>二、视图操作（虚表）</h2>

<p>例 9 建立一个只包括教师号、姓名和年龄的视图 FACULTY 。 ( 在视图定义中不能包含 orDER BY 子句 )
<code>
Create VIEW FACULTY AS Select TNO, TNAME, AGE FROM TEACHERS
</code></p>

<p>例 10 从学生表、课程表和选课表中产生一个视图 <code>GRADE_TABLE</code> ， 它包括学生姓名、课程名和成绩。
<code>
Create VIEW GRADE_TABLE AS Select SNAME,CNAME,GRADE
FROM STUDENTS,COURSES,ENROLLS
Where STUDENTS.SNO ＝ ENROLLS.SNO AND
COURSES.CNO=ENROLLS.CNO
</code></p>

<p>例 11 删除视图 <code>GRADE_TABLE</code>
<code>
Drop VIEW GRADE_TABLE RESTRICT
</code></p>

<h2>三、索引操作</h2>

<p>例 12 在学生表中按学号建立索引。
<code>
Create UNIQUE INDEX ST ON STUDENTS (SNO,ASC)
</code></p>

<p>例 13 删除按学号所建立的索引。
<code>
Drop INDEX ST
</code></p>

<h2>四、数据库模式操作</h2>

<p>例 14 创建一个简易教学数据库的数据库模式 <code>TEACHING_DB</code> ，属主为 <code>ZHANG</code> 。
<code>
Create SCHEMA TEACHING_DB AUTHRIZATION ZHANG
</code></p>

<p>例 15 删除简易教学数据库模式 <code>TEACHING_DB</code> 。<br/>
(1)选用 CASCADE ，即当删除数据库模式时，则本数据库模式和其下属的基本表、视图、索引等全部被删除。<br/>
(2 )选用 RESTRICT ，即本数据库模式下属的基本表、视图、索引等事先已清除，才能删除本数据库模式，否则拒绝删除。
<code>
Drop SCHEMA TEACHING_DB CASCADE
</code></p>

<h2>五、单表操作</h2>

<p>例 16 找出 3 个学分的课程号和课程名。
<code>
Select CNO, CNAME  FROM  COURSES  Where  CREDIT= 3
</code></p>

<p>例 17 查询年龄大于 22 岁的学生情况。
<code>
Select * FROM STUDENTS Where AGE ＞ 22
</code></p>

<p>例 18 找出籍贯为河北的男生的姓名和年龄。
<code>
Select  SNAME, AGE  FROM  STUDENTS
Where  BPLACE ＝ ' 河北 ' AND SEX ＝ ' 男 '
</code></p>

<p>例 19 找出年龄在 20 ～ 23 岁之间的学生的学号、姓名和年龄，并按年龄升序排序。 (ASC （升序）或 DESC （降序）声明排序的方式，缺省为升序。 )
<code>
Select SNO, SNAME, AGE  FROM  STUDENTS
Where  AGE  BETWEEN  20  AND  23
orDER BY AGE
</code></p>

<p>例 20 找出年龄小于 23 岁、籍贯是湖南或湖北的学生的姓名和性别。（条件比较运算符＝、＜ 和逻辑运算符 AND （与），此外还可以使用的
运算符有：＞（大于）、＞＝（大于等于）、＜＝（小于等于）、＜＞（不等于）、 NOT （非）、 or （或）等。 <br/>
谓词 LIKE 只能与字符串联用，常常是 <code>＜列名＞ LIKE pattern</code> 的格式。特殊字符 <code>“_”</code>和 <code>“%”</code> 作为通配符。 <br/>
谓词 IN 表示指定的属性应与后面的集合（括号中的值集或某个查询子句的结果）中的某个值相匹配，实际上是一系列的 or （或）的缩写。<br/>
谓词 NOT IN 表示指定的属性不与后面的集合中的某个值相匹配。 </br>
谓词 BETWEEN 是 “ 包含于 … 之中 ” 的意思。）
<code>
Select SNAME, SEX FROM STUDENTS
Where AGE ＜ 23 AND BPLACE LIKE' 湖％ '
</code>
或
<code>
Select SNAME, SEX FROM STUDENTS
Where AGE ＜ 23 AND BPLACE IN （ ' 湖南 ' ， ' 湖北 ' ）
</code></p>

<p> 例 21 找出学生表中籍贯是空值的学生的姓名和性别。（在 SQL 中不能使用条件：＜列名＞＝ NULL 。在 SQL 中只有一个特殊的查询条件允许查询 NULL 值：）
<code>
Select SNAME, SEX FROM STUDENTS Where BPLACE IS NULL
</code></p>

<h2>六、多表操作</h2>

<p>例 22 找出成绩为 95 分的学生的姓名。（子查询）
<code>
Select SNAME FROM STUDENTS
Where 　 SNO ＝
(Select SNO FROM ENROLLS Where GRADE ＝ 95)
</code></p>

<p>例 23 找出成绩在 90 分以上的学生的姓名。
<code>
Select SNAME FROM STUDENTS
Where SNO IN
(Select SNO FROM ENROLLS Where GRADE ＞ 90)
</code>
或
<code>
Select SNAME FROM STUDENTS
Where SNO ＝ ANY
(Select SNO FROM ENROLLS Where GRADE ＞ 90)
</code></p>

<p>例 24 查询全部学生的学生名和所学课程号及成绩。（连接查询）
<code>
Select SNAME, CNO, GRADE FROM STUDENTS, ENROLLS
Where STUDENTS.SNO ＝ ENROLLS.SNO
</code></p>

<p>例 25 找出籍贯为山西或河北，成绩为 90 分以上的学生的姓名、籍贯和成绩。<br/>
【当构造多表连接查询命令时，必须遵循两条规则。<br/>
第一，连接条件数正好比表数少 1 （若有三个表，就有两个连接条件 ) ；<br/>
第二，若一个表中的主关键字是由多个列组成，则对此主关键字中的每一个列都要有一个连接条件（也有少数例外情况）.】
<code>
Select SNAME, BPLACE, GRADE FROM STUDENTS, ENROLLS
Where BPLACE IN (‘ 山西 ' ， ‘ 河北 ') AND GRADE ＞＝ 90 AND 　 STUDENTS.SNO=ENROLLS.SNO
</code></p>

<p>例 26 查出课程成绩在 80 分以上的女学生的姓名、课程名和成绩。（ FROM 子句中的子查询）
<code>
Select SNAME,CNAME, GRADE FROM (Select SNAME, CNAME , GRADE
FROM STUDENTS, ENROLLS,COURSES Where SEX ＝'女')
AS TEMP (SNAME, CNAME,GRADE) Where GRADE ＞ 80
</code></p>

<h4>表达式与函数的使用</h4>

<p>例 27 查询各课程的学时数。（算术表达式由算术运算符＋、－、 * 、／与列名或数值常量所组成。）
<code>
Select CNAME,COURSE_TIME ＝ CREDIT*16 FROM COURSES
</code></p>

<p>例 28 找出教师的最小年龄。【内部函数： SQL 标准中只使用 COUNT 、 SUM 、 AVG 、 MAX 、 MIN 函数，
称之为聚集函数（ Set Function ）。 COUNT 函数的结果是该列统计值的总数目， SUM 函数求该列统计值之和，
AVG 函数求该列统计值之平均值， MAX 函数求该列最大值， MIN 函数求该列最小值。】
<code>
Select MIN(AGE) FROM TEACHERS
</code></p>

<p><p>例 29 统计年龄小于等于 22 岁的学生人数。（统计）
<code>
Select COUNT(*) FROM STUDENTS Where AGE &lt; ＝ 22
</code></p>

<p>例 30 找出学生的平均成绩和所学课程门数。
<code>
Select SNO, AVG(GRADE), COURSES ＝ COUNT(*) FROM ENROLLS GROUP BY SNO
</code></p>

<p>例 31 找出年龄超过平均年龄的学生姓名。
<code>
Select SNAME FROM STUDENTS
Where AGE ＞ (Select AVG(AGE) FROM STUDENTS)
</code></p>

<p>例 32 找出各课程的平均成绩，按课程号分组，且只选择学生超过 3 人的课程的成绩。【 GROUP BY 与 HAVING  GROUP BY 子句
把一个表按某一指定列（或一些列）上的值相等的原则分组，然后再对每组数据进行规定的操作。 GROUP BY 子句总是跟在 Where 子句
后面，当 Where 子句缺省时，它跟在 FROM 子句后面。 HAVING 子句常用于在计算出聚集之后对行的查询进行控制。】
<code>
Select CNO, AVG(GRADE), STUDENTS ＝ COUNT(*) FROM ENROLLS  GROUP BY CNO HAVING COUNT(*) &gt;= 3
</code></p>

<h2>七、相关子查询</h2>

<p>例 33 查询没有选任何课程的学生的学号和姓名。【当一个子查询涉及到一个来自外部查询的列时，称为相关子查询（ Correlated Subquery) 。
相关子查询要用到存在测试谓词 EXISTS 和 NOT EXISTS ，以及 ALL 、 ANY （ SOME ）等 。】
<code>
Select SNO, SNAME FROM STUDENTS Where NOT EXISTS
(Select * FROM ENROLLS Where ENROLLS.SNO=STUDENTS.SNO)
</code></p>

<p>例 34 查询哪些课程只有男生选读。
<code>
Select DISTINCT CNAME FROM COURSES C
Where ' 男 ' ＝ ALL
(Select SEX FROM ENROLLS , STUDENTS Where ENROLLS.SNO=STUDENTS.SNO AND ENROLLS.CNO=C.CNO)
</code></p>

<p>例 35 要求给出一张学生、籍贯列表，该表中的学生的籍贯省份，也是其他一些学生的籍贯省份。
<code>
Select SNAME, BPLACE FROM STUDENTS A Where EXISTS
(Select * FROM STUDENTS B Where A.BPLACE=B.BPLACE AND A.SNO &lt; &gt; B.SNO)
</code></p>

<p>例 36 找出选修了全部课程的学生的姓名。本查询可以改为：查询这样一些学生，没有一门课程是他不选修的。
<code>
Select SNAME FROM STUDENTS Where NOT EXISTS
(Select * FROM COURSES Where NOT EXISTS
(Select * FROM ENROLLS Where ENROLLS.SNO ＝ STUDENTS.SNO AND ENROLLS.CNO=
</code></p>

<h4>关系代数运算</h4>

<p>例 37 设有某商场工作人员的两张表：营业员表 SP_SUBORD 和营销经理表 SP_MGR ，其关系数据模式如下：
<code>
SP_SUBORD (SALPERS_ID, SALPERS_NAME, MANAGER_ID, OFFICE)
SP_MGR (SALPERS_ID, SALPERS_NAME, MANAGER_ID, OFFICE)
</code>
其中，属性 SALPERS_ID 为工作人员的编号 , SALPERS_NAME 为工作人员的姓名 , <code>MANAGER_ID</code> 为所在部门经理的编号 , <code>OFFICE</code> 为工作地点。
(1) 若查询全部商场工作人员，可以用下面的 SQL 语句：<br/>
<code>
(Select * FROM SP_SUBORD) UNION (Select * FROM SP_MGR)
</code>
或等价地用下面的 SQL 语句：<br/>
<code>
Select * FROM (TABLE SP_SUBORD UNION TABLE SP_MGR)
</code>
(2) <code>INTERSECT (Select * FROM SP_SUBORD)</code><br/>
<code>
INTERSECT (Select * FROM SP_MGR)
</code>
或等价地用下面的 SQL 语句：
<code>
Select * FROM (TABLE SP_SUBORD INTERSECT TABLE SP_MGR)
</code>
或用带 ALL 的 SQL 语句：
<code>
(Select * FROM SP_SUBORD) INTERSECT ALL (Select * FROM SP_MGR)
</code>
或
<code>
Select * FROM (TABLE SP_SUBORD INTERSECT ALL TABLE SP_MGR)
</code>
(3) <code>EXCEPT (Select * FROM SP_MGR)</code>
<code>
EXCEPT (Select * FROM SP_SUBORD)
</code>
或等价地用下面的 SQL 语句：
<code>
Select * FROM (TABLE SP_MGR EXCEPT TABLE SP_ SUBORD)
</code>
或用带 ALL 的 SQL 语句：
<code>
(Select * FROM SP_MGR) EXCEPT ALL (Select * FROM SP_SUBORD)
</code></p>

<p>例 38 查询籍贯为四川、课程成绩在 80 分以上的学生信息及其成绩。（自然连接）
<code>
(Select * FROM STUDENTS Where BPLACE=‘ 四川 ') NATURAL JOIN
(Select * FROM ENROLLS Where GRADE &gt;=80)
</code></p>

<p>例39 列出全部教师的姓名及其任课的课程号、班级。【外连接与外部并外连接允许在结果表中保留非匹配元组，空缺部分填以 NULL 。
外连接的作用是在做连接操作时避免丢失信息。 外连接有 3 类： <br/>
(1)左外连接（ Left Outer Join ）:连接运算谓词为 LEFT [OUTER] JOIN ，其结果表中保留左关系的所有元组。 <br/>
(2)右外连接（ Right Outer Join ）:连接运算谓词为 RIGHT [OUTER] JOIN ，其结果表中保留右关系的所有元组。 <br/>
(3)全外连接（ Full Outer Join ）:连接运算谓词为 FULL [OUTER] JOIN ，其结果表中保留左右两关系的所有元组。】
<code>
Select TNAME, CNO, CLASS FROM TEACHERS LEFT OUTER JOIN TEACHING USING (TNO)
</code></p>

<h2>八、SQL 的数据操纵</h2>

<p>例 40 把教师李映雪的记录加入到教师表 TEACHERS 中。（插入）
<code>
Insert INTO TEACHERS VALUES(1476 ， ' 李映雪 ' ， 44 ， ' 副教授 ')
</code></p>

<p>例 41 成绩优秀的学生将留下当教师。
<code>
Insert INTO TEACHERS (TNO ， TNAME) Select DISTINCT SNO ， SNAME FROM STUDENTS ， ENROLLS
Where STUDENTS.SNO＝ENROLLS.SNO AND GRADE ＞＝ 90
</code></p>

<p>例 42 把所有学生的年龄增加一岁。（修改）
<code>
Update STUDENTS SET AGE ＝ AGE+1
</code></p>

<p>例 43 学生张春明在数据库课考试中作弊，该课成绩应作零分计。
<code>
Update ENROLLS SET GRADE ＝ 0 Where CNO ＝ 'C1' AND
' 张春明 ' ＝ (Select SNAME FROM STUDENTS Where STUDENTS.SNO=ENROLLS.SNO)
</code></p>

<p>例 49 从教师表中删除年龄已到 60 岁的退休教师的数据。（删除）
<code>
Delete FROM TEACHERS Where AGE ＞＝ 60
</code></p>

<h2>九、SQL 的数据控制</h2>

<p>例 50 授予 LILI 有对表 STUDENTS 的查询权。（表／视图特权的授予 一个 SQL 特权允许一个被授权者在给定的数据库对象上进行特定的操作。
授权操作的数据库对象包括：表 / 视图、列、域等。授权的操作包括： Insert 、 Update 、 Delete 、 Select 、 REFERENCES 、 TRIGGER 、
UNDER 、 USAGE 、 EXECUTE 等。其中 Insert 、 Update 、 Delete 、 Select 、 REFERENCES 、 TRIGGER 有对表做相应操作的权限，故称为表特权。）
<code>
GRANT Select ON STUDENTS TO LILI WITH GRANT OPTION
</code></p>

<p>例 51 取消 LILI 的存取 STUDENTS 表的特权。
<code>
REVOKE ALL ON STUDENTS FROM LILI CASCADE
</code></p>

<p>本文来自: 脚本之家<a href="http://www.jb51.net/article/8863_3.htm">www.jb51.net</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Sql Server数据库常用的命令语句]]></title>
    <link href="https://ibillxia.github.io/blog/2009/07/20/useful-commands-in-MS-SQL-Server/"/>
    <updated>2009-07-20T14:44:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2009/07/20/useful-commands-in-MS-SQL-Server</id>
    <content type="html"><![CDATA[<p><strong>1. 查看数据库的版本</strong> <br/>
<code>
select @@version
</code></p>

<p><strong>2. 查看数据库所在机器操作系统参数</strong>
<code>
exec master..xp_msver
</code></p>

<p><strong>3. 查看数据库启动的参数</strong><br/>
<code>
sp_configure
</code></p>

<p><strong>4. 查看数据库启动时间</strong><br/>
<code>
select convert(varchar(30),login_time,120) from   master..sysprocesses where spid=1
</code>
查看数据库服务器名和实例名</p>

<pre><code>print 'Server Name...............: ' + convert(varchar(30),@@SERVERNAME) 
print 'Instance..................: ' + convert(varchar(30),@@SERVICENAME)
</code></pre>



<!--more-->


<p><strong>5. 查看所有数据库名称及大小</strong><br/>
<code>
sp_helpdb
</code><br/>
重命名数据库用的SQL<br/>
<code>
sp_renamedb 'old_dbname', 'new_dbname'
</code></p>

<p><strong>6. 查看所有数据库用户登录信息</strong><br/>
<code>
sp_helplogins
</code><br/>
查看所有数据库用户所属的角色信息
<code>
sp_helpsrvrolemember
</code><br/>
修复迁移服务器时孤立用户时,可以用的 <code>fix_orphan_user</code> 脚本或者 <code>LoneUser</code> 过程<br/>
更改某个数据对象的用户属主<br/>
<code>
sp_changeobjectowner [@objectname =] 'object', [@newowner =] 'owner'
</code>
注意: 更改对象名的任一部分都可能破坏脚本和存储过程。
把一台服务器上的数据库用户登录信息备份出来可以用 <code>add_login_to_aserver</code> 脚本</p></p>

<p><strong>7. 查看链接服务器</strong><br/>
<code>
sp_helplinkedsrvlogin
</code>
查看远端数据库用户登录信息<br/>
<code>
sp_helpremotelogin
</code></p>

<p><strong>8.查看某数据库下某个数据对象的大小</strong><br/>
<code>
sp_spaceused @objname
</code>
还可以用 <code>sp_toptables</code> 过程看最大的N(默认为50)个表<br/>
查看某数据库下某个数据对象的索引信息<br/>
<code>
sp_helpindex @objname
</code>
还可以用 <code>SP_NChelpindex</code> 过程查看更详细的索引情况<br/>
<code>
SP_NChelpindex @objname
</code>
<code>clustered</code> 索引是把记录按物理顺序排列的，索引占的空间比较少。</p>

<p>对键值DML操作十分频繁的表我建议用非 <code>clustered</code> 索引和约束，<code>fillfactor</code> 参数都用默认值。<br/>
查看某数据库下某个数据对象的的约束信息<br/>
<code>
sp_helpconstraint @objname
</code></p>

<p><strong>9.查看数据库里所有的存储过程和函数</strong><br/>
<code>
use @database_name
sp_stored_procedures
</code>
查看存储过程和函数的源代码<br/>
<code>
sp_helptext '@procedure_name'
</code>
查看包含某个字符串@str的数据对象名称<br/>
<code>
select distinct object_name(id) from syscomments where text like '%@str%'
</code>
创建加密的存储过程或函数在AS前面加WITH ENCRYPTION参数<br/>
解密加密过的存储过程和函数可以用 <code>sp_decrypt</code> 过程</p>

<p><strong>10.查看数据库里用户和进程的信息</strong><br/>
<code>
sp_who
</code>
查看SQL Server数据库里的活动用户和进程的信息<br/>
<code>
sp_who 'active'
</code>
查看SQL Server数据库里的锁的情况<br/>
<code>
sp_lock
</code>
进程号1--50是SQL Server系统内部用的,进程号大于50的才是用户的连接进程.<br/>
spid是进程编号,dbid是数据库编号,objid是数据对象编号<br/>
查看进程正在执行的SQL语句<br/>
<code>
dbcc inputbuffer ()  
</code>
推荐大家用经过改进后的 <code>sp_who3</code> 过程可以直接看到进程运行的SQL语句<br/>
<code>
sp_who3  
</code>
检查死锁用 <code>sp_who_lock</code> 过程<br/>
<code>
sp_who_lock
</code></p>

<p><strong>11.收缩数据库日志文件的方法</strong><br/>
收缩简单恢复模式数据库日志，收缩后 <code>@database_name_log</code> 的大小单位为M<br/>
<code>
backup log @database_name with no_log
dbcc shrinkfile (@database_name_log, 5)
</code></p>

<p><strong>12.分析SQL Server SQL 语句的方法</strong> <br/>
<code>
set statistics time {on | off}
set statistics io {on | off}
</code>
图形方式显示查询执行计划<br/>
在查询分析器->查询->显示估计的评估计划(D)-Ctrl-L 或者点击工具栏里的图形 <br/>
文本方式显示查询执行计划<br/>
<code>
set showplan_all {on | off}
set showplan_text { on | off }
set statistics profile { on | off }
</code></p>

<p><strong>13.出现不一致错误时，NT事件查看器里出3624号错误，修复数据库的方法</strong>
先注释掉应用程序里引用的出现不一致性错误的表，然后在备份或其它机器上先恢复然后做修复操作<br/>
<code>
alter database [@error_database_name] set single_user
</code>
修复出现不一致错误的表
<code>
dbcc checktable('@error_table_name',repair_allow_data_loss)
</code>
或者可惜选择修复出现不一致错误的小型数据库名<br/>
<code>
dbcc checkdb('@error_database_name',repair_allow_data_loss)
alter database [@error_database_name] set multi_user
</code>
CHECKDB 有3个参数: <br/>
<code>repair_allow_data_loss</code> 包括对行和页进行分配和取消分配以改正分配错误、结构行或页的错误，以及删除已损坏的文本对象，这些修复可能会导致一些数据丢失。修复操作可以在用户事务下完成以允许用户回滚所做的更改。如果回滚修复，则数据库仍会含有错误，应该从备份进行恢复。如果由于所提供修复等级的缘故遗漏某个错误的修复，则将遗漏任何取决于该修复的修复。修复完成后，请备份数据库。<br/>
<code>repair_fast</code> 进行小的、不耗时的修复操作，如修复非聚集索引中的附加键。 这些修复可以很快完成，并且不会有丢失数据的危险。<br/>
<code>repair_rebuild</code> 执行由 <code>repair_fast</code> 完成的所有修复，包括需要较长时间的修复（如重建索引）。<br/>
执行这些修复时不会有丢失数据的危险。</p></p>
]]></content>
  </entry>
  
</feed>