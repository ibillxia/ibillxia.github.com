<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Tag: 大数据 | Bill's Blog]]></title>
  <link href="https://ibillxia.github.io/blog/tags/da-shu-ju/atom.xml" rel="self"/>
  <link href="https://ibillxia.github.io/"/>
  <updated>2026-07-12T20:22:44+08:00</updated>
  <id>https://ibillxia.github.io/</id>
  <author>
    <name><![CDATA[Bill Xia]]></name>
    <email><![CDATA[ibillxia@gmail.com]]></email>
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[HDFS常用文件类型及压缩算法对比]]></title>
    <link href="https://ibillxia.github.io/blog/2024/05/22/hdfs-compression-algorithms/"/>
    <updated>2024-05-22T12:18:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2024/05/22/hdfs-compression-algorithms</id>
    <content type="html"><![CDATA[<center><img src="/images/2024/IMAG2024052201.webp"></center>


<p>首先我们要明确一点，为什么要进行数据压缩？</p>

<p>Cloudera 提出过数据压缩的基本准则：</p>

<ul>
<li><p>是否压缩数据以及使用何种压缩格式对性能具有重要的影响。</p></li>
<li><p>需要平衡压缩和解压缩数据所需的能力、读写数据所需的磁盘 IO，以及在网络中发送数据所需的网络带宽。</p></li>
</ul>


<p>此外，用到哪些压缩格式，为什么使用这些压缩格式而不是其他的压缩格式？</p>

<!--more-->


<p>主要考虑到：</p>

<ul>
<li><p>文件和压缩算法的组合是否支持可分片, MapReduce在读取数据的时候需要并行, 这就要求压缩后的文件可以分片读取。</p></li>
<li><p>io读取性能, 读取相同信息量的信息, 压缩后的文件不仅占用的存储空间低, 而且还会提高磁盘io的读取效率。提高程序的运行速度</p></li>
<li><p>CPU资源也是启用何种压缩算法不得不考虑的因素, 一般来说压缩效率越高的算法对io效率和存储利用率的提升越有促进作用, 但另一方面也会更高的消耗CPU资源。所以我们需要在这中间寻求一个平衡点。</p></li>
<li><p>共通性, 文件格式是否支持多种语言, 服务的读取。比如Hadoop主要的序列化格式为Writables, 但是Writables只支持Java, 所以后面衍生出了Avro, Thrift等格式。还如OrcFile是对Hive设计的一种列式存储格式, 但是他不支持Impala, 数据的共用性受到了制约。</p></li>
<li><p>错误处理能力, 有的文件的某一部分坏掉之后会影响整个表, 有的只会影响其后的数据, 有的只会影响坏掉数据块本身(Avro)。</p></li>
<li><p>读取和载入效率, RCFile的载入速度慢, 但是查询相应速度快, 相对更适合数据仓库一次插入多次读取的特性。</p></li>
</ul>


<h1>一、HDFS中的文件类型</h1>

<ul>
<li><p>基于文件存储</p></li>
<li><p>序列化和列式存储，例如：Avro、RCFile和Parquet</p></li>
</ul>


<p>下面我们依次来介绍。</p>

<h2>1.1 基于文件的SequenceFile</h2>

<p>sequenceFile文件是Hadoop用来存储二进制形式的[Key,Value]对而设计的一种平面文件(Flat File)。可以把SequenceFile当做是一个容器，把所有的文件打包到SequenceFile类中可以高效的对小文件进行存储和处理。SequenceFile文件并不按照其存储的Key进行排序存储，SequenceFile的内部类Writer提供了append功能。SequenceFile中的Key和Value可以是任意类型Writable或者是自定义Writable。</p>

<p>在存储结构上，SequenceFile主要由一个Header后跟多条Record组成，Header主要包含了Key classname，value classname，存储压缩算法，用户自定义元数据等信息，此外，还包含了一些同步标识，用于快速定位到记录的边界。每条Record以键值对的方式进行存储，用来表示它的字符数组可以一次解析成：记录的长度、Key的长度、Key值和value值，并且Value值的结构取决于该记录是否被压缩。</p>

<p>SequenceFile支持三种记录存储方式：</p>

<ul>
<li><p>无压缩, io效率较差. 相比压缩, 不压缩的情况下没有什么优势.</p></li>
<li><p>记录级压缩, 对每条记录都压缩. 这种压缩效率比较一般.</p></li>
<li><p>块级压缩, 这里的块不同于hdfs中的块的概念. 这种方式会将达到指定块大小的二进制数据压缩为一个块. 相对记录级压缩, 块级压缩拥有更高的压缩效率. 一般来说使用SequenceFile都会使用块级压缩.</p></li>
</ul>


<p>但是SequenceFile只支持Java, SequenceFile一般用来作为小文件的容器使用, 防止小文件占用过多的NameNode内存空间来存储其在DataNode位置的元数据。</p>

<h2>1.2 序列化存储格式和列式存储</h2>

<p>序列化指的是数据格式转化为字节流的过程, 主要用于远程传输或存储. hadoop采用的序列化格式主要是Writables. 但是它只能支持Java语言, 所以后来就出现了Thrift, Avro等格式。</p>

<ul>
<li>Thrift</li>
</ul>


<p>Thrift是Facebook开发的框架，用于实现跨语言提供服务和接口，满足跨平台通信。但是Thrift不支持分片，且缺少MapReduce的原生支持。所以我们可以忽略这个压缩算法。</p>

<ul>
<li>Avro</li>
</ul>


<p>Avro 是 Hadoop 中的一个子项目，也是 Apache 中一个独立的项目，Avro 是一个基于二进制数据传输高性能的中间件。在 Hadoop 的其他项目中，例如 HBase 和 Hive 的 Client 端与服务端的数据传输也采用了这个工具。</p>

<p>Avro是一个语言无关的数据序列化的系统，它的出现主要是为了解决Writables缺少跨语言移植的缺陷。Avro将模式存储在文件头中，所以每个文件都是自描述的，而且Avro还支持模式演进(schema evolution)，也就是说，读取文件的模式不需要与写入文件的模式严格匹配，当有新需求时，可以在模式中加入新的字段。</p>

<pre><code>- Avro支持分片, 即使是进行Gzip压缩之后

- 支持跨语言的支持
</code></pre>

<ul>
<li>ORCFile</li>
</ul>


<p>ORC的全称是(Optimized Row Columnar)，ORC文件格式是一种Hadoop生态圈中的列式存储格式，它的产生早在2013年初，最初产生自Apache Hive，用于降低Hadoop数据存储空间和加速Hive查询速度。和Parquet类似，它并不是一个单纯的列式存储格式，仍然是首先根据行组分割整个表，在每一个行组内进行按列存储。ORC文件是自描述的，它的元数据使用Protocol Buffers序列化，并且文件中的数据尽可能的压缩以降低存储空间的消耗，目前也被Spark SQL、Presto等查询引擎支持。2015年ORC项目被Apache项目基金会提升为Apache顶级项目。ORC具有以下一些优势：</p>

<pre><code>- ORC是列式存储，有多种文件压缩方式，并且有着很高的压缩比

- 文件是可切分（Split）的。因此，在Hive中使用ORC作为表的文件存储格式，不仅节省HDFS存储资源，查询任务的输入数据量减少，使用的MapTask也就减少了

- 提供了多种索引，row group index、bloom filter index

- ORC可以支持复杂的数据结构（比如Map等）

- 支持所有的hive类型, 包括复合类型: structs, lists, maps 和 unions

- 支持分片

- 可以仅返回查询的列, 减少io消耗, 提升性能

- 可以与Zlib, LZO和Snappy结合进一步压缩
</code></pre>

<h2>1.3 常用的存储格式对比</h2>

<p>存储格式的对比</p>

<table>
<thead>
<tr>
<th> 存储格式 </th>
<th> 是否默认 </th>
<th> 存储方式 </th>
<th> 压缩算法 </th>
</tr>
</thead>
<tbody>
<tr>
<td> TextFile </td>
<td> 是 </td>
<td> 行式存储 </td>
<td> Gzip、Bzip2 </td>
</tr>
<tr>
<td> SequenceFile </td>
<td> 否 </td>
<td> 行式存储 </td>
<td> NONE、RECORD、BLOCK </td>
</tr>
<tr>
<td> Parquet </td>
<td> 否 </td>
<td> 列式存储 </td>
<td> Uncompress(默认)、Snappy、Gzip、Lzo </td>
</tr>
<tr>
<td> RCFile </td>
<td> 否 </td>
<td> 数据按行分块，每块按列存储 </td>
<td> - </td>
</tr>
<tr>
<td> ORCFile(RCFile的进化版本) </td>
<td> 否 </td>
<td> 数据按行分块，每块按照列存储 </td>
<td> NONE、ZLIB(默认)、SNAPPY </td>
</tr>
</tbody>
</table>


<p>存储方式的对比</p>

<table>
<thead>
<tr>
<th> 存储方式 </th>
<th> 行查询效率 </th>
<th> 列查询效率 </th>
<th> 压缩效率 </th>
</tr>
</thead>
<tbody>
<tr>
<td> 行式存储 </td>
<td> 高 </td>
<td> 低 </td>
<td> 低 </td>
</tr>
<tr>
<td> 列式存储 </td>
<td> 低 </td>
<td> 高 </td>
<td> 高 </td>
</tr>
</tbody>
</table>


<h1>二、压缩算法</h1>

<h2>2.1 gzip压缩</h2>

<p>优点：压缩率比较高，而且压缩/解压速度也比较快；hadoop本身支持，在应用中处理gzip格式的文件就和直接处理文本一样；有hadoop native库；大部分linux系统都自带gzip命令，使用方便。缺点：不支持split。应用场景：当每个文件压缩之后在130M以内的（1个块大小内），都可以考虑用gzip压缩格式。譬如说一天或者一个小时的日志压缩成一个gzip文件，运行mapreduce程序的时候通过多个gzip文件达到并发。hive程序，streaming程序，和java写的mapreduce程序完全和文本处理一样，压缩之后原来的程序不需要做任何修改。</p>

<h2>2.2 lzo压缩</h2>

<p>优点：压缩/解压速度也比较快，合理的压缩率；支持split，是hadoop中最流行的压缩格式；支持hadoop native库；可以在linux系统下安装lzop命令，使用方便。缺点：压缩率比gzip要低一些；hadoop本身不支持，需要安装；在应用中对lzo格式的文件需要做一些特殊处理（为了支持split需要建索引，还需要指定inputformat为lzo格式）。应用场景：一个很大的文本文件，压缩之后还大于200M以上的可以考虑，而且单个文件越大，lzo优点越明显。</p>

<p>snappy压缩 优点：高速压缩速度和合理的压缩率；支持hadoop native库。缺点：不支持split；压缩率比gzip要低；hadoop本身不支持，需要安装；linux系统下没有对应的命令。应用场景：当mapreduce作业的map输出的数据比较大的时候，作为map到reduce的中间数据的压缩格式；或者作为一个mapreduce作业的输出和另外一个mapreduce作业的输入。</p>

<h2>2.3 bzip2压缩</h2>

<p>优点：支持split；具有很高的压缩率，比gzip压缩率都高；hadoop本身支持，但不支持native；在linux系统下自带bzip2命令，使用方便。缺点：压缩/解压速度慢；不支持native。应用场景：适合对速度要求不高，但需要较高的压缩率的时候，可以作为mapreduce作业的输出格式；或者输出之后的数据比较大，处理之后的数据需要压缩存档减少磁盘空间并且以后数据用得比较少的情况；或者对单个很大的文本文件想压缩减少存储空间，同时又需要支持split，而且兼容之前的应用程序（即应用程序不需要修改）的情况。</p>

<p>最后，我们用一张图来对这4种压缩格式进行比较：</p>

<center><img src="/images/2024/IMAG2024052202.webp"></center>


<h2>2.4 压缩算法对比</h2>

<p>Hive 在处理数据时，一般都需要经历文件读取和文件写出的过程，如果按照原文件的大小进行读写和传输，不仅效率查而且带宽的占用也很高，此时就需要我们这里所讲的 存储格式 和 压缩算法</p>

<h3>不同的存储格式+压缩算法性能比较</h3>

<p>文件存储格式不同对应不同的压缩算法，从而带来不同的性能，我们根据实际使用考虑压缩算法的性能，主要通过以下 3 个指标：</p>

<p><strong>① 压缩比</strong></p>

<p>压缩比越高，压缩后文件越小，所以压缩比越高越好；</p>

<p>压缩比越高，存储磁盘空间占用越小，可以降低单节点的磁盘IO，同时可以减少占用的带宽；</p>

<p><strong>② 压缩时间</strong></p>

<p>越快越好，加快数据在Hadoop集群流动的速度和磁盘 IO 的速度</p>

<p><strong>③ 压缩后的文件是否可以再分割</strong></p>

<p>可以分割的格式允许单一文件由多个Mapper程序处理，可以更好的并行化</p>

<table>
<thead>
<tr>
<th> 压缩方式 </th>
<th> 压缩比 </th>
<th> 压缩速度 </th>
<th> 解压缩速度 </th>
<th> 是否可分割 </th>
</tr>
</thead>
<tbody>
<tr>
<td> gzip </td>
<td> 13.4% </td>
<td> 21 MB/s </td>
<td> 118 MB/s </td>
<td> 否 </td>
</tr>
<tr>
<td> bzip2 </td>
<td> 13.2% </td>
<td> 2.4MB/s </td>
<td> 9.5MB/s </td>
<td> 是 </td>
</tr>
<tr>
<td> lzo </td>
<td> 20.5% </td>
<td> 135 MB/s </td>
<td> 410 MB/s </td>
<td> 是 </td>
</tr>
<tr>
<td> snappy </td>
<td> 22.2% </td>
<td> 172 MB/s </td>
<td> 409 MB/s </td>
<td> 否 </td>
</tr>
</tbody>
</table>


<h1>三、使用总结</h1>

<pre><code>*# 压缩比比较*ORC &gt;  PARQUET &gt; TEXTFILE*# 查询速度*ORC &gt;  TEXTFILE &gt; PARQUET*# 存储格式为ORC,不同的压缩算法比较压缩后的文件大小*ZLIB &lt; SNAPPY &lt; 非压缩
</code></pre>

<h2>① 数据量较大</h2>

<p>如果单个文件比较大，可以使用 Parquet 存储，lzo 压缩，可以避免由于读取不可分割大文件引发的数据倾斜</p>

<h2>② 计算逻辑比较少</h2>

<p>使用 ORC 存储 + ZLIB 压缩，可以尽量减少占用存储空间</p>

<h2>③ 计算逻辑比较多</h2>

<p>使用 ORC 存储 + SNAPPY 压缩，可以提高读写速度，从而提高整体计算性能</p>

<h1>参考资料</h1>

<ol>
<li><a href="https://cloud.tencent.com/developer/article/1777790">HDFS常用文件类型及压缩算法对比</a></li>
</ol>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Spark与Flink：下一代大数据计算引擎之争]]></title>
    <link href="https://ibillxia.github.io/blog/2020/05/31/spark-vs-flink-comparison/"/>
    <updated>2020-05-31T16:37:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2020/05/31/spark-vs-flink-comparison</id>
    <content type="html"><![CDATA[<h1>一、下一代大数据计算引擎</h1>

<p>自从数据处理需求超过了传统数据库能有效处理的数据量之后，Hadoop 等各种基于 MapReduce 的海量数据处理系统应运而生。从 2004 年 Google 发表 MapReduce 论文开始，经过近 10 年的发展，基于 Hadoop 开源生态或者其它相应系统的海量数据处理已经成为业界的基本需求。</p>

<p>但是，很多机构在开发自己的数据处理系统时都会发现需要面临一系列的问题。从数据中获取价值需要的投入远远超过预期。常见的问题包括：</p>

<ul>
<li>非常陡峭的学习曲线。刚接触这个领域的人经常会被需要学习的技术的数量砸晕。不像经过几十年发展的数据库一个系统可以解决大部分数据处理需求， Hadoop 等大数据生态里的一个系统往往在一些数据处理场景上比较擅长，另一些场景凑合能用，还有一些场景完全无法满足需求。结果就是需要好几个系统来处理不同的场景。</li>
</ul>


<center><img src="/images/2020/IMAG2020053101.png"></center>


<p>（来源： https://mapr.com/developercentral/lambda-architecture/ ）</p>

<!--more-->


<p>上图是一个典型的 lambda 架构，只是包含了批处理和流处理两种场景，就已经牵涉到至少四五种技术了，还不算每种技术的可替代选择。再加上实时查询，交互式分析，机器学习等场景，每个场景都有几种技术可以选择，每个技术涵盖的领域还有不同方式的重叠。结果就是一个业务经常需要使用四五种以上的技术才能支持好一个完整的数据处理流程。加上调研选型，需要了解的数目还要多得多。</p>

<p>下图是大数据领域的全景。有没有晕？</p>

<center><img src="/images/2020/IMAG2020053102.png"></center>




<center>大数据和 AI 全景 – 2018</center>


<p><br></p>

<ul>
<li><p>开发和运行效率低下。因为牵涉到多种系统，每种系统有自己的开发语言和工具，开发效率可想而知。而因为采用了多套系统，数据需要在各个系统之间传输，也造成了额外的开发和运行代价，数据的一致也难以保证。在很多机构，实际上一半以上的开发精力花在了数据在各个系统之间的传输上。</p></li>
<li><p>复杂的运维。多个系统，每个需要自己的运维，带来更高的运维代价的同时也提高了系统出问题的可能。</p></li>
<li><p>数据质量难以保证。数据出了问题难以跟踪解决。</p></li>
<li><p>最后，还有人的问题。在很多机构，由于系统的复杂性，各个子系统的支持和使用落实在不同部门负责。</p></li>
</ul>


<p>了解了这些问题以后，对 Spark 从 2014 年左右开始迅速流行就比较容易理解了。Spark 在当时除了在某些场景比 Hadoop MapReduce 带来几十到上百倍的性能提升外，还提出了用一个统一的引擎支持批处理，流处理，交互式查询，机器学习等常见的数据处理场景。看过在一个 Notebook 里完成上述所有场景的 Spark 演示，对比之前的数据流程开发，对很多开发者来说不难做出选择。经过几年的发展，Spark 已经被视为可以完全取代 Hadoop 中的 MapReduce 引擎。</p>

<p>正在 Spark 如日中天高速发展的时候，2016 年左右 Flink 开始进入大众的视野并逐渐广为人知。为什么呢？原来在人们开始使用 Spark 之后，发现 Spark 虽然支持各种场见场景，但并不是每一种都同样好用。数据流的实时处理就是其中相对较弱的一环。Flink 凭借更优的流处理引擎，同时也支持各种处理场景，成为 Spark 的有力挑战者。</p>

<p>Spark 和 Flink 是怎么做到这些的，它们之间又有那些异同，下面我们来具体看一下。</p>

<h1>二、Spark 和 Flink 的引擎技术</h1>

<p>这一部分主要着眼于 Spark 和 Flink 引擎的架构方面，更看重架构带来的潜力和限制。现阶段的实现成熟度和局限会在后续生态部分探讨。</p>

<h2>2.1 数据模型和处理模型</h2>

<p>要理解 Spark 和 Flink 的 引擎特点，首先从数据模型开始。</p>

<p>Spark 的数据模型是弹性分布式数据集 RDD（Resilient Distributed Datasets）。 比起 MapReduce 的文件模型，RDD 是一个更抽象的模型，RDD 靠血缘（lineage） 等方式来保证可恢复性。很多时候 RDD 可以实现为分布式共享内存或者完全虚拟化（即有的中间结果 RDD 当下游处理完全在本地时可以直接优化省略掉）。这样可以省掉很多不必要的 I/O，是早期 Spark 性能优势的主要原因。</p>

<p>Spark 用 RDD 上的变换（算子）来描述数据处理。每个算子（如 map，filter，join）生成一个新的 RDD。所有的算子组成一个有向无环图（DAG）。Spark 比较简单地把边分为宽依赖和窄依赖。上下游数据不需要 shuffle 的即为窄依赖，可以把上下游的算子放在一个阶段（stage） 里在本地连续处理，这时上游的结果 RDD 可以 省略。下图展示了相关的基本概念。更详细的介绍在网上比较容易找到，这里就不花太多篇幅了。</p>

<center><img src="/images/2020/IMAG2020053103.png"></center>




<center> Spark DAG</center>


<p><br></p>

<p>Flink 的基本数据模型是数据流，及事件（Event） 的序列。数据流作为数据的基本模型可能没有表或者数据块直观熟悉，但是可以证明是完全等效的。流可以是无边界的无限流，即一般意义上的流处理。也可以是有边界的有限流，这样就是批处理。</p>

<p>Flink 用数据流上的变换（算子）来描述数据处理。每个算子生成一个新的数据流。在算子，DAG，和上下游算子链接（chaining） 这些方面，和 Spark 大致等价。Flink 的节点（vertex）大致相当于 Spark 的阶段（stage），划分也会和上图的 Spark DAG 基本一样。</p>

<center><img src="/images/2020/IMAG2020053104.png"></center>




<center> Flink 任务图</center>


<p><br></p>

<p>在 DAG 的执行上，Spark 和 Flink 有一个比较显著的区别。 在 Flink 的流执行模式中，一个事件在一个节点处理完后的输出就可以发到下一个节点立即处理。这样执行引擎并不会引入额外的延迟。与之相应的，所有节点是需要同时运行的。而 Spark 的 micro batch 和一般的 batch 执行一样，处理完上游的 stage 得到输出之后才开始下游的 stage。</p>

<p>在 Flink 的流执行模式中，为了提高效率也可以把多个事件放在一起传输或者计算。但这完全是执行时的优化，可以在每个算子独立决定，也不用像 RDD 等批处理模型中一样和数据集边界绑定，可以做更加灵活的优化同时可以兼顾低延迟需求。</p>

<p>Flink 使用异步的 checkpoint 机制来达到任务状态的可恢复性，以保证处理的一致性，所以在处理的主流程上可以做到数据源和输出之间数据完全不用落盘，达到更高的性能和更低的延迟。</p>

<h2>2.2 数据处理场景</h2>

<p>除了批处理之外，Spark 还支持实时数据流处理，交互式查询，和机器学习，图计算等。</p>

<center><img src="/images/2020/IMAG2020053105.png"></center>


<p>（来源： https://databricks.com/spark/about ）</p>

<ul>
<li><p>实时数据流处理和批处理主要区别就是对低延时的要求。Spark 因为 RDD 是基于内存的，可以比较容易切成较小的块来处理。如果能对这些小块处理得足够快，就能达到低延时的效果。</p></li>
<li><p>交互式查询场景，如果数据能全在内存，处理得足够快的话，就可以支持交互式查询。</p></li>
<li><p>机器学习和图计算其实是和前几种场景不同的 RDD 算子类型。Spark 提供了库来支持常用的操作，用户或者第三方库也可以自己扩展。值得一提的是，Spark 的 RDD 模型和机器学习模型训练的迭代计算非常契合，从一开始就在有的场景带来了非常显著的性能提升。</p></li>
</ul>


<p>从这些可以看出来，比起 Hadoop MapReduce， Spark 本质上就是基于内存的更快的批处理。然后用足够快的批处理来实现各种场景。</p>

<center><img src="/images/2020/IMAG2020053106.png"></center>


<p>（来源： https://www.slideshare.net/ParisCarbone/state-management-in-apache-flink-consistent-stateful-distributed-stream-processing ）</p>

<p>前面说过，在 Flink 中，如果输入数据流是有边界的，就自然达到了批处理的效果。这样流和批的区别完全是逻辑上的，和处理实现独立，用户需要实现的逻辑也完全一样，应该是更干净的一种抽象。后续会在深入对比流计算方面的时候做更深入的讨论。</p>

<p>Flink 也提供了库来支持机器学习，图计算等场景。从这方面来说和 Spark 没有太大区别。</p>

<p>一个有意思的事情是用 Flink 的底层 API 可以支持只用 Flink 集群实现一些数据驱动的分布式服务。有一些公司用 Flink 集群实现了社交网络，网络爬虫等服务。这个也体现了 Flink 作为计算引擎的通用性，并得益于 Flink 内置的灵活的状态支持。</p>

<p>总的来说，Spark 和 Flink 都瞄准了在一个执行引擎上同时支持大多数数据处理场景，也应该都能做到这一点。主要区别就在于因为架构本身的局限在一些场景会受到限制。比较突出的地方就是 Spark Streaming 的 micro batch 执行模式。Spark 社区应该也意识到了这一点，最近在持续执行模式（continuous processing）方面开始发力。 具体情况会在后面介绍。</p>

<h2>2.3 有状态处理 （Stateful Processing）</h2>

<p>Flink 还有一个非常独特的地方是在引擎中引入了托管状态（managed state）。要理解托管状态，首先要从有状态处理说起。如果处理一个事件（或一条数据）的结果只跟事件本身的内容有关，称为无状态处理；反之结果还和之前处理过的事件有关，称为有状态处理。稍微复杂一点的数据处理，比如说基本的聚合，都是有状态处理。Flink 很早就认为没有好的状态支持是做不好留处理的，因此引入了 managed state 并提供了 API 接口。</p>

<center><img src="/images/2020/IMAG2020053107.png"></center>




<center>Flink 中的状态支持</center>


<p><br></p>

<p>一般在流处理的时候会比较关注有状态处理，但是仔细看的话批处理也是会受到影响的。比如常见的窗口聚合，如果批处理的数据时间段比窗口大，是可以不考虑状态的，用户逻辑经常会忽略这个问题。但是当批处理时间段变得比窗口小的时候，一个批的结果实际上依赖于以前处理过的批。这时，因为批处理引擎一般没有这个需求不会有很好的内置支持，维护状态就成为了用户需要解决的事情。比如窗口聚合的情况用户就要加一个中间结果表记住还没有完成的窗口的结果。这样当用户把批处理时间段变短的时候就会发现逻辑变复杂了。这是早期 Spark Streaming 用户 经常碰到的问题。直到 Structured Streaming 出来才得到缓解。</p>

<p>而像 Flink 这样以流处理为基本模型的引擎，因为一开始就避不开这个问题，所以引入了 managed state 来提供了一个通用的解决方案。比起用户实现的特定解决方案，不但用户开发更简单，而且能提供更好的性能。最重要的是能更好地保证处理结果的一致性。</p>

<p>简单来说，就是有一些內秉的数据处理逻辑，在批处理中容易被忽略或简化处理掉也能得到可用的结果，而在流处理中问题被暴露出来解决掉了。所以流计算引擎用有限流来处理批在逻辑上比较严谨，能自然达到正确性。主要做一些不同的实现来优化性能就可以了。而用更小的批来模拟流需要处理一些以前没有的问题。当计算引擎还没有通用解决方案的时候就需要用户自己解决了。类似的问题还有维表的变化（比如用户信息的更新），批处理数据的边界和迟到数据等等。</p>

<h2>2.4 编程模型</h2>

<center><img src="/images/2020/IMAG2020053108.png"></center>




<center>Spark 1.6 时的 API 状态</center>


<p><br></p>

<p>Spark 的初衷之一就是用统一的编程模型来解决用户的各种需求。在这方面一直很下功夫。最初基于 RDD 的 API 就可以做各种类型的数据处理。后来为了简化用户开发，逐渐推出了更高层的 DataFrame（在 RDD 中加了列变成结构化数据）和 Datasets（在 DataFrame 的列上加了类型），并在 Spark 2.0 中做了整合（DataFrame = DataSet[Row]）。Spark SQL 的支持也比较早就引入了。在加上各个处理类型 API 的不断改进，比如 Structured Streaming 以及和机器学习深度学习的交互，到了今天 Spark 的 API 可以说是非常好用的，也是 Spark 最强的方面之一。</p>

<center><img src="/images/2020/IMAG2020053109.png"></center>




<center>Spark 2.0 API</center>


<p><br></p>

<p>Flink 的 API 也有类似的目标和发展路线。Flink 和 Spark 的核心 API 可以说是可以基本对应的。今天 Spark API 总体上更完备一下，比如说最近一两年大力投入的和机器学习深度学习的整合方面。Flink 在流处理相关的方面还是领先一些，比如对 watermark，window，trigger 的各种支持。</p>

<center><img src="/images/2020/IMAG2020053110.png"></center>




<center>Flink API</center>


<p><br></p>

<h2>2.5 小结</h2>

<p>Spark 和 Flink 都是通用的能够支持超大规模数据处理，支持各种处理类型的计算引擎。两个系统都有很多值得探讨的方面在这里没有触及，比如 SQL 的优化，和机器学习的集成等等。这里主要是试图从最基本的架构和设计方面来比较一下两个系统。因为上层的功能在一定程度上是可以互相借鉴的，有足够的投入应该都能做好。而基本的设计改变起来会伤筋动骨，更困难一些。</p>

<p>Spark 和 Flink 的不同执行模型带来的最大的区别应该还是在对流计算的支持上。最开始的 Spark Streaming 对流计算想得过于简单，对复杂一点的计算用起来会有不少问题。从 Spark 2.0 开始引入的 Structured Streaming 重新整理了流计算的语义，支持按事件时间处理和端到端的一致性。虽然在功能上还有不少限制，比之前已经有了长足的进步。不过 micro batch 执行方式带来的问题还是存在，特别在规模上去以后性能问题会比较突出。最近 Spark 受一些应用场景的推动，也开始开发持续执行模式。2.3 里的实验性发布还只支持简单的 map 类的操作。从最近 Spark+AI Summit 大会上的介绍来看（下图），会发展成一个和 Flink 的流处理模式比较相似的执行引擎。不过从下图来看，主要的功能都还在开发中或者待开发。对将来能做到什么程度，和 Spark 原来的 batch 执行引擎怎么结合，我们拭目以待。</p>

<center><img src="/images/2020/IMAG2020053111.png"></center>




<center>Spark 持续执行模式状态</center>


<p><br></p>

<h1>三、生态和未来</h1>

<h2>3.1 概况</h2>

<center><img src="/images/2020/IMAG2020053112.png"></center>


<p><br></p>

<p>Spark 是最活跃的 Apache 项目之一。从 2014 年左右开始得到广泛关注。Spark 的开源社区一度达到上千的活跃贡献者。最主要推动者是 Databricks，由最初的 Spark 创造者们成立的公司。今年 6 月的 Spark+AI 峰会参加人数超过 4000。 Spark 因为在引擎方面比 MapReduce 全面占优，经过几年发展和 Hadoop 生态结合较好，已经被广泛视为 Hadoop MapReduce 引擎的取代者。</p>

<center><img src="/images/2020/IMAG2020053113.png"></center>


<p><br></p>

<p>Flink 也是 Apache 顶级项目，创始者们成立了 Data Artisans。社区规模还无法和 Spark 相比。不过在业界，特别是流处理方面，有不错的口碑。在大规模流处理方面走在最前沿，也是需求最强的几个美国公司，包括 Netflix、 LinkedIn、Uber、Lyft 等，除 LinkedIn 有自己的 Samza 外，都已经采用 Flink 作为流处理引擎或者有了较大投入。</p>

<p>阿里集团在 Flink 社区也有较大影响力。最近 Flink 1.3 到 1.5 里都有几个重磅功能是阿里和 Data Artisans 合作或者独立开发的。阿里还有可能是世界上最大的流计算集群，也是在 Flink 的基础上开发的。</p>

<h2>3.2 Unified Analytic platform – 包含机器学习的统一分析平台</h2>

<p>最近的 Spark+AI 峰会上， Databricks 主打的主题是统一分析平台（Unified Analytics Platform）。三大新发布：Databricks delta、Databricks Runtime for ML 和 ML flow，都是围绕这一主题。随着近年来机器学习（包括深度学习）在数据处理中占比越来越高，可以说 Databricks 又一次把握住了时代的脉搏。</p>

<p>统一分析平台回应了 Spark 的初衷。经过几年的探索，对初始问题，即用户可以在一个系统里解决绝大部分大数据的需求，有了一个比较明确具体的解决方案。</p>

<center><img src="/images/2020/IMAG2020053114.png"></center>


<p><br></p>

<p>不过有意思的是可以看出 Databricks 在 AI 方面策略的转变。在深度学习流行前，Spark 自带的 MLLib 功能上应该是够用的，但是可能是由于兼容性原因并没有取得预期中的广泛采用。</p>

<p>对深度学习的新宠 TensorFlow，Spark 曾经推出过 TensorFrames 和 Spark 引擎做了一些集成。结果应该不是很成功，可能还没有 Yahoo 从外面搭建的 TensorFlowOnSpark 影响力大。</p>

<p>从这次来看，Spark 转向了集成的策略。Databricks Runtime for ML 实际上就是预装了各个机器学习框架，然后支持在 Spark 任务里启动一个比如 TensorFlow 自己的集群。Spark 引擎方面做的主要改进就是 gang scheduling，即支持一次申请多个 executor 以便 TensorFlow 集群能正常启动。</p>

<p>MLFlow 更是和 Spark 引擎无关。作为一个工作流工具，MLFlow 的目标是帮助数据科学家提高工作效率。主要功能是以项目为单位记录和管理所做的机器学习试验，并支持分享。设计要点是可重复试验，以及对各种工具的灵活易用的支持。看起来 Spark 暂时在作为 AI 引擎方面可能没什么大动作了。</p>

<p>Flink 的目标其实和 Spark 很相似。包含 AI 的统一平台也是 Flink 的发展方向。Flink 从技术上也是可以支持较好的机器学习集成和整条链路的，而且有一些大规模线上学习的使用实例。不过看起来在现阶段 Flink 这方面的平台化还没有 Spark 成熟。值得一提的是 Flink 由于流处理引擎的优势，在线上学习方面可能能支持得更好一些。</p>

<h2>3.3 数据使用者</h2>

<p>产品和生态归根结底是要解决大数据使用者的问题，从数据中产生价值。了解数据的使用者和他们的需求可以帮助我们在在讨论生态的各方面时有一个比较清晰的脉络。</p>

<p>数据相关的工作者大致可以分为以下角色。实际情况中一个组织里很可能几个角色在人员上是重合的。各个角色也没有公认的定义和明确的界限。</p>

<center><img src="/images/2020/IMAG2020053115.png"></center>


<p><br></p>

<ul>
<li><p>数据采集：在产品和系统中合适的地方产生或收集数据发送到数据平台。</p></li>
<li><p>平台：提供数据导入，存储，计算的环境和工具等等。</p></li>
<li><p>数据工程师：使用数据平台把原始数据加工成可以供后续高效使用的数据集。把分析师和数据科学家创建的指标和模型等等生产化成为高效可靠的的自动处理。</p></li>
<li><p>数据分析师和数据科学家（关于这两者的异同有很多讨论。感兴趣的可以自行搜索。 https://www.jianshu.com/p/cfd94d9e4466 这里的译文可以提供一个视角）：为数据赋予意义，发现内含的价值。 下文再不特别区分的地方统称为数据分析。</p></li>
<li><p>产品经理，管理和决策层：根据以上产生的数据调整产品和组织行为。</p></li>
</ul>


<p>这些构成了一个完整的环。上面的顺序是数据流动的方向，而需求的驱动是反过来的方向。</p>

<p>本文所说的 Spark 和 Flink 的生态主要是对应到数据平台这一层。直接面向的用户主要是数据工程师、数据分析师和数据科学家。好的生态能够大大简化数据平台和数据工程师的工作，并使得数据分析师和数据科学家更加自主化同时提高效率。</p>

<h2>3.4 开发环境</h2>

<h3>API</h3>

<p>从 API 上来看，Spark 和 Flink 提供的功能领域大致相当。当然具体看各个方向支持的程度会有差异。总体来看 Spark 的 API 经过几轮迭代，在易用性，特别是机器学习的集成方面，更强一些。Flink 在流计算方面更成熟一些。</p>

<center><img src="/images/2020/IMAG2020053116.png"></center>


<p><br></p>

<p>支持的语言也大致相当。Spark 发展的时间长一些还是有优势，特别是数据分析常用的 Python 和 R。</p>

<center><img src="/images/2020/IMAG2020053117.png"></center>


<p><br></p>

<h3>Connectors</h3>

<p>有了 API，再有数据就可以开工了。Spark 和 Flink 都能对接大部分比较常用的系统。如果暂时还没有支持的，也都能比较好地支持自己写一个 connector。</p>

<center><img src="/images/2020/IMAG2020053118.png"></center>


<p>https://databricks.com/spark/about </p>

<center><img src="/images/2020/IMAG2020053119.png"></center>


<p>https://www.slideshare.net/chobeat/data-intensive-applications-with-apache-flink</p>

<h2>3.5 集成开发工具</h2>

<p>这方面数据工程师和数据分析的需求有一些不同。</p>

<p>数据分析的工作性质比较偏探索性，更强调交互性和分享。Notebook 能比较好地满足这些需求，是比较理想的开发工具，用来做演示效果也相当不错。比较流行的 Notebook 有 Apache Zeppelin，Jupyter 等。Databricks 更是自己开发了 Databricks Notebook 并将之作为服务的主要入口。Zeppelin 支持 Spark 和 Flink，Jupyter 还只支持 Spark。</p>

<p>数据工程师的工作更倾向于把比较确定的数据处理生产化，能快速把代码写出来是一方面。另外还有项目管理，版本管理，测试，配置，调试，部署，监控等等工作，需求和传统的集成开发工具比较相似。 还经常出现需要复用已有的业务逻辑代码库的情况。Notebook 对其中一些需求并不能很好地满足。比较理想的开发工具可能是类似 IntelliJ 加上 Spark/Flink 插件，再加上一些插件能直接提交任务到集群并进行调试，并对接 Apache Oozie 之类的工作流管理等等。在开源社区还没有见到能把这些集成到一起的。在商业产品中倒是见过一些比较接近的。Spark 和 Flink 在这方面差不多。</p>

<h2>3.6 运行环境</h2>

<h3>部署模式 / 集群管理 / 开源闭源</h3>

<p>应用开发完后要提交到运行环境。Spark 和 Flink 都支持各种主流的部署环境，在这方面都算做得比较好的。</p>

<center><img src="/images/2020/IMAG2020053120.png"></center>


<p><br></p>

<h2>3.7 企业级平台</h2>

<p>既然 Spark 和 Flink 都支持各种部署方式，那一个企业是否可以使用开源代码快速搭建一个支持 Spark 或者 Flink 的平台呢？</p>

<p>这个要看想要达到什么效果了。最简单的模式可能是给每个任务起一个独占集群，或着给小团队一个独立集群。这个确实可以很快做到，但是用户多了以后，统一运维的成本可能太高，需要用户参与运维。还有一个缺点是资源分配固定，而负载会有变化，导致资源利用率上不去。比较理想的是多租户的共享大集群，可以提高运维效率的同时最大限度地提高资源利用率。而这就需要一系列的工作，比如不同的作业提交方式，数据安全与隔离等等。对一些企业来说，可能利用托管服务（包括云服务）是一种值得考虑的开始方式。</p>

<h2>3.8 社区</h2>

<p>Spark 社区在规模和活跃程度上都是领先的，毕竟多了几年发展时间。而且作为一个德国公司，Data Artisans 想在美国扩大影响力要更难一些。不过 Flink 社区也有一批稳定的支持者，达到了可持续发展的规模。</p>

<p>在中国情况可能会不一样一些。比起美国公司，中国公司做事情速度更快，更愿意尝试新技术。中国的一些创新场景也对实时性有更高的需求。这些都对 Flink 更友好一些。</p>

<p>近期 Flink 的中国社区有一系列动作，是了解 Flink 的好机会。</p>

<p>Spark 的中文文档在 http://www.apachecn.org/bigdata/spark/27.html 。</p>

<p>Flink 的中文社区在 http://flink-china.org/ 。</p>

<p>另外，今年年底 Flink 中文社区也会在北京举办 Flink Forward China 大会，感兴趣的朋友可以关注。</p>

<h2>3.9 未来发展趋势</h2>

<p>近两年一个明显的趋势就是机器学习在数据处理中的比重增长。Spark 和 Flink 都能支持在一个系统中做机器学习和其它数据处理。谁能做得更好就能掌握先机。</p>

<p>另一个可能没有那么明显的趋势是，随着 IOT 的增长以及计算资源和网络的持续发展，实时处理需求会越来越多。现在其实真正对低延迟有很高追求的业务并没有那么多，所以每一次流计算新技术的出现都能看到那几家公司的身影。随着新应用场景的出现和竞争环境的发展，实时处理可能会变得越来越重要。Flink 现在在这方面是领先的，如果发挥得好可以成为核心优势。</p>

<p>还有一点值得一提的是，因为用户不想锁定供应商，担心持续的支持等原因，是否开源已经成为用户选择数据产品的一个重要考量。闭源产品如果没有决定性优势会越来越难和基于开源技术的产品竞争。</p>

<h1>四、总结</h1>

<p>Spark 和 Flink 都是通用的开源大规模处理引擎，目标是在一个系统中支持所有的数据处理以带来效能的提升。两者都有相对比较成熟的生态系统。是下一代大数据引擎最有力的竞争者。Spark 的生态总体更完善一些，在机器学习的集成和易用性上暂时领先。Flink 在流计算上有明显优势，核心架构和模型也更透彻和灵活一些。在易用性方面两者也都还有一些地方有较大的改进空间。接下来谁能尽快补上短板发挥强项就有更多的机会。</p>

<p><br><br>
作者介绍: 王海涛，曾经在微软的 SQL  Server 和大数据平台组工作多年。带领团队建立了微软对内的 Spark 服务，主打 Spark Streaming。去年加入阿里实时计算部门，参与改进阿里基于 Apache Flink 的 Blink 平台。</p>

<p>原文链接：</p>

<p><a href="https://flink-learning.org.cn/article/detail/5910c07489b5411c32f22963e06ddeed">Spark VS Flink 下一代大数据计算引擎之争，谁主浮沉（上）</a></p>

<p><a href="https://flink-learning.org.cn/article/detail/4f8e1cf7a9bc160f96ff04cb91b7da49">比拼生态和未来，Spark 和 Flink 哪家强（下）</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[ClickHouse基础知识与实践]]></title>
    <link href="https://ibillxia.github.io/blog/2019/10/08/clickhouse-basics/"/>
    <updated>2019-10-08T16:27:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2019/10/08/clickhouse-basics</id>
    <content type="html"><![CDATA[<center><img src="/images/2019/IMAG2019100801.png"></center>


<p><br></p>

<h1>一、简介</h1>

<p>Clickhouse是一个用于联机分析处理（OLAP）的列式数据库管理系统（columnar DBMS）。</p>

<p>传统数据库在数据大小比较小，索引大小适合内存，数据缓存命中率足够高的情形下能正常提供服务。但残酷的是，这种理想情形最终会随着业务的增长走到尽头，查询会变得越来越慢。你可能通过增加更多的内存，订购更快的磁盘等等来解决问题（纵向扩展），但这只是拖延解决本质问题。如果你的需求是解决怎样快速查询出结果，那么ClickHouse也许可以解决你的问题。</p>

<!--more-->


<h2>应用场景</h2>

<p>1.绝大多数请求都是用于读访问的</p>

<p>2.数据需要以大批次（大于1000行）进行更新，而不是单行更新；或者根本没有更新操作</p>

<p>3.数据只是添加到数据库，没有必要修改</p>

<p>4.读取数据时，会从数据库中提取出大量的行，但只用到一小部分列</p>

<p>5.表很“宽”，即表中包含大量的列</p>

<p>6.查询频率相对较低（通常每台服务器每秒查询数百次或更少）</p>

<p>7.对于简单查询，允许大约50毫秒的延迟</p>

<p>8.列的值是比较小的数值和短字符串（例如，每个URL只有60个字节）</p>

<p>9.在处理单个查询时需要高吞吐量（每台服务器每秒高达数十亿行）</p>

<p>10.不需要事务</p>

<p>11.数据一致性要求较低</p>

<p>12.每次查询中只会查询一个大表。除了一个大表，其余都是小表</p>

<p>13.查询结果显著小于数据源。即数据有过滤或聚合。返回结果不超过单个服务器内存大小</p>

<h2>使用限制</h2>

<p>1.不支持真正的删除/更新支持 不支持事务（期待后续版本支持）</p>

<p>2.不支持二级索引</p>

<p>3.有限的SQL支持，join实现与众不同</p>

<p>4.不支持窗口功能</p>

<p>5.元数据管理需要人工干预维护</p>

<h1>二、基础用法</h1>

<h2>常用SQL语法</h2>

<ul>
<li>列出数据库列表 show databases;</li>
<li>列出数据库中表列表 show tables;</li>
<li>创建数据库 create database test;</li>
<li>删除一个表 drop table if exists test.t1;</li>
<li>创建第一个表 create /<em>temporary</em>/ table /<em>if not exists</em>/ test.m1 ( id UInt16,name String) ENGINE = Memory;</li>
<li>插入测试数据 insert into test.m1 (id, name) values (1, 'abc'), (2, 'bbbb');</li>
<li>查询 select * from test.m1;</li>
</ul>


<h2>默认值</h2>

<p>默认值 的处理方面， ClickHouse 中，默认值总是有的，如果没有显示式指定的话，会按字段类型处理：</p>

<p>数字类型， 0</p>

<p>字符串，空字符串</p>

<p>数组，空数组</p>

<p>日期， 0000-00-00</p>

<p>时间， 0000-00-00 00:00:00</p>

<p>注：NULLs 是不支持的</p>

<h2>数据类型</h2>

<p>1.整型：UInt8,UInt16,UInt32,UInt64,Int8,Int16,Int32,Int64</p>

<p>范围U开头-2N/2~2N-1;非U开头0～2<sup>N</sup>-1</p>

<p>2.枚举类型：Enum8,Enum16</p>

<p>Enum('hello'=1,'test'=-1),Enum是有符号的整型映射的，因此负数也是可以的</p>

<p>3.字符串型：FixedString(N),String</p>

<p>N是最大字节数，不是字符长度，如果是UTF8字符串，那么就会占3个字节，GBK会占2字节;String可以用来替换VARCHAR,BLOB,CLOB等数据类型</p>

<p>4.时间类型：Date</p>

<p>5.数组类型：Array(T)</p>

<p>T是一个基本类型，包括arry在内，官方不建议使用多维数组</p>

<p>6.元组：Tuple</p>

<p>7.结构：Nested(name1 Type1,name2 Type2,...)</p>

<p>一种类似map的结结构</p>

<h2>物化列</h2>

<p>指定 MATERIALIZED 表达式，即将一个列作为物化列处理了，这意味着这个列的值不能从insert 语句获取，只能是自己计算出来的。同时，</p>

<p>物化列也不会出现在 select * 的结果中：</p>

<pre><code>drop table if exists test.m2;
create table test.m2 ( a MATERIALIZED (b+1),b UInt16) ENGINE = Memory;
insert into test.m2 (b) values (1);
select * from test.m2;select a, b from test.m2;
</code></pre>

<h2>表达式列</h2>

<p>ALIAS 表达式列某方面跟物化列相同，就是它的值不能从 insert 语句获取。不同的是， 物化列 是会真正保存数据（这样查询时不需要再计算），</p>

<p>而表达式列不会保存数据（这样查询时总是需要计算），只是在查询时返回表达式的结果。</p>

<pre><code>create table test.m3 (a ALIAS (b+1), b UInt16) ENGINE = Memory;
insert into test.m3(b) values (1);
select * from test.m3;
select a, b from test.m3;
</code></pre>

<h1>三、引擎/engine</h1>

<p>引擎是clickhouse设计的精华部分</p>

<h2>3.1 TinyLog</h2>

<p>最简单的一种引擎，每一列保存为一个文件，里面的内容是压缩过的，不支持索引</p>

<p>这种引擎没有并发控制，所以，当你需要在读，又在写时，读会出错。并发写，内容都会坏掉。</p>

<p>应用场景:</p>

<ol type="a">
<li><p>基本上就是那种只写一次</p></li>
<li><p>然后就是只读的场景。</p></li>
<li><p>不适用于处理量大的数据，官方推荐，使用这种引擎的表最多 100 万行的数据</p></li>
</ol>


<pre><code>drop table if exists test.tinylog;
create table test.tinylog (a UInt16, b UInt16) ENGINE = TinyLog;
insert into test.tinylog(a,b) values (7,13);
</code></pre>

<p>此时/var/lib/clickhouse/data/test/tinylog保存数据的目录结构：</p>

<pre><code>├── a.bin   
├── b.bin   
└── sizes.json  
</code></pre>

<p>a.bin 和 b.bin 是压缩过的对应的列的数据， sizes.json 中记录了每个 *.bin 文件的大小</p>

<h2>3.2 Log</h2>

<p>这种引擎跟 TinyLog 基本一致</p>

<p>它的改进点，是加了一个 __marks.mrk 文件，里面记录了每个数据块的偏移</p>

<p>这样做的一个用处，就是可以准确地切分读的范围，从而使用并发读取成为可能</p>

<p>但是，它是不能支持并发写的，一个写操作会阻塞其它读写操作</p>

<p>Log 不支持索引，同时因为有一个 __marks.mrk 的冗余数据，所以在写入数据时，一旦出现问题，这个表就废了</p>

<p>应用场景:</p>

<p>同 TinyLog 差不多，它适用的场景也是那种写一次之后，后面就是只读的场景，临时数据用它保存也可以</p>

<pre><code>drop table if exists test.log;
create table test.log (a UInt16, b UInt16) ENGINE = Log;
insert into test.log(a,b) values (7,13);
</code></pre>

<p>此时/var/lib/clickhouse/data/test/log保存数据的目录结构：</p>

<pre><code>├── __marks.mrk
├── a.bin
├── b.bin
└── sizes.json
</code></pre>

<h2>3.3 Memory</h2>

<p>内存引擎，数据以未压缩的原始形式直接保存在内存当中，服务器重启数据就会消失</p>

<p>可以并行读，读写互斥锁的时间也非常短</p>

<p>不支持索引，简单查询下有非常非常高的性能表现</p>

<p>应用场景:</p>

<ol type="a">
<li><p>进行测试</p></li>
<li><p>在需要非常高的性能，同时数据量又不太大（上限大概 1 亿行）的场景</p></li>
</ol>


<h2>3.4 Merge</h2>

<p>一个工具引擎，本身不保存数据，只用于把指定库中的指定多个表链在一起。</p>

<p>这样，读取操作可以并发执行，同时也可以利用原表的索引，但是，此引擎不支持写操作</p>

<p>指定引擎的同时，需要指定要链接的库及表，库名可以使用一个表达式，表名可以使用正则表达式指定</p>

<pre><code>create table test.tinylog1 (id UInt16, name String) ENGINE=TinyLog;
create table test.tinylog2 (id UInt16, name String) ENGINE=TinyLog;
create table test.tinylog3 (id UInt16, name String) ENGINE=TinyLog;
insert into test.tinylog1(id, name) values (1, 'tinylog1');
insert into test.tinylog2(id, name) values (2, 'tinylog2');
insert into test.tinylog3(id, name) values (3, 'tinylog3');
use test;
create table test.merge (id UInt16, name String) ENGINE=Merge(currentDatabase(), '^tinylog[0-9]+');
select _table,* from test.merge order by id desc
</code></pre>



<pre><code>┌─_table───┬─id─┬─name─────┐
│ tinylog3 │ 3 │ tinylog3 │
│ tinylog2 │ 2 │ tinylog2 │
│ tinylog1 │ 1 │ tinylog1 │
└──────────┴────┴──────────┘
</code></pre>

<p>注：_table 这个列，是因为使用了 Merge 多出来的一个的一个虚拟列</p>

<ol type="a">
<li><p>它表示原始数据的来源表，它不会出现在 show table 的结果当中</p></li>
<li><p>select * 不会包含它</p></li>
</ol>


<h2>3.5 Distributed</h2>

<p>与 Merge 类似， Distributed 也是通过一个逻辑表，去访问各个物理表，设置引擎时的样子是：</p>

<p><code>Distributed(remote_group, database, table [, sharding_key])</code></p>

<p>其中：</p>

<p>remote_group 是/etc/clickhouse-server/config.xml中remote_servers参数</p>

<p>database 是各服务器中的库名</p>

<p>table 是表名</p>

<p>sharding_key 是一个寻址表达式，可以是一个列名，也可以是像 rand() 之类的函数调用，它与 remote_servers 中的 weight 共同作用，决定在 写 时往哪个 shard 写</p>

<p>配置文件中的 remote_servers</p>

<pre><code>&lt;remote_servers&gt;
    &lt;log&gt;
        &lt;shard&gt;
            &lt;weight&gt;1&lt;weight&gt;
            &lt;internal_replication&gt;false&lt;internal_replication&gt;
            &lt;replica&gt;
                &lt;host&gt;172.17.0.3&lt;host&gt;
                &lt;port&gt;9000&lt;port&gt;
            &lt;replica&gt;
        &lt;shard&gt;
        &lt;shard&gt;
            &lt;weight&gt;2&lt;weight&gt;
            &lt;internal_replication&gt;false&lt;internal_replication&gt;
            &lt;replica&gt;
                &lt;host&gt;172.17.0.4&lt;host&gt;
                &lt;port&gt;9000&lt;port&gt;
            &lt;replica&gt;
        &lt;shard&gt;
    &lt;log&gt;
&lt;remote_servers&gt;
</code></pre>

<ul>
<li><p>log 是某个 shard 组的名字，就是上面的 remote_group 的值</p></li>
<li><p>shard 是固定标签</p></li>
<li><p>weight 是权重，前面说的 sharding_key 与这个有关。</p></li>
</ul>


<p>简单来说，上面的配置，理论上来看:</p>

<p>第一个 shard “被选中”的概率是 1 / (1 + 2) ，第二个是 2 / (1 + 2) ，这很容易理解。但是， sharding_key 的工作情况，是按实际数字的“命中区间”算的，即第一个的区间是 [0, 1) 的周期，第二个区间是 [1, 1+2) 的周期。比如把 sharding_key 设置成 id ，当 id=0 或 id=3 时，一定是写入到第一个 shard 中，如果把 sharding_key 设置成 rand() ，那系统会对应地自己作一般化转换吧，这种时候就是一种概率场景了。</p>

<ul>
<li>internal_replication 是定义针对多个 replica 时的写入行为的。</li>
</ul>


<p>如果为 false ，则会往所有的 replica 中写入数据，但是并不保证数据写入的一致性，所以这种情况时间一长，各 replica 的数据很可能出现差异。如果为 true ，则只会往第一个可写的 replica 中写入数据（剩下的事“物理表”自己处理）。</p>

<ul>
<li>replica 就是定义各个冗余副本的，选项有 host ， port ， user ， password 等</li>
</ul>


<p>看一个实际的例子，我们先在两台机器上创建好物理表并插入一些测试数据：</p>

<pre><code>create table test.tinylog_d1(id UInt16, name String) ENGINE=TinyLog;
insert into test.tinylog_d1(id, name) values (1, 'Distributed record 1');
insert into test.tinylog_d1(id, name) values (2, 'Distributed record 2');
</code></pre>

<p>在其中一台创建逻辑表：</p>

<pre><code>create table test.tinylog_d (id UInt16, name String) ENGINE=Distributed(log, test,tinylog_d1 , id);-- 插入数据到逻辑表，观察数据分发情况
insert into test.tinylog_d(id, name) values (0, 'main');
insert into test.tinylog_d(id, name) values (1, 'main');
insert into test.tinylog_d(id, name) values (2, 'main');
select name,sum(id),count(id) from test.tinylog_d group by name;
</code></pre>

<p>注：逻辑表中的写入操作是异步的，会先缓存在本机的文件系统上，并且，对于物理表的不可访问状态，并没有严格控制，所以写入失败丢数据的情况是可能发生的</p>

<h2>3.6 Null</h2>

<p>空引擎，写入的任何数据都会被忽略，读取的结果一定是空。</p>

<p>但是注意，虽然数据本身不会被存储，但是结构上的和数据格式上的约束还是跟普通表一样是存在的，同时，你也可以在这个引擎上创建视图</p>

<h2>3.7 Buffer</h2>

<p>1.Buffer 引擎，像是Memory 存储的一个上层应用似的（磁盘上也是没有相应目录的）</p>

<p>2.它的行为是一个缓冲区，写入的数据先被放在缓冲区，达到一个阈值后，这些数据会自动被写到指定的另一个表中</p>

<p>3.和Memory 一样，有很多的限制，比如没有索引</p>

<p>4.Buffer 是接在其它表前面的一层，对它的读操作，也会自动应用到后面表，但是因为前面说到的限制的原因，一般我们读数据，就直接从源表读就好了，缓冲区的这点数据延迟，只要配置得当，影响不大的</p>

<p>5.Buffer 后面也可以不接任何表，这样的话，当数据达到阈值，就会被丢弃掉</p>

<p>一些特点：</p>

<ul>
<li><p>如果一次写入的数据太大或太多，超过了 max 条件，则会直接写入源表。</p></li>
<li><p>删源表或改源表的时候，建议 Buffer 表删了重建。</p></li>
<li><p>“友好重启”时， Buffer 数据会先落到源表，“暴力重启”， Buffer 表中的数据会丢失。</p></li>
<li><p>即使使用了 Buffer ，多次的小数据写入，对比一次大数据写入，也 慢得多 （几千行与百万行的差距）</p></li>
</ul>


<p>-- 创建源表</p>

<pre><code>create table test.mergetree (sdt  Date, id UInt16, name String, point UInt16) ENGINE=MergeTree(sdt, (id, name), 10);
-- 创建 Buffer表-- 
Buffer(database, table, num_layers, min_time, max_time, min_rows, max_rows, min_bytes, max_bytes)
create table test.mergetree_buffer as test.mergetree ENGINE=Buffer(test, mergetree, 16, 3, 20, 2, 10, 1, 10000);
insert into test.mergetree (sdt, id, name, point) values ('2017-07-10', 1, 'a', 20);
insert into test.mergetree_buffer (sdt, id, name, point) values ('2017-07-10', 1, 'b', 10);
select * from test.mergetree;
select '------';
select * from test.mergetree_buffer;
</code></pre>

<p>database 数据库</p>

<p>table 源表，这里除了字符串常量，也可以使用变量的。</p>

<p>num_layers 是类似“分区”的概念，每个分区的后面的 min / max 是独立计算的，官方推荐的值是 16 。</p>

<p>min / max 这组配置荐，就是设置阈值的，分别是 时间（秒），行数，空间（字节）。</p>

<p>阈值的规则: 是“所有的 min 条件都满足， 或 至少一个 max 条件满足”。</p>

<p>如果按上面我们的建表来说，所有的 min 条件就是：过了 3秒，2条数据，1 Byte。一个 max 条件是：20秒，或 10 条数据，或有 10K</p>

<h2>3.8 Set</h2>

<p>Set 这个引擎有点特殊，因为它只用在 IN 操作符右侧，你不能对它 select</p>

<pre><code>create table test.set(id UInt16, name String) ENGINE=Set;
insert into test.set(id, name) values (1, 'hello');
-- select 1 where (1, 'hello') in test.set; -- 默认UInt8 需要手动进行类型转换
select 1 where (toUInt16(1), 'hello') in test.set;
</code></pre>

<p>注: Set 引擎表，是全内存运行的，但是相关数据会落到磁盘上保存，启动时会加载到内存中。所以，意外中断或暴力重启，是可能产生数据丢失问题的</p>

<h2>3.9 MergeTree</h2>

<p><strong>这个引擎是 ClickHouse 的重头戏</strong>，它支持一个日期和一组主键的两层式索引，还可以实时更新数据。同时，索引的粒度可以自定义，外加直接支持采样功能</p>

<p>MergeTree(EventDate, (CounterID, EventDate), 8192)MergeTree(EventDate, intHash32(UserID), (CounterID, EventDate, intHash32(UserID)), 8192)</p>

<p>EventDate 一个日期的列名</p>

<p>intHash32(UserID) 采样表达式</p>

<p>(CounterID, EventDate) 主键组（里面除了列名，也支持表达式），也可以是一个表达式</p>

<p>8192 主键索引的粒度</p>

<pre><code>drop table if exists test.mergetree1;
create table test.mergetree1 (sdt  Date, id UInt16, name String, cnt UInt16) ENGINE=MergeTree(sdt, (id, name), 10);
-- 日期的格式，好像必须是 yyyy-mm-dd
insert into test.mergetree1(sdt, id, name, cnt) values ('2018-06-01', 1, 'aaa', 10);
insert into test.mergetree1(sdt, id, name, cnt) values ('2018-06-02', 4, 'bbb', 10);
insert into test.mergetree1(sdt, id, name, cnt) values ('2018-06-03', 5, 'ccc', 11);
</code></pre>

<p>此时/var/lib/clickhouse/data/test/mergetree1的目录结构：</p>

<pre><code>├── 20180601_20180601_1_1_0│   
├── checksums.txt│   
├── columns.txt│   
├── id.bin│   
├── id.mrk│   
├── name.bin│   
├── name.mrk│   
├── cnt.bin│   
├── cnt.mrk │   
├── cnt.idx│   
├── primary.idx│   
├── sdt.bin│   
└── sdt.mrk -- 保存一下块偏移量
├── 20180602_20180602_2_2_0│   
└── ...
├── 20180603_20180603_3_3_0│   
└── ...
├── format_version.txt
└── detached
</code></pre>

<h2>3.10 ReplacingMergeTree</h2>

<p>1.在 MergeTree 的基础上，添加了“处理重复数据”的功能=>实时数据场景</p>

<p>2.相比 MergeTree ,ReplacingMergeTree 在最后加一个"版本列",它跟时间列配合一起，用以区分哪条数据是"新的"，并把旧的丢掉(这个过程是在 merge 时处理，不是数据写入时就处理了的，平时重复的数据还是保存着的，并且查也是跟平常一样会查出来的)</p>

<p>3.主键列组用于区分重复的行</p>

<pre><code>-- 版本列 允许的类型是， UInt 一族的整数，或 Date 或 DateTime
create table test.replacingmergetree (sdt  Date, id UInt16, name String, cnt UInt16) ENGINE=ReplacingMergeTree(sdt, (name), 10, cnt);
insert into test.replacingmergetree (sdt, id, name, cnt) values ('2018-06-10', 1, 'a', 20);
insert into test.replacingmergetree (sdt, id, name, cnt) values ('2018-06-10', 1, 'a', 30);
insert into test.replacingmergetree (sdt, id, name, cnt) values ('2018-06-11', 1, 'a', 20);
insert into test.replacingmergetree (sdt, id, name, cnt) values ('2018-06-11', 1, 'a', 30);
insert into test.replacingmergetree (sdt, id, name, cnt) values ('2018-06-11', 1, 'a', 10);
select * from test.replacingmergetree;-- 如果记录未执行merge，可以手动触发一下 merge 行为
optimize table test.replacingmergetree;
</code></pre>



<pre><code>┌────────sdt─┬─id─┬─name─┬─cnt─┐
│ 2018-06-11 │ 1 │ a │ 30 │
└────────────┴────┴──────┴─────┘
</code></pre>

<h2>3.11 SummingMergeTree</h2>

<p>1.SummingMergeTree 就是在 merge 阶段把数据sum求和</p>

<p>2.sum求和的列可以指定，不可加的未指定列，会取一个最先出现的值</p>

<pre><code>create table test.summingmergetree (sdt Date, name String, a UInt16, b UInt16) ENGINE=SummingMergeTree(sdt, (sdt, name), 8192, (a));
insert into test.summingmergetree (sdt, name, a, b) values ('2018-06-10', 'a', 1, 20);
insert into test.summingmergetree (sdt, name, a, b) values ('2018-06-10', 'b', 2, 11);
insert into test.summingmergetree (sdt, name, a, b) values ('2018-06-11', 'b', 3, 18);
insert into test.summingmergetree (sdt, name, a, b) values ('2018-06-11', 'b', 3, 82);
insert into test.summingmergetree (sdt, name, a, b) values ('2018-06-11', 'a', 3, 11);
insert into test.summingmergetree (sdt, name, a, b) values ('2018-06-12', 'c', 1, 35);-- 手动触发一下 merge 行为
optimize table test.summingmergetree;
select * from test.summingmergetree;
</code></pre>



<pre><code>┌────────sdt─┬─name─┬─a─┬──b─┐
│ 2018-06-10 │ a │ 1 │ 20 │
│ 2018-06-10 │ b │ 2 │ 11 │
│ 2018-06-11 │ a │ 3 │ 11 │
│ 2018-06-11 │ b │ 6 │ 18 │
│ 2018-06-12 │ c │ 1 │ 35 │
└────────────┴──────┴───┴────┘
</code></pre>

<p>注: 可加列不能是主键中的列，并且如果某行数据可加列都是 null ，则这行会被删除</p>

<h2>3.13 AggregatingMergeTree</h2>

<p>AggregatingMergeTree 是在 MergeTree 基础之上，针对聚合函数结果，作增量计算优化的一个设计，它会在 merge 时，针对主键预处理聚合的数据</p>

<p>应用于AggregatingMergeTree 上的聚合函数除了普通的 sum, uniq等，还有 sumState , uniqState ，及 sumMerge ， uniqMerge 这两组</p>

<p>1.聚合数据的预计算</p>

<p>是一种“空间换时间”的权衡，并且是以减少维度为代价的</p>

<table>
<thead>
<tr>
<th> dim1 </th>
<th> dim2 </th>
<th> dim3 </th>
<th> measure1 </th>
</tr>
</thead>
<tbody>
<tr>
<td> aaaa </td>
<td> a </td>
<td> 1 </td>
<td> 1 </td>
</tr>
<tr>
<td> aaaa </td>
<td> b </td>
<td> 2 </td>
<td> 1 </td>
</tr>
<tr>
<td> bbbb </td>
<td> b </td>
<td> 3 </td>
<td> 1 </td>
</tr>
<tr>
<td> cccc </td>
<td> b </td>
<td> 2 </td>
<td> 1 </td>
</tr>
<tr>
<td> cccc </td>
<td> c </td>
<td> 1 </td>
<td> 1 </td>
</tr>
<tr>
<td> dddd </td>
<td> c </td>
<td> 2 </td>
<td> 1 </td>
</tr>
<tr>
<td> dddd </td>
<td> a </td>
<td> 1 </td>
<td> 1 </td>
</tr>
</tbody>
</table>


<p>假设原始有三个维度，一个需要 count 的指标</p>

<table>
<thead>
<tr>
<th> dim1 </th>
<th> dim2 </th>
<th> dim3 </th>
<th> measure1 </th>
</tr>
</thead>
<tbody>
<tr>
<td> aaaa </td>
<td> a </td>
<td> 1 </td>
<td> 1 </td>
</tr>
<tr>
<td> aaaa </td>
<td> b </td>
<td> 2 </td>
<td> 1 </td>
</tr>
<tr>
<td> bbbb </td>
<td> b </td>
<td> 3 </td>
<td> 1 </td>
</tr>
<tr>
<td> cccc </td>
<td> b </td>
<td> 2 </td>
<td> 1 </td>
</tr>
<tr>
<td> cccc </td>
<td> c </td>
<td> 1 </td>
<td> 1 </td>
</tr>
<tr>
<td> dddd </td>
<td> c </td>
<td> 2 </td>
<td> 1 </td>
</tr>
<tr>
<td> dddd </td>
<td> a </td>
<td> 1 </td>
<td> 1 </td>
</tr>
</tbody>
</table>


<p>通过减少一个维度的方式，来以 count 函数聚合一次 M</p>

<table>
<thead>
<tr>
<th> dim2 </th>
<th> dim3 </th>
<th> count(measure1) </th>
</tr>
</thead>
<tbody>
<tr>
<td> a </td>
<td> 1 </td>
<td> 3 </td>
</tr>
<tr>
<td> b </td>
<td> 2 </td>
<td> 2 </td>
</tr>
<tr>
<td> b </td>
<td> 3 </td>
<td> 1 </td>
</tr>
<tr>
<td> c </td>
<td> 1 </td>
<td> 1 </td>
</tr>
<tr>
<td> c </td>
<td> 2 </td>
<td> 1 </td>
</tr>
</tbody>
</table>


<p>2.聚合数据的增量计算</p>

<p>对于 AggregatingMergeTree 引擎的表，不能使用普通的 INSERT 去添加数据，可以用：</p>

<ol type="a">
<li><p>INSERT SELECT 来插入数据</p></li>
<li><p>更常用的，是可以创建一个物化视图</p></li>
</ol>


<pre><code>drop table if exists test.aggregatingmergetree;
create table test.aggregatingmergetree(sdt Date, dim1 String, dim2 String, dim3 String, measure1 UInt64) ENGINE=MergeTree(sdt, (sdt, dim1, dim2, dim3), 8192);
-- 创建一个物化视图，使用 AggregatingMergeTree
drop table if exists test.aggregatingmergetree_view;
create materialized view test.aggregatingmergetree_view ENGINE = AggregatingMergeTree(sdt,(dim2, dim3), 8192)as 
select sdt,dim2, dim3, uniqState(dim1) as uv from test.aggregatingmergetreegroup by sdt,dim2, dim3;
insert into test.aggregatingmergetree (sdt, dim1, dim2, dim3, measure1) values ('2018-06-10', 'aaaa', 'a', '10', 1);
insert into test.aggregatingmergetree (sdt, dim1, dim2, dim3, measure1) values ('2018-06-10', 'aaaa', 'a', '10', 1);
insert into test.aggregatingmergetree (sdt, dim1, dim2, dim3, measure1) values ('2018-06-10', 'aaaa', 'b', '20', 1);
insert into test.aggregatingmergetree (sdt, dim1, dim2, dim3, measure1) values ('2018-06-10', 'bbbb', 'b', '30', 1);
insert into test.aggregatingmergetree (sdt, dim1, dim2, dim3, measure1) values ('2018-06-10', 'cccc', 'b', '20', 1);
insert into test.aggregatingmergetree (sdt, dim1, dim2, dim3, measure1) values ('2018-06-10', 'cccc', 'c', '10', 1);
insert into test.aggregatingmergetree (sdt, dim1, dim2, dim3, measure1) values ('2018-06-10', 'dddd', 'c', '20', 1);
insert into test.aggregatingmergetree (sdt, dim1, dim2, dim3, measure1) values ('2018-06-10', 'dddd', 'a', '10', 1);
-- 按 dim2 和 dim3 聚合 count(measure1)
select dim2, dim3, count(measure1) from test.aggregatingmergetree group by dim2, dim3;
-- 按 dim2 聚合 UV
select dim2, uniq(dim1) from test.aggregatingmergetree group by dim2;
-- 手动触发merge
OPTIMIZE TABLE test.aggregatingmergetree_view;
select * from test.aggregatingmergetree_view;
-- 查 dim2 的 uv
select dim2, uniqMerge(uv) from test.aggregatingmergetree_view group by dim2 order by dim2;
</code></pre>

<h2>3.14 CollapsingMergeTree</h2>

<p>是专门为 OLAP 场景下，一种“变通”存数做法而设计的，在数据是不能改，更不能删的前提下，通过“运算”的方式，去抹掉旧数据的影响，把旧数据“减”去即可，从而解决"最终状态"类的问题，比如 当前有多少人在线？</p>

<p>“以加代删”的增量存储方式，带来了聚合计算方便的好处，代价却是存储空间的翻倍，并且，对于只关心最新状态的场景，中间数据都是无用的</p>

<p>CollapsingMergeTree 在创建时与 MergeTree 基本一样，除了最后多了一个参数，需要指定 Sign 位（必须是 Int8 类型）</p>

<pre><code>create table test.collapsingmergetree(sign Int8, sdt Date, name String, cnt UInt16) ENGINE=CollapsingMergeTree(sdt, (sdt, name), 8192, sign);
</code></pre>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Apache Spark的API三剑客：RDD、DataFrame和Dataset]]></title>
    <link href="https://ibillxia.github.io/blog/2019/09/17/spark-rdd-dataframe-dataset/"/>
    <updated>2019-09-17T15:15:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2019/09/17/spark-rdd-dataframe-dataset</id>
    <content type="html"><![CDATA[<h1>一、Spark 1.X</h1>

<p>spark 2.X开始，三者的关系发生了变化，可以参考《且谈Apache Spark的API三剑客：RDD、DataFrame和Dataset》 ,在2.X中DataFrame=DataSet[Row],其实是不知道类型。下面介绍是1.X，以免误导大家。</p>

<p>RDD、DataFrame和DataSet是容易产生混淆的概念，必须对其相互之间对比，才可以知道其中异同。</p>

<h2>RDD和DataFrame</h2>

<center><img src="/images/2019/IMAG2019091701.webp"></center>


<p><br></p>

<!--more-->


<h2>RDD-DataFrame</h2>

<p>上图直观地体现了DataFrame和RDD的区别。左侧的RDD[Person]虽然以Person为类型参数，但Spark框架本身不了解Person类的内部结构。而右侧的DataFrame却提供了详细的结构信息，使得Spark SQL可以清楚地知道该数据集中包含哪些列，每列的名称和类型各是什么。DataFrame多了数据的结构信息，即schema。RDD是分布式的Java对象的集合。DataFrame是分布式的Row对象的集合。DataFrame除了提供了比RDD更丰富的算子以外，更重要的特点是提升执行效率、减少数据读取以及执行计划的优化，比如filter下推、裁剪等。</p>

<h2>提升执行效率</h2>

<p>RDD API是函数式的，强调不变性，在大部分场景下倾向于创建新对象而不是修改老对象。这一特点虽然带来了干净整洁的API，却也使得Spark应用程序在运行期倾向于创建大量临时对象，对GC造成压力。在现有RDD API的基础之上，我们固然可以利用mapPartitions方法来重载RDD单个分片内的数据创建方式，用复用可变对象的方式来减小对象分配和GC的开销，但这牺牲了代码的可读性，而且要求开发者对Spark运行时机制有一定的了解，门槛较高。另一方面，Spark SQL在框架内部已经在各种可能的情况下尽量重用对象，这样做虽然在内部会打破了不变性，但在将数据返回给用户时，还会重新转为不可变数据。利用 DataFrame API进行开发，可以免费地享受到这些优化效果。</p>

<h2>减少数据读取</h2>

<p>分析大数据，最快的方法就是 ——忽略它。这里的“忽略”并不是熟视无睹，而是根据查询条件进行恰当的剪枝。</p>

<p>上文讨论分区表时提到的分区剪 枝便是其中一种——当查询的过滤条件中涉及到分区列时，我们可以根据查询条件剪掉肯定不包含目标数据的分区目录，从而减少IO。</p>

<p>对于一些“智能”数据格 式，Spark SQL还可以根据数据文件中附带的统计信息来进行剪枝。简单来说，在这类数据格式中，数据是分段保存的，每段数据都带有最大值、最小值、null值数量等 一些基本的统计信息。当统计信息表名某一数据段肯定不包括符合查询条件的目标数据时，该数据段就可以直接跳过（例如某整数列a某段的最大值为100，而查询条件要求a > 200）。</p>

<p>此外，Spark SQL也可以充分利用RCFile、ORC、Parquet等列式存储格式的优势，仅扫描查询真正涉及的列，忽略其余列的数据。</p>

<h2>执行优化</h2>

<center><img src="/images/2019/IMAG2019091702.webp"></center>


<p><br></p>

<h2>人口数据分析示例</h2>

<p>为了说明查询优化，我们来看上图展示的人口数据分析的示例。图中构造了两个DataFrame，将它们join之后又做了一次filter操作。如果原封不动地执行这个执行计划，最终的执行效率是不高的。因为join是一个代价较大的操作，也可能会产生一个较大的数据集。如果我们能将filter下推到 join下方，先对DataFrame进行过滤，再join过滤后的较小的结果集，便可以有效缩短执行时间。而Spark SQL的查询优化器正是这样做的。简而言之，逻辑查询计划优化就是一个利用基于关系代数的等价变换，将高成本的操作替换为低成本操作的过程。</p>

<p>得到的优化执行计划在转换成物 理执行计划的过程中，还可以根据具体的数据源的特性将过滤条件下推至数据源内。最右侧的物理执行计划中Filter之所以消失不见，就是因为溶入了用于执行最终的读取操作的表扫描节点内。</p>

<p>对于普通开发者而言，查询优化 器的意义在于，即便是经验并不丰富的程序员写出的次优的查询，也可以被尽量转换为高效的形式予以执行。</p>

<h1>RDD和DataSet</h1>

<ul>
<li><p>DataSet以Catalyst逻辑执行计划表示，并且数据以编码的二进制形式被存储，不需要反序列化就可以执行sorting、shuffle等操作。</p></li>
<li><p>DataSet创立需要一个显式的Encoder，把对象序列化为二进制，可以把对象的scheme映射为Spark</p></li>
</ul>


<p>SQl类型，然而RDD依赖于运行时反射机制。</p>

<p>通过上面两点，DataSet的性能比RDD的要好很多，可以参见[3]</p>

<h1>DataFrame和DataSet</h1>

<p>Dataset可以认为是DataFrame的一个特例，主要区别是Dataset每一个record存储的是一个强类型值而不是一个Row。因此具有如下三个特点：</p>

<ul>
<li><p>DataSet可以在编译时检查类型</p></li>
<li><p>并且是面向对象的编程接口。用wordcount举例：</p></li>
</ul>


<pre><code>//DataFrame// Load a text file and interpret each line as a java.lang.String
val ds = sqlContext.read.text("/home/spark/1.6/lines").as[String]
val result = ds  
    .flatMap(_.split(" "))// Split on whitespace
    .filter(_ !="")// Filter empty words
    .toDF()// Convert to DataFrame to perform aggregation / sorting
    .groupBy($"value")// Count number of occurences of each word
    .agg(count("*") as "numOccurances")
    .orderBy($"numOccurances" desc)// Show most common words first

//DataSet,完全使用scala编程，不要切换到DataFrame
val wordCount =   ds
.flatMap(_.split(" "))
.filter(_ !="")
.groupBy(_.toLowerCase())// Instead of grouping on a column expression (i.e. $"value") we pass a lambda function
.count()
</code></pre>

<ul>
<li>后面版本DataFrame会继承DataSet，DataFrame是面向Spark SQL的接口。</li>
</ul>


<p>DataFrame和DataSet可以相互转化，df.as[ElementType]这样可以把DataFrame转化为DataSet，ds.toDF()这样可以把DataSet转化为DataFrame。</p>

<hr />

<h1>二、Spark 2.X</h1>

<p>本文翻译自 A Tale of Three Apache Spark APIs: RDDs, DataFrames, and Datasets ，翻译已获得原作者 Jules S. Damji 的授权。</p>

<p>最令开发者们高兴的事莫过于有一组 API，可以大大提高开发者们的工作效率，容易使用、非常直观并且富有表现力。Apache Spark 广受开发者们欢迎的一个重要原因也在于它那些非常容易使用的 API，可以方便地通过多种语言，如 Scala、Java、Python 和 R 等来操作大数据集。</p>

<p>在本文中，我将深入讲讲 Apache Spark 2.2 以及以上版本提供的三种 API——RDD、DataFrame 和 Dataset，在什么情况下你该选用哪一种以及为什么，并概述它们的性能和优化点，列举那些应该使用 DataFrame 和 Dataset 而不是 RDD 的场景。我会更多地关注 DataFrame 和 Dataset，因为在 Apache Spark 2.0 中这两种 API 被整合起来了。</p>

<p>这次整合背后的动机在于我们希望可以让使用 Spark 变得更简单，方法就是减少你需要掌握的概念的数量，以及提供处理结构化数据的办法。在处理结构化数据时，Spark 可以像针对特定领域的语言所提供的能力一样，提供高级抽象和 API。</p>

<h2>弹性分布式数据集（Resilient Distributed Dataset，RDD）</h2>

<p>从一开始 RDD 就是 Spark 提供的面向用户的主要 API。从根本上来说，一个 RDD 就是你的数据的一个不可变的分布式元素集合，在集群中跨节点分布，可以通过若干提供了转换和处理的底层 API 进行并行处理。</p>

<h3>在什么情况下使用 RDD？</h3>

<p>下面是使用 RDD 的场景和常见案例：</p>

<ul>
<li><p>你希望可以对你的数据集进行最基本的转换、处理和控制；</p></li>
<li><p>你的数据是非结构化的，比如流媒体或者字符流；</p></li>
<li><p>你想通过函数式编程而不是特定领域内的表达来处理你的数据；</p></li>
<li><p>你不希望像进行列式处理一样定义一个模式，通过名字或字段来处理或访问数据属性；</p></li>
<li><p>你并不在意通过 DataFrame 和 Dataset 进行结构化和半结构化数据处理所能获得的一些优化和性能上的好处；</p></li>
</ul>


<h3>Apache Spark 2.0 中的 RDD 有哪些改变？</h3>

<p>可能你会问：RDD 是不是快要降级成二等公民了？是不是快要退出历史舞台了？</p>

<p>答案是非常坚决的：不！</p>

<p>而且，接下来你还将了解到，你可以通过简单的 API 方法调用在 DataFrame 或 Dataset 与 RDD 之间进行无缝切换，事实上 DataFrame 和 Dataset 也正是基于 RDD 提供的。</p>

<h2>DataFrame</h2>

<p>与 RDD 相似， DataFrame 也是数据的一个不可变分布式集合。但与 RDD 不同的是，数据都被组织到有名字的列中，就像关系型数据库中的表一样。设计 DataFrame 的目的就是要让对大型数据集的处理变得更简单，它让开发者可以为分布式的数据集指定一个模式，进行更高层次的抽象。它提供了特定领域内专用的 API 来处理你的分布式数据，并让更多的人可以更方便地使用 Spark，而不仅限于专业的数据工程师。</p>

<p>在我们的 Apache Spark 2.0 网络研讨会以及后续的博客中，我们提到在Spark 2.0 中，DataFrame 和 Dataset 的 API 将融合到一起，完成跨函数库的数据处理能力的整合。在整合完成之后，开发者们就不必再去学习或者记忆那么多的概念了，可以通过一套名为 Dataset 的高级并且类型安全的 API 完成工作。</p>

<center><img src="/images/2019/IMAG2019091703.png"></center>


<p><br></p>

<h2>Dataset</h2>

<p>如下面的表格所示，从 Spark 2.0 开始，Dataset 开始具有两种不同类型的 API 特征：有明确类型的 API 和无类型的 API。从概念上来说，你可以把 DataFrame 当作一些通用对象 Dataset[Row] 的集合的一个别名，而一行就是一个通用的无类型的 JVM 对象。与之形成对比，Dataset 就是一些有明确类型定义的 JVM 对象的集合，通过你在 Scala 中定义的 Case Class 或者 Java 中的 Class 来指定。</p>

<p>有类型和无类型的 API</p>

<table>
<thead>
<tr>
<th> 语言 </th>
<th> 主要抽象 </th>
</tr>
</thead>
<tbody>
<tr>
<td> Scala </td>
<td> Dataset[T] &amp; DataFrame (Dataset[Row] 的别名) </td>
</tr>
<tr>
<td> Java </td>
<td> Dataset[T] </td>
</tr>
<tr>
<td> Python </td>
<td> DataFrame </td>
</tr>
<tr>
<td> R </td>
<td> DataFrame </td>
</tr>
</tbody>
</table>


<p>注意：因为 Python 和 R 没有编译时类型安全，所以我们只有称之为 DataFrame 的无类型 API。</p>

<h3>Dataset API 的优点</h3>

<p>在 Spark 2.0 里，DataFrame 和 Dataset 的统一 API 会为 Spark 开发者们带来许多方面的好处。</p>

<h4>1、静态类型与运行时类型安全</h4>

<p>从 SQL 的最小约束到 Dataset 的最严格约束，把静态类型和运行时安全想像成一个图谱。比如，如果你用的是 Spark SQL 的查询语句，要直到运行时你才会发现有语法错误（这样做代价很大），而如果你用的是 DataFrame 和 Dataset，你在编译时就可以捕获错误（这样就节省了开发者的时间和整体代价）。也就是说，当你在 DataFrame 中调用了 API 之外的函数时，编译器就可以发现这个错。不过，如果你使用了一个不存在的字段名字，那就要到运行时才能发现错误了。</p>

<p>图谱的另一端是最严格的 Dataset。因为 Dataset API 都是用 lambda 函数和 JVM 类型对象表示的，所有不匹配的类型参数都可以在编译时发现。而且在使用 Dataset 时，你的分析错误也会在编译时被发现，这样就节省了开发者的时间和代价。</p>

<p>所有这些最终都被解释成关于类型安全的图谱，内容就是你的 Spark 代码里的语法和分析错误。在图谱中，Dataset 是最严格的一端，但对于开发者来说也是效率最高的。</p>

<center><img src="/images/2019/IMAG2019091704.png"></center>


<p><br></p>

<h4>2、关于结构化和半结构化数据的高级抽象和定制视图</h4>

<p>把 DataFrame 当成 Dataset[Row] 的集合，就可以对你的半结构化数据有了一个结构化的定制视图。比如，假如你有个非常大量的用 JSON 格式表示的物联网设备事件数据集。因为 JSON 是半结构化的格式，那它就非常适合采用 Dataset 来作为强类型化的 Dataset[DeviceIoTData] 的集合。</p>

<pre><code>{"device_id": 198164, "device_name": "sensor-pad-198164owomcJZ", 
"ip": "80.55.20.25", "cca2": "PL", "cca3": "POL", "cn": "Poland", 
"latitude": 53.080000, "longitude": 18.620000, "scale": "Celsius", 
"temp": 21, "humidity": 65, "battery_level": 8, "c02_level": 1408, 
"lcd": "red", "timestamp" :1458081226051}
</code></pre>

<p>你可以用一个 Scala Case Class 来把每条 JSON 记录都表示为一条 DeviceIoTData，一个定制化的对象。</p>

<pre><code>case class DeviceIoTData (battery_level: Long, c02_level: Long, cca2: String, cca3: String, cn: String, 
device_id: Long, device_name: String, humidity: Long, ip: String, latitude: Double, lcd: String, 
longitude: Double, scale:String, temp: Long, timestamp: Long)
</code></pre>

<p>接下来，我们就可以从一个 JSON 文件中读入数据。</p>

<pre><code>// read the json file and create the dataset from the 
// case class DeviceIoTData
// ds is now a collection of JVM Scala objects DeviceIoTData
val ds = spark.read.json(“/databricks-public-datasets/data/iot/iot_devices.json”).as[DeviceIoTData]
</code></pre>

<p>上面的代码其实可以细分为三步：</p>

<ol>
<li><p>Spark 读入 JSON，根据模式创建出一个 DataFrame 的集合；</p></li>
<li><p>在这时候，Spark 把你的数据用“DataFrame = Dataset[Row]”进行转换，变成一种通用行对象的集合，因为这时候它还不知道具体的类型；</p></li>
<li><p>然后，Spark 就可以按照类 DeviceIoTData 的定义，转换出“Dataset[Row] -> Dataset[DeviceIoTData]”这样特定类型的 Scala JVM 对象了。</p></li>
</ol>


<p>许多和结构化数据打过交道的人都习惯于用列的模式查看和处理数据，或者访问对象中的某个特定属性。将 Dataset 作为一个有类型的 Dataset[ElementType] 对象的集合，你就可以非常自然地又得到编译时安全的特性，又为强类型的 JVM 对象获得定制的视图。而且你用上面的代码获得的强类型的 Dataset[T] 也可以非常容易地用高级方法展示或处理。</p>

<center><img src="/images/2019/IMAG2019091705.png"></center>


<p><br></p>

<h4>3、方便易用的结构化 API</h4>

<p>虽然结构化可能会限制你的 Spark 程序对数据的控制，但它却提供了丰富的语义，和方便易用的特定领域内的操作，后者可以被表示为高级结构。事实上，用 Dataset 的高级 API 可以完成大多数的计算。比如，它比用 RDD 数据行的数据字段进行 agg、select、sum、avg、map、filter 或 groupBy 等操作简单得多，只需要处理 Dataset 类型的 DeviceIoTData 对象即可。</p>

<p>用一套特定领域内的 API 来表达你的算法，比用 RDD 来进行关系代数运算简单得多。比如，下面的代码将用 filter() 和 map() 来创建另一个不可变 Dataset。</p>

<pre><code>// Use filter(), map(), groupBy() country, and compute avg() 
// for temperatures and humidity. This operation results in 
// another immutable Dataset. The query is simpler to read, 
// and expressive
val dsAvgTmp = ds.filter(d =&gt; {d.temp &gt; 25}).map(d =&gt; (d.temp, d.humidity, d.cca3))
    .groupBy($"_3").avg()//display the resulting datasetdisplay(dsAvgTmp)
</code></pre>



<center><img src="/images/2019/IMAG2019091706.gif"></center>


<p><br></p>

<h4>4、性能与优化</h4>

<p>除了上述优点之外，你还要看到使用 DataFrame 和 Dataset API 带来的空间效率和性能提升。原因有如下两点：</p>

<p>首先，因为 DataFrame 和 Dataset API 都是基于 Spark SQL 引擎构建的，它使用 Catalyst 来生成优化后的逻辑和物理查询计划。所有 R、Java、Scala 或 Python 的 DataFrame/Dataset API，所有的关系型查询的底层使用的都是相同的代码优化器，因而会获得空间和速度上的效率。尽管有类型的 Dataset[T] API 是对数据处理任务优化过的，无类型的 Dataset[Row]（别名 DataFrame）却运行得更快，适合交互式分析。</p>

<center><img src="/images/2019/IMAG2019091707.png"></center>


<p><br></p>

<p>其次， Spark 作为一个编译器，它可以理解 Dataset 类型的 JVM 对象，它会使用编码器来把特定类型的JVM 对象映射成Tungsten 的内部内存表示。结果，Tungsten 的编码器就可以非常高效地将JVM 对象序列化或反序列化，同时生成压缩字节码，这样执行效率就非常高了。</p>

<p>该什么时候使用 DataFrame 或 Dataset 呢？</p>

<ul>
<li><p>如果你需要丰富的语义、高级抽象和特定领域专用的 API，那就使用 DataFrame 或 Dataset；</p></li>
<li><p>如果你的处理需要对半结构化数据进行高级处理，如 filter、map、aggregation、average、sum、SQL 查询、列式访问或使用 lambda 函数，那就使用 DataFrame 或 Dataset；</p></li>
<li><p>如果你想在编译时就有高度的类型安全，想要有类型的 JVM 对象，用上 Catalyst 优化，并得益于 Tungsten 生成的高效代码，那就使用 Dataset；</p></li>
<li><p>如果你想在不同的 Spark 库之间使用一致和简化的 API，那就使用 DataFrame 或 Dataset；</p></li>
<li><p>如果你是 R 语言使用者，就用 DataFrame；</p></li>
<li><p>如果你是 Python 语言使用者，就用 DataFrame，在需要更细致的控制时就退回去使用 RDD；</p></li>
</ul>


<p>注意只需要简单地调用一下.rdd，就可以无缝地将 DataFrame 或 Dataset 转换成 RDD。例子如下：</p>

<pre><code>// select specific fields from the Dataset, apply a predicate
// using the where() method, convert to an RDD, and show first 10
// RDD rows
val deviceEventsDS = ds.select($"device_name", $"cca3", $"c02_level")
    .where($"c02_level" &gt; 1300)
// convert to RDDs and take the first 10 rows
val eventsRDD = deviceEventsDS.rdd.take(10)
</code></pre>



<center><img src="/images/2019/IMAG2019091708.png"></center>


<p><br></p>

<h2>总结</h2>

<p>总之，在什么时候该选用 RDD、DataFrame 或 Dataset 看起来好像挺明显。前者可以提供底层的功能和控制，后者支持定制的视图和结构，可以提供高级和特定领域的操作，节约空间并快速运行。</p>

<p>当我们回顾从早期版本的 Spark 中获得的经验教训时，我们问自己该如何为开发者简化 Spark 呢？该如何优化它，让它性能更高呢？我们决定把底层的 RDD API 进行高级抽象，成为 DataFrame 和 Dataset，用它们在 Catalyst 优化器和 Tungsten 之上构建跨库的一致数据抽象。</p>

<p>DataFrame 和 Dataset，或 RDD API，按你的实际需要和场景选一个来用吧，当你像大多数开发者一样对数据进行结构化或半结构化的处理时，我不会有丝毫惊讶。</p>

<h1>三、参考</h1>

<p><a href="https://www.infoq.cn/news/three-apache-spark-apis-rdds-dataframes-and-datasets">且谈 Apache Spark 的 API 三剑客：RDD、DataFrame 和 Dataset</a></p>

<p><a href="http://www.iteye.com/news/30658">Spark SQL结构化分析</a></p>

<p><a href="http://www.infoq.com/cn/articles/2015-Review-Spark">解读2015之Spark篇：新生态系统的形成</a></p>

<p><a href="https://databricks.com/blog/2016/01/04/introducing-spark-datasets.html">Introducing Spark Datasets</a></p>

<p><a href="https://docs.cloud.databricks.com/docs/spark/1.6/index.html#examples/Dataset%20Wordcount.html">databricks example</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Apache Flink入门教程与资料汇总]]></title>
    <link href="https://ibillxia.github.io/blog/2019/08/11/apache-flink-introduction/"/>
    <updated>2019-08-11T16:53:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2019/08/11/apache-flink-introduction</id>
    <content type="html"><![CDATA[<center><img src="/images/2019/IMAG2019081100.png"></center>


<p><br></p>

<h1>一、教程&amp;博客</h1>

<h2>1. Flink中文视频教程</h2>

<p>Flink系列视频教程：https://github.com/flink-china/flink-training-course</p>

<p>基础篇 https://ververica.cn/developers/flink-training-course-basics/</p>

<p>进阶篇 https://ververica.cn/developers/flink-training-course-advanced/</p>

<p>运维篇 https://ververica.cn/developers/flink-training-course-operation/</p>

<!--more-->


<p>实时数仓篇 https://ververica.cn/developers/flink-training-course-data-warehouse/</p>

<p>技术生态篇 https://ververica.cn/developers/flink-training-course-technological-ecology/</p>

<p>社区成长篇 https://ververica.cn/developers/flink-training-course-growup/</p>

<p>1.10版特别篇 https://ververica.cn/developers/flink-training-course-1-10/</p>

<h2>2.博客</h2>

<p>1）CN: 伍 翀（WuChong） | EN: Jark | 花名: 云邪 的博客</p>

<p>http://wuchong.me/archives/</p>

<p>https://wuchong.me/categories/Flink/</p>

<p>2）王无知</p>

<p>https://www.cnblogs.com/importbigdata/p/11431859.html</p>

<p>https://www.cnblogs.com/importbigdata/tag/Flink/</p>

<hr />

<h1>二、Flink简介</h1>

<center><img src="/images/2019/IMAG2019081101.png"></center>


<p><br></p>

<h2>1、Features:</h2>

<ul>
<li><p>support for stream and batch processing</p></li>
<li><p>sophisticated state management</p></li>
<li><p>event-time processing semantics</p></li>
<li><p>exactly-once consistency guarantees for state</p></li>
<li><p>can be deployed on various resource providers</p></li>
</ul>


<h2>2、Architecture</h2>

<p>1） Process bounded and unbounded data</p>

<p>precise control of time and state</p>

<p>2）Deploy applications anywhere</p>

<p>Flink integrates with all common cluster resource managers such as Hadoop YARN, Apache Mesos, and Kubernetes but can also be setup to run as a stand-alone cluster.</p>

<p>All communication to submit or control an application happens via REST calls.</p>

<p>3） Run applications at any scale</p>

<p>4）Leverage in-memory performance</p>

<p>Task state is always maintained in memory or, if the state size exceeds the available memory, in access-efficient on-disk data structures.</p>

<h2>3、Applications</h2>

<p>1）Building blocks for stream applications</p>

<p>stream,state,time</p>

<p>2）Layered APIs</p>

<p>three layered APIs:</p>

<center><img src="/images/2019/IMAG2019081102.png"></center>


<p><br></p>

<p>3）Libraries</p>

<p>CEP/DataSet API/Gelly</p>

<h2>4、Operations</h2>

<p>1）7*24h non-stop</p>

<p>2）Update, Migrate, Suspend, and Resume</p>

<p>3）Monitor and Control</p>

<h1>5、Use Cases</h1>

<ul>
<li><p>Event-driven Apps</p></li>
<li><p>Data Analytics Apps</p></li>
<li><p>Data Pipeline Apps</p></li>
</ul>

]]></content>
  </entry>
  
</feed>