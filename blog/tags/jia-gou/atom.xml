<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Tag: 架构 | Bill's Blog]]></title>
  <link href="https://ibillxia.github.io/blog/tags/jia-gou/atom.xml" rel="self"/>
  <link href="https://ibillxia.github.io/"/>
  <updated>2026-07-12T20:22:44+08:00</updated>
  <id>https://ibillxia.github.io/</id>
  <author>
    <name><![CDATA[Bill Xia]]></name>
    <email><![CDATA[ibillxia@gmail.com]]></email>
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[神策分析的技术选型与架构实现]]></title>
    <link href="https://ibillxia.github.io/blog/2016/11/15/sensors-analytics-architecture/"/>
    <updated>2016-11-15T21:17:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2016/11/15/sensors-analytics-architecture</id>
    <content type="html"><![CDATA[<center><img src="/images/2016/IMAG2016111500.png"></center>


<p>很多朋友很想知道神策分析（Sensors Analytics）是如何实现在每天十亿级别数据的情况下可以做到秒级导入和秒级查询，是如何做到不需要预先指定指标和维度就能实现多维查询的。今天正好在这篇文章里面，和大家交流一下我们的技术选型与具体的架构实现，希望能够对大家有所启发。</p>

<p>当然，脱离客户需求谈产品设计，不太现实；而脱离产品设计，纯粹谈技术选型与架构实现，也不现实。因此，我们首先会跟大家探讨一下神策分析从产品角度，是如何从客户需求抽象产品设计的，而产品设计，又是如何确定我们的技术选型。然后，我们则会从产品的整体架构出发，逐步介绍每一个模块和子系统的具体实现。</p>

<!--more-->


<h2>1. 客户需求决定产品设计，产品设计决定技术选型</h2>

<h3>1.1 私有化部署</h3>

<p>在决定做神策分析这个产品的最开始，我们就准备满足这样一类客户的需求，即对数据的安全与隐私有顾虑，或者希望能够积累自己的用户行为数据资产，并且完成数据的深度应用与二次开发。</p>

<center><img src="/images/2016/IMAG2016111501.jpeg"></center>


<p>因此，这就决定我们的产品需要是一个可以私有化部署的产品，可以部署在客户的内网中，这基本也构成是我们产品的核心设计理念。</p>

<p>而正因为需要私有化部署，我们在设计上，也必须考虑到因为这一点而带来的一系列在运维、审计方面的技术调整；同时，为了方便客户能够基于我们的系统二次开发，充分发挥数据的价值，这就要求我们在技术选型上，尽可能选择热门的开源技术，必须保留最细粒度原始数据，同时，最好在数据处理从采集、传输到存储、查询的各个环节都对外提供普适易用的接口，降低客户的开发代价。</p>

<h3>1.2 用户行为分析</h3>

<p>数据分析是一个很大的领域，而我们最核心地是满足客户对于用户行为分析这一个特定领域的需求。帮助他们回答这样一些问题：用户使用产品的活跃情况和频次分布；核心流程转化情况；分析上周流失用户的行为特征等。同时，我们期望神策分析可以适应不同的行业的不同产品，而这些不同产品肯定又会有不同的技术架构。</p>

<p>针对这种需求，我们在技术上做了两个关键性的决策：首先，我们对分析模型进行抽象，期望用少数几个分析模型能够满足大部分需求，而剩余未满足的需求则通过自定义查询来实现，这样的好处，就是可以集中精力来优化查询速度；其次，为了减少 ETL 的代价，我们尽量简化数据模型，从而能够减少 ETL 的环节，保证神策分析产品在不同行业的适用性。</p>

<h3>1.3 全端数据采集</h3>

<p>随着互联网的发展越来越深入，一个用户在同一个产品上的行为，已经需要从多个不同的来源来进行采集了，这些来源包括 iOS、安卓、Web、H5、微信、业务数据库、第三方配送服务、客服系统等。不仅仅是需要采集到，还需要能够将同一个用户在不同来源的数据进行打通。</p>

<p>针对这种需求，神策分析决定提供全端的数据采集方案，需要包括主流的客户端平台和主流语言的 SDK，Restful 风格的数据导入 API，全埋点与可视化埋点等埋点辅助手段。同时，为了方便埋点的迭代与管理，还需要在这方面下大力气。同时，为了解决跨屏贯通一个用户的问题，还需要提供 ID-Mapping 方面的解决方案。</p>

<h3>1.4 灵活的多维分析</h3>

<p>相比较传统的网页或者 App 统计工具，如百度统计、Google Analytics 等，我们想解决的是用户更加灵活更加深入的分析需求。例如，分析不同地域、不同品类的销量对比；分析比较不同客服的服务质量；对比不同年龄段用户的转化情况；分渠道分析转化效果；察看使用不同功能后对留存的影响等。</p>

<center><img src="/images/2016/IMAG2016111502.jpeg"></center>


<p>针对这一需求，神策分析需要提供全功能的多维分析能力，能够满足如下技术指标：维度、指标不需要预定义；漏斗、留存、回访分析都可以任意下钻。而因为这些需求，所以现在很火的开源多维分析系统，如 Apache Kylin、Druid 等都无法满足我们的需要。同时，这也决定了我们不能够存储聚合数据，而是应该存储最细粒度的明细数据。当然，这一决策也是受到私有化部署提供数据进行二次开发的影响。</p>

<h3>1.5 秒级导入和秒级查询</h3>

<p>在数据分析的某些应用场景下，对于数据导入后多长时间能够查到，其实是有一定需求的。例如，分析彩票投注截止前的最后30分钟投注情况，或者广告投放后立刻察看效果以便调整策略等。为了满足这部分需求，更好地让数据为决策服务，神策分析需要让数据能够做到秒级导入，即一条数据接收后，在秒级就能被查到。因此，我们应该有实时数据导入流，并且尽可能地减少 ETL 的复杂程度，从而降低数据导入的开销。</p>

<p>我们一直推崇数据驱动，推崇数据的需求方可以自助式地满足数据分析需求。因此，这就需要我们的产品能够满足秒级查询。这也就意味着我们在查询引擎的选择方面，就不能够选择 Hive，而应该考虑类似于 Impala、Spark SQL 等 MPP 架构的方案。</p>

<center><img src="/images/2016/IMAG2016111503.jpeg"></center>


<h3>1.6 目前为止的技术决策</h3>

<p>现在，我们回顾一下，由于之前我们列出的这一系列客户需求，我们基本确定了如下的一些技术决策：</p>

<ul>
<li><p>具有私有化部署能力，需要解决运维问题；</p></li>
<li><p>技术选型以开源方案为主，便于复用和客户二次开发；</p></li>
<li><p>数据模型尽量简洁，减少 ETL 代价；</p></li>
<li><p>每天十亿级别数据量下，秒级导入，秒级查询；</p></li>
<li><p>需要存储明细数据，采用 ROLAP 而不是 MOLAP 的方案。</p></li>
</ul>


<p>那么，下面，就让我们进一步来看看，在这些技术决策之下，神策分析具体是怎么实现的。</p>

<h2>2. 技术实现</h2>

<h3>2.1 整体架构</h3>

<p>通常情况下，一个常见的数据平台，整个数据处理的过程，可以分为如下 5 步：</p>

<center><img src="/images/2016/IMAG2016111504.jpeg"></center>


<p>神策分析设计上也基本遵循这一原则。下图是神策分析的架构图：</p>

<center><img src="/images/2016/IMAG2016111505.jpeg"></center>


<p>我们后面依次介绍各个子系统的具体技术实现。</p>

<h3>2.2 数据采集子系统</h3>

<p>神策分析主要支持采集三类数据，分别是前端操作、后端日志和业务数据。</p>

<h4>2.2.1 采集前端操作</h4>

<p>其中前端操作，主要是用户在客户端，如 iOS App、安卓 App 和网页上的一些操作，目前主流有三种采集方案，分别是全埋点、可视化埋点和代码埋点。</p>

<p>其中，全埋点是指默认地不加区分地采集所有能够采集的控件操作和交互，然后再在后端筛选出需要的数据；可视化埋点则是通过可视化交互的方式，提前选择要采集哪些控件的操作，然后再在操作发生时向后端发送数据；代码埋点则是最传统和广泛使用的一种技术采集手段，是以代码调用 SDK 接口的方式，来发送要采集的用户操作。</p>

<p>三种采集方式，在使用上，全埋点最简单，只需要嵌入 SDK 即可，但是会带来额外的数据开销；可视化埋点不会有额外的数据开销，嵌入 SDK 后需要用可视化的方式进行控件的点选。由于国内 App 开发普遍存在不遵循开发规范的情况，导致上述两种采集方案都存在一定的兼容性问题。代码埋点则是每一个点都需要一行或者更多的代码，更新迭代时使用较为复杂。</p>

<p>但是反过来，代码埋点却是采集能力最强的一种采集手段。我们以用户在京东的提交订单页面，来分别描述三者在采集能力上的差异：</p>

<center><img src="/images/2016/IMAG2016111506.jpeg"></center>


<p>在这个界面上，使用三种采集方案的采集能力大概如下面所描述的那样：</p>

<ul>
<li><p>使用全埋点，可以知道某时某刻某人点击了某个按钮；</p></li>
<li><p>使用可视化埋点，可以跟进一步知道某时某刻某人提交了一个订单；</p></li>
<li><p>使用代码埋点，则可以更进一步地获取订单金额、商品名称、用户级别等自定义属性。</p></li>
</ul>


<p>为了方便客户的使用，神策分析对于采集前端操作提供的方案则是：</p>

<ul>
<li><p>以代码埋点为主，提供了 iOS、安卓和 JS 三种 SDK，特别是针对混合开发的 App，专门提供了原生 App 和 H5 之前交换数据，打通用户 ID 的接口；</p></li>
<li><p>以可视化埋点为辅，并且努力解决国内 App 的兼容性问题；</p></li>
<li><p>对于一些例如页面浏览、App 启动、App 进入后台等默认事件，则提供全埋点的方案，可以嵌入 SDK 之后直接采集；</p></li>
<li><p>建议客户根据不同的使用场景，选择最合适的前端埋点方案。</p></li>
</ul>


<p>同时，为了最大限度保证嵌入了我们 SDK 的客户 App 的用户体验，我们专门针对 SDK 的数据发送策略做了很多优化，包括：</p>

<ul>
<li><p>数据首先缓存在本地，达到一定规模或间隔时才发送；</p></li>
<li><p>仅在 3G/4G/Wi-Fi 时发送数据，发送时会对数据进行打包、压缩；</p></li>
<li><p>在产生首条数据、进入后台、退出程序时都会尝试发送，以尽可能兼容那些安装了 App 简单尝试后就快速删除的用户；</p></li>
<li><p>提供了强制发送接口，也让客户自己可以控制发送策略。</p></li>
</ul>


<h4>2.2.2 采集后端日志</h4>

<p>后端日志主要指产品的服务端模块在运行时打印出的日志。相比采集前端操作，采集后端日志会有如下一系列的优势：</p>

<ul>
<li><p>传输时效性：如前面描述的那样，为了保证用户体验，前端采集数据是不能实时向后端发送的，所以会带来传输时效性的问题，而采集后端日志就不存在这个问题；</p></li>
<li><p>数据可靠性：前端采集需要通过公网进行数据传输，肯定会存在数据可靠性的问题，采集后端日志配合私有部署，则可以做到纯内网传输数据，这一问题大大缓解；</p></li>
<li><p>能够获取的信息丰富程度：有很多信息，例如商品库存、商品成本、用户风险级别、用户潜在价值等，在前端都采集不到的，只能在后端采集。</p></li>
</ul>


<p>正因为这一点，我们建议一个行为在前端和后端都可以采集时，优先在后端进行采集，并且为此提供了一系列的后端语言 SDK、日志采集工具和数据批量导入工具等。</p>

<h4>2.2.3 采集业务数据</h4>

<p>业务数据主要指一些供销存数据库数据、从第三方系统拿到的订单配送数据和客户数据等，针对这一类数据，我们提供了相应的数据导入工具，以及 RESTful 风格的导入 API，用于完成数据的导入。</p>

<h4>2.2.4 ID-Mapping</h4>

<p>对于前面三类不同的数据源，我们期望能够打通同一个用户在这三类数据源中的行为，并且为此提供了如下的技术手段：</p>

<ul>
<li><p>不同端可以自定义唯一的用户 ID，如设备 ID、Cookie ID、注册 ID等，客户可以自己定义自己选择；当然，神策分析系统内部会有唯一的 user_id；</p></li>
<li><p>提供一次性的 track_signup 接口，将两个 ID 贯通起来，例如，可以将一个用户在浏览器上的 Cookie ID 与他在产品里面的注册 ID 贯通起来，然后这个用户以注册 ID 在手机 App 上登录时，我们依然能知道是同一个用户；</p></li>
<li><p>我们目前采用的方案不需要回溯数据，但是应用是有限制的，即只能支持一对一的 ID-Mapping，这也是一个典型的功能与性能的折衷。</p></li>
</ul>


<h3>2.3 数据接入子系统</h3>

<p>不管是采用哪种采集方式，数据都是通过 HTTP API 发送给系统的。</p>

<p>而在数据接入子系统部分，我们采用了 Nginx 来接收通过 API 发送的数据，并且将之写到日志文件上。使用 Nginx 主要是考虑到它的高可靠性与高可扩展性，并且在我们的应用场景下， Nginx 单机可以轻松地做到每秒接收 1 万条请求，考虑到一条请求通常都不止一条用户行为，可以认为很轻松就能做到数万 TPS。</p>

<p>对于 Nginx 打印到文件的日志，会由我们自己开发的 Extractor 模块来实时读取和处理 Nginx 日志，并将处理结果发布到 Kafka 中。在这个过程中，Extractor 模块还会进行数据格式的校验，属性类型的识别与相关元数据的操作。与 ID-Mapping 的处理也是在这个阶段完成的。一些字段的解析和扩展工作，如基于 IP 判断国家、省份、城市，基于 UserAgent 判断浏览器、操作系统等，也是在这个阶段完成的。前面提到了，我们不需要用户预先指定指标和维度，基本实现了 schema-free，就是在 Extractor 处理阶段，对这些列进行校验，并且完成相关的元数据操作。</p>

<p>Kafka 是一个广泛使用的高可用的分布式消息队列，作为数据接入与数据处理两个流程之间的缓冲，同时也作为近期数据的一个备份。另外，这个阶段也对外提供访问 API，客户可以直接从 Kafka 中将数据引走，进入自己的实时计算流。</p>

<h3>2.4 数据模型</h3>

<p>在介绍数据导入模块与数据存储模块之前，我们需要先讨论一下神策分析的数据模型设计。</p>

<p>前面已经提到，神策分析主要解决的是用户行为分析这么一个特定领域的数据分析需求，并且期望尽量简化数据模型以降低 ETL 代价。最终，我们选择了业内非常流行的 Event + User 模型，可以覆盖客户的绝大部分分析需求，并且对于采集到的数据不需要有太多的 ETL 工作。</p>

<p>Event 主要是描述用户做了什么事情。每一条 Event 数据对应用户的一次事件，由 用户 ID、Event 名称、自定义属性三部分组成。Event 名称主要是对 Event 的一个分类，例如"PageView"、"Search"、"PayOrder"等等。我们在客户端会默认采集设备 ID 或者 Cookie ID 作为用户 ID，客户也可以自己设置一个合理的用户 ID。而我们最多可以支持一万个自定义属性，并且开发者并不需要事先告之系统，系统会自动从接收的数据中解析发现新的字段并且进行相应的处理。Event 数据以追加为主，不可修改，这也符合事件这一概念的实际物理意义。不过，为了方便后续系统的运维以及客户的使用，我们特别为 Event 数据提供了有限的数据删除能力，这一点会在后续存储部分更详尽地描述。</p>

<p>User 则主要描述用户是个什么样的人。它用 用户 ID 与自定义属性两部分组成。自定义属性是年龄、所在地、Tag等。在神策分析中，它的来源主要有三类，一类是使用者自己采集并且通过接口告之系统的，例如用户的注册信息；一类是基于使用者的第一方数据挖掘得到的用户画像数据；还有一类则是通过第三方供应商得到的用户在第三方行为所体现出来的属性和特质。在神策分析中，User 数据，是每一行对应一个用户，并且可以任意修改的。</p>

<h3>2.5 数据导入与存储</h3>

<p>正如前面所说，由于神策分析自身的产品需求，如导入时效性、不需要预先指定指标和维度等，目前比较热的开源 OLAP 系统如 Apache Kylin 和 Druid，我们并不能拿来使用。而最后，我们选择的是存储最细粒度数据，在每次查询时都从最细粒度数据开始使用 Impala 进行聚合和计算，而为了实现秒级查询，我们在存储部分做了很多优化，尽可能减少需要扫描的数据量以便加快数据的查询速度。</p>

<p>具体来说，虽然存储都是构建在 HDFS 之上，但是为了满足秒级导入和秒级查询，我们将存储分为 WOS（Write Optimized Store）和 ROS（Read Optimized Store）两部分，分别为写入和读取进行优化，并且 WOS 中的数据会逐渐转入 ROS 之中。</p>

<p>对于 ROS，我们选择了 Parquet，这样一个面向分析型业务的列式存储格式。并且，根据我们面临的业务的具体查询需求，对数据的分区方式做了很细致的优化。首先，我们是按照事件发生的日期和事件的名称，对数据做 Partition；同一个 Partition 内，会有多个文件，文件大小尽量保持在 512 MB 左右；每个文件内部先按照 userid 的 hash 有序，再按照 userid 有序，最后则按照事件发生的时间有序；会有一个单独的索引文件记录每个 user_id 对应数据的文件 offset。另外，与大多数列式存储一样，Parquet 对于不同类型的列，本身有着很好的压缩性能，也能大大减少扫描数据时的磁盘吞吐。简单来说，利用列式存储，只扫描必要的列，利用我们自己的数据分区方案，则在这个基础之上进一步只扫描需要的行，两者一起作用，共同减少需要扫描的数据量。</p>

<p>虽然 Parquet 是一个查询性能很好的列式存储，但是它是不能实时追加的，因此在 WOS 部分，我们选择了 Kudu。在向 Kudu 中写入数据时，我们选择了类似于 0/1 切换的方案，即同一时间只写入一张表，当这张表的写入达到阈值时，就写入新表中，而老表则会开始转为 Parquet 格式。</p>

<p>由于这样一个转换过程，不可避免地会带来 Parquet 的碎文件问题，因此也需要专门解决。</p>

<p>下图比较详细地展示了这样一个转化的过程：</p>

<center><img src="/images/2016/IMAG2016111507.jpeg"></center>


<p>在这个导入过程中，有如下几个关键的工作模块：</p>

<ul>
<li><p>KafkaConsumer：一个常驻内存的 MapReduce 程序 （只有 Mapper），负责实时从 Kafka 中订阅数据，并且写入到 Kudu 中。</p></li>
<li><p>KuduToParquet：一个不定时启动的 MapReduce 程序，在 Kudu 单个表写入达到阈值并且不再被写入时，将它转成多个 Parquet 文件，并且移动到对应的 Partition 中。</p></li>
<li><p>LoaderDemon：一个后台调度程序，完成一些元数据操作；</p></li>
<li><p>Merger：一个定时的 MapReduce 任务，定期合并 Parquet 中每个 Partition 内的碎文件。</p></li>
</ul>


<h3>2.6 数据查询子系统</h3>

<p>在数据查询部分，我们是通过 WebServer 这个模块，接收客户通过我们的 UI 界面或者通过 API 发起的查询请求，WebServer 并不做额外的处理，而是将这些查询请求直接转发给 QueryEngine 模块。QueryEngine 模块则是将查询请求翻译成 SQL，并在 Impala 中发起查询。Impala 会访问 Kudu 与 Parquet 数据共同构成的 View，完成对应的聚合与聚散。</p>

<p>特别提一下，在查询引擎部分，我们选择 Impala 而不是 Spark SQL，并不是出于性能或者稳定性的考虑，仅仅是因为我们团队之前有比较多的基于 Impala 的工作经验。</p>

<p>为了保证秒级的查询性能，我们除了在存储部分做文章以外，还在查询部分做了很多的优化，这些优化包括：</p>

<ul>
<li><p>界面上提供的查询模型有限，但是能满足客户绝大部分需求，因此专门针对这些查询模型做有针对性的优化；</p></li>
<li><p>使用 UDF/UDAF/UDAnF 等聚合函数替代 Join，提高查询效率，特别一提的是，由于 Impala 不支持 UDAnF，这里我们修改了 Impala 的源代码；</p></li>
<li><p>比较精细的缓存，只对有变化的数据才进行重查；</p></li>
<li><p>提供按用户抽样的功能，客户可以通过抽样数据快速尝试不同的猜想，并最终在全量数据上获取准确结果。</p></li>
</ul>


<h3>2.7 元数据与监控</h3>

<p>我们主要使用 MySQL、ZooKeeper 来存储元数据，主要包括类似于 Schema、维度字典、数据概览、漏斗、分群、预测的配置、任务调度、权限等信息。我们还用到了 Redis 来存储查询缓存信息。</p>

<p>我们有 Monitor 这样一个常驻内存的模块，对系统的各个部分进行语义监控，并且进行异常状态的修复。当然，作为一个商业系统，我们对于 license 的处理也是放在这个模块之中的。</p>

<p>同时，为了减少运维的代价，对一些不常用的功能，虽然不提供界面但是也工具化了，包括数据清理工具、版本升级工具、性能分析工具、多项目管理工具等。</p>

<h3>2.8 用户分群与用户行为预测</h3>

<p>用户分群与用户行为预测，我们单独放在最后面进行探讨。</p>

<p>神策分析对用户分群的定义，是根据用户以往的行为，给用户打标签。例如，找到那些"上个月有购买行为的用户"，"最近三个月有登录的用户"等。它是用户画像的一部分，但肯定不是用户画像的全部。</p>

<p>而与之相对的，用户行为预测则是根据用户过往的行为，预测将来做某个行为的概率。</p>

<h2>参考资料</h2>

<p>[1]<a href="https://demo.sensorsdata.cn/blog/technical_implementation_of_sensors_analytics">神策分析的技术选型与架构实现</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[【高可用架构】腾讯基于用户画像大数据的电商防刷架构]]></title>
    <link href="https://ibillxia.github.io/blog/2015/11/24/tencent-anti-fraud-architecture/"/>
    <updated>2015-11-24T13:53:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2015/11/24/tencent-anti-fraud-architecture</id>
    <content type="html"><![CDATA[<h2>一、背景介绍</h2>

<p>最近1~2年电商行业飞速发展，各种创业公司犹如雨后春笋大量涌现，商家通过各种活动形式的补贴来获取用户、培养用户的消费习惯。</p>

<p>但任何一件事情都具有两面性，高额的补贴、优惠同时了也催生了"羊毛党"。</p>

<p>"羊毛党"的行为距离欺诈只有一步之遥，他们的存在严重破环了活动的目的，侵占了活动的资源，使得正常的用户享受不到活动的直接好处。</p>

<p>今天主要分享下腾讯自己是如何通过大数据、用户画像、建模来防止被刷、恶意撞库的。</p>

<!--more-->


<h2>二、黑产现状介绍</h2>

<p>"羊毛党"一般先利用自动机注册大量的目标网站的账号，当目标网站搞促销、优惠等活动的时候，利用这些账号参与活动刷取较多的优惠，最后通过淘宝等电商平台转卖获益。</p>

<h3>2.1、羊毛党分工</h3>

<p>他们内部有着明确的分工，形成了几大团伙，全国在20万人左右：</p>

<ol>
<li><p>软件制作团伙：专门制作各种自动、半自动的黑产工具，比如注册自动机、刷单自动机等；他们主要靠出售各种黑产工具、提供升级服务等形式来获利。</p></li>
<li><p>短信代接平台：实现手机短信的自动收发，其实一些平台亦正亦邪，不但提供给正常的商家使用，一些黑产也会购买相关的服务。</p></li>
<li><p>账号出售团伙：他们主要是大量注册各种账号，通过转卖账号来获利；该团伙与刷单团伙往往属于同一团伙。</p></li>
<li><p>刷单团伙：到各种电商平台刷单，获取优惠，并且通过第三方的电商平台出售优惠，实现套现。</p></li>
</ol>


<center><img src="/images/2015/IMAG2015112401.png"></center>


<h3>2.2、"羊毛党"从业特点</h3>

<p>这些黑产团队，有三个特点：</p>

<ol>
<li><p>专业化：专业团队、人员、机器来做。</p></li>
<li><p>团伙化：黑产已经形成一定规模的团伙，而且分工明确；从刷单软件制作、短信代收发平台、电商刷单到变卖套现等环节，已经形成完整的刷单团伙。</p></li>
<li><p>地域化：黑产刷单团伙基本分布在沿海的一些经济发达城市，比如，北京、上海、广东等城市，这或许跟发达城市更加容易接触到新事物、新观念有关。</p></li>
</ol>


<center><img src="/images/2015/IMAG2015112402.png"></center>


<h3>2.3、对抗刷单的思路</h3>

<p>对抗刷单，一般来讲主要从三个环节入手：</p>

<ol>
<li><p>注册环节：识别虚假注册、减少"羊毛党"能够使用的账号量。在注册环节识别虚假注册的账号，并进行拦截和打击。</p></li>
<li><p>登录场景：提高虚假账号登录门槛，从而减少能够到达活动环节的虚假账号量。比如，登录环节通过验证码、短信验证码等手段来降低自动机的登录效率，从而达到减少虚假账号登录量、减轻活动现场安全压力的目的。</p></li>
<li><p>活动环节：这个是防刷单对抗的主战场，也是减少"羊毛党"获利的直接战场；这里的对抗措施，一般有两个方面：</p></li>
</ol>


<p>1）通过验证码（短信、语音）降低黑产刷单的效率。</p>

<p>2）大幅度降低异常账号的优惠力度。</p>

<h2>三、腾讯内部防刷架构</h2>

<h3>3.1、腾讯内部防刷的架构图</h3>

<center><img src="/images/2015/IMAG2015112403.png"></center>


<h3>3.2、模块详细介绍</h3>

<h4>1、风险学习引擎</h4>

<p>风险学习引擎：效率问题。由于主要的工作都是线下进行，所以线上系统不存在学习的效率问题。线上采用的都是C++实现的DBScan等针对大数据的快速聚类算法，基本不用考虑性能问题。</p>

<p>风险学习引擎：采用了黑/白双分类器风险判定机制。之所以采用黑/白双分类器的原因就在于减少对正常用户的误伤。</p>

<p>例如，某个IP是恶意的IP，那么该IP上可能会有一些正常的用户，比如大网关IP。</p>

<p>再比如，黑产通过ADSL拨号上网，那么就会造成恶意与正常用户共用一个IP的情况。</p>

<p>黑分类器：根据特征、机器学习算法、规则/经验模型，来判断本次请求异常的概率。</p>

<p>白分类器：判断属于正常请求的概率。</p>

<center><img src="/images/2015/IMAG2015112404.jpeg"></center>


<h4>2、矩阵式逻辑框架</h4>

<p>我们以黑分类器为例来剖析下分类器的整个逻辑框架。</p>

<p>总的来讲我们采用了矩阵式的逻辑框架，最开始的黑分类器我们也是一把抓，随意的建立一个个针对黑产的检测规则、模型。</p>

<p>结果发现不是这个逻辑漏过了，而是那个逻辑误伤量大，要对那一类的账号加强安全打击力度，改动起来也非常麻烦。</p>

<p>因此我们就设计了这个一个矩阵式的框架来解决上述问题。</p>

<center><img src="/images/2015/IMAG2015112405.jpeg"></center>


<p>矩阵的横向采用了Adaboost方法，该方法是一种迭代算法，其核心思想是针对同一个训练集训练不同的弱分类器，然后把这些分类器集合起来，构成一个最终的分类器。</p>

<p>而我们这里每一个弱分类器都只能解决一种帐号类型的安全风险判断，集中起来才能解决所有账户的风险检测。</p>

<p>那么在工程实践上带来三个好处：</p>

<ol>
<li><p>便于实现轻重分离，比如某平台虚假账号集中在邮箱账号，策略就可以加大对邮箱账号的打击力度，影响范围也局限在邮箱帐号，而不是该平台所有的账号。</p></li>
<li><p>减少模型训练的难度，模型训练最大的难度在于样本的均衡性问题，拆分成子问题，就不需要考虑不同账号类型之间的数据配比、均衡性问题，大大降低了模型训练时正负样本比率的问题。</p></li>
<li><p>逻辑的健壮性，某一个分类器的训练出现了问题，受影响的范围不至于扩展到全局。</p></li>
</ol>


<p>矩阵纵向采用了Bagging方法，该方法是一种用来提高学习算法准确度的方法，该方法在同一个训练集合上构造预测函数系列，然后以一定的方法将他们组合成一个预测函数，从而来提高预测结果的准确性。</p>

<h2>四、腾讯大数据收集纬度</h2>

<p>大数据一直在安全对抗领域发挥着重要的作用，从我们的对抗经验来看，大数据不仅仅是数据规模很大，而且还包括两个方面：</p>

<ol>
<li><p>数据广度：要有丰富的数据类型。比如，不仅仅要有社交领域的数据、还要有游戏、支付、自媒体等领域的数据，这样就提供了一个广阔的视野让我们来看待黑产的行为特点。</p></li>
<li><p>数据深度：黑产的对抗。我们一直强调纵深防御，我们不仅仅要有注册数据，还要有登录，以及账号的使用的数据，这样我们才能更好的识别恶意。</p></li>
</ol>


<p>所以想要做风控和大数据的团队，一定要注意在自己的产品上多埋点，拿到足够多的数据，先沉淀下来。</p>

<h2>五、腾讯大数据处理平台－魔方</h2>

<p>我们的团队研发了一个叫魔方的大数据处理和分析的平台，底层我们集成了MySQL、MongoDB，Spark、Hadoop等技术，在用户层面我们只需要写一些简单的SQL语句、完成一些配置就可以实现例行分析。</p>

<p>这里我们收集了社交、电商、支付、游戏等场景的数据，针对这些数据我们建立一些模型，发现哪些是恶意的数据，并且将数据沉淀下来。</p>

<p>沉淀下来的对安全有意义的数据，一方面就存储在魔方平台上，供线下审计做模型使用；另一方面会做成实时的服务，提供给线上的系统查询使用。</p>

<h3>5.1、腾讯用户画像沉淀方法</h3>

<p>画像，本质上就是给账号、设备等打标签。</p>

<p>用户画像 ＝ 打标签</p>

<p>我们这里主要从安全的角度出发来打标签，比如IP画像，我们会标注IP是不是代理IP，这些对我们做策略是有帮助的。</p>

<p>以QQ的画像为例，比如，一个QQ只登录IM、不登录其他腾讯的业务、不聊天、频繁的加好友、被好友删除、QQ空间要么没开通、要么开通了QQ空间但是评论多但回复少，这种号码我们一般会标注QQ养号（色情、营销），类似的我们也会给QQ打上其他标签。</p>

<p>标签的类别和明细，需要做风控的人自己去设定，比如：地理位置，按省份标记。性别，安男女标记。其他细致规则以此规律自己去设定。</p>

<p>我们看看腾讯的IP画像，沉淀的逻辑如下图：</p>

<center><img src="/images/2015/IMAG2015112406.jpeg"></center>


<p>一般的业务都有针对IP的频率、次数限制的策略，那么黑产为了对抗，必然会大量采用代理IP来绕过限制。</p>

<p>既然代理IP的识别如此重要，那我们就以代理IP为例来谈下腾讯识别代理IP的过程。</p>

<p>识别一个IP是不是代理IP，技术不外乎就是如下四种：</p>

<ol>
<li><p>反向探测技术：扫描IP是不是开通了80,8080等代理服务器经常开通的端口，显然一个普通的用户IP不太可能开通如上的端口。</p></li>
<li><p>HTTP头部的X_Forwarded_For：开通了HTTP代理的IP可以通过此法来识别是不是代理IP；如果带有XFF信息，该IP是代理IP无疑。</p></li>
<li><p>Keep-alive报文：如果带有Proxy-Connection的Keep-alive报文，该IP毫无疑问是代理IP。</p></li>
<li><p>查看IP上端口：如果一个IP有的端口大于10000，那么该IP大多也存在问题，普通的家庭IP开这么大的端口几乎是不可能的。</p></li>
</ol>


<p>以上代理IP检测的方法几乎都是公开的，但是盲目去扫描全网的IP，被拦截不说，效率也是一个很大的问题。</p>

<p>因此，我们的除了利用网络爬虫爬取代理IP外，还利用如下办法来加快代理IP的收集：通过业务建模，收集恶意IP（黑产使用代理IP的可能性比较大）然后再通过协议扫描的方式来判断这些IP是不是代理IP。每天腾讯都能发现千万级别的恶意IP，其中大部分还是代理IP。</p>

<h3>5.2、腾讯用户画像类别概览</h3>

<center><img src="/images/2015/IMAG2015112407.jpeg"></center>


<h3>5.3、防御逻辑</h3>

<center><img src="/images/2015/IMAG2015112408.jpeg"></center>


<p>实时系统使用C/C++开发实现，所有的数据通过共享内存的方式进行存储，相比其他的系统，安全系统更有他自己特殊的情况，因此这里我们可以使用"有损"的思路来实现，大大降低了开发成本和难度。</p>

<p>数据一致性，多台机器，使用共享内存，如何保障数据一致性？</p>

<p>其实，安全策略不需要做到强数据一致性。</p>

<p>从安全本身的角度看，风险本身就是一个概率值，不确定，所以有一点数据不一致，不影响全局。</p>

<p>但是安全系统也有自己的特点，安全系统一般突发流量比较大，我们这里就需要设置各种应急开关，而且需要微信号、短信等方式方便快速切换，避免将影响扩散到后端系统。</p>

<h3>5.4、接入系统</h3>

<center><img src="/images/2015/IMAG2015112409.png"></center>




<center><img src="/images/2015/IMAG2015112410.jpeg"></center>


<p>适应的场景包括：</p>

<ul>
<li><p>电商o2o刷单、刷券、刷红包</p></li>
<li><p>防止虚假账号注册</p></li>
<li><p>防止用户名、密码被撞库</p></li>
<li><p>防止恶意登录</p></li>
</ul>


<h2>Q&amp;A</h2>

<p>Q：风险学习引擎是自研的，还是使用的开源库？</p>

<p>风险学习引擎包括两个部分，线上和线下两部分：</p>

<ul>
<li><p>线上：自己利用c/c++来实现。</p></li>
<li><p>线下：涉及利用python开源库来做的，主要是一些通用算法的训练和调优。</p></li>
</ul>


<p>Q：请问魔方平台中用到的MongDB是不是经过改造？因为MongDB一直不被看好，出现问题也比较多。</p>

<p>我们做了部分改造，主要是DB的引擎方面。</p>

<p>Q：请问黑分类器和白分类器有什么区别？</p>

<p>白分类器主要用来识别正常用户，黑分类器识别虚假用户。</p>

<p>Q：风险概率的权重指标是如何考虑的？</p>

<p>先通过正负样本进行训练，并且做参数显著性检查；然后，人工会抽查一些参数的权重，看看跟经验是否相符。</p>

<p>Q：安全跟风控职责如何区分呢？</p>

<p>相比安全，风控的外延更丰富，更注重宏观全局；针对一个公司来讲，风控是包括安全、法务、公关、媒体、客服等在内一整套应急处理预案。</p>

<p>Q：如果识别错了，误伤了正常用户会造成什么后果么？比如影响单次操作还是会一直失败。</p>

<p>如果识别错了正常用户不会被误伤，但是会导致体验多加了一个环节，如弹出验证码、或者人工客服核对等。</p>

<p>作者：颜国平，原腾讯云－天御系统研发负责人。一直负责腾讯自有验证码、业务安全、防刷、账号安全等研发工作。内部支持的产品（游戏、电商、腾讯投资的O2O企业）非常广泛。在业务安全领域项目经验丰富，并且具备深度学习、大数据架构搭建等实战经验。</p>

<h2>参考资料</h2>

<p>[1]<a href="https://cloud.tencent.com/developer/article/1004423">基于用户画像大数据的电商防刷架构 - 高可用架构</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[【标点符】腾讯分析系统架构解析]]></title>
    <link href="https://ibillxia.github.io/blog/2015/11/03/tencent-analytics-architecture/"/>
    <updated>2015-11-03T15:54:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2015/11/03/tencent-analytics-architecture</id>
    <content type="html"><![CDATA[<center><img src="/images/2015/IMAG2015110300.jpg"></center>


<p>TA（Tencent Analytics，腾讯分析）是一款面向第三方站长的免费网站分析系统，在数据稳定性、及时性方面广受站长好评，其秒级的实时数据更新频率也获得业界的认可。本文将从实时数据处理、数据存储等多个方面带你深入探寻TA的系统架构及实现原理。</p>

<p>网站分析（Web Analytics）主要指的是基于网站的用户浏览行为，对网站的点击流数据和运营数据进行分析，以监控网站的运营状况，为网站的优化提供决策依据。网站分析系统已成为站长日常运营必不可少的工具，业界比较流行的网站分析系统主要有Google Analytics、CNZZ和百度统计等产品。</p>

<p>TA作为网站分析产品的后起之秀在社区分析、用户画像、网站工具等多方面形成了自己的特色，其秒级的实时数据更新频率更是业界翘楚。在数据稳定性、准确性和及时性方面，TA在站长圈也是享有良好的口碑。随着接入业务量的不断发展，TA日均需要处理和计算的数据量达到TB级。如此庞大的数据量想要达到秒级实时且保证系统的高可用并非件易事。</p>

<p>TA的实时计算框架借鉴了一些业界流行的流式计算系统的思路。虽然在构建系统中遇到了一些问题，但由于海量数据的实时处理、实时存储具备一定的典型性与通用性，所以将TA的解决方案分享出来，希望能给大家一些启示。</p>

<!--more-->


<h2>基本原理及系统架构</h2>

<p>TA的基本原理是通过嵌入站长网站的JavaScript脚本收集用户访问行为数据，并发送TA采集集群，采集集群收到数据后将其过滤、编码、格式化后继续向后分发。数据处理集群负责按照业务逻辑计算数据，并将计算结果"写入"到数据存储集群，最后将结果数据展现给广大站长使用。TA的基本原理如图所示。</p>

<center><img src="/images/2015/IMAG2015110301.png"></center>


<p>TA后台是一套完整的数据流处理系统：由JavaScript采集的用户行为数据像川流不息的河水一样流入TA后台，经过清洗、计算后源源不断地流出到TA存储集群，供用户浏览和查询。TA的具体架构及核心部件如图所示。</p>

<center><img src="/images/2015/IMAG2015110302.png"></center>


<p>TA的后台分为离线和实时两部分：实时部分负责系统的主要功能计算，数据更新频率为秒级；离线部分负责系统复杂的关联分析及跨天计算，数据更新频率为天级。</p>

<ul>
<li><p>Http Access：主要负责HTTP协议的解析，数据的清洗及格式化。</p></li>
<li><p>ESC：Event Streaming Coder，主要负责将系统不可枚举的数据类型编码成为整型，并将对应关系持久化。</p></li>
<li><p>ESP：Event Streaming Processor，主要负责将数据按照站点、UID重新组织并计算PV、UV、停留时长和蹦失率等网站分析指标。</p></li>
<li><p>ESA：Event Streaming Aggregator，主要负责汇总ESP计算后的数据按照站点，并写入到Redis。</p></li>
<li><p>Center：系统的中心节点，负责系统配置、数据路由管理，并承担容灾切换功能。</p></li>
<li><p>Logserver：负责将Access收集到的数据以字符串形式写入文件，并上传到TDCP上。</p></li>
<li><p>TDCP：腾讯分布式计算平台，负责离线数据的计算，并由脚本将结果数据写入MySQL中。</p></li>
</ul>


<h2>实时解决方案</h2>

<p>在介绍TA实时解决方案前，我们先来了解下TA支撑的业务量。当前TA日均需要处理几十万网站的上TB级数据，处理过后的URL个数仍有上亿条，系统存储的key个数超过十亿。如何高效、低延迟地处理如此大量的业务数据是TA实时系统面临的主要挑战。TA解决方案的主要思路可以概括为数据全二进制化、计算全内存化、存储NoSQL化。下面就实时计算和实时存储这两大子系统进行深入的讨论。</p>

<h3>实时计算</h3>

<p>对于计算子系统，我们参考了Hadoop、S4和Storm等开源项目，力图设计为一个较为通用，扩展性较强的全内存实时Event处理系统（或者套用流行的术语称为流式实时Event处理系统）。对于这样的一个系统，我们设计支持的典型输入输出流程大致如图所示。</p>

<center><img src="/images/2015/IMAG2015110303.png"></center>


<p>实时计算系统的设计要点在数据组织、协议和增量计算模型上。</p>

<p>数据组织。万物皆int，考虑到内存以及计算过程的性能需求，我们将所有非int的数据类型转化为int。可以枚举的数据类型，将其配置化映射为唯一int；不可枚举的数据类型，则利用MD5算法近似得到唯一的int。例如，页面URL属于不可枚举的类型，则预处理通过MD5算法近似得到唯一的int；UserAgent里的浏览器类型字符串则属于可枚举的数据，则预先配置化映射为int。这个方法节省了较多内存，提高了整个系统的计算性能。</p>

<p>协议。协议层面上，我们首先设计实现了一种可扩展的Event结构，这种Event结构支持半自动化的序列化/反序列化机制（参考自msgpack的设计）和紧凑的二进制编码（基于Zigzag编码，参考Protobuf的实现）。这种Event结构在流式高性能I/O（网络传输和持久化）方面表现得相当良好。实时计算子系统被设计为可以扩展支持任意的Event实现。</p>

<p>增量计算模型。增量计算模型，指的是基本计算过程，被定义为以下三部分（如图所示）</p>

<center><img src="/images/2015/IMAG2015110304.png"></center>


<ul>
<li><p>Processor：负责具体业务逻辑的计算处理。</p></li>
<li><p>Data Holder：负责保存增量结果数据，以及计算依赖的中间状态数据。</p></li>
<li><p>Emitter：负责定期输出清空增量计算结果。</p></li>
</ul>


<p>具体到流程方面，分为以下三步（如图所示）。</p>

<center><img src="/images/2015/IMAG2015110305.png"></center>


<ul>
<li><p>接收Event，计算处理—Processor。</p></li>
<li><p>保存计算结果以及计算依赖中间数据—DataHolder。</p></li>
<li><p>定时触发输出时间片内计算结果，清空计算结果—Emitter。</p></li>
</ul>


<p>增量计算模型弱化了分布式系统中单台机器的事务状态，相应地简化了分布式计算系统的实现，同时也提高了整个系统的性能。</p>

<h3>实时存储</h3>

<p>在TA系统中，实时存储的数据都是需要通过Web展示层读取的统计数据。这类数据存在两个典型特点。</p>

<ul>
<li><p>频繁更新写。更新频度视系统实时性而定，每条统计结果更新频度最快可以达到1秒。</p></li>
<li><p>少量读取。"少量"是相对上述更新而言的。同时根据业务逻辑，可将统计数据划分为两类。</p></li>
<li><p>固定不变数据：主要是URL、搜索关键词等数据。这一部分数据理论上是在不停地增加，不会修改旧有数据。</p></li>
<li><p>动态数据：主要是频繁更新的结果统计数据。这一部分数据则需要不停地更新。例如，www.qq.com域名下的PV和UV统计结果。</p></li>
</ul>


<p>考虑到上述的TA实时统计数据的特点，我们选择NoSQL实现我们的存储系统；同时，针对两类不同的数据类型，分别选用LevelDB和Redis来存储。</p>

<h3>Redis</h3>

<p>TA实时存储的主要构件。考虑到TA系统本身就是一个比较完善的分布式集群系统，因此我们需要的存储构件是"not clustering, but sharding"。也就是说像HBase和MongoDB这样的"重武器"并不适合TA，而NoSQL数据库中的"瑞士军刀"Redis凭借其出色的性能走入我们的视野。同</p>

<p>时TA的结果数据类型也比较丰富，有像站点PV、UV、VV和IP等Hash类型的数据，也有像用户访问轨迹这样set类型的"动态数据"，而Redis丰富的数据结构很好地完成了这项任务。</p>

<p>选择Redis的另一个原因是它足够简单且易于扩展。在实际应用的过程中，我们发现的问题都可以通过扩展Redis命令来解决。</p>

<p>例如，TA中有这样的一种应用场景：为了消除ESA模块的状态，存储在Redis中的数据往往并不是最终的结果数据，而是还需要进一步运算的中间数据。像bounce rate这个指标（bouncerate=bounce session数/total session数），需要前台查询两次再做一次运算后最终展示给用户。在高并发的情况下，无疑会影响系统的响应速度。</p>

<p>本着"移动计算，而不是移动数据"的原则，我们对Redis的sort、hmget命令进行了扩展使其支持四则运算，成功地将原来的两次查询优化为一次。扩展四则运算的另外一个目的是可以"通过计算换取存储"，例如需要将两种类型加总成总和的类型数据，可以只存储两份，加总数据"通过计算换取"。</p>

<p>除了数据读取，数据的写入也可以进行类似合并数据的优化。例如，TA在写入URL的PV、UV、VV、IP、停留时长和bounce rate这6个指标时，需要调用6次Redis命令。而实际上这6个指标是存储在同一个Hash内的，通过扩展hmincrby命令，支持将Hash的所有field一次更改，便能将调用次数优化至一次。上线之后也取得了良好的效果，峰值时的CPU利用率几乎下降了一半，同时也大幅提升了上层模块ESA的吞吐量。</p>

<h3>LevelDB</h3>

<p>它是Redis的有效补充。考虑到Redis为内存数据库，而使用内存的成本要高于硬盘，因此选择引入了基于磁盘存储的LevelDB作为补充。由于LevelDB的写性能足够好，而读性能也远远超过目前"在线少量读取"的需求，所以我们选择LevelDB存储"固定不变数据"。</p>

<p>在数据存储的架构设计上，由于实时数据服务与在线系统，可靠性要求较高，因此我们主要采取双写复制+Sharding的设计方法。</p>

<p>双写复制。所有的数据存储都会至少同步写两份，以提高在线系统服务的可用性。</p>

<p>数据分片（Sharding）。</p>

<p>基于域名：所有的数据以域名为单位组织分片；任何域名可以调整到任意分片中；单个域名数据原则上存储在一个分片中。</p>

<p>动态调整（如图所示）：只调整分片策略，不移动数据；基于数据量计算分片负载。</p>

<center><img src="/images/2015/IMAG2015110306.png"></center>


<p>此外，针对分片集群数据的查询，我们主要做了三项工作（如图所示）。</p>

<center><img src="/images/2015/IMAG2015110307.png"></center>


<ul>
<li><p>Redis Protocol Stack是一个较为完整的Redis协议栈，是上层应用的基础。直接用Redis协议作为对外提供查询的通用协议，这样外部用户可直接通过目前各种Redis Client实现来查询访问数据。Query Rule Engine是一个灵活的查询引擎。能够根据规则智能地在多个Redis、LevelDB数据源中查询，执行类join的操作；也简单扩展支持其他的异构数据源，如MySQL、HBase等。</p></li>
<li><p>Query Compute Engine是一个实时查询计算引擎，能根据基础查询结果实时计算。引入此部分的主要目的在于减少Redis数据空间占用。</p></li>
</ul>


<h2>未来展望</h2>

<p>目前TA虽然在后台上已经做到数据秒级更新，但展示方式仍为传统的静态方式。后续TA会在数据的动态刷新上进行更多尝试，让站长可以第一时间了解网站营销效果，时刻感受网站心跳。</p>

<h2>参考资料</h2>

<p>[1]<a href="https://www.biaodianfu.com/tencent-analytics.html">腾讯分析系统架构解析 - 标点符</a></p>
]]></content>
  </entry>
  
</feed>