<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Tag: 流计算 | Bill's Blog]]></title>
  <link href="https://ibillxia.github.io/blog/tags/liu-ji-suan/atom.xml" rel="self"/>
  <link href="https://ibillxia.github.io/"/>
  <updated>2026-07-12T20:22:44+08:00</updated>
  <id>https://ibillxia.github.io/</id>
  <author>
    <name><![CDATA[Bill Xia]]></name>
    <email><![CDATA[ibillxia@gmail.com]]></email>
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Apache Flink漫谈系列：State状态管理]]></title>
    <link href="https://ibillxia.github.io/blog/2022/06/01/flink-state-management/"/>
    <updated>2022-06-01T11:28:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2022/06/01/flink-state-management</id>
    <content type="html"><![CDATA[<p>原文链接：<a href="https://developer.aliyun.com/article/667562">阿里云开发者社区：Apache Flink 漫谈系列(04) - State</a></p>

<h1>实际问题</h1>

<p>在流计算场景中，数据会源源不断的流入Apache Flink系统，每条数据进入Apache Flink系统都会触发计算。如果我们想进行一个Count聚合计算，那么每次触发计算是将历史上所有流入的数据重新新计算一次，还是每次计算都是在上一次计算结果之上进行增量计算呢？答案是肯定的，Apache Flink是基于上一次的计算结果进行增量计算的。那么问题来了: “上一次的计算结果保存在哪里，保存在内存可以吗？”，</p>

<p>答案是否定的，如果保存在内存，在由于网络，硬件等原因造成某个计算节点失败的情况下，上一次计算结果会丢失，在节点恢复的时候，就需要将历史上所有数据（可能十几天，上百天的数据）重新计算一次，所以为了避免这种灾难性的问题发生，Apache Flink 会利用State存储计算结果。本篇将会为大家介绍Apache Flink State的相关内容。</p>

<!--more-->


<h1>什么是State</h1>

<p>这个问题似乎有些”弱智”？不管问题的答案是否显而易见，但我还是想简单说一下在Apache Flink里面什么是State？State是指流计算过程中计算节点的中间计算结果或元数据属性，比如 在aggregation过程中要在state中记录中间聚合结果，比如 Apache Kafka 作为数据源时候，我们也要记录已经读取记录的offset，这些State数据在计算过程中会进行持久化(插入或更新)。所以Apache Flink中的State就是与时间相关的，Apache Flink任务的内部数据（计算数据和元数据属性）的快照。</p>

<h1>为什么需要State</h1>

<p>与批计算相比，State是流计算特有的，批计算没有failover机制，要么成功，要么重新计算。流计算在 大多数场景 下是增量计算，数据逐条处理（大多数场景)，每次计算是在上一次计算结果之上进行处理的，这样的机制势必要将上一次的计算结果进行存储（生产模式要持久化），另外由于 机器，网络，脏数据等原因导致的程序错误，在重启job时候需要从成功的检查点(checkpoint，后面篇章会专门介绍)进行state的恢复。增量计算，Failover这些机制都需要state的支撑。</p>

<h1>State 实现</h1>

<p>Apache Flink内部有四种state的存储实现，具体如下：</p>

<ul>
<li><p>基于内存的HeapStateBackend - 在debug模式使用，不 建议在生产模式下应用；</p></li>
<li><p>基于HDFS的FsStateBackend - 分布式文件持久化，每次读写都产生网络IO，整体性能不佳；</p></li>
<li><p>基于RocksDB的RocksDBStateBackend - 本地文件+异步HDFS持久化；</p></li>
<li><p>还有一个是基于Niagara(Alibaba企业版Flink)NiagaraStateBackend - 分布式持久化- 在Alibaba生产环境应用；</p></li>
</ul>


<h2>State 持久化逻辑</h2>

<ul>
<li>Apache Flink版本选择用RocksDB+HDFS的方式进行State的存储，State存储分两个阶段，首先本地存储到RocksDB，然后异步的同步到远程的HDFS。 这样而设计既消除了HeapStateBackend的局限（内存大小，机器坏掉丢失等），也减少了纯分布式存储的网络IO开销。</li>
</ul>


<h2>State 分类</h2>

<p>Apache Flink 内部按照算子和数据分组角度将State划分为如下两类：</p>

<ul>
<li><p>KeyedState - 这里面的key是我们在SQL语句中对应的GroupBy/PartitioneBy里面的字段，key的值就是groupby/PartitionBy字段组成的Row的字节数组，每一个key都有一个属于自己的State，key与key之间的State是不可见的；</p></li>
<li><p>OperatorState - Apache Flink内部的Source Connector的实现中就会用OperatorState来记录source数据读取的offset。</p></li>
</ul>


<h2>State 扩容重新分配</h2>

<p>Apache Flink是一个大规模并行分布式系统，允许大规模的有状态流处理。 为了可伸缩性，Apache Flink作业在逻辑上被分解成operator graph，并且每个operator的执行被物理地分解成多个并行运算符实例。 从概念上讲，Apache Flink中的每个并行运算符实例都是一个独立的任务，可以在自己的机器上调度到网络连接的其他机器运行。</p>

<p>Apache Flink的DAG图中只有边相连的节点有网络通信，也就是整个DAG在垂直方向有网络IO，在水平方向如下图的stateful节点之间没有网络通信，这种模型也保证了每个operator实例维护一份自己的state，并且保存在本地磁盘（远程异步同步）。通过这种设计，任务的所有状态数据都是本地的，并且状态访问不需要任务之间的网络通信。 避免这种流量对于像Apache Flink这样的大规模并行分布式系统的可扩展性至关重要。</p>

<p>如上我们知道Apache Flink中State有OperatorState和KeyedState，那么在进行扩容时候（增加并发）State如何分配呢？比如：外部Source有5个partition，在Apache Flink上面由Srouce的1个并发扩容到2个并发，中间Stateful Operation 节点由2个并发并扩容的3个并发，如下图所示:</p>

<center><img src="/images/2022/IMAG2022060101.png"></center>


<p>在Apache Flink中对不同类型的State有不同的扩容方法，接下来我们分别介绍。</p>

<h2>OperatorState对扩容的处理</h2>

<p>我们选取Apache Flink中某个具体Connector实现实例进行介绍，以MetaQ为例，MetaQ以topic方式订阅数据，每个topic会有N>0个分区，以上图为例，加上我们订阅的MetaQ的topic有5个分区，那么当我们source由1个并发调整为2个并发时候，State是怎么恢复的呢？state 恢复的方式与Source中OperatorState的存储结构有必然关系，我们先看MetaQSource的实现是如何存储State的。首先MetaQSource 实现了ListCheckpointed，其中的T是Tuple2，我们在看ListCheckpointed接口的内部定义如下：</p>

<pre><code>public interface ListCheckpointed&lt;T extends Serializable&gt;; {
    List&lt;T&gt; snapshotState(long var1, long var3) throws Exception;

    void restoreState(List&amp;lt;T&amp;gt; var1) throws Exception;
}
</code></pre>

<p>我们发现 snapshotState方法的返回值是一个List,T是Tuple2，也就是snapshotState方法返回List>,这个类型说明state的存储是一个包含partiton和offset信息的列表，InputSplit代表一个分区，Long代表当前partition读取的offset。InputSplit有一个方法如下：</p>

<pre><code>public interface InputSplit extends Serializable {
    int getSplitNumber();
}
</code></pre>

<p>也就是说，InputSplit我们可以理解为是一个Partition索引，有了这个数据结构我们在看看上面图所示的case是如何工作的？当Source的并行度是1的时候，所有打partition数据都在同一个线程中读取，所有partition的state也在同一个state中维护，State存储信息格式如下：</p>

<center><img src="/images/2022/IMAG2022060102.png"></center>


<p>如果我们现在将并发调整为2，那么我们5个分区的State将会在2个独立的任务（线程）中进行维护，在内部实现中我们有如下算法进行分配每个Task所处理和维护partition的State信息，如下：</p>

<pre><code>List&lt;Integer&gt; assignedPartitions = new LinkedList&lt;&gt;();
for (int i = 0; i &lt; partitions; i++) {
        if (i % consumerCount == consumerIndex) {
                assignedPartitions.add(i);
        }
}
</code></pre>

<p>这个求mod的算法，决定了每个并发所处理和维护partition的State信息，针对我们当前的case具体的存储情况如下：</p>

<center><img src="/images/2022/IMAG2022060103.png"></center>


<p>那么到现在我们发现上面扩容后State得以很好的分配得益于OperatorState采用了List的数据结构的设计。另外大家注意一个问题，相信大家已经发现上面分配partition的算法有一个限制，那就是Source的扩容（并发数）是否可以超过Source物理存储的partition数量呢？答案是否定的，不能。目前Apache Flink的做法是提前报错，即使不报错也是资源的浪费，因为超过partition数量的并发永远分配不到待管理的partition。</p>

<h2>KeyedState对扩容的处理</h2>

<p>对于KeyedState最容易想到的是hash(key) mod parallelism(operator) 方式分配state，就和OperatorState一样，这种分配方式大多数情况是恢复的state不是本地已有的state，需要一次网络拷贝，这种效率比较低，OperatorState采用这种简单的方式进行处理是因为OperatorState的state一般都比较小，网络拉取的成本很小，对于KeyedState往往很大，我们会有更好的选择，在Apache Flink中采用的是Key-Groups方式进行分配。</p>

<h3>什么是Key-Groups</h3>

<p>Key-Groups 是Apache Flink中对keyed state按照key进行分组的方式，每个key-group中会包含N>0个key，一个key-group是State分配的原子单位。在Apache Flink中关于Key-Group的对象是 KeyGroupRange, 如下：</p>

<pre><code>public class KeyGroupRange implements KeyGroupsList, Serializable {
        ...
        ...
        private final int startKeyGroup;
        private final int endKeyGroup;
        ...
        ...
}
</code></pre>

<p>KeyGroupRange两个重要的属性就是 startKeyGroup和endKeyGroup，定义了startKeyGroup和endKeyGroup属性后Operator上面的Key-Group的个数也就确定了。</p>

<h3>什么决定Key-Groups的个数</h3>

<p>key-group的数量在job启动前必须是确定的且运行中不能改变。由于key-group是state分配的原子单位，而每个operator并行实例至少包含一个key-group，因此operator的最大并行度不能超过设定的key-group的个数，那么在Apache Flink的内部实现上key-group的数量就是最大并行度的值。</p>

<p>GroupRange.of(0, maxParallelism)如何决定key属于哪个Key-Group确定好GroupRange之后，如何决定每个Key属于哪个Key-Group呢？我们采取的是取mod的方式，在KeyGroupRangeAssignment中的assignToKeyGroup方法会将key划分到指定的key-group中，如下：</p>

<pre><code>public static int assignToKeyGroup(Object key, int maxParallelism) {
      return computeKeyGroupForKeyHash(key.hashCode(), maxParallelism);
}

public static int computeKeyGroupForKeyHash(int keyHash, int maxParallelism) {
      return HashPartitioner.INSTANCE.partition(keyHash, maxParallelism);
}

@Override
public int partition(T key, int numPartitions) {
      return MathUtils.murmurHash(Objects.hashCode(key)) % numPartitions;
}
</code></pre>

<p>如上实现我们了解到分配Key到指定的key-group的逻辑是利用key的hashCode和maxParallelism进行取余操作来分配的。如下图当parallelism=2,maxParallelism=10的情况下流上key与key-group的对应关系如下图所示：</p>

<center><img src="/images/2022/IMAG2022060104.png"></center>


<p>如上图key(a)的hashCode是97，与最大并发10取余后是7，被分配到了KG-7中，流上每个event都会分配到KG-0至KG-9其中一个Key-Group中。每个Operator实例如何获取Key-Groups 了解了Key-Groups概念和如何分配每个Key到指定的Key-Groups之后，我们看看如何计算每个Operator实例所处理的Key-Groups。 在KeyGroupRangeAssignment的computeKeyGroupRangeForOperatorIndex方法描述了分配算法：</p>

<pre><code>public static KeyGroupRange computeKeyGroupRangeForOperatorIndex(
      int maxParallelism,
      int parallelism,
      int operatorIndex) {
        GroupRange splitRange = GroupRange.of(0, maxParallelism).getSplitRange(parallelism, operatorIndex);
        int startGroup = splitRange.getStartGroup();
        int endGroup = splitRange.getEndGroup();
   return new KeyGroupRange(startGroup, endGroup - 1);
}

public GroupRange getSplitRange(int numSplits, int splitIndex) {
        ...
        final int numGroupsPerSplit = getNumGroups() / numSplits;
        final int numFatSplits = getNumGroups() % numSplits;

        int startGroupForThisSplit;
        int endGroupForThisSplit;
        if (splitIndex &amp;lt; numFatSplits) {
            startGroupForThisSplit = getStartGroup() + splitIndex * (numGroupsPerSplit + 1);
            endGroupForThisSplit =   startGroupForThisSplit + numGroupsPerSplit + 1;
        } else {
            startGroupForThisSplit = getStartGroup() + splitIndex * numGroupsPerSplit + numFatSplits;
            endGroupForThisSplit =  startGroupForThisSplit + numGroupsPerSplit;
        }
        if (startGroupForThisSplit &amp;gt;= endGroupForThisSplit) {
                return GroupRange.emptyGroupRange();
        } else {
                return new GroupRange(startGroupForThisSplit, endGroupForThisSplit);
        }
}
</code></pre>

<p>上面代码的核心逻辑是先计算每个Operator实例至少分配的Key-Group个数，将不能整除的部分N个，平均分给前N个实例。最终每个Operator实例管理的Key-Groups会在GroupRange中表示，本质是一个区间值；下面我们就上图的case，说明一下如何进行分配以及扩容后如何重新分配。假设上面的Stateful Operation节点的最大并行度maxParallelism的值是10，也就是我们一共有10个Key-Group，当我们并发是2的时候和并发是3的时候分配的情况如下图：如上算法我们发现在进行扩容时候，大部分state还是落到本地的，如Task0只有KG-4被分出去，其他的还是保持在本地。同时我们也发现，一个job如果修改了maxParallelism的值那么会直接影响到Key-Groups的数量和key的分配，也会打乱所有的Key-Group的分配，目前在Apache Flink系统中统一将maxParallelism的默认值调整到4096，最大程度的避免无法扩容的情况发生。</p>

<center><img src="/images/2022/IMAG2022060105.png"></center>


<h2>小结</h2>

<p>本篇简单介绍了Apache Flink中State的概念，并重点介绍了OperatorState和KeyedState在扩容时候的处理方式。Apache Flink State是支撑Apache Flink中failover，增量计算，Window等重要机制和功能的核心设施。后续介绍failover，增量计算，Window等相关篇章中也会涉及State的利用，当涉及到本篇没有覆盖的内容时候再补充介绍。</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Spark与Flink：下一代大数据计算引擎之争]]></title>
    <link href="https://ibillxia.github.io/blog/2020/05/31/spark-vs-flink-comparison/"/>
    <updated>2020-05-31T16:37:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2020/05/31/spark-vs-flink-comparison</id>
    <content type="html"><![CDATA[<h1>一、下一代大数据计算引擎</h1>

<p>自从数据处理需求超过了传统数据库能有效处理的数据量之后，Hadoop 等各种基于 MapReduce 的海量数据处理系统应运而生。从 2004 年 Google 发表 MapReduce 论文开始，经过近 10 年的发展，基于 Hadoop 开源生态或者其它相应系统的海量数据处理已经成为业界的基本需求。</p>

<p>但是，很多机构在开发自己的数据处理系统时都会发现需要面临一系列的问题。从数据中获取价值需要的投入远远超过预期。常见的问题包括：</p>

<ul>
<li>非常陡峭的学习曲线。刚接触这个领域的人经常会被需要学习的技术的数量砸晕。不像经过几十年发展的数据库一个系统可以解决大部分数据处理需求， Hadoop 等大数据生态里的一个系统往往在一些数据处理场景上比较擅长，另一些场景凑合能用，还有一些场景完全无法满足需求。结果就是需要好几个系统来处理不同的场景。</li>
</ul>


<center><img src="/images/2020/IMAG2020053101.png"></center>


<p>（来源： https://mapr.com/developercentral/lambda-architecture/ ）</p>

<!--more-->


<p>上图是一个典型的 lambda 架构，只是包含了批处理和流处理两种场景，就已经牵涉到至少四五种技术了，还不算每种技术的可替代选择。再加上实时查询，交互式分析，机器学习等场景，每个场景都有几种技术可以选择，每个技术涵盖的领域还有不同方式的重叠。结果就是一个业务经常需要使用四五种以上的技术才能支持好一个完整的数据处理流程。加上调研选型，需要了解的数目还要多得多。</p>

<p>下图是大数据领域的全景。有没有晕？</p>

<center><img src="/images/2020/IMAG2020053102.png"></center>




<center>大数据和 AI 全景 – 2018</center>


<p><br></p>

<ul>
<li><p>开发和运行效率低下。因为牵涉到多种系统，每种系统有自己的开发语言和工具，开发效率可想而知。而因为采用了多套系统，数据需要在各个系统之间传输，也造成了额外的开发和运行代价，数据的一致也难以保证。在很多机构，实际上一半以上的开发精力花在了数据在各个系统之间的传输上。</p></li>
<li><p>复杂的运维。多个系统，每个需要自己的运维，带来更高的运维代价的同时也提高了系统出问题的可能。</p></li>
<li><p>数据质量难以保证。数据出了问题难以跟踪解决。</p></li>
<li><p>最后，还有人的问题。在很多机构，由于系统的复杂性，各个子系统的支持和使用落实在不同部门负责。</p></li>
</ul>


<p>了解了这些问题以后，对 Spark 从 2014 年左右开始迅速流行就比较容易理解了。Spark 在当时除了在某些场景比 Hadoop MapReduce 带来几十到上百倍的性能提升外，还提出了用一个统一的引擎支持批处理，流处理，交互式查询，机器学习等常见的数据处理场景。看过在一个 Notebook 里完成上述所有场景的 Spark 演示，对比之前的数据流程开发，对很多开发者来说不难做出选择。经过几年的发展，Spark 已经被视为可以完全取代 Hadoop 中的 MapReduce 引擎。</p>

<p>正在 Spark 如日中天高速发展的时候，2016 年左右 Flink 开始进入大众的视野并逐渐广为人知。为什么呢？原来在人们开始使用 Spark 之后，发现 Spark 虽然支持各种场见场景，但并不是每一种都同样好用。数据流的实时处理就是其中相对较弱的一环。Flink 凭借更优的流处理引擎，同时也支持各种处理场景，成为 Spark 的有力挑战者。</p>

<p>Spark 和 Flink 是怎么做到这些的，它们之间又有那些异同，下面我们来具体看一下。</p>

<h1>二、Spark 和 Flink 的引擎技术</h1>

<p>这一部分主要着眼于 Spark 和 Flink 引擎的架构方面，更看重架构带来的潜力和限制。现阶段的实现成熟度和局限会在后续生态部分探讨。</p>

<h2>2.1 数据模型和处理模型</h2>

<p>要理解 Spark 和 Flink 的 引擎特点，首先从数据模型开始。</p>

<p>Spark 的数据模型是弹性分布式数据集 RDD（Resilient Distributed Datasets）。 比起 MapReduce 的文件模型，RDD 是一个更抽象的模型，RDD 靠血缘（lineage） 等方式来保证可恢复性。很多时候 RDD 可以实现为分布式共享内存或者完全虚拟化（即有的中间结果 RDD 当下游处理完全在本地时可以直接优化省略掉）。这样可以省掉很多不必要的 I/O，是早期 Spark 性能优势的主要原因。</p>

<p>Spark 用 RDD 上的变换（算子）来描述数据处理。每个算子（如 map，filter，join）生成一个新的 RDD。所有的算子组成一个有向无环图（DAG）。Spark 比较简单地把边分为宽依赖和窄依赖。上下游数据不需要 shuffle 的即为窄依赖，可以把上下游的算子放在一个阶段（stage） 里在本地连续处理，这时上游的结果 RDD 可以 省略。下图展示了相关的基本概念。更详细的介绍在网上比较容易找到，这里就不花太多篇幅了。</p>

<center><img src="/images/2020/IMAG2020053103.png"></center>




<center> Spark DAG</center>


<p><br></p>

<p>Flink 的基本数据模型是数据流，及事件（Event） 的序列。数据流作为数据的基本模型可能没有表或者数据块直观熟悉，但是可以证明是完全等效的。流可以是无边界的无限流，即一般意义上的流处理。也可以是有边界的有限流，这样就是批处理。</p>

<p>Flink 用数据流上的变换（算子）来描述数据处理。每个算子生成一个新的数据流。在算子，DAG，和上下游算子链接（chaining） 这些方面，和 Spark 大致等价。Flink 的节点（vertex）大致相当于 Spark 的阶段（stage），划分也会和上图的 Spark DAG 基本一样。</p>

<center><img src="/images/2020/IMAG2020053104.png"></center>




<center> Flink 任务图</center>


<p><br></p>

<p>在 DAG 的执行上，Spark 和 Flink 有一个比较显著的区别。 在 Flink 的流执行模式中，一个事件在一个节点处理完后的输出就可以发到下一个节点立即处理。这样执行引擎并不会引入额外的延迟。与之相应的，所有节点是需要同时运行的。而 Spark 的 micro batch 和一般的 batch 执行一样，处理完上游的 stage 得到输出之后才开始下游的 stage。</p>

<p>在 Flink 的流执行模式中，为了提高效率也可以把多个事件放在一起传输或者计算。但这完全是执行时的优化，可以在每个算子独立决定，也不用像 RDD 等批处理模型中一样和数据集边界绑定，可以做更加灵活的优化同时可以兼顾低延迟需求。</p>

<p>Flink 使用异步的 checkpoint 机制来达到任务状态的可恢复性，以保证处理的一致性，所以在处理的主流程上可以做到数据源和输出之间数据完全不用落盘，达到更高的性能和更低的延迟。</p>

<h2>2.2 数据处理场景</h2>

<p>除了批处理之外，Spark 还支持实时数据流处理，交互式查询，和机器学习，图计算等。</p>

<center><img src="/images/2020/IMAG2020053105.png"></center>


<p>（来源： https://databricks.com/spark/about ）</p>

<ul>
<li><p>实时数据流处理和批处理主要区别就是对低延时的要求。Spark 因为 RDD 是基于内存的，可以比较容易切成较小的块来处理。如果能对这些小块处理得足够快，就能达到低延时的效果。</p></li>
<li><p>交互式查询场景，如果数据能全在内存，处理得足够快的话，就可以支持交互式查询。</p></li>
<li><p>机器学习和图计算其实是和前几种场景不同的 RDD 算子类型。Spark 提供了库来支持常用的操作，用户或者第三方库也可以自己扩展。值得一提的是，Spark 的 RDD 模型和机器学习模型训练的迭代计算非常契合，从一开始就在有的场景带来了非常显著的性能提升。</p></li>
</ul>


<p>从这些可以看出来，比起 Hadoop MapReduce， Spark 本质上就是基于内存的更快的批处理。然后用足够快的批处理来实现各种场景。</p>

<center><img src="/images/2020/IMAG2020053106.png"></center>


<p>（来源： https://www.slideshare.net/ParisCarbone/state-management-in-apache-flink-consistent-stateful-distributed-stream-processing ）</p>

<p>前面说过，在 Flink 中，如果输入数据流是有边界的，就自然达到了批处理的效果。这样流和批的区别完全是逻辑上的，和处理实现独立，用户需要实现的逻辑也完全一样，应该是更干净的一种抽象。后续会在深入对比流计算方面的时候做更深入的讨论。</p>

<p>Flink 也提供了库来支持机器学习，图计算等场景。从这方面来说和 Spark 没有太大区别。</p>

<p>一个有意思的事情是用 Flink 的底层 API 可以支持只用 Flink 集群实现一些数据驱动的分布式服务。有一些公司用 Flink 集群实现了社交网络，网络爬虫等服务。这个也体现了 Flink 作为计算引擎的通用性，并得益于 Flink 内置的灵活的状态支持。</p>

<p>总的来说，Spark 和 Flink 都瞄准了在一个执行引擎上同时支持大多数数据处理场景，也应该都能做到这一点。主要区别就在于因为架构本身的局限在一些场景会受到限制。比较突出的地方就是 Spark Streaming 的 micro batch 执行模式。Spark 社区应该也意识到了这一点，最近在持续执行模式（continuous processing）方面开始发力。 具体情况会在后面介绍。</p>

<h2>2.3 有状态处理 （Stateful Processing）</h2>

<p>Flink 还有一个非常独特的地方是在引擎中引入了托管状态（managed state）。要理解托管状态，首先要从有状态处理说起。如果处理一个事件（或一条数据）的结果只跟事件本身的内容有关，称为无状态处理；反之结果还和之前处理过的事件有关，称为有状态处理。稍微复杂一点的数据处理，比如说基本的聚合，都是有状态处理。Flink 很早就认为没有好的状态支持是做不好留处理的，因此引入了 managed state 并提供了 API 接口。</p>

<center><img src="/images/2020/IMAG2020053107.png"></center>




<center>Flink 中的状态支持</center>


<p><br></p>

<p>一般在流处理的时候会比较关注有状态处理，但是仔细看的话批处理也是会受到影响的。比如常见的窗口聚合，如果批处理的数据时间段比窗口大，是可以不考虑状态的，用户逻辑经常会忽略这个问题。但是当批处理时间段变得比窗口小的时候，一个批的结果实际上依赖于以前处理过的批。这时，因为批处理引擎一般没有这个需求不会有很好的内置支持，维护状态就成为了用户需要解决的事情。比如窗口聚合的情况用户就要加一个中间结果表记住还没有完成的窗口的结果。这样当用户把批处理时间段变短的时候就会发现逻辑变复杂了。这是早期 Spark Streaming 用户 经常碰到的问题。直到 Structured Streaming 出来才得到缓解。</p>

<p>而像 Flink 这样以流处理为基本模型的引擎，因为一开始就避不开这个问题，所以引入了 managed state 来提供了一个通用的解决方案。比起用户实现的特定解决方案，不但用户开发更简单，而且能提供更好的性能。最重要的是能更好地保证处理结果的一致性。</p>

<p>简单来说，就是有一些內秉的数据处理逻辑，在批处理中容易被忽略或简化处理掉也能得到可用的结果，而在流处理中问题被暴露出来解决掉了。所以流计算引擎用有限流来处理批在逻辑上比较严谨，能自然达到正确性。主要做一些不同的实现来优化性能就可以了。而用更小的批来模拟流需要处理一些以前没有的问题。当计算引擎还没有通用解决方案的时候就需要用户自己解决了。类似的问题还有维表的变化（比如用户信息的更新），批处理数据的边界和迟到数据等等。</p>

<h2>2.4 编程模型</h2>

<center><img src="/images/2020/IMAG2020053108.png"></center>




<center>Spark 1.6 时的 API 状态</center>


<p><br></p>

<p>Spark 的初衷之一就是用统一的编程模型来解决用户的各种需求。在这方面一直很下功夫。最初基于 RDD 的 API 就可以做各种类型的数据处理。后来为了简化用户开发，逐渐推出了更高层的 DataFrame（在 RDD 中加了列变成结构化数据）和 Datasets（在 DataFrame 的列上加了类型），并在 Spark 2.0 中做了整合（DataFrame = DataSet[Row]）。Spark SQL 的支持也比较早就引入了。在加上各个处理类型 API 的不断改进，比如 Structured Streaming 以及和机器学习深度学习的交互，到了今天 Spark 的 API 可以说是非常好用的，也是 Spark 最强的方面之一。</p>

<center><img src="/images/2020/IMAG2020053109.png"></center>




<center>Spark 2.0 API</center>


<p><br></p>

<p>Flink 的 API 也有类似的目标和发展路线。Flink 和 Spark 的核心 API 可以说是可以基本对应的。今天 Spark API 总体上更完备一下，比如说最近一两年大力投入的和机器学习深度学习的整合方面。Flink 在流处理相关的方面还是领先一些，比如对 watermark，window，trigger 的各种支持。</p>

<center><img src="/images/2020/IMAG2020053110.png"></center>




<center>Flink API</center>


<p><br></p>

<h2>2.5 小结</h2>

<p>Spark 和 Flink 都是通用的能够支持超大规模数据处理，支持各种处理类型的计算引擎。两个系统都有很多值得探讨的方面在这里没有触及，比如 SQL 的优化，和机器学习的集成等等。这里主要是试图从最基本的架构和设计方面来比较一下两个系统。因为上层的功能在一定程度上是可以互相借鉴的，有足够的投入应该都能做好。而基本的设计改变起来会伤筋动骨，更困难一些。</p>

<p>Spark 和 Flink 的不同执行模型带来的最大的区别应该还是在对流计算的支持上。最开始的 Spark Streaming 对流计算想得过于简单，对复杂一点的计算用起来会有不少问题。从 Spark 2.0 开始引入的 Structured Streaming 重新整理了流计算的语义，支持按事件时间处理和端到端的一致性。虽然在功能上还有不少限制，比之前已经有了长足的进步。不过 micro batch 执行方式带来的问题还是存在，特别在规模上去以后性能问题会比较突出。最近 Spark 受一些应用场景的推动，也开始开发持续执行模式。2.3 里的实验性发布还只支持简单的 map 类的操作。从最近 Spark+AI Summit 大会上的介绍来看（下图），会发展成一个和 Flink 的流处理模式比较相似的执行引擎。不过从下图来看，主要的功能都还在开发中或者待开发。对将来能做到什么程度，和 Spark 原来的 batch 执行引擎怎么结合，我们拭目以待。</p>

<center><img src="/images/2020/IMAG2020053111.png"></center>




<center>Spark 持续执行模式状态</center>


<p><br></p>

<h1>三、生态和未来</h1>

<h2>3.1 概况</h2>

<center><img src="/images/2020/IMAG2020053112.png"></center>


<p><br></p>

<p>Spark 是最活跃的 Apache 项目之一。从 2014 年左右开始得到广泛关注。Spark 的开源社区一度达到上千的活跃贡献者。最主要推动者是 Databricks，由最初的 Spark 创造者们成立的公司。今年 6 月的 Spark+AI 峰会参加人数超过 4000。 Spark 因为在引擎方面比 MapReduce 全面占优，经过几年发展和 Hadoop 生态结合较好，已经被广泛视为 Hadoop MapReduce 引擎的取代者。</p>

<center><img src="/images/2020/IMAG2020053113.png"></center>


<p><br></p>

<p>Flink 也是 Apache 顶级项目，创始者们成立了 Data Artisans。社区规模还无法和 Spark 相比。不过在业界，特别是流处理方面，有不错的口碑。在大规模流处理方面走在最前沿，也是需求最强的几个美国公司，包括 Netflix、 LinkedIn、Uber、Lyft 等，除 LinkedIn 有自己的 Samza 外，都已经采用 Flink 作为流处理引擎或者有了较大投入。</p>

<p>阿里集团在 Flink 社区也有较大影响力。最近 Flink 1.3 到 1.5 里都有几个重磅功能是阿里和 Data Artisans 合作或者独立开发的。阿里还有可能是世界上最大的流计算集群，也是在 Flink 的基础上开发的。</p>

<h2>3.2 Unified Analytic platform – 包含机器学习的统一分析平台</h2>

<p>最近的 Spark+AI 峰会上， Databricks 主打的主题是统一分析平台（Unified Analytics Platform）。三大新发布：Databricks delta、Databricks Runtime for ML 和 ML flow，都是围绕这一主题。随着近年来机器学习（包括深度学习）在数据处理中占比越来越高，可以说 Databricks 又一次把握住了时代的脉搏。</p>

<p>统一分析平台回应了 Spark 的初衷。经过几年的探索，对初始问题，即用户可以在一个系统里解决绝大部分大数据的需求，有了一个比较明确具体的解决方案。</p>

<center><img src="/images/2020/IMAG2020053114.png"></center>


<p><br></p>

<p>不过有意思的是可以看出 Databricks 在 AI 方面策略的转变。在深度学习流行前，Spark 自带的 MLLib 功能上应该是够用的，但是可能是由于兼容性原因并没有取得预期中的广泛采用。</p>

<p>对深度学习的新宠 TensorFlow，Spark 曾经推出过 TensorFrames 和 Spark 引擎做了一些集成。结果应该不是很成功，可能还没有 Yahoo 从外面搭建的 TensorFlowOnSpark 影响力大。</p>

<p>从这次来看，Spark 转向了集成的策略。Databricks Runtime for ML 实际上就是预装了各个机器学习框架，然后支持在 Spark 任务里启动一个比如 TensorFlow 自己的集群。Spark 引擎方面做的主要改进就是 gang scheduling，即支持一次申请多个 executor 以便 TensorFlow 集群能正常启动。</p>

<p>MLFlow 更是和 Spark 引擎无关。作为一个工作流工具，MLFlow 的目标是帮助数据科学家提高工作效率。主要功能是以项目为单位记录和管理所做的机器学习试验，并支持分享。设计要点是可重复试验，以及对各种工具的灵活易用的支持。看起来 Spark 暂时在作为 AI 引擎方面可能没什么大动作了。</p>

<p>Flink 的目标其实和 Spark 很相似。包含 AI 的统一平台也是 Flink 的发展方向。Flink 从技术上也是可以支持较好的机器学习集成和整条链路的，而且有一些大规模线上学习的使用实例。不过看起来在现阶段 Flink 这方面的平台化还没有 Spark 成熟。值得一提的是 Flink 由于流处理引擎的优势，在线上学习方面可能能支持得更好一些。</p>

<h2>3.3 数据使用者</h2>

<p>产品和生态归根结底是要解决大数据使用者的问题，从数据中产生价值。了解数据的使用者和他们的需求可以帮助我们在在讨论生态的各方面时有一个比较清晰的脉络。</p>

<p>数据相关的工作者大致可以分为以下角色。实际情况中一个组织里很可能几个角色在人员上是重合的。各个角色也没有公认的定义和明确的界限。</p>

<center><img src="/images/2020/IMAG2020053115.png"></center>


<p><br></p>

<ul>
<li><p>数据采集：在产品和系统中合适的地方产生或收集数据发送到数据平台。</p></li>
<li><p>平台：提供数据导入，存储，计算的环境和工具等等。</p></li>
<li><p>数据工程师：使用数据平台把原始数据加工成可以供后续高效使用的数据集。把分析师和数据科学家创建的指标和模型等等生产化成为高效可靠的的自动处理。</p></li>
<li><p>数据分析师和数据科学家（关于这两者的异同有很多讨论。感兴趣的可以自行搜索。 https://www.jianshu.com/p/cfd94d9e4466 这里的译文可以提供一个视角）：为数据赋予意义，发现内含的价值。 下文再不特别区分的地方统称为数据分析。</p></li>
<li><p>产品经理，管理和决策层：根据以上产生的数据调整产品和组织行为。</p></li>
</ul>


<p>这些构成了一个完整的环。上面的顺序是数据流动的方向，而需求的驱动是反过来的方向。</p>

<p>本文所说的 Spark 和 Flink 的生态主要是对应到数据平台这一层。直接面向的用户主要是数据工程师、数据分析师和数据科学家。好的生态能够大大简化数据平台和数据工程师的工作，并使得数据分析师和数据科学家更加自主化同时提高效率。</p>

<h2>3.4 开发环境</h2>

<h3>API</h3>

<p>从 API 上来看，Spark 和 Flink 提供的功能领域大致相当。当然具体看各个方向支持的程度会有差异。总体来看 Spark 的 API 经过几轮迭代，在易用性，特别是机器学习的集成方面，更强一些。Flink 在流计算方面更成熟一些。</p>

<center><img src="/images/2020/IMAG2020053116.png"></center>


<p><br></p>

<p>支持的语言也大致相当。Spark 发展的时间长一些还是有优势，特别是数据分析常用的 Python 和 R。</p>

<center><img src="/images/2020/IMAG2020053117.png"></center>


<p><br></p>

<h3>Connectors</h3>

<p>有了 API，再有数据就可以开工了。Spark 和 Flink 都能对接大部分比较常用的系统。如果暂时还没有支持的，也都能比较好地支持自己写一个 connector。</p>

<center><img src="/images/2020/IMAG2020053118.png"></center>


<p>https://databricks.com/spark/about </p>

<center><img src="/images/2020/IMAG2020053119.png"></center>


<p>https://www.slideshare.net/chobeat/data-intensive-applications-with-apache-flink</p>

<h2>3.5 集成开发工具</h2>

<p>这方面数据工程师和数据分析的需求有一些不同。</p>

<p>数据分析的工作性质比较偏探索性，更强调交互性和分享。Notebook 能比较好地满足这些需求，是比较理想的开发工具，用来做演示效果也相当不错。比较流行的 Notebook 有 Apache Zeppelin，Jupyter 等。Databricks 更是自己开发了 Databricks Notebook 并将之作为服务的主要入口。Zeppelin 支持 Spark 和 Flink，Jupyter 还只支持 Spark。</p>

<p>数据工程师的工作更倾向于把比较确定的数据处理生产化，能快速把代码写出来是一方面。另外还有项目管理，版本管理，测试，配置，调试，部署，监控等等工作，需求和传统的集成开发工具比较相似。 还经常出现需要复用已有的业务逻辑代码库的情况。Notebook 对其中一些需求并不能很好地满足。比较理想的开发工具可能是类似 IntelliJ 加上 Spark/Flink 插件，再加上一些插件能直接提交任务到集群并进行调试，并对接 Apache Oozie 之类的工作流管理等等。在开源社区还没有见到能把这些集成到一起的。在商业产品中倒是见过一些比较接近的。Spark 和 Flink 在这方面差不多。</p>

<h2>3.6 运行环境</h2>

<h3>部署模式 / 集群管理 / 开源闭源</h3>

<p>应用开发完后要提交到运行环境。Spark 和 Flink 都支持各种主流的部署环境，在这方面都算做得比较好的。</p>

<center><img src="/images/2020/IMAG2020053120.png"></center>


<p><br></p>

<h2>3.7 企业级平台</h2>

<p>既然 Spark 和 Flink 都支持各种部署方式，那一个企业是否可以使用开源代码快速搭建一个支持 Spark 或者 Flink 的平台呢？</p>

<p>这个要看想要达到什么效果了。最简单的模式可能是给每个任务起一个独占集群，或着给小团队一个独立集群。这个确实可以很快做到，但是用户多了以后，统一运维的成本可能太高，需要用户参与运维。还有一个缺点是资源分配固定，而负载会有变化，导致资源利用率上不去。比较理想的是多租户的共享大集群，可以提高运维效率的同时最大限度地提高资源利用率。而这就需要一系列的工作，比如不同的作业提交方式，数据安全与隔离等等。对一些企业来说，可能利用托管服务（包括云服务）是一种值得考虑的开始方式。</p>

<h2>3.8 社区</h2>

<p>Spark 社区在规模和活跃程度上都是领先的，毕竟多了几年发展时间。而且作为一个德国公司，Data Artisans 想在美国扩大影响力要更难一些。不过 Flink 社区也有一批稳定的支持者，达到了可持续发展的规模。</p>

<p>在中国情况可能会不一样一些。比起美国公司，中国公司做事情速度更快，更愿意尝试新技术。中国的一些创新场景也对实时性有更高的需求。这些都对 Flink 更友好一些。</p>

<p>近期 Flink 的中国社区有一系列动作，是了解 Flink 的好机会。</p>

<p>Spark 的中文文档在 http://www.apachecn.org/bigdata/spark/27.html 。</p>

<p>Flink 的中文社区在 http://flink-china.org/ 。</p>

<p>另外，今年年底 Flink 中文社区也会在北京举办 Flink Forward China 大会，感兴趣的朋友可以关注。</p>

<h2>3.9 未来发展趋势</h2>

<p>近两年一个明显的趋势就是机器学习在数据处理中的比重增长。Spark 和 Flink 都能支持在一个系统中做机器学习和其它数据处理。谁能做得更好就能掌握先机。</p>

<p>另一个可能没有那么明显的趋势是，随着 IOT 的增长以及计算资源和网络的持续发展，实时处理需求会越来越多。现在其实真正对低延迟有很高追求的业务并没有那么多，所以每一次流计算新技术的出现都能看到那几家公司的身影。随着新应用场景的出现和竞争环境的发展，实时处理可能会变得越来越重要。Flink 现在在这方面是领先的，如果发挥得好可以成为核心优势。</p>

<p>还有一点值得一提的是，因为用户不想锁定供应商，担心持续的支持等原因，是否开源已经成为用户选择数据产品的一个重要考量。闭源产品如果没有决定性优势会越来越难和基于开源技术的产品竞争。</p>

<h1>四、总结</h1>

<p>Spark 和 Flink 都是通用的开源大规模处理引擎，目标是在一个系统中支持所有的数据处理以带来效能的提升。两者都有相对比较成熟的生态系统。是下一代大数据引擎最有力的竞争者。Spark 的生态总体更完善一些，在机器学习的集成和易用性上暂时领先。Flink 在流计算上有明显优势，核心架构和模型也更透彻和灵活一些。在易用性方面两者也都还有一些地方有较大的改进空间。接下来谁能尽快补上短板发挥强项就有更多的机会。</p>

<p><br><br>
作者介绍: 王海涛，曾经在微软的 SQL  Server 和大数据平台组工作多年。带领团队建立了微软对内的 Spark 服务，主打 Spark Streaming。去年加入阿里实时计算部门，参与改进阿里基于 Apache Flink 的 Blink 平台。</p>

<p>原文链接：</p>

<p><a href="https://flink-learning.org.cn/article/detail/5910c07489b5411c32f22963e06ddeed">Spark VS Flink 下一代大数据计算引擎之争，谁主浮沉（上）</a></p>

<p><a href="https://flink-learning.org.cn/article/detail/4f8e1cf7a9bc160f96ff04cb91b7da49">比拼生态和未来，Spark 和 Flink 哪家强（下）</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Flink原理与实现：数据流上的类型和操作]]></title>
    <link href="https://ibillxia.github.io/blog/2020/03/01/flink-datastream-types-operations/"/>
    <updated>2020-03-01T17:50:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2020/03/01/flink-datastream-types-operations</id>
    <content type="html"><![CDATA[<p><a href="https://wuchong.me/blog/2016/05/20/flink-internals-streams-and-operations-on-streams/">原文链接</a>，作者wuchong，阿里花名云邪。</p>

<p>Flink 为流处理和批处理分别提供了 DataStream API 和 DataSet API。正是这种高层的抽象和 flunent API 极大地便利了用户编写大数据应用。不过很多初学者在看到官方 Streaming 文档中那一大坨的转换时，常常会蒙了圈，文档中那些只言片语也很难讲清它们之间的关系。所以本文将介绍几种关键的数据流类型，它们之间是如何通过转换关联起来的。下图展示了 Flink 中目前支持的主要几种流的类型，以及它们之间的转换关系。</p>

<center><img src="/images/2020/IMAG2020030101.png"></center>


<p><br></p>

<!--more-->


<h1>DataStream</h1>

<p>DataStream 是 Flink 流处理 API 中最核心的数据结构。它代表了一个运行在多个分区上的并行流。一个 DataStream 可以从 StreamExecutionEnvironment 通过env.addSource(SourceFunction) 获得。</p>

<p>DataStream 上的转换操作都是逐条的，比如 map()，flatMap()，filter()。DataStream 也可以执行 rebalance（再平衡，用来减轻数据倾斜）和 broadcaseted（广播）等分区转换。</p>

<pre><code>val stream: DataStream[MyType] = env.addSource(newFlinkKafkaConsumer08[String](...))
val str1: DataStream[(String, MyType)] = stream.flatMap { ... }
val str2: DataStream[(String, MyType)] = stream.rebalance()
val str3: DataStream[AnotherType] = stream.map { ... }
</code></pre>

<p>上述 DataStream 上的转换在运行时会转换成如下的执行图：</p>

<center><img src="/images/2020/IMAG2020030102.png"></center>


<p><br></p>

<p>如上图的执行图所示，DataStream 各个算子会并行运行，算子之间是数据流分区。如 Source 的第一个并行实例（S1）和 flatMap() 的第一个并行实例（m1）之间就是一个数据流分区。而在 flatMap() 和 map() 之间由于加了 rebalance()，它们之间的数据流分区就有3个子分区（m1的数据流向3个map()实例）。这与 Apache Kafka 是很类似的，把流想象成 Kafka Topic，而一个流分区就表示一个 Topic Partition，流的目标并行算子实例就是 Kafka Consumers。</p>

<h2>KeyedStream</h2>

<p>KeyedStream用来表示根据指定的key进行分组的数据流。一个KeyedStream可以通过调用DataStream.keyBy()来获得。而在KeyedStream上进行任何transformation都将转变回DataStream。在实现中，KeyedStream是把key的信息写入到了transformation中。每条记录只能访问所属key的状态，其上的聚合函数可以方便地操作和保存对应key的状态。</p>

<p>WindowedStream &amp; AllWindowedStream</p>

<p>WindowedStream代表了根据key分组，并且基于WindowAssigner切分窗口的数据流。所以WindowedStream都是从KeyedStream衍生而来的。而在WindowedStream上进行任何transformation也都将转变回DataStream。</p>

<pre><code>val stream: DataStream[MyType] = ...
val windowed: WindowedDataStream[MyType] = stream
    .keyBy("userId")
    .window(TumblingEventTimeWindows.of(Time.seconds(5))) // Last 5 seconds of data
val result: DataStream[ResultType] = windowed.reduce(myReducer)
</code></pre>

<p>上述 WindowedStream 的样例代码在运行时会转换成如下的执行图：</p>

<center><img src="/images/2020/IMAG2020030103.png"></center>


<p><br></p>

<p>Flink 的窗口实现中会将到达的数据缓存在对应的窗口buffer中（一个数据可能会对应多个窗口）。当到达窗口发送的条件时（由Trigger控制），Flink 会对整个窗口中的数据进行处理。Flink 在聚合类窗口有一定的优化，即不会保存窗口中的所有值，而是每到一个元素执行一次聚合函数，最终只保存一份数据即可。</p>

<p>在key分组的流上进行窗口切分是比较常用的场景，也能够很好地并行化（不同的key上的窗口聚合可以分配到不同的task去处理）。不过有时候我们也需要在普通流上进行窗口的操作，这就是 AllWindowedStream。AllWindowedStream是直接在DataStream上进行windowAll(...)操作。AllWindowedStream 的实现是基于 WindowedStream 的（Flink 1.1.x 开始）。Flink 不推荐使用AllWindowedStream，因为在普通流上进行窗口操作，就势必需要将所有分区的流都汇集到单个的Task中，而这个单个的Task很显然就会成为整个Job的瓶颈。</p>

<h2>JoinedStreams &amp; CoGroupedStreams</h2>

<p>双流 Join 也是一个非常常见的应用场景。深入源码你可以发现，JoinedStreams 和 CoGroupedStreams 的代码实现有80%是一模一样的，JoinedStreams 在底层又调用了 CoGroupedStreams 来实现 Join 功能。除了名字不一样，一开始很难将它们区分开来，而且为什么要提供两个功能类似的接口呢？？</p>

<p>实际上这两者还是很点区别的。首先 co-group 侧重的是group，是对同一个key上的两组集合进行操作，而 join 侧重的是pair，是对同一个key上的每对元素进行操作。co-group 比 join 更通用一些，因为 join 只是 co-group 的一个特例，所以 join 是可以基于 co-group 来实现的（当然有优化的空间）。而在 co-group 之外又提供了 join 接口是因为用户更熟悉 join（源于数据库吧），而且能够跟 DataSet API 保持一致，降低用户的学习成本。</p>

<p>JoinedStreams 和 CoGroupedStreams 是基于 Window 上实现的，所以 CoGroupedStreams 最终又调用了 WindowedStream 来实现。</p>

<pre><code>val firstInput: DataStream[MyType] = ...
val secondInput: DataStream[AnotherType] = ...

val result: DataStream[(MyType, AnotherType)] = firstInput.join(secondInput)
    .where("userId").equalTo("id")
    .window(TumblingEventTimeWindows.of(Time.seconds(3)))
    .apply (newJoinFunction () {...})
</code></pre>

<p>上述 JoinedStreams 的样例代码在运行时会转换成如下的执行图：</p>

<center><img src="/images/2020/IMAG2020030104.png"></center>


<p><br></p>

<p>双流上的数据在同一个key的会被分别分配到同一个window窗口的左右两个篮子里，当window结束的时候，会对左右篮子进行笛卡尔积从而得到每一对pair，对每一对pair应用 JoinFunction。不过目前（Flink 1.1.x）JoinedStreams 只是简单地实现了流上的join操作而已，距离真正的生产使用还是有些距离。因为目前 join 窗口的双流数据都是被缓存在内存中的，也就是说如果某个key上的窗口数据太多就会导致 JVM OOM（然而数据倾斜是常态）。双流join的难点也正是在这里，这也是社区后面对 join 操作的优化方向，例如可以借鉴Flink在批处理join中的优化方案，也可以用ManagedMemory来管理窗口中的数据，并当数据超过阈值时能spill到硬盘。</p>

<h2>ConnectedStreams</h2>

<p>在 DataStream 上有一个 union 的转换 dataStream.union(otherStream1, otherStream2, ...)，用来合并多个流，新的流会包含所有流中的数据。union 有一个限制，就是所有合并的流的类型必须是一致的。ConnectedStreams 提供了和 union 类似的功能，用来连接两个流，但是与 union 转换有以下几个区别：</p>

<ol>
<li><p>ConnectedStreams 只能连接两个流，而 union 可以连接多于两个流。</p></li>
<li><p>ConnectedStreams 连接的两个流类型可以不一致，而 union 连接的流的类型必须一致。</p></li>
<li><p>ConnectedStreams 会对两个流的数据应用不同的处理方法，并且双流之间可以共享状态。这在第一个流的输入会影响第二个流时, 会非常有用。</p></li>
</ol>


<p>如下 ConnectedStreams 的样例，连接 input 和 other 流，并在input流上应用map1方法，在other上应用map2方法，双流可以共享状态（比如计数）。</p>

<pre><code>val input: DataStream[MyType] = ...
val other: DataStream[AnotherType] = ...

val connected: ConnectedStreams[MyType, AnotherType] = input.connect(other)

val result: DataStream[ResultType] = 
    connected.map(newCoMapFunction[MyType, AnotherType, ResultType]() {
        overridedefmap1(value: MyType): ResultType = { ... }
        overridedefmap2(value: AnotherType): ResultType = { ... }
    })
</code></pre>

<p>当并行度为2时，其执行图如下所示：</p>

<center><img src="/images/2020/IMAG2020030105.png"></center>


<p><br></p>

<h1>总结</h1>

<p>本文介绍通过不同数据流类型的转换图来解释每一种数据流的含义、转换关系。后面的文章会深入讲解 Window 机制的实现，双流 Join 的实现等。</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Apache Flink入门教程与资料汇总]]></title>
    <link href="https://ibillxia.github.io/blog/2019/08/11/apache-flink-introduction/"/>
    <updated>2019-08-11T16:53:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2019/08/11/apache-flink-introduction</id>
    <content type="html"><![CDATA[<center><img src="/images/2019/IMAG2019081100.png"></center>


<p><br></p>

<h1>一、教程&amp;博客</h1>

<h2>1. Flink中文视频教程</h2>

<p>Flink系列视频教程：https://github.com/flink-china/flink-training-course</p>

<p>基础篇 https://ververica.cn/developers/flink-training-course-basics/</p>

<p>进阶篇 https://ververica.cn/developers/flink-training-course-advanced/</p>

<p>运维篇 https://ververica.cn/developers/flink-training-course-operation/</p>

<!--more-->


<p>实时数仓篇 https://ververica.cn/developers/flink-training-course-data-warehouse/</p>

<p>技术生态篇 https://ververica.cn/developers/flink-training-course-technological-ecology/</p>

<p>社区成长篇 https://ververica.cn/developers/flink-training-course-growup/</p>

<p>1.10版特别篇 https://ververica.cn/developers/flink-training-course-1-10/</p>

<h2>2.博客</h2>

<p>1）CN: 伍 翀（WuChong） | EN: Jark | 花名: 云邪 的博客</p>

<p>http://wuchong.me/archives/</p>

<p>https://wuchong.me/categories/Flink/</p>

<p>2）王无知</p>

<p>https://www.cnblogs.com/importbigdata/p/11431859.html</p>

<p>https://www.cnblogs.com/importbigdata/tag/Flink/</p>

<hr />

<h1>二、Flink简介</h1>

<center><img src="/images/2019/IMAG2019081101.png"></center>


<p><br></p>

<h2>1、Features:</h2>

<ul>
<li><p>support for stream and batch processing</p></li>
<li><p>sophisticated state management</p></li>
<li><p>event-time processing semantics</p></li>
<li><p>exactly-once consistency guarantees for state</p></li>
<li><p>can be deployed on various resource providers</p></li>
</ul>


<h2>2、Architecture</h2>

<p>1） Process bounded and unbounded data</p>

<p>precise control of time and state</p>

<p>2）Deploy applications anywhere</p>

<p>Flink integrates with all common cluster resource managers such as Hadoop YARN, Apache Mesos, and Kubernetes but can also be setup to run as a stand-alone cluster.</p>

<p>All communication to submit or control an application happens via REST calls.</p>

<p>3） Run applications at any scale</p>

<p>4）Leverage in-memory performance</p>

<p>Task state is always maintained in memory or, if the state size exceeds the available memory, in access-efficient on-disk data structures.</p>

<h2>3、Applications</h2>

<p>1）Building blocks for stream applications</p>

<p>stream,state,time</p>

<p>2）Layered APIs</p>

<p>three layered APIs:</p>

<center><img src="/images/2019/IMAG2019081102.png"></center>


<p><br></p>

<p>3）Libraries</p>

<p>CEP/DataSet API/Gelly</p>

<h2>4、Operations</h2>

<p>1）7*24h non-stop</p>

<p>2）Update, Migrate, Suspend, and Resume</p>

<p>3）Monitor and Control</p>

<h1>5、Use Cases</h1>

<ul>
<li><p>Event-driven Apps</p></li>
<li><p>Data Analytics Apps</p></li>
<li><p>Data Pipeline Apps</p></li>
</ul>

]]></content>
  </entry>
  
</feed>