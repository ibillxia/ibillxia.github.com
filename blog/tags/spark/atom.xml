<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Tag: Spark | Bill's Blog]]></title>
  <link href="https://ibillxia.github.io/blog/tags/spark/atom.xml" rel="self"/>
  <link href="https://ibillxia.github.io/"/>
  <updated>2026-07-12T20:22:44+08:00</updated>
  <id>https://ibillxia.github.io/</id>
  <author>
    <name><![CDATA[Bill Xia]]></name>
    <email><![CDATA[ibillxia@gmail.com]]></email>
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Spark内存管理及调优]]></title>
    <link href="https://ibillxia.github.io/blog/2024/05/15/spark-memory-management-tuning/"/>
    <updated>2024-05-15T17:25:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2024/05/15/spark-memory-management-tuning</id>
    <content type="html"><![CDATA[<p>原文链接：<a href="https://qiankunli.github.io/2022/05/05/spark_detail.html">https://qiankunli.github.io/2022/05/05/spark_detail.html</a></p>

<h1>内存划分及用途</h1>

<h2>内存划分</h2>

<center><img src="/images/2024/IMAG2024051501.png"></center>




<!--more-->


<h2>内存用途</h2>

<table>
<thead>
<tr>
<th> 内存区域划分 </th>
<th> 堆内内存 </th>
<th> 对外内存<br>spark.memory.offHeap.enabled </th>
<th> 作用 </th>
</tr>
</thead>
<tbody>
<tr>
<td> 内存空间总大小 </td>
<td> spark.executor.memory </td>
<td> spark.memory.offHeap.size </td>
<td>  </td>
</tr>
<tr>
<td> Reserved memory </td>
<td> 固定为300M </td>
<td> 无 </td>
<td> 用于存储 Spark 内部对象 </td>
</tr>
<tr>
<td> User memory </td>
<td> 1-spark.memory.fraction </td>
<td> 无 </td>
<td> 用于存储用户自定义的数据结构 </td>
</tr>
<tr>
<td> Storage memory </td>
<td> spark.executor.memory * <br>spark.memory.fraction * spark.memory.storageFraction </td>
<td> spark.memory.offHeap.size * <br> spark.memory.storageFraction </td>
<td> 用来容纳 RDD 缓存和广播变量 </td>
</tr>
<tr>
<td> Executor memory </td>
<td> spark.executor.memory * <br> spark.memory.fraction * (1 - spark.memory.storageFraction) </td>
<td> spark.memory.offHeap.size * <br>(1-spark.memory.storageFraction) </td>
<td> 用于分布式任务执行，如 Shuffle、Sort 和 Aggregate 等操作 </td>
</tr>
</tbody>
</table>


<p>代码示例</p>

<pre><code>val dict: List[String] = List(“spark”, “scala”)  // 字典在 Driver 端生成，它在后续的 RDD 调用中会随着任务一起分发到 Executor 端，Executor 将其存储在 User Memory 区域
val words: RDD[String] = sparkContext.textFile(“~/words.csv”)
val keywords: RDD[String] = words.filter(word =&gt; dict.contains(word))
keywords.cache  // Storage Memory 内存区域
keywords.count
keywords.map((_, 1)).reduceByKey(_ + _).collect // reduceByKey 算子会引入 Shuffle，而 Shuffle 过程中所涉及的内部数据结构，如映射、排序、聚合等操作所仰仗的 Buffer、Array 和 HashMap，都会消耗 Execution Memory 区域中的内存
</code></pre>

<p>Spark 区分堆内内存和堆外内存：对于堆外内存来说，Spark 通过调用 Java Unsafe 的 allocateMemory 和 freeMemory 方法，直接在操作系统内存中申请、释放内存空间，管理成本较高；对于堆内内存来说，无需 Spark 亲自操刀而是由 JVM 代理。但频繁的 JVM GC 对执行性能来说是一大隐患。</p>

<center><img src="/images/2024/IMAG2024051502.png"></center>


<p>execution memory、storage memory的划分是逻辑上的。Spark在JVM之上的内存管理，实际上是一种基于“审计”的管理，也就是Spark会记录不同内存区域的内存消耗，但实际的内存申请与释放，还是依赖JVM本身，比如对象的实际删除，是要等到GC的时候。</p>

<h1>Spark性能调优之资源分配</h1>

<p>大体上是core 和mem两个方面，分配哪些资源？executor、core per executor、memory per executor、driver memory。</p>

<p>yarn资源队列。hadoop/spark/storm 每一个队列都有各自的资源（cpu mem），比如500G内存；100个cpu core；50个executor。平均每个executor：10G内存；2个cpu core。</p>

<h2>并行度</h2>

<p>Spark的并行度指的是什么？spark作业中，各个stage的task的数量，也就代表了spark作业在各个阶段stage的并行度！比如50个executor ，每个executor 有3个core ，也就是说，Application 任何一个stage运行的时候，都有总数150个cpu core 可以并行运行。但如果并行度只设置为100，平均分配一下，那么同时在运行的task只有100个，每个executor 只会并行运行 2个task，每个executor 剩下的一个cpu core 就浪费掉了。</p>

<p>如何去提高并行度？</p>

<ol>
<li><p>task数量，官方推荐，task数量设置成spark Application 总cpu core数量的2到3倍 ，比如150个cpu core ，可以设置 task数量为 300到500。 有些task 会运行快一点，比如50s 就完了，有些task 可能会慢一点，要一分半才运行完，所以如果你的task数量，刚好设置的跟cpu core 数量相同，可能会导致资源的浪费。比如150task ，10个先运行完了，剩余140个还在运行，但是这个时候，就有10个cpu core空闲出来了，导致浪费。如果设置2~3倍，那么一个task运行完以后，另外一个task马上补上来，尽量让cpu core不要空闲。同时尽量提升spark运行效率和速度。提升性能。</p></li>
<li><p>如何设置一个Spark Application的并行度？spark.defalut.parallelism 用于设置 RDD 的默认并行度</p></li>
<li><p>RDD.repartition，给RDD重新设置partition的数量</p></li>
<li><p>reduceByKey的算子指定partition的数量 <br/>
 <code>
 val rdd2 = rdd1.reduceByKey(_+_,10)   
 val rdd3 = rdd2.map.filter.reduceByKey(_+_)
</code></p></li>
<li><p><code>val rdd3 = rdd1.join（rdd2）</code> rdd3里面partiiton的数量是由父RDD中最多的partition数量来决定，因此使用join算子的时候，增加父RDD中partition的数量。</p></li>
</ol>


<h2>Executor 并发度</h2>

<p>分布式任务由 Driver 分发到 Executor 后，Executor 将 Task 封装为 TaskRunner，然后将其交给可回收缓存线程池（newCachedThreadPool）。线程池中的线程领取到 TaskRunner 之后，向 Execution Memory 申请内存，然后开始执行任务。</p>

<ol>
<li><p>RDD在计算的时候，每个分区都会起一个task，所以rdd的分区数目决定了总的的task数目。</p></li>
<li><p>每个节点可以起一个或多个Executor。</p></li>
<li><p>Executor 的线程池大小由参数 spark.executor.cores 决定，每个任务在执行期间需要消耗的线程数由 spark.task.cpus 配置项给定。两者相除得到的商就是并发度，也就是同一时间内，一个 Executor 内部可以同时运行的最大任务数量。又因为，spark.task.cpus 默认数值为 1，并且通常不需要调整，所以，并发度基本由 spark.executor.cores 参数敲定。在同一个 Executor 中，当有多个（记为 N）线程尝试抢占执行内存时，需要遵循 2 条基本原则：</p>

<ol>
<li><p>可用内存总大小（记为 M）为两部分之和，一部分是 Execution Memory 初始大小，另一部分是 Storage Memory 剩余空间。在统一内存管理模式下，在 Storage Memory 没有被 RDD 缓存占满的情况下，执行任务可以动态地抢占 Storage Memory。</p></li>
<li><p>每个线程分到的可用内存有一定的上下限，下限是 M/N/2，上限是 M/N</p></li>
</ol>
</li>
<li><p>申请的计算节点（Executor）数目和每个计算节点核数，决定了你同一时刻可以并行执行的task。Task被执行的并发度 = Executor数目（SPARK_EXECUTOR_INSTANCES） * 每个Executor核数（SPARK_EXECUTOR_CORES）</p></li>
<li><p>就 Executor 的线程池来说，尽管线程本身可以复用，但每个线程在同一时间只能计算一个任务，每个任务负责处理一个数据分片。因此，在运行时，线程、任务与分区是一一对应的关系。</p></li>
</ol>


<p>在一个 Executor 内，每个 CPU 线程能够申请到的内存比例是有上下限的。在给定执行内存总量 M 和线程总数 N 的情况下，为了保证每个线程都有机会拿到适量的内存去处理数据，Spark 用 HashMap 数据结构，以（Key，Value）的方式来记录每个线程消耗的内存大小，并确保所有的 Value 值都不超过 M/N。在一些极端情况下，有些线程申请不到所需的内存空间，能拿到的内存合计还不到 M/N/2。这个时候，Spark 就会把线程挂起，直到其他线程释放了足够的内存空间为止。换句话说，尽管一个 Executor 中有 N 个 CPU 线程，但这 N 个线程不一定都在干活。在 Spark 任务调度的过程中，这 N 个线程不见得能同时拿到分布式任务，所以先拿到任务的线程就有机会申请到更多的内存。在某些极端的情况下，后拿到任务的线程甚至连一寸内存都申请不到。PS：线程挂起是CPU 低效原因之一</p>

<h2>建议</h2>

<p>提升性能的办法</p>

<ol>
<li><p>增加executor：如果executor数量比较少，那么，能够并行执行的task数量就比较少，就意味着，我们的Application的并行执行的能力就很弱。比如有3个executor，每个executor有2个cpu core，那么同时能够并行执行的task，就是6个。6个执行完以后，再换下一批6个task。增加了executor数量以后，那么，就意味着，能够并行执行的task数量，也就变多了。</p></li>
<li><p>增加每个executor的cpu core：也是增加了执行的并行能力。原本20个executor，每个才2个cpu core。能够并行执行的task数量，就是40个task。现在每个executor的cpu core，增加到了5个。能够并行执行的task数量，就是100个task。执行的速度，提升了2倍左右。</p></li>
<li><p>增加每个executor的内存量。增加了内存量以后，对性能的提升，有三点：</p>

<ol>
<li><p>如果需要对RDD进行cache，那么更多的内存，就可以缓存更多的数据，将更少的数据写入磁盘，甚至不写入磁盘。减少了磁盘IO。</p></li>
<li><p>对于shuffle操作，reduce端，会需要内存来存放拉取的数据并进行聚合。如果内存不够，也会写入磁盘。如果给executor分配更多内存以后，就有更少的数据，需要写入磁盘，甚至不需要写入磁盘。减少了磁盘IO，提升了性能。</p></li>
<li><p>对于task的执行，可能会创建很多对象。如果内存比较小，可能会频繁导致JVM堆内存满了，然后频繁GC，垃圾回收，minor GC和full GC。（速度很慢）。内存加大以后，带来更少的GC，垃圾回收，避免了速度变慢，性能提升。</p></li>
</ol>
</li>
</ol>


<p>在给定执行内存 M、线程池大小 N 和数据总量 D 的时候，想要有效地提升 CPU 利用率，我们就要计算出最佳并行度 P，计算方法是让数据分片的平均大小 D/P 坐落在（M/N*2, M/N）区间，让每个Task能够拿到并处理适量的数据。怎么理解适量呢？D/P是原始数据的尺寸，真正到内存里去，是会翻倍的，至于翻多少倍，这个和文件格式有关系。不过，不管他翻多少倍，只要原始的D/P和M/N在一个当量，那么我们大概率就能避开OOM的问题，不至于某些Tasks需要处理的数据分片过大而OOM。Shuffle过后每个Reduce Task也会产生数据分片，spark.sql.shuffle.partitions 控制Joins之中的Shuffle Reduce阶段并行度，spark.sql.shuffle.partitions = 估算结果文件大小 / [128M,256M]，确保shuffle 后的数据分片大小在[128M,256M]区间。PS： 核心思路是，根据“定下来的”，去调整“未定下来的”，就可以去设置每一个参数了。</p>

<p>假定Spark读取分布式文件，总大小512M，HDFS的分片是128M，那么并行度 = 512M / 128M = 4</p>

<ol>
<li><p>Executor 并发度=1，那么Executor 内存 M 应在 128M 到 256M 之间。</p></li>
<li><p>Executor 并发度=2，那么Executor 内存 M 应在 256M 到 512M 之间。</p></li>
</ol>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Spark Word2Vec源码详细解析]]></title>
    <link href="https://ibillxia.github.io/blog/2020/10/16/spark-word2vec-source-analysis/"/>
    <updated>2020-10-16T18:40:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2020/10/16/spark-word2vec-source-analysis</id>
    <content type="html"><![CDATA[<p>目录：</p>

<p>1.简单介绍spark word2vec</p>

<p>2.skip-gram 层次softmax版本的源码解析</p>

<p>3.word2vec 的原理 只需要看层次哈弗曼树skip-gram那部分</p>

<p>4.skip-gram negetive sample 的版本源码解析</p>

<!--more-->


<h1>1.简单介绍spark word2vec</h1>

<p>Word2Vec creates vector representation of words in a text corpus.</p>

<p>The algorithm first constructs a vocabulary from the corpus and then learns vector representation of words in the vocabulary.</p>

<p>The vector representation can be used as features in natural language processing and machine learning algorithms.</p>

<p>We used skip-gram model in our implementation and hierarchical softmax method to train the model. The variable names in the implementation matches the original C implementation.</p>

<p>For original C implementation, see https://code.google.com/p/word2vec/</p>

<p>For research papers, see</p>

<p>Efficient Estimation of Word Representations in Vector Space (paper1)</p>

<p>and</p>

<p>Distributed Representations of Words and Phrases and their Compositionality. (paper2)</p>

<p>word2vec算法创建了关于语料库里面词的代表————词向量。</p>

<p>该算法首先从语料库构建词汇表，然后学习词汇表中单词的向量表示。 向量表示可用作自然语言处理和机器学习算法中的特征。 sparkMLLIB只实现了skip-gram模型，并使用分层softmax方法来训练模型。spark的代码实现参考原始word2vecC语言代码一致。原始C语言实现见：https://code.google.com/p/word2vec/。相关研究论文见：Efficient Estimation of Word Representations in Vector Space和Distributed Representations of Words and Phrases and their Compositionality。</p>

<h1>2.skip-gram 层次softmax版本的源码解析</h1>

<pre><code>package org.apache.spark.mllib.feature
import java.lang.{Iterable =&gt; JavaIterable}
import scala.collection.JavaConverters._
import scala.collection.mutable
import com.github.fommil.netlib.BLAS.{getInstance =&gt; blas}
import org.json4s.DefaultFormats
import org.json4s.JsonDSL._
import org.json4s.jackson.JsonMethods._
import org.apache.spark.SparkContext
import org.apache.spark.annotation.Since
import org.apache.spark.api.java.JavaRDD
import org.apache.spark.broadcast.Broadcast
import org.apache.spark.internal.Logging
import org.apache.spark.mllib.linalg.{Vector, Vectors}
import org.apache.spark.mllib.util.{Loader, Saveable}
import org.apache.spark.rdd._
import org.apache.spark.sql.SparkSession
import org.apache.spark.util.BoundedPriorityQueue
import org.apache.spark.util.Utils
import org.apache.spark.util.random.XORShiftRandom
/**
*  Entry in vocabulary   定义词典的属性类   复习：scala的class类别
*/
private case class VocabWord(
  var word: String,   //词
  var cn: Int,        //频次
  var point: Array[Int],   // ARRAY 存的是这个词[叶子结点]的从根节点到叶子节点的路径经过的节点
  var code: Array[Int],   //记录Huffman编码
  var codeLen: Int        //code长度，路径长度 ，存储到达该叶子结点，要经过多少个结点
)
//本文只实现skip-gram hierarchical softmax 部分，参照C语言实现的代码：https://code.google.com/p/word2vec/
//参照两篇论文：Efficient Estimation of Word Representations in Vector Space &amp; Distributed Representations of Words and Phrases and their Compositionality
@Since("1.1.0")
class Word2Vec extends Serializable with Logging {
//默认参数
  private var vectorSize = 100  //训练vector的长度
  private var learningRate = 0.025  //训练时的学习率
  private var numPartitions = 1   //分区数
  private var numIterations = 1   //迭代次数
  private var seed = Utils.random.nextLong()  //随机种子
  private var minCount = 5   //词的最小出现频次
  private var maxSentenceLength = 1000  //句子的长度
//如果大于maxSentenceLength 句子的长度，将会截断为多个块。
  /**
   * Sets the maximum length (in words) of each sentence in the input data.
   * Any sentence longer than this threshold will be divided into chunks of
   * up to `maxSentenceLength` size (default: 1000)
   */
  @Since("2.0.0")
  def setMaxSentenceLength(maxSentenceLength: Int): this.type = {
    require(maxSentenceLength &gt; 0,
      s"Maximum length of sentences must be positive but got ${maxSentenceLength}")
    this.maxSentenceLength = maxSentenceLength
    this
  }
  /**
   * Sets vector size (default: 100).
   */
  @Since("1.1.0")
  def setVectorSize(vectorSize: Int): this.type = {
    require(vectorSize &gt; 0,
      s"vector size must be positive but got ${vectorSize}")
    this.vectorSize = vectorSize
    this
  }
  /**
   * Sets initial learning rate (default: 0.025).
   */
  @Since("1.1.0")
  def setLearningRate(learningRate: Double): this.type = {
    require(learningRate &gt; 0,
      s"Initial learning rate must be positive but got ${learningRate}")
    this.learningRate = learningRate
    this
  }
  /**
   * Sets number of partitions (default: 1). Use a small number for accuracy. 
   * //设置少数分区有利于准确性
   */
  @Since("1.1.0")
  def setNumPartitions(numPartitions: Int): this.type = {
    require(numPartitions &gt; 0,
      s"Number of partitions must be positive but got ${numPartitions}")
    this.numPartitions = numPartitions
    this
  }
  /**
   * Sets number of iterations (default: 1), which should be smaller than or equal to number of 
   * partitions. 
   *  //设置迭代次数，要小于或者等于分区数
   */
  @Since("1.1.0")
  def setNumIterations(numIterations: Int): this.type = {
    require(numIterations &gt;= 0,
      s"Number of iterations must be nonnegative but got ${numIterations}")
    this.numIterations = numIterations
    this
  }
  /**
   * Sets random seed (default: a random long integer).
   */
  @Since("1.1.0")
  def setSeed(seed: Long): this.type = {
    this.seed = seed
    this
  }
  /**
   * Sets the window of words (default: 5) 
   * //根据单个文本的长度合理设置，目前针对于标题40个字，设置为5
   */
  @Since("1.6.0")
  def setWindowSize(window: Int): this.type = {
    require(window &gt; 0,
      s"Window of words must be positive but got ${window}")
    this.window = window
    this
  }
  /**
   * Sets minCount, the minimum number of times a token must appear to be included in the word2vec
   * model's vocabulary (default: 5).
   * //根据文本的词的频次分布设置，保证覆盖大多数的文本。
   */
  @Since("1.3.0")
  def setMinCount(minCount: Int): this.type = {
    require(minCount &gt;= 0,
      s"Minimum number of times must be nonnegative but got ${minCount}")
    this.minCount = minCount
    this
  }
  private val EXP_TABLE_SIZE = 1000
  private val MAX_EXP = 6
  private val MAX_CODE_LENGTH = 40
  /** context words from [-window, window] */  
  //滑动窗口以中心词的左右各+-window选词。
  private var window = 5
  private var trainWordsCount = 0L
  private var vocabSize = 0
/*********transient 解释：
我们都知道一个对象只要实现了Serilizable接口，这个对象就可以被序列化，java的这种序列化模式为开发者提供了很多便利，我们可以不必关系具体序列化的过程，只要这个类实现了Serilizable接口，这个类的所有属性和方法都会自动序列化。
然而在实际开发过程中，我们常常会遇到这样的问题，这个类的有些属性需要序列化，而其他属性不需要被序列化，打个比方，如果一个用户有一些敏感信息（如密码，银行卡号等），为了安全起见，不希望在网络操作（主要涉及到序列化操作，本地序列化缓存也适用）中被传输，这些信息对应的变量就可以加上transient关键字。换句话说，这个字段的生命周期仅存于调用者的内存中而不会写到磁盘里持久化。
总之，java的transient关键字为我们提供了便利，你只需要实现Serilizable接口，将不需要序列化的属性前添加关键字transient，序列化对象的时候，这个属性就不会序列化到指定的目的地中。
*********transient 解释：
*/
  @transient private var vocab: Array[VocabWord] = null
  @transient private var vocabHash = mutable.HashMap.empty[String, Int]
/********************************************************************************************************************/
//from :org.apache.spark.ml.feature.Word2Vec#fit
override def fit(dataset: Dataset[_]): Word2VecModel = {
  transformSchema(dataset.schema, logging = true)
  val input = dataset.select($(inputCol)).rdd.map(_.getAs[Seq[String]](0))
  val wordVectors = new feature.Word2Vec()
    .setLearningRate($(stepSize))
    .setMinCount($(minCount))
    .setNumIterations($(maxIter))
    .setNumPartitions($(numPartitions))
    .setSeed($(seed))
    .setVectorSize($(vectorSize))
    .setWindowSize($(windowSize))
    .setMaxSentenceLength($(maxSentenceLength))
    .fit(input)
  copyValues(new Word2VecModel(uid, wordVectors).setParent(this))
}
/*********************************************************************************************************************/
//dataset来自上面的input，里面是:Seq[String]
  private def learnVocab[S &lt;: Iterable[String]](dataset: RDD[S]): Unit = {  //构建每个词的类
    val words = dataset.flatMap(x =&gt; x)  //把所有的词压平，统计词频
    vocab = words.map(w =&gt; (w, 1))
      .reduceByKey(_ + _)
      .filter(_._2 &gt;= minCount)  //过滤词频大于minCount的词
      .map(x =&gt; VocabWord(
        x._1,
        x._2,
        new Array[Int](MAX_CODE_LENGTH),
        new Array[Int](MAX_CODE_LENGTH),
        0))
      .collect()
      .sortWith((a, b) =&gt; a.cn &gt; b.cn)  //按频数从大到小排序
    vocabSize = vocab.length
    require(vocabSize &gt; 0, "The vocabulary size should be &gt; 0. You may need to check " +
      "the setting of minCount, which could be large enough to remove all your words in sentences.")
    var a = 0
    while (a &lt; vocabSize) {
      vocabHash += vocab(a).word -&gt; a   //@transient private var vocabHash = mutable.HashMap.empty[String, Int]，【词，词频】  生成hashMap（K：word，V：a）--&gt; 对词典中所有元素进行映射，方便查找
      trainWordsCount += vocab(a).cn    //训练词的个数统计
      a += 1
    }
    logInfo(s"vocabSize = $vocabSize, trainWordsCount = $trainWordsCount")
  }

//创建sigmoid函数查询表
  private def createExpTable(): Array[Float] = {
    val expTable = new Array[Float](EXP_TABLE_SIZE)
    var i = 0
    while (i &lt; EXP_TABLE_SIZE) {
      val tmp = math.exp((2.0 * i / EXP_TABLE_SIZE - 1.0) * MAX_EXP)
      expTable(i) = (tmp / (tmp + 1.0)).toFloat
      i += 1
    }
    expTable
  }

//构造哈夫曼树
  private def createBinaryTree(): Unit = {
    val count = new Array[Long](vocabSize * 2 + 1)  //二叉树中所有的结点
    val binary = new Array[Int](vocabSize * 2 + 1)  //设置每个结点的Huffman编码：左1，右0
    val parentNode = new Array[Int](vocabSize * 2 + 1)  //存储每个结点的父节点
    val code = new Array[Int](MAX_CODE_LENGTH)  //存储每个叶子结点的Huffman编码
    val point = new Array[Int](MAX_CODE_LENGTH)  //存储每个叶子结点的路径（经历过哪些结点）
    var a = 0
    while (a &lt; vocabSize) { //节点 0~vocabSize-1  赋值为该节点词的频次  左边都是叶子结点
      count(a) = vocab(a).cn
      a += 1
    }
    while (a &lt; 2 * vocabSize) {  //节点 vocabSize~2*vocabSize-1  赋值为1e9  右边都是父节点
      count(a) = 1e9.toInt
      a += 1
    }
    var pos1 = vocabSize - 1
    var pos2 = vocabSize
    //min1i和min2i是左右节点
    var min1i = 0
    var min2i = 0
    a = 0
    while (a &lt; vocabSize - 1) {
      if (pos1 &gt;= 0) {
        if (count(pos1) &lt; count(pos2)) {
          min1i = pos1
          pos1 -= 1
        } else {
          min1i = pos2
          pos2 += 1
        }
      } else {
        min1i = pos2
        pos2 += 1
      }
      if (pos1 &gt;= 0) {
        if (count(pos1) &lt; count(pos2)) {
          min2i = pos1
          pos1 -= 1
        } else {
          min2i = pos2
          pos2 += 1
        }
      } else {
        min2i = pos2
        pos2 += 1
      }
      count(vocabSize + a) = count(min1i) + count(min2i)   //从三个点里面找到和最小的两个点
      parentNode(min1i) = vocabSize + a    //父节点
      parentNode(min2i) = vocabSize + a    //父节点
      binary(min2i) = 1          //定义右子树为1
      a += 1
    }
    // Now assign binary code to each vocabulary word
    var i = 0
    a = 0
    while (a &lt; vocabSize) {
      var b = a
      i = 0
      while (b != vocabSize * 2 - 2) {  //哈弗曼树一共有2n-1个节点，所以vocabSize*2-2指的是根节点，遍历a二叉树路径上的每个节点，除了根节点
        code(i) = binary(b)         //第b个结点的Huffman编码是0 or 1
        point(i) = b                //存储路径，经过b结点
        i += 1
        b = parentNode(b)          //按照路径去找下一个节点，遍历b的下个节点
      }
      vocab(a).codeLen = i         //存储到达叶子结点a，要经过多少个结点
      vocab(a).point(0) = vocabSize - 2 //每个词的point(0)都是一样的为vocabSize-2，这个是根节点，在这里哈弗曼树已经建立完成了，point记录的是叶子结点a的从根节点以来的路径，因为哈弗曼树所有词的节点是叶子结点，从根节点到叶子节点上的路径都是中间节点如图一所示的，路径里面的节点都减了vocabSize,因为中间节点是vocabSize-1个，所以又都放在0到vocabSize-1的范围了。
      b = 0
      while (b &lt; i) {        //遍历a二叉树路径上的每个节点
        vocab(a).code(i - b - 1) = code(b)   //根据上一步的结果，对节点a的哈夫曼编码赋值
        vocab(a).point(i - b) = point(b) - vocabSize  //根据上一步的结果，对节点a的路径节点进行赋值
        b += 1
      }
      a += 1    //下一个词
    }
  }
  /**
   * Computes the vector representation of each word in vocabulary.
   * @param dataset an RDD of sentences,
   *                each sentence is expressed as an iterable collection of words
   * @return a Word2VecModel
   */
  @Since("1.1.0")
  def fit[S &lt;: Iterable[String]](dataset: RDD[S]): Word2VecModel = {
    learnVocab(dataset)         //构建词汇类
    createBinaryTree()          //构建 Huffman 树
    val sc = dataset.context
    val expTable = sc.broadcast(createExpTable())   //广播sigmod查询表
    val bcVocab = sc.broadcast(vocab)               //广播词汇类
    val bcVocabHash = sc.broadcast(vocabHash)       //广播词 词索引
    try {
      doFit(dataset, sc, expTable, bcVocab, bcVocabHash)  
    } finally {
      expTable.destroy(blocking = false)   //销毁广播变量
      bcVocab.destroy(blocking = false)
      bcVocabHash.destroy(blocking = false)
    }
  }

  private def doFit[S &lt;: Iterable[String]](
    dataset: RDD[S], sc: SparkContext,
    expTable: Broadcast[Array[Float]],
    bcVocab: Broadcast[Array[VocabWord]],
    bcVocabHash: Broadcast[mutable.HashMap[String, Int]]) = {
    // each partition is a collection of sentences,
    // will be translated into arrays of Index integer
    val sentences: RDD[Array[Int]] = dataset.mapPartitions { sentenceIter =&gt;  //RDD[S] S为RDD里面最细粒度的数据结构，里面装的就是这个类型S的数据
      // Each sentence will map to 0 or more Array[Int]
      sentenceIter.flatMap { sentence =&gt;
        // Sentence of words, some of which map to a word index
        val wordIndexes = sentence.flatMap(bcVocabHash.value.get) // flatMap对句子中每个词得到index，得到每个句子每个词的index
        // break wordIndexes into trunks of maxSentenceLength when has more
        wordIndexes.grouped(maxSentenceLength).map(_.toArray)  //如果有的句子的长度大于1000，就给它分组为1000单位，并是array | wordIndexes是个Iterable[Int]格式利用grouped函数对其分组。
           wordIndexes.grouped(maxSentenceLength)返回的是：Iterator[Array[Int]]
      }
    }
    //val newSentences = sentences.repartition(numPartitions).cache()   //按照给定的分区数，进行重分区  并且全部cache
    //可以改为：
    //todo 更改存储方式
    val newSentences = sentences.repartition(numPartitions).cache()
    //todo 对sentence进行checkpoint
    newSentences.sparkContext.setCheckpointDir("hdfs://ns4/user/dd_edw/tmp.db/item_relationship/item_embedding/graph_embedding_rdwalk")
    newSentences.checkpoint()
    newSentences.count
    bcVocabHash.destroy(blocking = false) //TODO 用完了 需要进行释放 销毁
    //    val newSentences = sentences.repartition(numPartitions).persist(StorageLevel.MEMORY_AND_DISK_SER)
    //    val newSentences = sentences.repartition(numPartitions).persist(StorageLevel.DISK_ONLY)
    val initRandom = new XORShiftRandom(seed)                         //
    if (vocabSize.toLong * vectorSize &gt;= Int.MaxValue) {   //如果词汇量*词向量长度 大于或等于 INT最大值 就抛出异常
      throw new RuntimeException("Please increase minCount or decrease vectorSize in Word2Vec" +
        " to avoid an OOM. You are highly recommended to make your vocabSize*vectorSize, " +
        "which is " + vocabSize + "*" + vectorSize + " for now, less than `Int.MaxValue`.")
    }
    val syn0Global =
      Array.fill[Float](vocabSize * vectorSize)((initRandom.nextFloat() - 0.5f) / vectorSize)   //初始化叶子节点，分词向量随机设置初始值
    val syn1Global = new Array[Float](vocabSize * vectorSize)                                   //初始化非叶子结点，参数向量设置初始值为0
    val totalWordsCounts = numIterations * trainWordsCount + 1                                  //迭代次数*所有分词的个数 +1 
    var alpha = learningRate                                                                    //学习率
    for (k &lt;- 1 to numIterations) {   //开始迭代
      val bcSyn0Global = sc.broadcast(syn0Global)     
      val bcSyn1Global = sc.broadcast(syn1Global)
      val numWordsProcessedInPreviousIterations = (k - 1) * trainWordsCount //已经迭代过的词数
      val partial = newSentences.mapPartitionsWithIndex { case (idx, iter) =&gt;
        val random = new XORShiftRandom(seed ^ ((idx + 1) &lt;&lt; 16) ^ ((-k - 1) &lt;&lt; 8))
        val syn0Modify = new Array[Int](vocabSize)
        val syn1Modify = new Array[Int](vocabSize)
        /**
        def foldLeft[B](z: B)(op: (B, A) =&gt; B): B = {
          var result = z
          this foreach (x =&gt; result = op(result, x))
          result
        }
        */
        val model = iter.foldLeft((bcSyn0Global.value, bcSyn1Global.value, 0L, 0L)) { //{}里面是OP[具体操作]，初始值是(bcSyn0Global.value, bcSyn1Global.value, 0L, 0L)，然后在每个分区里面串行运行，x是case ((syn0, syn1, lastWordCount, wordCount), sentence)，最终结果是：(syn0, syn1, lwc, wc) 和Z同种类型。最后的结果(syn0, syn1, lwc, wc)总是更新存在的。总是赋值给B类型。最后结果也是B，B就是(syn0, syn1, lwc, wc)类型的数据。iter每迭代一次sentence就会更新一次B
          case ((syn0, syn1, lastWordCount, wordCount), sentence) =&gt;  //每个分区里面的每个sentence
            var lwc = lastWordCount  //每次迭代的最新的
            var wc = wordCount
            if (wordCount - lastWordCount &gt; 10000) { //当句子迭代10000个词的时候。每迭代10000词的时候就更新一下alpha
              lwc = wordCount   //更改上次词数
              alpha = learningRate *
                (1 - (numPartitions * wordCount.toDouble + numWordsProcessedInPreviousIterations) /
                  totalWordsCounts)   //随着wordCount变大，alpha变小
              if (alpha &lt; learningRate * 0.0001) alpha = learningRate * 0.0001  //当小于learningRate * 0.0001时候，直接等于learningRate * 0.0001
              logInfo(s"wordCount = ${wordCount + numWordsProcessedInPreviousIterations}, " +
                s"alpha = $alpha")
            }
            wc += sentence.length //wc就是上次的wordCount，一直累加句子的长度。
            var pos = 0
            while (pos &lt; sentence.length) {  //开始迭代，一个sentence中的pos位置的词，pos从0开始
              val word = sentence(pos)
              val b = random.nextInt(window)  //b是window内的随机数
              // Train Skip-gram
              var a = b
              while (a &lt; window * 2 + 1 - b) {  //因为开始a = b ，从b开始到 window * 2 + 1 - b，也就是取pos词左右window - b 个词,迭代pos附近的窗口：window - b
                if (a != window) { //当a不是中心词
                  val c = pos - window + a   //pos位置的词pos-(window - a)[真实位置]
                  if (c &gt;= 0 &amp;&amp; c &lt; sentence.length) {  //pos的左右位置迭代取值可能是负的或者超出句子长度，限定范围
                    val lastWord = sentence(c)    //该词的index
                    val l1 = lastWord * vectorSize  //syn0的index
                    val neu1e = new Array[Float](vectorSize) //相当于公式里面的e，就是x的梯度迭代项
                    // Hierarchical softmax
                    var d = 0
                    while (d &lt; bcVocab.value(word).codeLen) {  //迭代中心词的路径哈夫曼二分类
                      val inner = bcVocab.value(word).point(d)  //路径上节点index
                      val l2 = inner * vectorSize               //syn1对应的index
                      // Propagate hidden -&gt; output    blas.sdot函数解释：sdot(int n, float[] sx, int _sx_offset, int incx, float[] sy, int _sy_offset, int incy)，结果是：sx .* sy,并且sx[_sx_offset,incx*n + _sx_offset],sy[_sy_offset,incy*n + _sy_offset]
                      var f = blas.sdot(vectorSize, syn0, l1, 1, syn1, l2, 1)   //向量点乘，syn0 .* syn1 , syn0[l1,l1+1*vectorSize],syn1[l2,l2+1*vectorSize]
                      if (f &gt; -MAX_EXP &amp;&amp; f &lt; MAX_EXP) {                    
                        val ind = ((f + MAX_EXP) * (EXP_TABLE_SIZE / MAX_EXP / 2.0)).toInt
                        f = expTable.value(ind)   //索引到sigmod函数表的值
                        val g = ((1 - bcVocab.value(word).code(d) - f) * alpha).toFloat   //梯度
                        blas.saxpy(vectorSize, g, syn1, l2, 1, neu1e, 0, 1)   //neu1e = g * syn1 + neu1e  blas.saxpy函数解释：saxpy(int n, float sa, float[] sx, int _sx_offset, int incx, float[] sy, int _sy_offset, int incy)，结果是：sy= sa*sx+sy，并且sx[_sx_offset,_sx_offset+incx*n],sy[_sy_offset,_sy_offset+incy*n]
                        blas.saxpy(vectorSize, g, syn0, l1, 1, syn1, l2, 1)   //syn1 = g * syn0 + syn1
                        syn1Modify(inner) += 1          //记录参数向量里面的点被更新次数
                      }
                      d += 1
                    }
                    blas.saxpy(vectorSize, 1.0f, neu1e, 0, 1, syn0, l1, 1)  //syn0 = 1.0f * neu1e + syn0   a的情况下，迭代完成中心词pos附近的一个词的参数向量和词向量
                    syn0Modify(lastWord) += 1
                  }
                }
                a += 1
              }
              pos += 1   //循环到这个句子的下一个中心词
            }
            (syn0, syn1, lwc, wc)
        }
        val syn0Local = model._1   //syn0 为叶子结点向量，即分词向量
        val syn1Local = model._2   //syn1 为非叶子结点向量，即参数向量
        // Only output modified vectors.   Iterator.tabulate函数： Creates an iterator producing the values of a given function over a range of integer values starting from 0.
        Iterator.tabulate(vocabSize) { index =&gt;
          if (syn0Modify(index) &gt; 0) {
            Some((index, syn0Local.slice(index * vectorSize, (index + 1) * vectorSize)))
          } else {
            None
          }
        }.flatten ++ Iterator.tabulate(vocabSize) { index =&gt;
          if (syn1Modify(index) &gt; 0) {
            Some((index + vocabSize, syn1Local.slice(index * vectorSize, (index + 1) * vectorSize)))
          } else {
            None
          }
        }.flatten    //得到n个词向量的结果，n-1个中间节点的向量结果，两个结果(index，array)拼接起来，并且中间参数节点向量的index 从vocabSize开始编号
      }
      val synAgg = partial.reduceByKey { case (v1, v2) =&gt;   //注意partial是所有分区内部的结果，按照同样的index下的array进行聚合，直接把所有分区的结果暴力累加
          blas.saxpy(vectorSize, 1.0f, v2, 1, v1, 1)
          v1
      }.collect()
      var i = 0
      while (i &lt; synAgg.length) {  //分别得到分词向量和参数向量
        val index = synAgg(i)._1
        if (index &lt; vocabSize) {
          Array.copy(synAgg(i)._2, 0, syn0Global, index * vectorSize, vectorSize)
        } else {
          Array.copy(synAgg(i)._2, 0, syn1Global, (index - vocabSize) * vectorSize, vectorSize)
        }
        i += 1
      }
      bcSyn0Global.destroy(false)
      bcSyn1Global.destroy(false)
    }
    newSentences.unpersist()
    val wordArray = vocab.map(_.word)
    new Word2VecModel(wordArray.zipWithIndex.toMap, syn0Global)  //得到分词向量
  }
  /**
   * Computes the vector representation of each word in vocabulary (Java version).
   * @param dataset a JavaRDD of words
   * @return a Word2VecModel
   */
  @Since("1.1.0")
  def fit[S &lt;: JavaIterable[String]](dataset: JavaRDD[S]): Word2VecModel = {
    fit(dataset.rdd.map(_.asScala))
  }
}
/**
* Word2Vec model
* @param wordIndex maps each word to an index, which can retrieve the corresponding
*                  vector from wordVectors
* @param wordVectors array of length numWords * vectorSize, vector corresponding
*                    to the word mapped with index i can be retrieved by the slice
*                    (i * vectorSize, i * vectorSize + vectorSize)
*/
@Since("1.1.0")
class Word2VecModel private[spark] (
    private[spark] val wordIndex: Map[String, Int],
    private[spark] val wordVectors: Array[Float]) extends Serializable with Saveable {
  private val numWords = wordIndex.size
  // vectorSize: Dimension of each word's vector.
  private val vectorSize = wordVectors.length / numWords
  // wordList: Ordered list of words obtained from wordIndex.
  private val wordList: Array[String] = {
    val (wl, _) = wordIndex.toSeq.sortBy(_._2).unzip
    wl.toArray
  }
  // wordVecNorms: Array of length numWords, each value being the Euclidean norm
  //               of the wordVector.   长度为numWords的数组，每个值都是wordVector的欧几里得范数。
  private val wordVecNorms: Array[Float] = {
    val wordVecNorms = new Array[Float](numWords)
    var i = 0
    while (i &lt; numWords) {
      val vec = wordVectors.slice(i * vectorSize, i * vectorSize + vectorSize)
      wordVecNorms(i) = blas.snrm2(vectorSize, vec, 1)  
      i += 1
    }
    wordVecNorms
  }
  @Since("1.5.0")
  def this(model: Map[String, Array[Float]]) = {
    this(Word2VecModel.buildWordIndex(model), Word2VecModel.buildWordVectors(model))
  }
  override protected def formatVersion = "1.0"
  @Since("1.4.0")
  def save(sc: SparkContext, path: String): Unit = {
    Word2VecModel.SaveLoadV1_0.save(sc, path, getVectors)
  }
  /**
   * Transforms a word to its vector representation
   * @param word a word
   * @return vector representation of word
   */
  @Since("1.1.0")
  def transform(word: String): Vector = {
    wordIndex.get(word) match {
      case Some(ind) =&gt;
        val vec = wordVectors.slice(ind * vectorSize, ind * vectorSize + vectorSize)
        Vectors.dense(vec.map(_.toDouble))
      case None =&gt;
        throw new IllegalStateException(s"$word not in vocabulary")
    }
  }
  /**
   * Find synonyms of a word; do not include the word itself in results.
   * @param word a word
   * @param num number of synonyms to find
   * @return array of (word, cosineSimilarity)
   */
  @Since("1.1.0")
  def findSynonyms(word: String, num: Int): Array[(String, Double)] = {
    val vector = transform(word)
    findSynonyms(vector, num, Some(word))
  }
  /**
   * Find synonyms of the vector representation of a word, possibly
   * including any words in the model vocabulary whose vector respresentation
   * is the supplied vector.
   * @param vector vector representation of a word
   * @param num number of synonyms to find
   * @return array of (word, cosineSimilarity)
   */
  @Since("1.1.0")
  def findSynonyms(vector: Vector, num: Int): Array[(String, Double)] = {
    findSynonyms(vector, num, None)
  }
  /**
   * Find synonyms of the vector representation of a word, rejecting
   * words identical to the value of wordOpt, if one is supplied.
   * @param vector vector representation of a word
   * @param num number of synonyms to find
   * @param wordOpt optionally, a word to reject from the results list
   * @return array of (word, cosineSimilarity)
   */
  private def findSynonyms(
      vector: Vector,   //需要找的这个词向量在所有词向量里面的相似结果
      num: Int,         //需要找的TOPN
      wordOpt: Option[String]): Array[(String, Double)] = {  //返回形式是 (词，相似度)
    require(num &gt; 0, "Number of similar words should &gt; 0")
    val fVector = vector.toArray.map(_.toFloat)  //由double类型变为Float类型，可以节省存储空间
    val cosineVec = new Array[Float](numWords)   //vector 与每个词向量直接的cosine相似度值
    val alpha: Float = 1
    val beta: Float = 0
    // Normalize input vector before blas.sgemv to avoid Inf value  这样对归一化后的结果避免了无穷大的异常出现
    val vecNorm = blas.snrm2(vectorSize, fVector, 1)  //blas.snrm2函数：SNRM2 := sqrt( x'*x ).通过函数名称返回向量的欧几里得范数
    if (vecNorm != 0.0f) {
      blas.sscal(vectorSize, 1 / vecNorm, fVector, 0, 1)  //blas.sscal函数：scales a vector by a constant, uses unrolled loops for increment equal to 1,sscal(int n, float sa, float[] sx, int _sx_offset, int incx) 结果是向量所元素sx*sa,其中 sx[_sx_offset,_sx_offset+incx*n]
    }
    blas.sgemv(
      "T", vectorSize, numWords, alpha, wordVectors, vectorSize, fVector, 1, beta, cosineVec, 1) //wordVectors * fVector   //wordVectors 500万*100维  fVector 100*1维  cosineVec 500万*1维
//sgemv(java.lang.String trans, int m, int n, float alpha, float[] a, int lda, float[] x, int incx, float beta, float[] y, int incy)  y := alpha*A*x + beta*y,   or   y := alpha*A'*x + beta*y
    var i = 0
    while (i &lt; numWords) {
      val norm = wordVecNorms(i)   //每个词向量的欧几里得范数
      if (norm == 0.0f) {
        cosineVec(i) = 0.0f
      } else {
        cosineVec(i) /= norm  //之前fVector已经除了vecNorm，后面只需各个词向量除以自己的范式就行了
      }
      i += 1
    }
//堆排序取数
    val pq = new BoundedPriorityQueue[(String, Float)](num + 1)(Ordering.by(_._2))
    var j = 0
    while (j &lt; numWords) {
      pq += Tuple2(wordList(j), cosineVec(j))
      j += 1
    }
    val scored = pq.toSeq.sortBy(-_._2)
    val filtered = wordOpt match {
      case Some(w) =&gt; scored.filter(tup =&gt; w != tup._1)
      case None =&gt; scored
    }
    filtered
      .take(num)
      .map { case (word, score) =&gt; (word, score.toDouble) }
      .toArray
  }
  /**
   * Returns a map of words to their vector representations.
   */
  @Since("1.2.0")
  def getVectors: Map[String, Array[Float]] = {
    wordIndex.map { case (word, ind) =&gt;
      (word, wordVectors.slice(vectorSize * ind, vectorSize * ind + vectorSize))
    }
  }
}
@Since("1.4.0")
object Word2VecModel extends Loader[Word2VecModel] {
  private def buildWordIndex(model: Map[String, Array[Float]]): Map[String, Int] = {
    model.keys.zipWithIndex.toMap
  }
  private def buildWordVectors(model: Map[String, Array[Float]]): Array[Float] = {
    require(model.nonEmpty, "Word2VecMap should be non-empty")
    val (vectorSize, numWords) = (model.head._2.length, model.size)
    val wordList = model.keys.toArray
    val wordVectors = new Array[Float](vectorSize * numWords)
    var i = 0
    while (i &lt; numWords) {
      Array.copy(model(wordList(i)), 0, wordVectors, i * vectorSize, vectorSize)
      i += 1
    }
    wordVectors
  }
  private object SaveLoadV1_0 {
    val formatVersionV1_0 = "1.0"
    val classNameV1_0 = "org.apache.spark.mllib.feature.Word2VecModel"
    case class Data(word: String, vector: Array[Float])
    def load(sc: SparkContext, path: String): Word2VecModel = {
      val spark = SparkSession.builder().sparkContext(sc).getOrCreate()
      val dataFrame = spark.read.parquet(Loader.dataPath(path))
      // Check schema explicitly since erasure makes it hard to use match-case for checking.
      Loader.checkSchema[Data](dataFrame.schema)
      val dataArray = dataFrame.select("word", "vector").collect()
      val word2VecMap = dataArray.map(i =&gt; (i.getString(0), i.getSeq[Float](1).toArray)).toMap
      new Word2VecModel(word2VecMap)
    }
    def save(sc: SparkContext, path: String, model: Map[String, Array[Float]]): Unit = {
      val spark = SparkSession.builder().sparkContext(sc).getOrCreate()
      val vectorSize = model.values.head.length
      val numWords = model.size
      val metadata = compact(render(
        ("class" -&gt; classNameV1_0) ~ ("version" -&gt; formatVersionV1_0) ~
        ("vectorSize" -&gt; vectorSize) ~ ("numWords" -&gt; numWords)))
      sc.parallelize(Seq(metadata), 1).saveAsTextFile(Loader.metadataPath(path))
      // We want to partition the model in partitions smaller than
      // spark.kryoserializer.buffer.max
      val bufferSize = Utils.byteStringAsBytes(
        spark.conf.get("spark.kryoserializer.buffer.max", "64m"))
      // We calculate the approximate size of the model
      // We only calculate the array size, considering an
      // average string size of 15 bytes, the formula is:
      // (floatSize * vectorSize + 15) * numWords
      val approxSize = (4L * vectorSize + 15) * numWords
      val nPartitions = ((approxSize / bufferSize) + 1).toInt
      val dataArray = model.toSeq.map { case (w, v) =&gt; Data(w, v) }
      spark.createDataFrame(dataArray).repartition(nPartitions).write.parquet(Loader.dataPath(path))
    }
  }
  @Since("1.4.0")
  override def load(sc: SparkContext, path: String): Word2VecModel = {
    val (loadedClassName, loadedVersion, metadata) = Loader.loadMetadata(sc, path)
    implicit val formats = DefaultFormats
    val expectedVectorSize = (metadata \ "vectorSize").extract[Int]
    val expectedNumWords = (metadata \ "numWords").extract[Int]
    val classNameV1_0 = SaveLoadV1_0.classNameV1_0
    (loadedClassName, loadedVersion) match {
      case (classNameV1_0, "1.0") =&gt;
        val model = SaveLoadV1_0.load(sc, path)
        val vectorSize = model.getVectors.values.head.length
        val numWords = model.getVectors.size
        require(expectedVectorSize == vectorSize,
          s"Word2VecModel requires each word to be mapped to a vector of size " +
          s"$expectedVectorSize, got vector of size $vectorSize")
        require(expectedNumWords == numWords,
          s"Word2VecModel requires $expectedNumWords words, but got $numWords")
        model
      case _ =&gt; throw new Exception(
        s"Word2VecModel.load did not recognize model with (className, format version):" +
        s"($loadedClassName, $loadedVersion).  Supported:\n" +
        s"  ($classNameV1_0, 1.0)")
    }
  }
}
</code></pre>

<h1>3.word2vec 的原理 只需要看层次哈弗曼树skip-gram那部分</h1>

<p>原理部分推荐链接：https://www.cnblogs.com/shixiangwan/p/7808249.html</p>

<p>其中Sparkword2vec使用过程中有以下问题：</p>

<p>1）当迭代次数或者分区过多的情况下，会产生Infinity的问题</p>

<p>2）训练过程中分区过多准确度会下降</p>

<p>3）内存消耗过大，全部cache了</p>

<p>4）哈夫曼树的方法时间消耗大。等问题，这些问题最近几天完善都一一解决了嘿嘿</p>

<p>问题1：</p>

<p>针对第一个问题解决方法：</p>

<p>基于以上的源码可以看见：</p>

<p>spark实现skip-gram直接复现原始word2vec-C语言版本。</p>

<p>spark实现得到词向量是累加所有分区和所有迭代的结果，随着迭代次数的增大和分区数增加导致词向量数值异常。采用归一化词向量迭代结果，把每次迭代和每个分区的结果累加并且归一化就可以了。</p>

<p>具体代码如下：</p>

<p>把这个代码</p>

<pre><code>val synAgg = partial.reduceByKey { case (v1, v2) =&gt;
        blas.saxpy(vectorSize, 1.0f, v2, 1, v1, 1)
        v1
</code></pre>

<p>改为：</p>

<pre><code>//修改思路就是把所有的向量结果取均值，计算每个向量的在所有分区出现的计数，然后再取均值
      // https://github.com/apache/spark/pull/26722
      // SPARK-24666: do normalization for aggregating weights from partitions.
      // Original Word2Vec either single-thread or multi-thread which do Hogwild-style aggregation.
      // Our approach needs to do extra normalization, otherwise adding weights continuously may
      // cause overflow on float and lead to infinity/-infinity weights.
       val synAgg = partial.mapPartitions { iter =&gt;
        iter.map { case (id, vec) =&gt;
          (id, (vec, 1))
        }
      }.reduceByKey { case ((v1, count1), (v2, count2)) =&gt;
        blas.saxpy(vectorSize, 1.0f, v2, 1, v1, 1)
        (v1, count1 + count2)
      }.map { case (id, (vec, count)) =&gt;
        blas.sscal(vectorSize, 1.0f / count, vec, 1)
        (id, vec)
</code></pre>

<p>就可以了。完美解决向量值大的问题。</p>

<p>问题2：</p>

<p>分区即是分治的思想，把数据mapPartitions一下每个分区维护自己的一套参数，后期处理把每个分区的参数累加处理，所以在数据迭代上只是并行迭代累加并未串行按照样本依次迭代。分区过多导致每个分区的数据量过小会减少准确度，但是word2vec的效果跟分词质量和数据量的大小有这很大关系。</p>

<p>可以把里面的bcVocabHash.destroy(blocking = false) // 用完了 需要进行释放 销毁，提前释放</p>

<h1>4.skip-gram negetive sample 的版本源码解析</h1>

<p>参考该代码：github SKNS的实现</p>

<pre><code>/*
 * Licensed to the Apache Software Foundation (ASF) under one or more
 * contributor license agreements.  See the NOTICE file distributed with
 * this work for additional information regarding copyright ownership.
 * The ASF licenses this file to You under the Apache License, Version 2.0
 * (the "License"); you may not use this file except in compliance with
 * the License.  You may obtain a copy of the License at
 *
 *    http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
package org.apache.spark.ml.feature.sgns
// https://github.com/shubhamchopra/spark/tree/Word2VecSGNS/mllib/src/main/scala/org/apache/spark/ml/feature
import com.github.fommil.netlib.BLAS.{getInstance =&gt; blas}
import org.apache.spark.internal.Logging
import org.apache.spark.mllib.feature
import org.apache.spark.rdd.RDD
import org.apache.spark.util.random.XORShiftRandom
object Word2VecCBOWSolver extends Logging {
  // learning rate is updated for every batch of size batchSize
  private val batchSize = 10000
  // power to raise the unigram distribution with
  private val power = 0.75
//  private val EXP_TABLE_SIZE = 1000
  private val EXP_TABLE_SIZE = 10000
  private val MAX_EXP = 6
  case class Vocabulary(
                         totalWordCount: Long,
                         vocabMap: Map[String, Int],
                         unigramTable: Array[Int],
                         samplingTable: Array[Float])
  /**
    * This method implements Word2Vec Continuous Bag Of Words based implementation using
    * negative sampling optimization, using BLAS for vectorizing operations where applicable.
    * The algorithm is parallelized in the same way as the skip-gram based estimation.
    * We divide input data into N equally sized random partitions.
    * We then generate initial weights and broadcast them to the N partitions. This way
    * all the partitions start with the same initial weights. We then run N independent
    * estimations that each estimate a model on a partition. The weights learned
    * from each of the N models are averaged and rebroadcast the weights.
    * This process is repeated `maxIter` number of times.
    *
    * @param input A RDD of strings. Each string would be considered a sentence.
    * @return Estimated word2vec model
    */
  def fit[S &lt;: Iterable[String]](
//                                spark:SparkSession, //TODO
                                  word2Vec: Word2Vec,
                                  skipGramMode: Boolean,
                                  input: RDD[S]): feature.Word2VecModel = {
    val negativeSamples = word2Vec.getNegativeSamples //负采样的词数目
    val sample = word2Vec.getSample //针对高频词的衰减系数
    /**
      * totalWordCount   Long类型的实数
      * vocabMap         Map[String, Int]长度为所有词的大小
      * unigramTable     Array[Int]   长度为设置的采样table的长度
      * samplingTable    Array[Float] 所有词的长度大小，采样表
      *
      */
    val Vocabulary(totalWordCount, vocabMap, uniTable, sampleTable) =
      generateVocab(input, word2Vec.getMinCount, sample, word2Vec.getUnigramTableSize)
    val vocabSize = vocabMap.size
    //TODO 确认负采样的词的数目要小于整个词汇的大小
    assert(negativeSamples &lt; vocabSize, s"Vocab size ($vocabSize) cannot be smaller" +
      s" than negative samples($negativeSamples)")
    val seed = word2Vec.getSeed
    val initRandom = new XORShiftRandom(seed)
    val vectorSize = word2Vec.getVectorSize
    val syn0Global = Array.fill(vocabSize * vectorSize)(initRandom.nextFloat - 0.5f)  //随机初始化的向量作为输入
    val syn1Global = Array.fill(vocabSize * vectorSize)(0.0f)
    val sc = input.context
    //以下是广播的数据量
    val vocabMapBroadcast = sc.broadcast(vocabMap)
    val unigramTableBroadcast = sc.broadcast(uniTable)
    val sampleTableBroadcast = sc.broadcast(sampleTable)
    val expTable = sc.broadcast(createExpTable())
    val windowSize = word2Vec.getWindowSize  //滑动窗口大小
    val maxSentenceLength = word2Vec.getMaxSentenceLength //最大的句子长度
    val numPartitions = word2Vec.getNumPartitions //运行时的分区数目
/*    import spark.implicits._
    val xxx = input.map(x =&gt; x.toSeq).toDF("seq")
    sc.parallelize(vocabMap.map(x =&gt; (x._1,x._2)).toArray,100).toDF("skuid","index")*/
    //就是把每个句子里的的word转换为index Int格式
    val digitSentences = input.flatMap { sentence =&gt;
      val wordIndexes = sentence.flatMap(vocabMapBroadcast.value.get) // 针对每个句子里面的词，得到该词的index，在flatMap里面应用这个函数[vocabMapBroadcast.value.get],得到每个
      //grouped按照maxSentenceLength分组，把wordIndexes按照最大长度分成几个部分，每个部分的长度不超过 maxSentenceLength
      wordIndexes.grouped(maxSentenceLength).map(_.toArray)
    }.repartition(numPartitions)  //指定分区数目
      .cache() //TODO cache 可以修改存储方式，减少内存
    digitSentences.sparkContext.setCheckpointDir("hdfs://ns4/user/dd_edw/tmp.db/item_relationship/item_embedding/graph_embedding_rdwalk")
    digitSentences.checkpoint()
    digitSentences.count //TODO action
    vocabMapBroadcast.destroy()  //TODO 用完了直接销毁
    val learningRate = word2Vec.getStepSize  //学习率设置的是0.025D
    val wordsPerPartition = totalWordCount / numPartitions   //每个partitions的数据量（以总的词数目不去重为准）
    logInfo(s"VocabSize: ${vocabMap.size}, TotalWordCount: $totalWordCount")
    val maxIter = word2Vec.getMaxIter
    for {iteration &lt;- 1 to maxIter} {   //迭代次数,每次迭代里面有若干个分组batch运行，注意是在每个partition里面的。
      logInfo(s"Starting iteration: $iteration")
      val iterationStartTime = System.nanoTime()
      val syn0bc = sc.broadcast(syn0Global)   //广播词参数
      val syn1bc = sc.broadcast(syn1Global)
      val partialFits = digitSentences.mapPartitionsWithIndex { case (i_, iter) =&gt;
        logInfo(s"Iteration: $iteration, Partition: $i_")
        val random = new XORShiftRandom(seed ^ ((i_ + 1) &lt;&lt; 16) ^ ((-iteration - 1) &lt;&lt; 8))
        val contextWordPairs = iter.flatMap { s =&gt; //iter为一个分区里的所有句子迭代器  s为遍历一个分区里的句子
          val doSample = sample &gt; Double.MinPositiveValue   //是否有采样系数 boolean类型
          /**
            *得到的是Iterator[(Seq[Int], Int)]，其中Seq[Int]是后面Int的上下窗口词集合，后者Int是中心词index
            */
          generateContextWordPairs(
            s,
            windowSize,
            doSample,
            sampleTableBroadcast.value,
            random)
        }
        //把所有的中心词对应的窗口词集合分组批次，按照batchSize大小  batchSize大小默认是10000
        val groupedBatches = contextWordPairs.grouped(batchSize)
        //负采样标签，negativeSamples负采样词的个数
        val negLabels = 1.0f +: Array.fill(negativeSamples)(0.0f)
        val syn0 = syn0bc.value
        val syn1 = syn1bc.value
        val unigramTable = unigramTableBroadcast.value //长度大小为负采样表的长度，负采样的table，目前设置的长度为2千万
        // initialize intermediate arrays
        val contextVec = new Array[Float](vectorSize)
        val l2Vectors = new Array[Float](vectorSize * (negativeSamples + 1))
        val gb = new Array[Float](negativeSamples + 1)
        val neu1e = new Array[Float](vectorSize)
        val wordIndices = new Array[Int](negativeSamples + 1)
        val time = System.nanoTime
        var batchTime = System.nanoTime
        var idx = -1L
        for (batch &lt;- groupedBatches) { // 一个batch就是Seq[(Seq[Int], Int)]集合
          idx = idx + 1 //每迭代一个batch就会idx增加1
          val wordRatio = //会随着idx和iteration的增大而增大，越往后迭代得到的wordRatio越小
            idx.toFloat * batchSize / (maxIter * (wordsPerPartition.toFloat + 1)) +
              ((iteration - 1).toFloat / maxIter)
            //学习率会随着wordRatio的增大而减小，但是不会小于设置的learningRate * 0.0001，越往后迭代学习率会越小
          val alpha = math.max(learningRate * 0.0001, learningRate * (1 - wordRatio)).toFloat
          if(idx % 10 == 0 &amp;&amp; idx &gt; 0) { //TODO 每迭代10个batch会做一次汇总，对当前运行过得batch的时间进行统计。
            logInfo(s"Partition: $i_, wordRatio = $wordRatio, alpha = $alpha") //打印各个分区index，学习率等数据
            val wordCount = batchSize * idx   //本分区总共已经迭代的中心词个数
            val timeTaken = (System.nanoTime - time) / 1e6  // 对每个分区定时器，计算到这步所用的时间
            val batchWordCount = 10 * batchSize  //
            val currentBatchTime = (System.nanoTime - batchTime) / 1e6  // 对每个分区定时器，计算到这步所用的时间
            batchTime = System.nanoTime
            logDebug(s"Partition: $i_, Batch time: $currentBatchTime ms, batch speed: " +
              s"${batchWordCount / currentBatchTime * 1000} words/s")
            logDebug(s"Partition: $i_, Cumulative time: $timeTaken ms, cumulative speed: " +
              s"${wordCount / timeTaken * 1000} words/s")
          }
          val errors = for ((ids, word) &lt;- batch) yield {  //遍历每个batch里面的内容
            val contexts = if (skipGramMode) { //如果是sg-ns模型
              ids.map(i =&gt; Seq(i))  //把每个上下窗口的词变成Seq集合
            } else {
              Seq(ids)  //Seq[Seq[int]]
            }
            val errs = for (contextIds &lt;- contexts) yield {
              // initialize vectors to 0
              zeroVector(contextVec)
              zeroVector(l2Vectors)
              zeroVector(gb)
              zeroVector(neu1e)
              val scale = 1.0f / contextIds.length  //上下SKU个数 如果是sg-ns的话，一个词就是一个Seq，如果是CNOW-ns的话上下SKU集合是一个Seq
              // feed forward   前馈
              contextIds.foreach { c =&gt;
                //blas.saxpy函数解释：saxpy(int n, float sa, float[] sx, int _sx_offset, int incx, float[] sy, int _sy_offset, int incy)，
                // 结果是：sy= sa*sx+sy，并且sx[_sx_offset , _sx_offset + incx*n],sy[ _sy_offset , _sy_offset + incy*n],
                // 本语句的意思是：contextVec = scale*syn0 + contextVec 其中的位移范围是一个vectorSize的长度
                blas.saxpy(vectorSize, scale, syn0, c * vectorSize, 1, contextVec, 0, 1)
              }
              //word是指当前中心词，针对每个中心词采样negativeSamples个词
              generateNegativeSamples(random, word, unigramTable, negativeSamples, wordIndices)
              Iterator.range(0, wordIndices.length).foreach { i =&gt;
                // copy(src: AnyRef, srcPos: Int, dest: AnyRef, destPos: Int, length: Int)
                // 把syn1复制到l2Vectors  l2Vectors是负采样词向量表
                Array.copy(syn1, vectorSize * wordIndices(i), l2Vectors, vectorSize * i, vectorSize)
              }
              // propagating hidden to output in batch  传播隐藏层到output层
              val rows = negativeSamples + 1
              val cols = vectorSize
              //sgemv(trans: String, m: Int, n: Int, alpha: Float, a: Array[Float], _a_offset: Int, lda: Int, x: Array[Float],
              // _x_offset: Int, incx: Int, beta: Float, y: Array[Float], _y_offset: Int, incy: Int)
              // y := alpha*A*x + beta*y,   or   y := alpha*A'*x + beta*y  |||||||====|||||||  gb = 1.0f * l2Vectors * contextVec + 0.0f * gb
              blas.sgemv("T", cols, rows, 1.0f, l2Vectors, 0, cols, contextVec, 0, 1, 0.0f, gb, 0, 1)
              Iterator.range(0, negativeSamples + 1).foreach { i =&gt;
                if (gb(i) &gt; -MAX_EXP &amp;&amp; gb(i) &lt; MAX_EXP) {
                  val ind = ((gb(i) + MAX_EXP) * (EXP_TABLE_SIZE / MAX_EXP / 2.0)).toInt  //TODO 修改sigmod函数的调用采用查表方式
                  val v = expTable.value(ind)
//                  val v = 1.0f / (1 + math.exp(-gb(i)).toFloat)  //sigmod计算，其实可以进行查表运算
                  // computing error gradient
                  val err = (negLabels(i) - v) * alpha  //梯度
                  // update hidden -&gt; output layer, syn1
                  // syn1 = err * contextVec + syn1
                  blas.saxpy(vectorSize, err, contextVec, 0, 1, syn1, wordIndices(i) * vectorSize, 1)
                  // update for word vectors
                  // neu1e = err * l2Vectors + neu1e
                  blas.saxpy(vectorSize, err, l2Vectors, i * vectorSize, 1, neu1e, 0, 1)
                  gb.update(i, err)
                } else {
                  gb.update(i, 0.0f)
                }
              }
              // update input -&gt; hidden layer, syn0
              contextIds.foreach { i =&gt;
                // syn0 = 1.0f * neu1e + syn0
                blas.saxpy(vectorSize, 1.0f, neu1e, 0, 1, syn0, i * vectorSize, 1)
              }
              gb.map(math.abs).sum / alpha
            }
            errs.sum  //每个中心词迭代后的误差
          }
          logInfo(s"Partition: $i_, Average Batch Error = ${errors.sum / batchSize}")
        }
        Iterator.tabulate(vocabSize) { index =&gt;
          (index, syn0.slice(index * vectorSize, (index + 1) * vectorSize))
        } ++ Iterator.tabulate(vocabSize) { index =&gt;
          (vocabSize + index, syn1.slice(index * vectorSize, (index + 1) * vectorSize))
        }
      }
      val aggedMatrices = partialFits.reduceByKey { case (v1, v2) =&gt;
        blas.saxpy(vectorSize, 1.0f, v2, 1, v1, 1)
        v1
      /*val aggedMatrices =  partialFits.mapPartitions { iter =&gt;
        iter.map { case (id, vec) =&gt;
          (id, (vec, 1))
        }
      }.reduceByKey { case ((v1, count1), (v2, count2)) =&gt;
        blas.saxpy(vectorSize, 1.0f, v2, 1, v1, 1)
        (v1, count1 + count2)
      }.map { case (id, (vec, count)) =&gt;
        blas.sscal(vectorSize, 1.0f / count, vec, 1)
        (id, vec)*/
      }.collect()
      val norm = 1.0f / numPartitions
      aggedMatrices.foreach {case (index, v) =&gt;
        blas.sscal(v.length, norm, v, 0, 1)
        if (index &lt; vocabSize) {
          Array.copy(v, 0, syn0Global, index * vectorSize, vectorSize)
        } else {
          Array.copy(v, 0, syn1Global, (index - vocabSize) * vectorSize, vectorSize)
        }
      }
      syn0bc.destroy(false)
      syn1bc.destroy(false)
      val timePerIteration = (System.nanoTime() - iterationStartTime) / 1e6
      logInfo(s"Total time taken per iteration: ${timePerIteration} ms")
    }
    digitSentences.unpersist()
//    vocabMapBroadcast.destroy()
    unigramTableBroadcast.destroy()
    sampleTableBroadcast.destroy()
    new feature.Word2VecModel(vocabMap, syn0Global)
  }
  /**
    * Similar to InitUnigramTable in the original code.   跟源码一样的操作
    */
  private def generateUnigramTable(normalizedWeights: Array[Double], tableSize: Int): Array[Int] = {
    val table = new Array[Int](tableSize)
    var index = 0
    var wordId = 0
    while (index &lt; table.length) { //遍历table
      table.update(index, wordId)
      //[index.toFloat / table.length]这个值最大值是1，normalizedWeights数组最大值也是1，强制把normalizedWeights分成tableSize个区间，
      // 按照table索引的刻度进行分割，最终得到的table里面的每个元素是相邻的是wordID正是table一个刻度所包含的word
      if (index.toFloat / table.length &gt;= normalizedWeights(wordId)) {
        wordId = math.min(normalizedWeights.length - 1, wordId + 1)
      }
      index += 1
    }
    table
  }
  private def createExpTable(): Array[Float] = {
    val expTable = new Array[Float](EXP_TABLE_SIZE)
    var i = 0
    while (i &lt; EXP_TABLE_SIZE) {
      val tmp = math.exp((2.0 * i / EXP_TABLE_SIZE - 1.0) * MAX_EXP)
      expTable(i) = (tmp / (tmp + 1.0)).toFloat
      i += 1
    }
    expTable
  }
  /**
    *
    * @param input
    * @param minCount  最小的频次限制
    * @param sample   sample的系数设置
    * @param unigramTableSize  采样的表格大小
    * @tparam S
    * @return
    */
  private def generateVocab[S &lt;: Iterable[String]](
                                                    input: RDD[S],
                                                    minCount: Int,
                                                    sample: Double,
                                                    unigramTableSize: Int): Vocabulary = {
    val sc = input.context
    val words = input.flatMap(x =&gt; x)
    //按照词的频次进行排序，并且zip上index
    val sortedWordCounts = words.map(w =&gt; (w, 1L))
      .reduceByKey(_ + _)
      .filter{case (w, c) =&gt; c &gt;= minCount}
      .collect()
      .sortWith{case ((w1, c1), (w2, c2)) =&gt; c1 &gt; c2}
      .zipWithIndex
    val totalWordCount = sortedWordCounts.map(_._1._2).sum   //所有词的总和
    //每个词的对应的index索引
    val vocabMap = sortedWordCounts.map{case ((w, c), i) =&gt;
      w -&gt; i
    }.toMap
    //所有词大小的 抽样表
    val samplingTable = new Array[Float](vocabMap.size)
    if (sample &gt; Double.MinPositiveValue) {  // sample 大于double最小值
      sortedWordCounts.foreach { case ((w, c), i) =&gt;
        val samplingRatio = sample * totalWordCount / c  //采样的概率
        samplingTable.update(i, (math.sqrt(samplingRatio) + samplingRatio).toFloat)
      }
    }
    val weights = sortedWordCounts.map{ case((_, x), _) =&gt; scala.math.pow(x, power)} //对每个词的频次进行 f^0.75次方
    val totalWeight = weights.sum   //所有的权重和
    //scanLeft:扫描，即对某个集合的所有元素做fold操作，但是会把产生的所有中间结果放置于一个集合中保存 ，跟foldLeft还是有区别的，foldLeft不存储中间结果
    //TODO normalizedCumWeights数组的长度为所有词的个数大小
    val normalizedCumWeights = weights.scanLeft(0.0)(_ + _).tail.map(x =&gt; x / totalWeight) //数组的tail操作，除了头全是尾部
    //Unigram table size. The unigram table is used to generate negative samples.    本程序设置的2千万
    val unigramTable = generateUnigramTable(normalizedCumWeights, unigramTableSize)
    /**
      * totalWordCount   Long类型的实数
      * vocabMap         Map[String, Int]长度为所有词的大小
      * unigramTable     Array[Int]   长度为设置的采样table的长度
      * samplingTable    Array[Float] 所有词的长度大小，采样表
      *
      */
    Vocabulary(totalWordCount, vocabMap, unigramTable, samplingTable)
  }
  private def zeroVector(v: Array[Float]): Unit = {
    var i = 0
    while(i &lt; v.length) {
      v.update(i, 0.0f)
      i+= 1
    }
  }
  /**
    *
    * @param sentence
    * @param window
    * @param doSample
    * @param samplingTable
    * @param random
    * @return   生成中心词的上下文 词对
    */
  private def generateContextWordPairs(
                                        sentence: Array[Int],
                                        window: Int,
                                        doSample: Boolean,
                                        samplingTable: Array[Float],
                                        random: XORShiftRandom): Iterator[(Seq[Int], Int)] = {
    val reducedSentence = if (doSample) {
      sentence.filter(i =&gt; samplingTable(i) &gt; random.nextFloat)  //每个句子里，随机选取一些词
    } else {
      sentence
    }
    val sentenceLength = reducedSentence.length
    Iterator.range(0, sentenceLength)//该句子的长度
      .map { i =&gt;
      val b = window - random.nextInt(window) // (window - a) in original code
    // pick b words around the current word index
    val start = math.max(0, i - b) // c in original code, floor ar 0
    val end = math.min(sentenceLength, i + b + 1) // cap at sentence length
    // make sure current word is not a part of the context
    val contextIds = Iterator.range(start, end).filter(_ != i).map(reducedSentence(_)) //得到start,end范围内的词的索引
      val word = reducedSentence(i)
      (contextIds.toSeq, word)
    }
  }
  /**
    *
    * @param random
    * @param word
    * @param unigramTable
    * @param numSamples
    * @param arr  最终返回的是arr，里面第一个是word中心词，后面依次是numSamples个采样词
    */
  // This essentially helps translate from uniform distribution to a distribution
  // resembling uni-gram frequency distribution.
  private def generateNegativeSamples(
                                       random: XORShiftRandom,
                                       word: Int,
                                       unigramTable: Array[Int],
                                       numSamples: Int,
                                       arr: Array[Int]): Unit = {
    assert(numSamples + 1 == arr.length,
      s"Input array should be large enough to hold ${numSamples} negative samples")
    arr.update(0, word)  //arr的第一个元素是word（中心词Word）
    var i = 1
    while (i &lt;= numSamples) { //迭代随机选取样本
      val negSample = unigramTable(random.nextInt(unigramTable.length))
      if(negSample != word) {
        arr.update(i, negSample)
        i += 1
      }
    }
  }
}
</code></pre>

<p>原文链接： <a href="https://blog.csdn.net/u014552678/article/details/104001725">https://blog.csdn.net/u014552678/article/details/104001725</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Spark与Flink：下一代大数据计算引擎之争]]></title>
    <link href="https://ibillxia.github.io/blog/2020/05/31/spark-vs-flink-comparison/"/>
    <updated>2020-05-31T16:37:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2020/05/31/spark-vs-flink-comparison</id>
    <content type="html"><![CDATA[<h1>一、下一代大数据计算引擎</h1>

<p>自从数据处理需求超过了传统数据库能有效处理的数据量之后，Hadoop 等各种基于 MapReduce 的海量数据处理系统应运而生。从 2004 年 Google 发表 MapReduce 论文开始，经过近 10 年的发展，基于 Hadoop 开源生态或者其它相应系统的海量数据处理已经成为业界的基本需求。</p>

<p>但是，很多机构在开发自己的数据处理系统时都会发现需要面临一系列的问题。从数据中获取价值需要的投入远远超过预期。常见的问题包括：</p>

<ul>
<li>非常陡峭的学习曲线。刚接触这个领域的人经常会被需要学习的技术的数量砸晕。不像经过几十年发展的数据库一个系统可以解决大部分数据处理需求， Hadoop 等大数据生态里的一个系统往往在一些数据处理场景上比较擅长，另一些场景凑合能用，还有一些场景完全无法满足需求。结果就是需要好几个系统来处理不同的场景。</li>
</ul>


<center><img src="/images/2020/IMAG2020053101.png"></center>


<p>（来源： https://mapr.com/developercentral/lambda-architecture/ ）</p>

<!--more-->


<p>上图是一个典型的 lambda 架构，只是包含了批处理和流处理两种场景，就已经牵涉到至少四五种技术了，还不算每种技术的可替代选择。再加上实时查询，交互式分析，机器学习等场景，每个场景都有几种技术可以选择，每个技术涵盖的领域还有不同方式的重叠。结果就是一个业务经常需要使用四五种以上的技术才能支持好一个完整的数据处理流程。加上调研选型，需要了解的数目还要多得多。</p>

<p>下图是大数据领域的全景。有没有晕？</p>

<center><img src="/images/2020/IMAG2020053102.png"></center>




<center>大数据和 AI 全景 – 2018</center>


<p><br></p>

<ul>
<li><p>开发和运行效率低下。因为牵涉到多种系统，每种系统有自己的开发语言和工具，开发效率可想而知。而因为采用了多套系统，数据需要在各个系统之间传输，也造成了额外的开发和运行代价，数据的一致也难以保证。在很多机构，实际上一半以上的开发精力花在了数据在各个系统之间的传输上。</p></li>
<li><p>复杂的运维。多个系统，每个需要自己的运维，带来更高的运维代价的同时也提高了系统出问题的可能。</p></li>
<li><p>数据质量难以保证。数据出了问题难以跟踪解决。</p></li>
<li><p>最后，还有人的问题。在很多机构，由于系统的复杂性，各个子系统的支持和使用落实在不同部门负责。</p></li>
</ul>


<p>了解了这些问题以后，对 Spark 从 2014 年左右开始迅速流行就比较容易理解了。Spark 在当时除了在某些场景比 Hadoop MapReduce 带来几十到上百倍的性能提升外，还提出了用一个统一的引擎支持批处理，流处理，交互式查询，机器学习等常见的数据处理场景。看过在一个 Notebook 里完成上述所有场景的 Spark 演示，对比之前的数据流程开发，对很多开发者来说不难做出选择。经过几年的发展，Spark 已经被视为可以完全取代 Hadoop 中的 MapReduce 引擎。</p>

<p>正在 Spark 如日中天高速发展的时候，2016 年左右 Flink 开始进入大众的视野并逐渐广为人知。为什么呢？原来在人们开始使用 Spark 之后，发现 Spark 虽然支持各种场见场景，但并不是每一种都同样好用。数据流的实时处理就是其中相对较弱的一环。Flink 凭借更优的流处理引擎，同时也支持各种处理场景，成为 Spark 的有力挑战者。</p>

<p>Spark 和 Flink 是怎么做到这些的，它们之间又有那些异同，下面我们来具体看一下。</p>

<h1>二、Spark 和 Flink 的引擎技术</h1>

<p>这一部分主要着眼于 Spark 和 Flink 引擎的架构方面，更看重架构带来的潜力和限制。现阶段的实现成熟度和局限会在后续生态部分探讨。</p>

<h2>2.1 数据模型和处理模型</h2>

<p>要理解 Spark 和 Flink 的 引擎特点，首先从数据模型开始。</p>

<p>Spark 的数据模型是弹性分布式数据集 RDD（Resilient Distributed Datasets）。 比起 MapReduce 的文件模型，RDD 是一个更抽象的模型，RDD 靠血缘（lineage） 等方式来保证可恢复性。很多时候 RDD 可以实现为分布式共享内存或者完全虚拟化（即有的中间结果 RDD 当下游处理完全在本地时可以直接优化省略掉）。这样可以省掉很多不必要的 I/O，是早期 Spark 性能优势的主要原因。</p>

<p>Spark 用 RDD 上的变换（算子）来描述数据处理。每个算子（如 map，filter，join）生成一个新的 RDD。所有的算子组成一个有向无环图（DAG）。Spark 比较简单地把边分为宽依赖和窄依赖。上下游数据不需要 shuffle 的即为窄依赖，可以把上下游的算子放在一个阶段（stage） 里在本地连续处理，这时上游的结果 RDD 可以 省略。下图展示了相关的基本概念。更详细的介绍在网上比较容易找到，这里就不花太多篇幅了。</p>

<center><img src="/images/2020/IMAG2020053103.png"></center>




<center> Spark DAG</center>


<p><br></p>

<p>Flink 的基本数据模型是数据流，及事件（Event） 的序列。数据流作为数据的基本模型可能没有表或者数据块直观熟悉，但是可以证明是完全等效的。流可以是无边界的无限流，即一般意义上的流处理。也可以是有边界的有限流，这样就是批处理。</p>

<p>Flink 用数据流上的变换（算子）来描述数据处理。每个算子生成一个新的数据流。在算子，DAG，和上下游算子链接（chaining） 这些方面，和 Spark 大致等价。Flink 的节点（vertex）大致相当于 Spark 的阶段（stage），划分也会和上图的 Spark DAG 基本一样。</p>

<center><img src="/images/2020/IMAG2020053104.png"></center>




<center> Flink 任务图</center>


<p><br></p>

<p>在 DAG 的执行上，Spark 和 Flink 有一个比较显著的区别。 在 Flink 的流执行模式中，一个事件在一个节点处理完后的输出就可以发到下一个节点立即处理。这样执行引擎并不会引入额外的延迟。与之相应的，所有节点是需要同时运行的。而 Spark 的 micro batch 和一般的 batch 执行一样，处理完上游的 stage 得到输出之后才开始下游的 stage。</p>

<p>在 Flink 的流执行模式中，为了提高效率也可以把多个事件放在一起传输或者计算。但这完全是执行时的优化，可以在每个算子独立决定，也不用像 RDD 等批处理模型中一样和数据集边界绑定，可以做更加灵活的优化同时可以兼顾低延迟需求。</p>

<p>Flink 使用异步的 checkpoint 机制来达到任务状态的可恢复性，以保证处理的一致性，所以在处理的主流程上可以做到数据源和输出之间数据完全不用落盘，达到更高的性能和更低的延迟。</p>

<h2>2.2 数据处理场景</h2>

<p>除了批处理之外，Spark 还支持实时数据流处理，交互式查询，和机器学习，图计算等。</p>

<center><img src="/images/2020/IMAG2020053105.png"></center>


<p>（来源： https://databricks.com/spark/about ）</p>

<ul>
<li><p>实时数据流处理和批处理主要区别就是对低延时的要求。Spark 因为 RDD 是基于内存的，可以比较容易切成较小的块来处理。如果能对这些小块处理得足够快，就能达到低延时的效果。</p></li>
<li><p>交互式查询场景，如果数据能全在内存，处理得足够快的话，就可以支持交互式查询。</p></li>
<li><p>机器学习和图计算其实是和前几种场景不同的 RDD 算子类型。Spark 提供了库来支持常用的操作，用户或者第三方库也可以自己扩展。值得一提的是，Spark 的 RDD 模型和机器学习模型训练的迭代计算非常契合，从一开始就在有的场景带来了非常显著的性能提升。</p></li>
</ul>


<p>从这些可以看出来，比起 Hadoop MapReduce， Spark 本质上就是基于内存的更快的批处理。然后用足够快的批处理来实现各种场景。</p>

<center><img src="/images/2020/IMAG2020053106.png"></center>


<p>（来源： https://www.slideshare.net/ParisCarbone/state-management-in-apache-flink-consistent-stateful-distributed-stream-processing ）</p>

<p>前面说过，在 Flink 中，如果输入数据流是有边界的，就自然达到了批处理的效果。这样流和批的区别完全是逻辑上的，和处理实现独立，用户需要实现的逻辑也完全一样，应该是更干净的一种抽象。后续会在深入对比流计算方面的时候做更深入的讨论。</p>

<p>Flink 也提供了库来支持机器学习，图计算等场景。从这方面来说和 Spark 没有太大区别。</p>

<p>一个有意思的事情是用 Flink 的底层 API 可以支持只用 Flink 集群实现一些数据驱动的分布式服务。有一些公司用 Flink 集群实现了社交网络，网络爬虫等服务。这个也体现了 Flink 作为计算引擎的通用性，并得益于 Flink 内置的灵活的状态支持。</p>

<p>总的来说，Spark 和 Flink 都瞄准了在一个执行引擎上同时支持大多数数据处理场景，也应该都能做到这一点。主要区别就在于因为架构本身的局限在一些场景会受到限制。比较突出的地方就是 Spark Streaming 的 micro batch 执行模式。Spark 社区应该也意识到了这一点，最近在持续执行模式（continuous processing）方面开始发力。 具体情况会在后面介绍。</p>

<h2>2.3 有状态处理 （Stateful Processing）</h2>

<p>Flink 还有一个非常独特的地方是在引擎中引入了托管状态（managed state）。要理解托管状态，首先要从有状态处理说起。如果处理一个事件（或一条数据）的结果只跟事件本身的内容有关，称为无状态处理；反之结果还和之前处理过的事件有关，称为有状态处理。稍微复杂一点的数据处理，比如说基本的聚合，都是有状态处理。Flink 很早就认为没有好的状态支持是做不好留处理的，因此引入了 managed state 并提供了 API 接口。</p>

<center><img src="/images/2020/IMAG2020053107.png"></center>




<center>Flink 中的状态支持</center>


<p><br></p>

<p>一般在流处理的时候会比较关注有状态处理，但是仔细看的话批处理也是会受到影响的。比如常见的窗口聚合，如果批处理的数据时间段比窗口大，是可以不考虑状态的，用户逻辑经常会忽略这个问题。但是当批处理时间段变得比窗口小的时候，一个批的结果实际上依赖于以前处理过的批。这时，因为批处理引擎一般没有这个需求不会有很好的内置支持，维护状态就成为了用户需要解决的事情。比如窗口聚合的情况用户就要加一个中间结果表记住还没有完成的窗口的结果。这样当用户把批处理时间段变短的时候就会发现逻辑变复杂了。这是早期 Spark Streaming 用户 经常碰到的问题。直到 Structured Streaming 出来才得到缓解。</p>

<p>而像 Flink 这样以流处理为基本模型的引擎，因为一开始就避不开这个问题，所以引入了 managed state 来提供了一个通用的解决方案。比起用户实现的特定解决方案，不但用户开发更简单，而且能提供更好的性能。最重要的是能更好地保证处理结果的一致性。</p>

<p>简单来说，就是有一些內秉的数据处理逻辑，在批处理中容易被忽略或简化处理掉也能得到可用的结果，而在流处理中问题被暴露出来解决掉了。所以流计算引擎用有限流来处理批在逻辑上比较严谨，能自然达到正确性。主要做一些不同的实现来优化性能就可以了。而用更小的批来模拟流需要处理一些以前没有的问题。当计算引擎还没有通用解决方案的时候就需要用户自己解决了。类似的问题还有维表的变化（比如用户信息的更新），批处理数据的边界和迟到数据等等。</p>

<h2>2.4 编程模型</h2>

<center><img src="/images/2020/IMAG2020053108.png"></center>




<center>Spark 1.6 时的 API 状态</center>


<p><br></p>

<p>Spark 的初衷之一就是用统一的编程模型来解决用户的各种需求。在这方面一直很下功夫。最初基于 RDD 的 API 就可以做各种类型的数据处理。后来为了简化用户开发，逐渐推出了更高层的 DataFrame（在 RDD 中加了列变成结构化数据）和 Datasets（在 DataFrame 的列上加了类型），并在 Spark 2.0 中做了整合（DataFrame = DataSet[Row]）。Spark SQL 的支持也比较早就引入了。在加上各个处理类型 API 的不断改进，比如 Structured Streaming 以及和机器学习深度学习的交互，到了今天 Spark 的 API 可以说是非常好用的，也是 Spark 最强的方面之一。</p>

<center><img src="/images/2020/IMAG2020053109.png"></center>




<center>Spark 2.0 API</center>


<p><br></p>

<p>Flink 的 API 也有类似的目标和发展路线。Flink 和 Spark 的核心 API 可以说是可以基本对应的。今天 Spark API 总体上更完备一下，比如说最近一两年大力投入的和机器学习深度学习的整合方面。Flink 在流处理相关的方面还是领先一些，比如对 watermark，window，trigger 的各种支持。</p>

<center><img src="/images/2020/IMAG2020053110.png"></center>




<center>Flink API</center>


<p><br></p>

<h2>2.5 小结</h2>

<p>Spark 和 Flink 都是通用的能够支持超大规模数据处理，支持各种处理类型的计算引擎。两个系统都有很多值得探讨的方面在这里没有触及，比如 SQL 的优化，和机器学习的集成等等。这里主要是试图从最基本的架构和设计方面来比较一下两个系统。因为上层的功能在一定程度上是可以互相借鉴的，有足够的投入应该都能做好。而基本的设计改变起来会伤筋动骨，更困难一些。</p>

<p>Spark 和 Flink 的不同执行模型带来的最大的区别应该还是在对流计算的支持上。最开始的 Spark Streaming 对流计算想得过于简单，对复杂一点的计算用起来会有不少问题。从 Spark 2.0 开始引入的 Structured Streaming 重新整理了流计算的语义，支持按事件时间处理和端到端的一致性。虽然在功能上还有不少限制，比之前已经有了长足的进步。不过 micro batch 执行方式带来的问题还是存在，特别在规模上去以后性能问题会比较突出。最近 Spark 受一些应用场景的推动，也开始开发持续执行模式。2.3 里的实验性发布还只支持简单的 map 类的操作。从最近 Spark+AI Summit 大会上的介绍来看（下图），会发展成一个和 Flink 的流处理模式比较相似的执行引擎。不过从下图来看，主要的功能都还在开发中或者待开发。对将来能做到什么程度，和 Spark 原来的 batch 执行引擎怎么结合，我们拭目以待。</p>

<center><img src="/images/2020/IMAG2020053111.png"></center>




<center>Spark 持续执行模式状态</center>


<p><br></p>

<h1>三、生态和未来</h1>

<h2>3.1 概况</h2>

<center><img src="/images/2020/IMAG2020053112.png"></center>


<p><br></p>

<p>Spark 是最活跃的 Apache 项目之一。从 2014 年左右开始得到广泛关注。Spark 的开源社区一度达到上千的活跃贡献者。最主要推动者是 Databricks，由最初的 Spark 创造者们成立的公司。今年 6 月的 Spark+AI 峰会参加人数超过 4000。 Spark 因为在引擎方面比 MapReduce 全面占优，经过几年发展和 Hadoop 生态结合较好，已经被广泛视为 Hadoop MapReduce 引擎的取代者。</p>

<center><img src="/images/2020/IMAG2020053113.png"></center>


<p><br></p>

<p>Flink 也是 Apache 顶级项目，创始者们成立了 Data Artisans。社区规模还无法和 Spark 相比。不过在业界，特别是流处理方面，有不错的口碑。在大规模流处理方面走在最前沿，也是需求最强的几个美国公司，包括 Netflix、 LinkedIn、Uber、Lyft 等，除 LinkedIn 有自己的 Samza 外，都已经采用 Flink 作为流处理引擎或者有了较大投入。</p>

<p>阿里集团在 Flink 社区也有较大影响力。最近 Flink 1.3 到 1.5 里都有几个重磅功能是阿里和 Data Artisans 合作或者独立开发的。阿里还有可能是世界上最大的流计算集群，也是在 Flink 的基础上开发的。</p>

<h2>3.2 Unified Analytic platform – 包含机器学习的统一分析平台</h2>

<p>最近的 Spark+AI 峰会上， Databricks 主打的主题是统一分析平台（Unified Analytics Platform）。三大新发布：Databricks delta、Databricks Runtime for ML 和 ML flow，都是围绕这一主题。随着近年来机器学习（包括深度学习）在数据处理中占比越来越高，可以说 Databricks 又一次把握住了时代的脉搏。</p>

<p>统一分析平台回应了 Spark 的初衷。经过几年的探索，对初始问题，即用户可以在一个系统里解决绝大部分大数据的需求，有了一个比较明确具体的解决方案。</p>

<center><img src="/images/2020/IMAG2020053114.png"></center>


<p><br></p>

<p>不过有意思的是可以看出 Databricks 在 AI 方面策略的转变。在深度学习流行前，Spark 自带的 MLLib 功能上应该是够用的，但是可能是由于兼容性原因并没有取得预期中的广泛采用。</p>

<p>对深度学习的新宠 TensorFlow，Spark 曾经推出过 TensorFrames 和 Spark 引擎做了一些集成。结果应该不是很成功，可能还没有 Yahoo 从外面搭建的 TensorFlowOnSpark 影响力大。</p>

<p>从这次来看，Spark 转向了集成的策略。Databricks Runtime for ML 实际上就是预装了各个机器学习框架，然后支持在 Spark 任务里启动一个比如 TensorFlow 自己的集群。Spark 引擎方面做的主要改进就是 gang scheduling，即支持一次申请多个 executor 以便 TensorFlow 集群能正常启动。</p>

<p>MLFlow 更是和 Spark 引擎无关。作为一个工作流工具，MLFlow 的目标是帮助数据科学家提高工作效率。主要功能是以项目为单位记录和管理所做的机器学习试验，并支持分享。设计要点是可重复试验，以及对各种工具的灵活易用的支持。看起来 Spark 暂时在作为 AI 引擎方面可能没什么大动作了。</p>

<p>Flink 的目标其实和 Spark 很相似。包含 AI 的统一平台也是 Flink 的发展方向。Flink 从技术上也是可以支持较好的机器学习集成和整条链路的，而且有一些大规模线上学习的使用实例。不过看起来在现阶段 Flink 这方面的平台化还没有 Spark 成熟。值得一提的是 Flink 由于流处理引擎的优势，在线上学习方面可能能支持得更好一些。</p>

<h2>3.3 数据使用者</h2>

<p>产品和生态归根结底是要解决大数据使用者的问题，从数据中产生价值。了解数据的使用者和他们的需求可以帮助我们在在讨论生态的各方面时有一个比较清晰的脉络。</p>

<p>数据相关的工作者大致可以分为以下角色。实际情况中一个组织里很可能几个角色在人员上是重合的。各个角色也没有公认的定义和明确的界限。</p>

<center><img src="/images/2020/IMAG2020053115.png"></center>


<p><br></p>

<ul>
<li><p>数据采集：在产品和系统中合适的地方产生或收集数据发送到数据平台。</p></li>
<li><p>平台：提供数据导入，存储，计算的环境和工具等等。</p></li>
<li><p>数据工程师：使用数据平台把原始数据加工成可以供后续高效使用的数据集。把分析师和数据科学家创建的指标和模型等等生产化成为高效可靠的的自动处理。</p></li>
<li><p>数据分析师和数据科学家（关于这两者的异同有很多讨论。感兴趣的可以自行搜索。 https://www.jianshu.com/p/cfd94d9e4466 这里的译文可以提供一个视角）：为数据赋予意义，发现内含的价值。 下文再不特别区分的地方统称为数据分析。</p></li>
<li><p>产品经理，管理和决策层：根据以上产生的数据调整产品和组织行为。</p></li>
</ul>


<p>这些构成了一个完整的环。上面的顺序是数据流动的方向，而需求的驱动是反过来的方向。</p>

<p>本文所说的 Spark 和 Flink 的生态主要是对应到数据平台这一层。直接面向的用户主要是数据工程师、数据分析师和数据科学家。好的生态能够大大简化数据平台和数据工程师的工作，并使得数据分析师和数据科学家更加自主化同时提高效率。</p>

<h2>3.4 开发环境</h2>

<h3>API</h3>

<p>从 API 上来看，Spark 和 Flink 提供的功能领域大致相当。当然具体看各个方向支持的程度会有差异。总体来看 Spark 的 API 经过几轮迭代，在易用性，特别是机器学习的集成方面，更强一些。Flink 在流计算方面更成熟一些。</p>

<center><img src="/images/2020/IMAG2020053116.png"></center>


<p><br></p>

<p>支持的语言也大致相当。Spark 发展的时间长一些还是有优势，特别是数据分析常用的 Python 和 R。</p>

<center><img src="/images/2020/IMAG2020053117.png"></center>


<p><br></p>

<h3>Connectors</h3>

<p>有了 API，再有数据就可以开工了。Spark 和 Flink 都能对接大部分比较常用的系统。如果暂时还没有支持的，也都能比较好地支持自己写一个 connector。</p>

<center><img src="/images/2020/IMAG2020053118.png"></center>


<p>https://databricks.com/spark/about </p>

<center><img src="/images/2020/IMAG2020053119.png"></center>


<p>https://www.slideshare.net/chobeat/data-intensive-applications-with-apache-flink</p>

<h2>3.5 集成开发工具</h2>

<p>这方面数据工程师和数据分析的需求有一些不同。</p>

<p>数据分析的工作性质比较偏探索性，更强调交互性和分享。Notebook 能比较好地满足这些需求，是比较理想的开发工具，用来做演示效果也相当不错。比较流行的 Notebook 有 Apache Zeppelin，Jupyter 等。Databricks 更是自己开发了 Databricks Notebook 并将之作为服务的主要入口。Zeppelin 支持 Spark 和 Flink，Jupyter 还只支持 Spark。</p>

<p>数据工程师的工作更倾向于把比较确定的数据处理生产化，能快速把代码写出来是一方面。另外还有项目管理，版本管理，测试，配置，调试，部署，监控等等工作，需求和传统的集成开发工具比较相似。 还经常出现需要复用已有的业务逻辑代码库的情况。Notebook 对其中一些需求并不能很好地满足。比较理想的开发工具可能是类似 IntelliJ 加上 Spark/Flink 插件，再加上一些插件能直接提交任务到集群并进行调试，并对接 Apache Oozie 之类的工作流管理等等。在开源社区还没有见到能把这些集成到一起的。在商业产品中倒是见过一些比较接近的。Spark 和 Flink 在这方面差不多。</p>

<h2>3.6 运行环境</h2>

<h3>部署模式 / 集群管理 / 开源闭源</h3>

<p>应用开发完后要提交到运行环境。Spark 和 Flink 都支持各种主流的部署环境，在这方面都算做得比较好的。</p>

<center><img src="/images/2020/IMAG2020053120.png"></center>


<p><br></p>

<h2>3.7 企业级平台</h2>

<p>既然 Spark 和 Flink 都支持各种部署方式，那一个企业是否可以使用开源代码快速搭建一个支持 Spark 或者 Flink 的平台呢？</p>

<p>这个要看想要达到什么效果了。最简单的模式可能是给每个任务起一个独占集群，或着给小团队一个独立集群。这个确实可以很快做到，但是用户多了以后，统一运维的成本可能太高，需要用户参与运维。还有一个缺点是资源分配固定，而负载会有变化，导致资源利用率上不去。比较理想的是多租户的共享大集群，可以提高运维效率的同时最大限度地提高资源利用率。而这就需要一系列的工作，比如不同的作业提交方式，数据安全与隔离等等。对一些企业来说，可能利用托管服务（包括云服务）是一种值得考虑的开始方式。</p>

<h2>3.8 社区</h2>

<p>Spark 社区在规模和活跃程度上都是领先的，毕竟多了几年发展时间。而且作为一个德国公司，Data Artisans 想在美国扩大影响力要更难一些。不过 Flink 社区也有一批稳定的支持者，达到了可持续发展的规模。</p>

<p>在中国情况可能会不一样一些。比起美国公司，中国公司做事情速度更快，更愿意尝试新技术。中国的一些创新场景也对实时性有更高的需求。这些都对 Flink 更友好一些。</p>

<p>近期 Flink 的中国社区有一系列动作，是了解 Flink 的好机会。</p>

<p>Spark 的中文文档在 http://www.apachecn.org/bigdata/spark/27.html 。</p>

<p>Flink 的中文社区在 http://flink-china.org/ 。</p>

<p>另外，今年年底 Flink 中文社区也会在北京举办 Flink Forward China 大会，感兴趣的朋友可以关注。</p>

<h2>3.9 未来发展趋势</h2>

<p>近两年一个明显的趋势就是机器学习在数据处理中的比重增长。Spark 和 Flink 都能支持在一个系统中做机器学习和其它数据处理。谁能做得更好就能掌握先机。</p>

<p>另一个可能没有那么明显的趋势是，随着 IOT 的增长以及计算资源和网络的持续发展，实时处理需求会越来越多。现在其实真正对低延迟有很高追求的业务并没有那么多，所以每一次流计算新技术的出现都能看到那几家公司的身影。随着新应用场景的出现和竞争环境的发展，实时处理可能会变得越来越重要。Flink 现在在这方面是领先的，如果发挥得好可以成为核心优势。</p>

<p>还有一点值得一提的是，因为用户不想锁定供应商，担心持续的支持等原因，是否开源已经成为用户选择数据产品的一个重要考量。闭源产品如果没有决定性优势会越来越难和基于开源技术的产品竞争。</p>

<h1>四、总结</h1>

<p>Spark 和 Flink 都是通用的开源大规模处理引擎，目标是在一个系统中支持所有的数据处理以带来效能的提升。两者都有相对比较成熟的生态系统。是下一代大数据引擎最有力的竞争者。Spark 的生态总体更完善一些，在机器学习的集成和易用性上暂时领先。Flink 在流计算上有明显优势，核心架构和模型也更透彻和灵活一些。在易用性方面两者也都还有一些地方有较大的改进空间。接下来谁能尽快补上短板发挥强项就有更多的机会。</p>

<p><br><br>
作者介绍: 王海涛，曾经在微软的 SQL  Server 和大数据平台组工作多年。带领团队建立了微软对内的 Spark 服务，主打 Spark Streaming。去年加入阿里实时计算部门，参与改进阿里基于 Apache Flink 的 Blink 平台。</p>

<p>原文链接：</p>

<p><a href="https://flink-learning.org.cn/article/detail/5910c07489b5411c32f22963e06ddeed">Spark VS Flink 下一代大数据计算引擎之争，谁主浮沉（上）</a></p>

<p><a href="https://flink-learning.org.cn/article/detail/4f8e1cf7a9bc160f96ff04cb91b7da49">比拼生态和未来，Spark 和 Flink 哪家强（下）</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Apache Spark的API三剑客：RDD、DataFrame和Dataset]]></title>
    <link href="https://ibillxia.github.io/blog/2019/09/17/spark-rdd-dataframe-dataset/"/>
    <updated>2019-09-17T15:15:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2019/09/17/spark-rdd-dataframe-dataset</id>
    <content type="html"><![CDATA[<h1>一、Spark 1.X</h1>

<p>spark 2.X开始，三者的关系发生了变化，可以参考《且谈Apache Spark的API三剑客：RDD、DataFrame和Dataset》 ,在2.X中DataFrame=DataSet[Row],其实是不知道类型。下面介绍是1.X，以免误导大家。</p>

<p>RDD、DataFrame和DataSet是容易产生混淆的概念，必须对其相互之间对比，才可以知道其中异同。</p>

<h2>RDD和DataFrame</h2>

<center><img src="/images/2019/IMAG2019091701.webp"></center>


<p><br></p>

<!--more-->


<h2>RDD-DataFrame</h2>

<p>上图直观地体现了DataFrame和RDD的区别。左侧的RDD[Person]虽然以Person为类型参数，但Spark框架本身不了解Person类的内部结构。而右侧的DataFrame却提供了详细的结构信息，使得Spark SQL可以清楚地知道该数据集中包含哪些列，每列的名称和类型各是什么。DataFrame多了数据的结构信息，即schema。RDD是分布式的Java对象的集合。DataFrame是分布式的Row对象的集合。DataFrame除了提供了比RDD更丰富的算子以外，更重要的特点是提升执行效率、减少数据读取以及执行计划的优化，比如filter下推、裁剪等。</p>

<h2>提升执行效率</h2>

<p>RDD API是函数式的，强调不变性，在大部分场景下倾向于创建新对象而不是修改老对象。这一特点虽然带来了干净整洁的API，却也使得Spark应用程序在运行期倾向于创建大量临时对象，对GC造成压力。在现有RDD API的基础之上，我们固然可以利用mapPartitions方法来重载RDD单个分片内的数据创建方式，用复用可变对象的方式来减小对象分配和GC的开销，但这牺牲了代码的可读性，而且要求开发者对Spark运行时机制有一定的了解，门槛较高。另一方面，Spark SQL在框架内部已经在各种可能的情况下尽量重用对象，这样做虽然在内部会打破了不变性，但在将数据返回给用户时，还会重新转为不可变数据。利用 DataFrame API进行开发，可以免费地享受到这些优化效果。</p>

<h2>减少数据读取</h2>

<p>分析大数据，最快的方法就是 ——忽略它。这里的“忽略”并不是熟视无睹，而是根据查询条件进行恰当的剪枝。</p>

<p>上文讨论分区表时提到的分区剪 枝便是其中一种——当查询的过滤条件中涉及到分区列时，我们可以根据查询条件剪掉肯定不包含目标数据的分区目录，从而减少IO。</p>

<p>对于一些“智能”数据格 式，Spark SQL还可以根据数据文件中附带的统计信息来进行剪枝。简单来说，在这类数据格式中，数据是分段保存的，每段数据都带有最大值、最小值、null值数量等 一些基本的统计信息。当统计信息表名某一数据段肯定不包括符合查询条件的目标数据时，该数据段就可以直接跳过（例如某整数列a某段的最大值为100，而查询条件要求a > 200）。</p>

<p>此外，Spark SQL也可以充分利用RCFile、ORC、Parquet等列式存储格式的优势，仅扫描查询真正涉及的列，忽略其余列的数据。</p>

<h2>执行优化</h2>

<center><img src="/images/2019/IMAG2019091702.webp"></center>


<p><br></p>

<h2>人口数据分析示例</h2>

<p>为了说明查询优化，我们来看上图展示的人口数据分析的示例。图中构造了两个DataFrame，将它们join之后又做了一次filter操作。如果原封不动地执行这个执行计划，最终的执行效率是不高的。因为join是一个代价较大的操作，也可能会产生一个较大的数据集。如果我们能将filter下推到 join下方，先对DataFrame进行过滤，再join过滤后的较小的结果集，便可以有效缩短执行时间。而Spark SQL的查询优化器正是这样做的。简而言之，逻辑查询计划优化就是一个利用基于关系代数的等价变换，将高成本的操作替换为低成本操作的过程。</p>

<p>得到的优化执行计划在转换成物 理执行计划的过程中，还可以根据具体的数据源的特性将过滤条件下推至数据源内。最右侧的物理执行计划中Filter之所以消失不见，就是因为溶入了用于执行最终的读取操作的表扫描节点内。</p>

<p>对于普通开发者而言，查询优化 器的意义在于，即便是经验并不丰富的程序员写出的次优的查询，也可以被尽量转换为高效的形式予以执行。</p>

<h1>RDD和DataSet</h1>

<ul>
<li><p>DataSet以Catalyst逻辑执行计划表示，并且数据以编码的二进制形式被存储，不需要反序列化就可以执行sorting、shuffle等操作。</p></li>
<li><p>DataSet创立需要一个显式的Encoder，把对象序列化为二进制，可以把对象的scheme映射为Spark</p></li>
</ul>


<p>SQl类型，然而RDD依赖于运行时反射机制。</p>

<p>通过上面两点，DataSet的性能比RDD的要好很多，可以参见[3]</p>

<h1>DataFrame和DataSet</h1>

<p>Dataset可以认为是DataFrame的一个特例，主要区别是Dataset每一个record存储的是一个强类型值而不是一个Row。因此具有如下三个特点：</p>

<ul>
<li><p>DataSet可以在编译时检查类型</p></li>
<li><p>并且是面向对象的编程接口。用wordcount举例：</p></li>
</ul>


<pre><code>//DataFrame// Load a text file and interpret each line as a java.lang.String
val ds = sqlContext.read.text("/home/spark/1.6/lines").as[String]
val result = ds  
    .flatMap(_.split(" "))// Split on whitespace
    .filter(_ !="")// Filter empty words
    .toDF()// Convert to DataFrame to perform aggregation / sorting
    .groupBy($"value")// Count number of occurences of each word
    .agg(count("*") as "numOccurances")
    .orderBy($"numOccurances" desc)// Show most common words first

//DataSet,完全使用scala编程，不要切换到DataFrame
val wordCount =   ds
.flatMap(_.split(" "))
.filter(_ !="")
.groupBy(_.toLowerCase())// Instead of grouping on a column expression (i.e. $"value") we pass a lambda function
.count()
</code></pre>

<ul>
<li>后面版本DataFrame会继承DataSet，DataFrame是面向Spark SQL的接口。</li>
</ul>


<p>DataFrame和DataSet可以相互转化，df.as[ElementType]这样可以把DataFrame转化为DataSet，ds.toDF()这样可以把DataSet转化为DataFrame。</p>

<hr />

<h1>二、Spark 2.X</h1>

<p>本文翻译自 A Tale of Three Apache Spark APIs: RDDs, DataFrames, and Datasets ，翻译已获得原作者 Jules S. Damji 的授权。</p>

<p>最令开发者们高兴的事莫过于有一组 API，可以大大提高开发者们的工作效率，容易使用、非常直观并且富有表现力。Apache Spark 广受开发者们欢迎的一个重要原因也在于它那些非常容易使用的 API，可以方便地通过多种语言，如 Scala、Java、Python 和 R 等来操作大数据集。</p>

<p>在本文中，我将深入讲讲 Apache Spark 2.2 以及以上版本提供的三种 API——RDD、DataFrame 和 Dataset，在什么情况下你该选用哪一种以及为什么，并概述它们的性能和优化点，列举那些应该使用 DataFrame 和 Dataset 而不是 RDD 的场景。我会更多地关注 DataFrame 和 Dataset，因为在 Apache Spark 2.0 中这两种 API 被整合起来了。</p>

<p>这次整合背后的动机在于我们希望可以让使用 Spark 变得更简单，方法就是减少你需要掌握的概念的数量，以及提供处理结构化数据的办法。在处理结构化数据时，Spark 可以像针对特定领域的语言所提供的能力一样，提供高级抽象和 API。</p>

<h2>弹性分布式数据集（Resilient Distributed Dataset，RDD）</h2>

<p>从一开始 RDD 就是 Spark 提供的面向用户的主要 API。从根本上来说，一个 RDD 就是你的数据的一个不可变的分布式元素集合，在集群中跨节点分布，可以通过若干提供了转换和处理的底层 API 进行并行处理。</p>

<h3>在什么情况下使用 RDD？</h3>

<p>下面是使用 RDD 的场景和常见案例：</p>

<ul>
<li><p>你希望可以对你的数据集进行最基本的转换、处理和控制；</p></li>
<li><p>你的数据是非结构化的，比如流媒体或者字符流；</p></li>
<li><p>你想通过函数式编程而不是特定领域内的表达来处理你的数据；</p></li>
<li><p>你不希望像进行列式处理一样定义一个模式，通过名字或字段来处理或访问数据属性；</p></li>
<li><p>你并不在意通过 DataFrame 和 Dataset 进行结构化和半结构化数据处理所能获得的一些优化和性能上的好处；</p></li>
</ul>


<h3>Apache Spark 2.0 中的 RDD 有哪些改变？</h3>

<p>可能你会问：RDD 是不是快要降级成二等公民了？是不是快要退出历史舞台了？</p>

<p>答案是非常坚决的：不！</p>

<p>而且，接下来你还将了解到，你可以通过简单的 API 方法调用在 DataFrame 或 Dataset 与 RDD 之间进行无缝切换，事实上 DataFrame 和 Dataset 也正是基于 RDD 提供的。</p>

<h2>DataFrame</h2>

<p>与 RDD 相似， DataFrame 也是数据的一个不可变分布式集合。但与 RDD 不同的是，数据都被组织到有名字的列中，就像关系型数据库中的表一样。设计 DataFrame 的目的就是要让对大型数据集的处理变得更简单，它让开发者可以为分布式的数据集指定一个模式，进行更高层次的抽象。它提供了特定领域内专用的 API 来处理你的分布式数据，并让更多的人可以更方便地使用 Spark，而不仅限于专业的数据工程师。</p>

<p>在我们的 Apache Spark 2.0 网络研讨会以及后续的博客中，我们提到在Spark 2.0 中，DataFrame 和 Dataset 的 API 将融合到一起，完成跨函数库的数据处理能力的整合。在整合完成之后，开发者们就不必再去学习或者记忆那么多的概念了，可以通过一套名为 Dataset 的高级并且类型安全的 API 完成工作。</p>

<center><img src="/images/2019/IMAG2019091703.png"></center>


<p><br></p>

<h2>Dataset</h2>

<p>如下面的表格所示，从 Spark 2.0 开始，Dataset 开始具有两种不同类型的 API 特征：有明确类型的 API 和无类型的 API。从概念上来说，你可以把 DataFrame 当作一些通用对象 Dataset[Row] 的集合的一个别名，而一行就是一个通用的无类型的 JVM 对象。与之形成对比，Dataset 就是一些有明确类型定义的 JVM 对象的集合，通过你在 Scala 中定义的 Case Class 或者 Java 中的 Class 来指定。</p>

<p>有类型和无类型的 API</p>

<table>
<thead>
<tr>
<th> 语言 </th>
<th> 主要抽象 </th>
</tr>
</thead>
<tbody>
<tr>
<td> Scala </td>
<td> Dataset[T] &amp; DataFrame (Dataset[Row] 的别名) </td>
</tr>
<tr>
<td> Java </td>
<td> Dataset[T] </td>
</tr>
<tr>
<td> Python </td>
<td> DataFrame </td>
</tr>
<tr>
<td> R </td>
<td> DataFrame </td>
</tr>
</tbody>
</table>


<p>注意：因为 Python 和 R 没有编译时类型安全，所以我们只有称之为 DataFrame 的无类型 API。</p>

<h3>Dataset API 的优点</h3>

<p>在 Spark 2.0 里，DataFrame 和 Dataset 的统一 API 会为 Spark 开发者们带来许多方面的好处。</p>

<h4>1、静态类型与运行时类型安全</h4>

<p>从 SQL 的最小约束到 Dataset 的最严格约束，把静态类型和运行时安全想像成一个图谱。比如，如果你用的是 Spark SQL 的查询语句，要直到运行时你才会发现有语法错误（这样做代价很大），而如果你用的是 DataFrame 和 Dataset，你在编译时就可以捕获错误（这样就节省了开发者的时间和整体代价）。也就是说，当你在 DataFrame 中调用了 API 之外的函数时，编译器就可以发现这个错。不过，如果你使用了一个不存在的字段名字，那就要到运行时才能发现错误了。</p>

<p>图谱的另一端是最严格的 Dataset。因为 Dataset API 都是用 lambda 函数和 JVM 类型对象表示的，所有不匹配的类型参数都可以在编译时发现。而且在使用 Dataset 时，你的分析错误也会在编译时被发现，这样就节省了开发者的时间和代价。</p>

<p>所有这些最终都被解释成关于类型安全的图谱，内容就是你的 Spark 代码里的语法和分析错误。在图谱中，Dataset 是最严格的一端，但对于开发者来说也是效率最高的。</p>

<center><img src="/images/2019/IMAG2019091704.png"></center>


<p><br></p>

<h4>2、关于结构化和半结构化数据的高级抽象和定制视图</h4>

<p>把 DataFrame 当成 Dataset[Row] 的集合，就可以对你的半结构化数据有了一个结构化的定制视图。比如，假如你有个非常大量的用 JSON 格式表示的物联网设备事件数据集。因为 JSON 是半结构化的格式，那它就非常适合采用 Dataset 来作为强类型化的 Dataset[DeviceIoTData] 的集合。</p>

<pre><code>{"device_id": 198164, "device_name": "sensor-pad-198164owomcJZ", 
"ip": "80.55.20.25", "cca2": "PL", "cca3": "POL", "cn": "Poland", 
"latitude": 53.080000, "longitude": 18.620000, "scale": "Celsius", 
"temp": 21, "humidity": 65, "battery_level": 8, "c02_level": 1408, 
"lcd": "red", "timestamp" :1458081226051}
</code></pre>

<p>你可以用一个 Scala Case Class 来把每条 JSON 记录都表示为一条 DeviceIoTData，一个定制化的对象。</p>

<pre><code>case class DeviceIoTData (battery_level: Long, c02_level: Long, cca2: String, cca3: String, cn: String, 
device_id: Long, device_name: String, humidity: Long, ip: String, latitude: Double, lcd: String, 
longitude: Double, scale:String, temp: Long, timestamp: Long)
</code></pre>

<p>接下来，我们就可以从一个 JSON 文件中读入数据。</p>

<pre><code>// read the json file and create the dataset from the 
// case class DeviceIoTData
// ds is now a collection of JVM Scala objects DeviceIoTData
val ds = spark.read.json(“/databricks-public-datasets/data/iot/iot_devices.json”).as[DeviceIoTData]
</code></pre>

<p>上面的代码其实可以细分为三步：</p>

<ol>
<li><p>Spark 读入 JSON，根据模式创建出一个 DataFrame 的集合；</p></li>
<li><p>在这时候，Spark 把你的数据用“DataFrame = Dataset[Row]”进行转换，变成一种通用行对象的集合，因为这时候它还不知道具体的类型；</p></li>
<li><p>然后，Spark 就可以按照类 DeviceIoTData 的定义，转换出“Dataset[Row] -> Dataset[DeviceIoTData]”这样特定类型的 Scala JVM 对象了。</p></li>
</ol>


<p>许多和结构化数据打过交道的人都习惯于用列的模式查看和处理数据，或者访问对象中的某个特定属性。将 Dataset 作为一个有类型的 Dataset[ElementType] 对象的集合，你就可以非常自然地又得到编译时安全的特性，又为强类型的 JVM 对象获得定制的视图。而且你用上面的代码获得的强类型的 Dataset[T] 也可以非常容易地用高级方法展示或处理。</p>

<center><img src="/images/2019/IMAG2019091705.png"></center>


<p><br></p>

<h4>3、方便易用的结构化 API</h4>

<p>虽然结构化可能会限制你的 Spark 程序对数据的控制，但它却提供了丰富的语义，和方便易用的特定领域内的操作，后者可以被表示为高级结构。事实上，用 Dataset 的高级 API 可以完成大多数的计算。比如，它比用 RDD 数据行的数据字段进行 agg、select、sum、avg、map、filter 或 groupBy 等操作简单得多，只需要处理 Dataset 类型的 DeviceIoTData 对象即可。</p>

<p>用一套特定领域内的 API 来表达你的算法，比用 RDD 来进行关系代数运算简单得多。比如，下面的代码将用 filter() 和 map() 来创建另一个不可变 Dataset。</p>

<pre><code>// Use filter(), map(), groupBy() country, and compute avg() 
// for temperatures and humidity. This operation results in 
// another immutable Dataset. The query is simpler to read, 
// and expressive
val dsAvgTmp = ds.filter(d =&gt; {d.temp &gt; 25}).map(d =&gt; (d.temp, d.humidity, d.cca3))
    .groupBy($"_3").avg()//display the resulting datasetdisplay(dsAvgTmp)
</code></pre>



<center><img src="/images/2019/IMAG2019091706.gif"></center>


<p><br></p>

<h4>4、性能与优化</h4>

<p>除了上述优点之外，你还要看到使用 DataFrame 和 Dataset API 带来的空间效率和性能提升。原因有如下两点：</p>

<p>首先，因为 DataFrame 和 Dataset API 都是基于 Spark SQL 引擎构建的，它使用 Catalyst 来生成优化后的逻辑和物理查询计划。所有 R、Java、Scala 或 Python 的 DataFrame/Dataset API，所有的关系型查询的底层使用的都是相同的代码优化器，因而会获得空间和速度上的效率。尽管有类型的 Dataset[T] API 是对数据处理任务优化过的，无类型的 Dataset[Row]（别名 DataFrame）却运行得更快，适合交互式分析。</p>

<center><img src="/images/2019/IMAG2019091707.png"></center>


<p><br></p>

<p>其次， Spark 作为一个编译器，它可以理解 Dataset 类型的 JVM 对象，它会使用编码器来把特定类型的JVM 对象映射成Tungsten 的内部内存表示。结果，Tungsten 的编码器就可以非常高效地将JVM 对象序列化或反序列化，同时生成压缩字节码，这样执行效率就非常高了。</p>

<p>该什么时候使用 DataFrame 或 Dataset 呢？</p>

<ul>
<li><p>如果你需要丰富的语义、高级抽象和特定领域专用的 API，那就使用 DataFrame 或 Dataset；</p></li>
<li><p>如果你的处理需要对半结构化数据进行高级处理，如 filter、map、aggregation、average、sum、SQL 查询、列式访问或使用 lambda 函数，那就使用 DataFrame 或 Dataset；</p></li>
<li><p>如果你想在编译时就有高度的类型安全，想要有类型的 JVM 对象，用上 Catalyst 优化，并得益于 Tungsten 生成的高效代码，那就使用 Dataset；</p></li>
<li><p>如果你想在不同的 Spark 库之间使用一致和简化的 API，那就使用 DataFrame 或 Dataset；</p></li>
<li><p>如果你是 R 语言使用者，就用 DataFrame；</p></li>
<li><p>如果你是 Python 语言使用者，就用 DataFrame，在需要更细致的控制时就退回去使用 RDD；</p></li>
</ul>


<p>注意只需要简单地调用一下.rdd，就可以无缝地将 DataFrame 或 Dataset 转换成 RDD。例子如下：</p>

<pre><code>// select specific fields from the Dataset, apply a predicate
// using the where() method, convert to an RDD, and show first 10
// RDD rows
val deviceEventsDS = ds.select($"device_name", $"cca3", $"c02_level")
    .where($"c02_level" &gt; 1300)
// convert to RDDs and take the first 10 rows
val eventsRDD = deviceEventsDS.rdd.take(10)
</code></pre>



<center><img src="/images/2019/IMAG2019091708.png"></center>


<p><br></p>

<h2>总结</h2>

<p>总之，在什么时候该选用 RDD、DataFrame 或 Dataset 看起来好像挺明显。前者可以提供底层的功能和控制，后者支持定制的视图和结构，可以提供高级和特定领域的操作，节约空间并快速运行。</p>

<p>当我们回顾从早期版本的 Spark 中获得的经验教训时，我们问自己该如何为开发者简化 Spark 呢？该如何优化它，让它性能更高呢？我们决定把底层的 RDD API 进行高级抽象，成为 DataFrame 和 Dataset，用它们在 Catalyst 优化器和 Tungsten 之上构建跨库的一致数据抽象。</p>

<p>DataFrame 和 Dataset，或 RDD API，按你的实际需要和场景选一个来用吧，当你像大多数开发者一样对数据进行结构化或半结构化的处理时，我不会有丝毫惊讶。</p>

<h1>三、参考</h1>

<p><a href="https://www.infoq.cn/news/three-apache-spark-apis-rdds-dataframes-and-datasets">且谈 Apache Spark 的 API 三剑客：RDD、DataFrame 和 Dataset</a></p>

<p><a href="http://www.iteye.com/news/30658">Spark SQL结构化分析</a></p>

<p><a href="http://www.infoq.com/cn/articles/2015-Review-Spark">解读2015之Spark篇：新生态系统的形成</a></p>

<p><a href="https://databricks.com/blog/2016/01/04/introducing-spark-datasets.html">Introducing Spark Datasets</a></p>

<p><a href="https://docs.cloud.databricks.com/docs/spark/1.6/index.html#examples/Dataset%20Wordcount.html">databricks example</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Spark在Windows下的环境搭建]]></title>
    <link href="https://ibillxia.github.io/blog/2018/07/22/spark-windows-setup/"/>
    <updated>2018-07-22T15:34:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2018/07/22/spark-windows-setup</id>
    <content type="html"><![CDATA[<p>由于Spark是用Scala来写的，所以Spark对Scala肯定是原生态支持的，因此这里以Scala为主来介绍Spark环境的搭建，主要包括四个步骤，分别是：JDK的安装，Scala的安装，Spark的安装，Hadoop的下载和配置。为了突出”From Scratch”的特点（都是标题没选好的缘故），所以下面的步骤稍显有些啰嗦，老司机大可不必阅读，直接跳过就好。</p>

<!--more-->


<h1>一．JDK的安装与环境变量的设置</h1>

<h2>1.1 JDK的安装</h2>

<p>JDK（全称是JavaTM Platform Standard Edition Development Kit）的安装，下载地址是Java SE Downloads，一般进入页面后，会默认显示一个最新版的JDK，如下图所示，当前最新版本是JDK 8，更为详细具体的地址是Java SE Development Kit 8 Downloads：</p>

<center><img src="/images/2018/IMAG2018072201.png"></center>


<p><br></p>

<p>　　上图中两个用红色标记的地方都是可以点击的，点击进去之后可以看到这个最新版本的一些更为详细的信息，如下图所示：</p>

<center><img src="/images/2018/IMAG2018072202.png"></center>


<p><br></p>

<p>　　首先，这里主要包含有8u101和8u102这两个版本，Java给出的官方说明是：</p>

<blockquote><p>“Java SE 8u101 includes important security fixes. Oracle strongly recommends that all Java SE 8 users upgrade to this release. Java SE 8u102 is a patch-set update, including all of 8u101 plus additional features (described in the release notes). ”</p></blockquote>

<p>　　也就是说Java推荐所有开发人员从以前的版本升级到JDK 8u101，而JDK 8u102则除了包括101的所有特性之外，还有一些其他的特性。对于版本的选择，自行选择就好了，其实对于普通开发人员来说，体现不了太大的区别，我这里就是使用的JDK 8u101版本。</p>

<p>　　选好8u101版本后，再选择你的对应开发平台，由于我的机器是64位的，所以我这里选择Windows64位的版本。记得在下载之前，必须要接受上方的许可协议，在上图中用红色圈出。</p>

<p>　　除了下载最新版本的JDK，也可以在Oracle Java Archive下载到历史版本的JDK，但官方建议只做测试用。</p>

<p>　　JDK在windows下的安装非常简单，按照正常的软件安装思路去双击下载得到的exe文件，然后设定你自己的安装目录（安装目录在设置环境变量的时候需要用到）即可。</p>

<h2>1.2 环境变量的设置</h2>

<p>　　接下来设置相应的环境变量，设置方法为：在桌面右击【计算机】－－【属性】－－【高级系统设置】，然后在系统属性里选择【高级】－－【环境变量】，然后在系统变量中找到“Path”变量，并选择“编辑”按钮后出来一个对话框，可以在里面添加上一步中所安装的JDK目录下的bin文件夹路径名，我这里的bin文件夹路径名是：F:\Program Files\Java\jdk1.8.0_101\bin，所以将这个添加到path路径名下，注意用英文的分号“;”进行分割。这样设置好后，便可以在任意目录下打开的cmd命令行窗口下运行</p>

<pre><code class="bash">java -version
</code></pre>

<p>观察是否能够输出相关java的版本信息，如果能够输出，说明JDK安装这一步便全部结束了。</p>

<p>　　全部流程如下图所示（后续软件安装的系统变量设置都是这套流程）：</p>

<center><img src="/images/2018/IMAG2018072203.png"></center>


<p><br></p>

<h2>1.3 一些题外话</h2>

<p>　　这里讲两句题外话，各位看官不关心的话可以跳过这里，不影响后续的安装步骤。</p>

<p>　　在软件安装的时候，相信各位没少遇到过环境变量和系统变量，所以这里就来扒一扒令人头疼的PATH, CLASSPATH和JAVA_HOME等参数的具体含义。</p>

<h3>1.3.1 环境变量、系统变量和用户变量</h3>

<ul>
<li><p>环境变量包括系统变量和用户变量</p></li>
<li><p>系统变量的设置针对该操作系统下的所有用户起作用；</p></li>
<li><p>用户变量的设置只针对当前用户起作用</p></li>
</ul>


<p>如果对这些概念还不是特别熟悉的，建议先看完下面几个点之后，再回过头来看这三句话。</p>

<h3>1.3.2 PATH</h3>

<p>　　也就是上一步设置的系统变量，告诉操作系统去哪里找到Java.exe的执行路径，当你在命令行窗口冷不丁的敲上如下命令的时候，</p>

<pre><code class="bash">java -version
</code></pre>

<p>操作系统首先会一惊，What the hell does “java” mean? 不过吐槽归吐槽，活还是得干，于是悠悠的记起来了盖茨爸爸说过的三句话：</p>

<ol>
<li><p>当你看不懂命令行窗口中的一个命令的时候，你首先去你所在的当前目录下找找，是否有这个命令的.exe程序？如果有，那就用它来启动执行；</p></li>
<li><p>如果没有，千万别放弃，记得要去Path系统变量下的那些目录下去找一找，如果找到了，启动并执行命令；</p></li>
<li><p>如果上面两个地方依然还没找到，那你就撒个娇，报个错好了。</p></li>
</ol>


<p>所以我们将JDK安装目录下的bin文件夹添加到Path系统变量的目的也就在这里，告诉操作系统：如果在当前目录下找不到java.exe，就去Path系统变量里的那些路径下挨个找一找，直到找到java.exe为止。那为什么要设置bin文件夹，而不是JDK安装的根目录呢？原因就在于根目录下没有java.exe啊，只有bin文件夹下才有啊喂……</p>

<p>如果只是在命令行窗口下运行一下java的命令，那其实也可以不设置系统变量，只是每次在命令行窗口运行java的命令时，都必须带上一长串路径名，来直接指定java.exe的位置，如下所示。</p>

<pre><code>C:\Users\weizierxu&gt;F:\Program Files\Java\jdk1.8.0_101\bin\java.exe -version
'F:\Program' 不是内部或外部命令，也不是可运行的程序或批处理文件。
</code></pre>

<p>注意：这里报错的原因并不是说直接指定java.exe的路径名这种方式有问题，而是命令行下无法解析带有空格的路径名，所以需要用到双引号，如下：</p>

<pre><code class="bash">C:\Users\weizierxu&gt;"F:\Program Files"\Java\jdk1.8.0_101\bin\java.exe -version
java version "1.8.0_101"
Java(TM) SE Runtime Environment (build 1.8.0_101-b13)
Java HotSpot(TM) 64-Bit Server VM (build 25.101-b13, mixed mode)
</code></pre>

<h3>1.3.3 CLASSPATH</h3>

<p>　　CLASSPATH是在Java执行一个已经编译好的class文件时，告诉Java去哪些目录下找到这个class文件，比如你的程序里用到某个Jar包（Jar包里的都是已经编译好的class文件），那么在执行的时候，Java需要找到这个Jar包才行，去哪找呢？从CLASSPATH指定的目录下，从左至右开始寻找（用分号区分开的那些路径名），直到找到你指定名字的class文件，如果找不到就会报错。这里做一个实验，就能明白具体是什么意思了。</p>

<p>　　首先，我在F:\Program Files\Java目录下，利用Windows自带的记事本写了一个类似于Hello World的程序，保存为testClassPath.java文件（注意后缀名得改成java），内容如下：</p>

<pre><code class="java">public class testClassPath{
    public static void main(String[] args){
        System.out.println("Hello, this is a test on CLASSPATH!");
    }
}
</code></pre>

<p>然后，我将cmd的当前目录切换到（通过cd命令）F:\Program Files\Java目录下，然后用javac命令来对这个.java文件进行编译，如下图所示：</p>

<center><img src="/images/2018/IMAG2018072204.png"></center>


<p><br></p>

<p>从上图中可以看到，javac命令可以正常使用（没有任何输出的就表明正确编译了），这是因为执行该命令的javac.exe同样存在于JDK安装路径下的bin目录中，而这个目录我们已经添加到Path系统变量中去了，所以cmd能够认识这个命令。这个时候可以看到F:\Program Files\Java目录下多了一个testClassPath.class文件。不过运行这个class文件的时候，报错了。这个时候，CLASSPATH就派上用场了，和1.2节中对Path系统变量设置的方法一样，这里在CLASSPATH（如果系统变量的列表中没有CLASSPATH这个选项，那么点击新建，然后添加路径即可）中最后面添加上;.，英文的分号表示和前面已有的路径分割开，后面的小点.表示当前目录的意思。</p>

<p>这个时候记得要另起一个新的cmd窗口，然后利用cd命令切换到testClassPath.class所在目录，然后再去执行，便可以成功得到结果了。</p>

<pre><code class="bash">F:\Program Files\Java&gt;java test
ClassPathHello, this is a test on CLASSPATH!
</code></pre>

<p>　　因此，和Path变量不同的是，Java在执行某个class文件的时候，并不会有默认的先从当前目录找这个文件，而是只去CLASSPATH指定的目录下找这个class文件，如果CLASSPATH指定的目录下有这个class文件，则开始执行，如果没有则报错（这里有去当前目录下找这个class文件，是因为当前路径通过.的方式，已经添加到了CLASSPATH系统变量中）。</p>

<p>　　上面讲的指定CLASSPATH系统变量的方法，都是直接写死在系统变量中的，为了避免造成干扰（比如多个同名class文件存在于多个路径中，这些路径都有添加到CLASSPATH系统变量中，由于在找class文件的时候，是从左往右扫描CLASSPATH系统变量中的路径的，所以在利用java testClassPath方法执行的时候，运行的便是位置在CLASSPATH系统变量中最左边的那个路径中，对应的class文件，而这显然不是我们想要的结果），因此在诸如Eclipse等等这些IDE中，并不需要人为手动设定CLASSPATH系统变量，而是只设定当前程序的特定的CLASSPATH系统变量，这样便不会影响到其他程序的运行了。</p>

<h3>1.3.4 JAVA_HOME</h3>

<p>JAVA_HOME并不是Java本身所需要的参数，而是其他的一些第三方工具需要这个参数来配置它们自己的参数，它存在的意义无非是告诉那些软件，我的JDK安装在这个目录下，你如果要用到我的Java程序的话，直接来我这个目录下找就好了，而JAVA_HOME就是JDK的安装路径名。比如我的JDK安装在F:\Program Files\Java\jdk1.8.0_101目录下（注意该目录下的bin目录，就是在1.3.2节里Path系统变量中要添加的值），那么JAVA_HOME里要添加的值便是F:\Program Files\Java\jdk1.8.0_101，以后碰到类似HOME的系统变量，都是软件的安装目录。</p>

<h1>二. Scala的安装</h1>

<p>　　首先从DOWNLOAD PREVIOUS VERSIONS下载到对应的版本，在这里需要注意的是，Spark的各个版本需要跟相应的Scala版本对应，比如我这里使用的Spark 1.6.2就只能使用Scala 2.10的各个版本，目前最新的Spark 2.0就只能使用Scala 2.11的各个版本，所以下载的时候，需要注意到这种Scala版本与Spark版本相互对应的关系。我这里现在用的是Scala 2.10.6，适配Spark从1.3.0到Spark 1.6.2之间的各个版本。在版本页面DOWNLOAD PREVIOUS VERSIONS选择一个适合自己需要的版本后，会进入到该版本的具体下载页面，如下图所示，记得下载二进制版本的Scala，点击图中箭头所指，下载即可：</p>

<center><img src="/images/2018/IMAG2018072205.png"></center>


<p><br></p>

<p>　　下载得到Scala的msi文件后，可以双击执行安装。安装成功后，默认会将Scala的bin目录添加到PATH系统变量中去（如果没有，和JDK安装步骤中类似，将Scala安装目录下的bin目录路径，添加到系统变量PATH中），为了验证是否安装成功，开启一个新的cmd窗口，输入scala然后回车，如果能够正常进入到Scala的交互命令环境则表明安装成功。如下图所示：</p>

<center><img src="/images/2018/IMAG2018072206.png"></center>


<p><br></p>

<p>如果不能显示版本信息，并且未能进入Scala的交互命令行，通常有两种可能性：</p>

<ul>
<li><p>Path系统变量中未能正确添加Scala安装目录下的bin文件夹路径名，按照JDK安装中介绍的方法添加即可。</p></li>
<li><p>Scala未能够正确安装，重复上面的步骤即可。</p></li>
</ul>


<h1>三. Spark的安装</h1>

<p>　　Spark的安装非常简单，直接去Download Apache Spark。有两个步骤：</p>

<ul>
<li><p>选择好对应Hadoop版本的Spark版本，如下图中所示；</p></li>
<li><p>然后点击下图中箭头所指的spark-1.6.2-bin-hadoop2.6.tgz，等待下载结束即可。</p></li>
</ul>


<center><img src="/images/2018/IMAG2018072207.png"></center>


<p><br></p>

<p>　　这里使用的是Pre-built的版本，意思就是已经编译了好了，下载来直接用就好，Spark也有源码可以下载，但是得自己去手动编译之后才能使用。下载完成后将文件进行解压（可能需要解压两次），最好解压到一个盘的根目录下，并重命名为Spark，简单不易出错。并且需要注意的是，在Spark的文件目录路径名中，不要出现空格，类似于“Program Files”这样的文件夹名是不被允许的。</p>

<p>　　解压后基本上就差不多可以到cmd命令行下运行了。但这个时候每次运行spark-shell（spark的命令行交互窗口）的时候，都需要先cd到Spark的安装目录下，比较麻烦，因此可以将Spark的bin目录添加到系统变量PATH中。例如我这里的Spark的bin目录路径为D:\Spark\bin，那么就把这个路径名添加到系统变量的PATH中即可，方法和JDK安装过程中的环境变量设置一致，设置完系统变量后，在任意目录下的cmd命令行中，直接执行spark-shell命令，即可开启Spark的交互式命令行模式。</p>

<h1>四．HADOOP下载</h1>

<p>　　系统变量设置后，就可以在任意当前目录下的cmd中运行spark-shell，但这个时候很有可能会碰到各种错误，这里主要是因为Spark是基于Hadoop的，所以这里也有必要配置一个Hadoop的运行环境。在Hadoop Releases里可以看到Hadoop的各个历史版本，这里由于下载的Spark是基于Hadoop 2.6的（在Spark安装的第一个步骤中，我们选择的是Pre-built for Hadoop 2.6），我这里选择2.6.4版本，选择好相应版本并点击后，进入详细的下载页面，如下图所示，选择图中红色标记进行下载，这里上面的src版本就是源码，需要对Hadoop进行更改或者想自己进行编译的可以下载对应src文件，我这里下载的就是已经编译好的版本，即图中的’hadoop-2.6.4.tar.gz’文件。</p>

<center><img src="/images/2018/IMAG2018072208.png"></center>


<p><br></p>

<p>　　下载并解压到指定目录，然后到环境变量部分设置HADOOP_HOME为Hadoop的解压目录，我这里是F:\Program Files\hadoop，然后再设置该目录下的bin目录到系统变量的PATH下，我这里也就是F:\Program Files\hadoop\bin，如果已经添加了HADOOP_HOME系统变量，也可以用%HADOOP_HOME%\bin来指定bin文件夹路径名。这两个系统变量设置好后，开启一个新的cmd，然后直接输入spark-shell命令。</p>

<p>　　正常情况下是可以运行成功并进入到Spark的命令行环境下的，但是对于有些用户可能会遇到空指针的错误。这个时候，主要是因为Hadoop的bin目录下没有winutils.exe文件的原因造成的。这里的解决办法是：</p>

<ul>
<li><p>去 https://github.com/steveloughran/winutils 选择你安装的Hadoop版本号，然后进入到bin目录下，找到winutils.exe文件，下载方法是点击winutils.exe文件，进入之后在页面的右上方部分有一个Download按钮，点击下载即可。</p></li>
<li><p>下载好winutils.exe后，将这个文件放入到Hadoop的bin目录下，我这里是F:\Program Files\hadoop\bin。</p></li>
<li><p>在打开的cmd中输入</p></li>
</ul>


<pre><code>F:\Program Files\hadoop\bin\winutils.exe chmod 777 /tmp/hive
</code></pre>

<p>这个操作是用来修改权限的。注意前面的F:\Program Files\hadoop\bin部分要对应的替换成实际你所安装的bin目录所在位置。</p>

<p>　　经过这几个步骤之后，然后再次开启一个新的cmd窗口，如果正常的话，应该就可以通过直接输入spark-shell来运行Spark了。</p>

<p>正常的运行界面应该如下图所示：</p>

<center><img src="/images/2018/IMAG2018072209.png"></center>


<p><br></p>

<p>从图中可以看到，在直接输入spark-shell命令后，Spark开始启动，并且输出了一些日志信息，大多数都可以忽略，需要注意的是两句话：</p>

<blockquote><p>Spark context available as sc.
SQL context available as sqlContext.</p></blockquote>

<p>Spark context和SQL context分别是什么，后续再讲，现在只需要记住，只有看到这两个语句了，才说明Spark真正的成功启动了。</p>

<h1>五. Python下的PySpark</h1>

<p>　　针对Python下的Spark，和Scala下的spark-shell类似，也有一个PySpark，它同样也是一个交互式的命令行工具，可以对Spark进行一些简单的调试和测试，和spark-shell的作用类似。对于需要安装Python的来说，这里建议使用Python(x,y)，它的优点就是集合了大多数的工具包，不需要自己再单独去下载而可以直接import来使用，并且还省去了繁琐的环境变量配置，下载地址是Python(x,y) - Downloads，下载完成后，双击运行安装即可。因为本教程主要以Scala为主，关于Python的不做过多讲解。</p>

<p>　　并且，pyspark的执行文件和spark-shell所在路径一致，按照上述方式解压好spark后，可以直接在cmd的命令行窗口下执行pyspark的命令，启动python的调试环境。</p>

<p>　　但是如果需要在python中或者在类似于IDEA IntelliJ或者PyCharm等IDE中使用PySpark的话，需要在系统变量中新建一个PYTHONPATH的系统变量，然后</p>

<pre><code>PATHONPATH=%SPARK_HOME%\python;%SPARK_HOME%\python\lib\py4j-0.10.4-src.zip
</code></pre>

<p>设置好后，建议使用PyCharm作为IDE（因为IDEA IntelliJ的设置繁琐很多，没耐心了设置一堆参数啦，哈哈哈）</p>

<h1>六. 小结</h1>

<p>　　至此，基本的Spark本地调试环境便拥有了，对于初步的Spark学习也是足够的。但是这种模式在实际的Spark开发的时候，依然是不够用的，需要借助于一个比较好用的IDE来辅助开发过程。下一讲就主要讲解ItelliJ IDEA以及Maven的配置过程。</p>

<h1>七. Tips</h1>

<ul>
<li><p>血的教训：永远不要在软件的安装路径中留有任何的空格</p></li>
<li><p>网上找Hadoop 2.7.2的winutils.exe找不到的时候，直接用2.7.1的winutils.exe，照样能用</p></li>
</ul>


<h1>参考</h1>

<ul>
<li><p>PATH and CLASSPATH（Oracle官方给出的一些关于Path和CLASSPATH的解释，推荐）</p></li>
<li><p>Difference among JAVA_HOME,JRE_HOME,CLASSPATH and PATH</p></li>
<li><p>Java中设置classpath、path、JAVA_HOME的作用</p></li>
<li><p>Why does starting spark-shell fail with NullPointerException on Windows?(关于如何解决启动spark-shell时遇到的NullPointerException问题)</p></li>
</ul>

]]></content>
  </entry>
  
</feed>