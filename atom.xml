<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Bill's Blog]]></title>
  <link href="https://ibillxia.github.io/atom.xml" rel="self"/>
  <link href="https://ibillxia.github.io/"/>
  <updated>2026-07-12T20:22:44+08:00</updated>
  <id>https://ibillxia.github.io/</id>
  <author>
    <name><![CDATA[Bill Xia]]></name>
    <email><![CDATA[ibillxia@gmail.com]]></email>
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[什么是 FDE？从它的爆火看软件研发的职能大洗牌]]></title>
    <link href="https://ibillxia.github.io/blog/2026/07/03/FDE-what-why-and-future/"/>
    <updated>2026-07-03T22:02:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2026/07/03/FDE-what-why-and-future</id>
    <content type="html"><![CDATA[<center><img src="https://ibillxia.github.io/images/2026/IMAG2026070301.png"></center>


<p></p>

<p>如果你最近关注 IT、独立开发者或者 AI 创业圈子，一定频繁被一个新词刷屏——FDE（Forward Deployed Engineer，前沿部署工程师 / 前沿开发工程师）。  <br/>
这个在海外被 OpenAI、Anthropic 疯狂抢夺，国内被字节、阿里、腾讯真金白银用百万年薪高薪诚聘的岗位，正在成为 AI 时代最炙手可热的“黄金赛道”。  <br/>
很多人会好奇：FDE 到底是个什么角色？它和我们熟知的程序员、售前、实施顾问有什么区别？它是真的行业趋势，还是又一个包装出来的新名词？今天，我们就用一张“硬核对比表”，带大家看懂 FDE 的本质。</p>

<!--more-->   


<h2>一图看懂：FDE 与传统岗位的全方位对比</h2>

<p>为了让你直观感受到差异，我们从工作起点、核心能力、交付物等多个维度，将 FDE（分为硅谷/大厂流派和本土“土”流派）与售前、实施、外包、传统工程师进行了深度拆解：</p>

<table>
<thead>
<tr>
<th> 维度 </th>
<th> 售前 </th>
<th> 驻厂 / 实施 </th>
<th> 外包 </th>
<th> 工程师 </th>
<th> 硅谷 FDE </th>
<th> 土 FDE </th>
</tr>
</thead>
<tbody>
<tr>
<td> 最像什么人 </td>
<td> 方案销售 </td>
<td> 实施顾问 </td>
<td> 接单开发团队 </td>
<td> 程序员 </td>
<td> 创业公司 CTO + 顾问 + 工程师 </td>
<td> 野战 CTO + 产品经理 + 业务顾问 + 全栈工程师 </td>
</tr>
<tr>
<td> 工作起点 </td>
<td> 客户说“我可能要买” </td>
<td> 合同已经签了 </td>
<td> 客户说“我要做这个功能” </td>
<td> 需求已经排进来了 </td>
<td> 客户有高价值问题，但想法不清楚 </td>
<td> 老板有一堆乱流程，但没人说得清楚 </td>
</tr>
<tr>
<td> 问题定义权 </td>
<td> 部分有 </td>
<td> 很少 </td>
<td> 几乎没有 </td>
<td> 很少 </td>
<td> 很强 </td>
<td> 必须有，否则会被客户牵着鼻子走 </td>
</tr>
<tr>
<td> 技术实现权 </td>
<td> 弱 </td>
<td> 弱到中 </td>
<td> 强，但受需求限制 </td>
<td> 强 </td>
<td> 强 </td>
<td> 强 </td>
</tr>
<tr>
<td> 业务结果责任 </td>
<td> 弱，签单为主 </td>
<td> 中，上线为主 </td>
<td> 弱，验收为主 </td>
<td> 弱，交付功能为主 </td>
<td> 强，production outcome </td>
<td> 很强，老板只看有没有省钱 / 提效 </td>
</tr>
<tr>
<td> 主要交付物 </td>
<td> PPT、方案、报价、Demo </td>
<td> 配置、培训、上线、数据迁移 </td>
<td> 页面、系统、功能、接口 </td>
<td> 功能、系统、代码、Bug 修复 </td>
<td> 原型、集成、生产系统、可复用组件 </td>
<td> 能跑的系统、AI 工作流、流程改造、ROI 结果 </td>
</tr>
<tr>
<td> 核心能力 </td>
<td> 表达、方案、关系推进 </td>
<td> 耐心、流程、培训、协调 </td>
<td> 按需求开发、项目管理、成本控制 </td>
<td> 架构、代码、稳定性 </td>
<td> 抽象能力、工程能力、客户沟通、产品判断 </td>
<td> 业务嗅觉、快速开发、现场拆流程、低成本交付 </td>
</tr>
<tr>
<td> 客户关系 </td>
<td> 交易前关系 </td>
<td> 交付期关系 </td>
<td> 甲乙方关系 </td>
<td> 内部协作关系 </td>
<td> 战略伙伴关系 </td>
<td> 深度嵌入关系 </td>
</tr>
<tr>
<td> 收入逻辑 </td>
<td> 靠签单 </td>
<td> 靠实施服务 </td>
<td> 靠项目报价 / 人天 </td>
<td> 靠工资 </td>
<td> 靠平台价值和大客户采用 </td>
<td> 靠真实提效、省人、省时间、长期复购 </td>
</tr>
<tr>
<td> 最怕什么 </td>
<td> 讲完没人买 </td>
<td> 客户不会用 </td>
<td> 需求反复改、被压价 </td>
<td> 需求变来变去 </td>
<td> 做成 consulting，没法产品化 </td>
<td> 做成外包，越做越累，不可复制 </td>
</tr>
</tbody>
</table>


<p>注：该表格来自小红书博主 Lawted 的一个分享。</p>

<h2>为什么 FDE 会在今天突然爆火？</h2>

<p>FDE 绝对不是简单的“程序员+销售”。它的爆火，本质上是 AI 行业从“拼大模型参数”全面卷入“拼业务落地” 的必然结果。</p>

<ol>
<li>大模型落地的“最后一公里”太难了  <br/>
通用大模型谁都会用，但要把它深度接进企业内部的数据库、复杂的权限体系和审批流程，让它真正“懂业务”并跑出 ROI（投资回报率），中间的脏活累活巨大。这必须靠懂技术、懂业务的专家到现场去，把 AI“硬焊”进企业的工作流里。</li>
<li>AI 工具解放了纯生产力，放大了“超级个体”  <br/>
随着 AI 编程工具的普及，纯写代码的门槛被极大地拉低了。过去需要一整支“研发+售前+产品”团队才能交付的定制化项目，现在一两个具备全栈能力的 FDE 靠 AI 辅助，就能在最前线快速搞定。</li>
<li>商业模式的转变  <br/>
现在大厂发现只卖 API 接口根本卖不动。FDE 深入大客户内部构建的 AI 系统越深，这套系统就会成为企业的“承重墙”（换不掉），后续长期的 Token 消耗或智能体（Agent）服务费就是源源不断的高粘性收入。</li>
</ol>


<h2>FDE 的前世今生：从五角大楼到 OpenAI 战场</h2>

<p>虽然这个岗位最近因为 AI 爆火，但它并不是新发明。</p>

<ul>
<li>鼻祖与发源地： 最早由美国大数据巨头 Palantir 在近二十年前首创。当时为了解决五角大楼、中情局（CIA）等大客户极其复杂的机密网络和业务问题，Palantir 首创了把工程师直接派驻到客户现场联合办公、边看业务边写代码的交付模式。</li>
<li>本轮 AI 浪潮的推手： 真正把它推向全行业风口的是顶尖的 AI 实验室。OpenAI 斥巨资单独立起一条企业级 AI 部署业务线，甚至直接收购了咨询公司打包带走 FDE 团队；Anthropic、Google Cloud、AWS 等巨头也紧随其后，纷纷建立千人规模的 FDE 团队。</li>
</ul>


<p>而在演进过程中，FDE 也分化出了两种不同的硬核形态：</p>

<ul>
<li>硅谷 FDE（高大上的正统流派）： 面对的是企业高层的高价值问题。利用极强的抽象能力，把客户的个性化问题沉淀为平台的“可复用组件”，既帮客户解决了问题，又完成了自有产品的标准化。</li>
<li>土 FDE（接地气的野战流派）： 国内 AI 落地潮中最常见的角色，俗称“野战 CTO”。面对老板一堆混乱、说不清楚的原始流程，他们凭借敏锐的业务嗅觉，现场拆解业务，用 AI 工具和全栈开发能力实现“低成本、高回报”的交付。</li>
</ul>


<h2>揭秘大厂 FDE 的“硬核”招聘要求</h2>

<p>FDE 拿的是顶薪，其面试和岗位要求自然也远超普通的纯研发岗位。各大厂不收“只会闷头写代码的码农”，他们开出的 JD（岗位描述）翻译过来就是八个字：技术拉满，情商逆天。  <br/>
以下是国内外主流大厂 FDE 岗位的核心要求拆解：</p>

<h3>1. 字节跳动（火山引擎 / 豆包大模型团队）—— 卷“高并发、高可用落地的多面手”</h3>

<p>字节的豆包大模型目前在国内出货量极大，其 FDE 岗位高度看重快速构建 Demo、高并发架构和跨部门死磕的能力。</p>

<ul>
<li>硬性技术要求：

<ul>
<li>至少精通 Golang / Python / Java 中的一种，具备扎实的数据结构与高性能后端架构能力。</li>
<li>熟悉大模型应用生态，必须熟练掌握 LangChain / LlamaIndex 等 Agent 框架。</li>
<li>拥有丰富的 RAG（检索增强生成）优化经验，精通向量数据库（如 Milvus, Pinecone 等）的调优与准确率提升。</li>
</ul>
</li>
<li>业务与软实力：

<ul>
<li>具备极强的“产品感”，能直接跨过产品经理，根据大客户的模糊痛点，在 2-3 天内用 AI 快速写出一个能跑的 PoC（概念验证）原型。</li>
<li>能够承受高频出差，和销售、架构师一起直接驻扎在客户现场“打硬仗”。</li>
</ul>
</li>
</ul>


<h3>2. 阿里巴巴（阿里云智能 / 蚂蚁数科）—— 卷“行业深度与可复用组件提炼”</h3>

<p>阿里云作为国内云老大哥，其 FDE 偏向“硅谷正统流派”：他们不仅要帮客户解决眼下的问题，还要把问题抽象成阿里云的标准化产品组件。</p>

<ul>
<li>硬性技术要求：

<ul>
<li>具备深厚的云计算与云原生（Kubernetes / Docker）基础，能在大客户复杂的物理隔离网络、专有云环境中完成大模型的私有化部署。</li>
<li>熟练掌握大模型的微调（Fine-tuning）及 Prompt（提示词）工程优化，能针对特定行业调整模型表现。</li>
</ul>
</li>
<li>业务与软实力：

<ul>
<li>行业背景是硬通货。 通常要求候选人深耕过某一个垂直行业（如：5 年以上金融、能源、汽车或供应链数字化经验）。</li>
<li>具备极强的架构抽象能力。大厂最怕 FDE 做成“纯项目外包”，因此要求你必须能把多个客户的个性化需求，提炼成可复用的平台组件。</li>
</ul>
</li>
</ul>


<h3>3. 智谱 AI —— 卷“大模型应用层的天花板”</h3>

<p>作为国内头部大模型独角兽，智谱的 FDE 处于真正的最前线，要求对技术前沿的嗅觉极度灵敏。</p>

<ul>
<li>硬性技术要求：

<ul>
<li>精通 Agent（智能体体系）构建，不仅是调用 API，更要懂多智能体协同（Multi-Agent System）机制。</li>
<li>能够对模型生成的幻觉（Hallucination）进行深度治理，熟悉企业级数据安全与合规准入。</li>
</ul>
</li>
<li>业务与软实力：

<ul>
<li>极强的抗压和商务沟通能力。面对还没被 AI 教育过的传统企业老板，你得能用“人话”把复杂的技术 ROI 讲明白，具备极强的现场拆流程和“破冰”能力。</li>
</ul>
</li>
</ul>


<h3>4. OpenAI &amp; Anthropic —— 卷“技术研究员 + 商业咨询顾问的终极合体”</h3>

<p>海外顶尖实验室的 FDE 要求甚至接近全栈科学家。</p>

<ul>
<li>硬性技术要求：

<ul>
<li>拥有顶尖的系统工程（System Engineering）能力，能够优化超大规模 LLM 的推理延迟和吞吐量（Throughput）。</li>
<li>能直接深入到模型底层进行定制开发，而不仅仅是写应用层代码。</li>
</ul>
</li>
<li>业务与软实力：

<ul>
<li>必须具备财富 500 强企业高层的顶级沟通技巧。你面对的是华尔街投行高管或跨国制药巨头 CTO，你需要像麦肯锡顾问一样，帮他们重新设计公司的 AI 战略和顶层工作流。</li>
</ul>
</li>
</ul>


<h2>2026 现状：大厂疯狂抢人，Base 在哪里？薪资如何？</h2>

<p>目前，国内外大厂和头部 AI 独角兽都在用真金白银疯狂抢夺 FDE 核心人才，其岗位薪资堪称逆势暴涨。</p>

<h3>1. 国内大厂：狂卷“降本增效”</h3>

<p>国内由于各行各业急迫利用大模型“落地拿结果”，“土 FDE”的招聘需求呈现爆发式增长，薪资多在 年薪 60 万 ~ 100 万+ 之间。</p>

<ul>
<li>字节跳动（火山引擎 / 豆包团队）： Base：北京、深圳、上海。</li>
<li>阿里巴巴（阿里云智能 / 蚂蚁数科）： Base：杭州、北京、上海。</li>
<li>智谱 AI： Base：北京。</li>
<li>腾讯（CSIG）： Base：深圳、北京。</li>
</ul>


<h3>2. 海外大厂：顶尖实验室的抢人大战</h3>

<p>海外顶尖实验室资深 FDE 的年总薪酬中位数高达 48.5 万 ~ 72.5 万美元。</p>

<ul>
<li>OpenAI &amp; Anthropic： 主要 Base 在旧金山，需要经常出差常驻各金融、零售大客户现场。</li>
<li>Google Cloud &amp; Salesforce： 开启了数十亿美元的抢人大战，组建千人 FDE 团，主要 Base 在山景城、西雅图、纽约。</li>
</ul>


<h2>未来：FDE作为独立岗位能持续多久？</h2>

<p>很多技术人在面对高薪爆火的新岗位时，都会担心它会不会像当年的“大数据分析师”或“区块链工程师”一样，热炒几年后就迅速降温，重新被拆解融合进传统的岗位中。  <br/>
客观来看，FDE（前沿部署工程师）作为一个“独立岗位名称”，其黄金红利期预计能持续 3 到 5 年；但 FDE 所代表的“技术 + 业务”的核心职能，将是未来十年不可逆的长期趋势。</p>

<p>我们可以从以下三个发展阶段，来推演 FDE 这个岗位的生命周期：</p>

<h3>第一阶段：爆发期（当前 ~ 未来3年）—— 独立岗位的黄金期</h3>

<p>在这期间，FDE 将作为一个独立且极其昂贵的岗位高调存在。</p>

<ul>
<li>原因： 大模型（LLM）与企业核心业务系统（如 ERP、CRM）之间存在着巨大的技术断层。现在的 AI 依然有幻觉、不稳定、不懂企业私有数据。企业老板们既兴奋又焦虑，他们急需一个“特种兵”来到现场，迅速把 AI 焊进现有的工作流里。</li>
<li>现状： 因为这种“既懂底层大模型、又懂高并发架构、还能跟高管谈商业 ROI”的复合型人才在市场上极度稀缺，所以大厂和独角兽愿意开出百万年薪独立招人。</li>
</ul>


<h3>第二阶段：沉淀期（未来3 ~ 5年）—— 岗位走向分化与基建化</h3>

<p>随着 AI 工具链和企业大模型基建的完善，FDE 岗位会发生明显的两极分化：</p>

<ol>
<li>工具链成熟降低门槛： 3-5 年后，随着 Agent 框架（如 LangChain、LlamaIndex 等）和零代码/低代码 AI 搭建平台变得极其成熟，大模型落地将不再需要资深工程师去现场“生啃”代码。</li>
<li>“普通 FDE”被降维打击： 如果一个 FDE 的工作只是调用大厂 API、连一下向量数据库、调一调 Prompt，那么这部分工作会被更加成熟、便宜的“AI 实施顾问”或者“大模型应用开发工程师”所取代。</li>
<li>“高端 FDE”演变为“AI 架构师 / AI 顾问”： 真正顶尖的 FDE，会凭借积累的深厚行业经验（如：深刻理解金融反洗钱流程或医疗供应链），演变成企业的 AI 架构专家，独立岗位的含金量依然很高。</li>
</ol>


<h3>第三阶段：终局（5年后）—— 岗位消亡，技能“泛化”</h3>

<p>从长期来看，FDE 作为一个独立岗位的概念可能会逐渐模糊甚至消失，但这不是因为需求没了，而是因为它变成了所有研发人员的标配技能。</p>

<ul>
<li>历史的规律： 就像十几年前“移动互联网”刚爆发时，市场上重金抢夺“iOS 工程师”和“Android 工程师”，但今天已经很少有纯粹的移动端工程师了，因为“全栈”或“前端”开发默认都要会写移动端。</li>
<li>未来的消亡： 5 年后，当大模型彻底成为像数据库、服务器一样的软件基础设施时，所有的传统程序员、产品经理都必须具备 FDE 的能力——你必须懂业务、懂如何用 AI 提效、懂如何直接交付端到端的解决方案。到那时，“不能定义问题、不能走向前线”的传统格子间程序员，反而会成为最先被淘汰的一批人。</li>
</ul>


<h2>总结：对技术人的启示</h2>

<p>FDE 岗位的出现，本质上是软件工程正在从“代码驱动”向“价值驱动”转型。你不需要担心这个岗位名字能叫多久，因为它的底层逻辑是不可逆的。</p>

<p>FDE 的暴火告诉我们：企业不再需要一味坐在格子间里等待需求文档的传统程序员，而是需要能够走向战场前线、听得见枪声、能用技术直接帮企业解决高价值问题的“特种兵” 。它不适合毫无经验的应届生，它更青睐“会写代码的产品经理”或者“极具商业嗅觉和沟通能力的全栈工程师”。</p>

<p>普通程序员要转型，最大的坎不是技术，而是“走出格子间，直面客户和混乱需求”的勇气和能力。</p>

<p>建议从现在开始有意识地培养三项硬实力：死磕 Agent 落地、训练自己的业务 ROI 嗅觉、以及提升“把技术讲成白话”的非技术沟通能力。</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Claude Code源码解读]]></title>
    <link href="https://ibillxia.github.io/blog/2026/04/26/how-claude-code-works/"/>
    <updated>2026-04-26T20:08:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2026/04/26/how-claude-code-works</id>
    <content type="html"><![CDATA[<p>这应该是目前看过的最详细、最深入的技术解读系列文章。</p>

<p>第1章原文链接： <a href="https://diwang.info/how-claude-code-works/#/docs/01-overview">https://diwang.info/how-claude-code-works/#/docs/01-overview</a></p>

<blockquote><p><strong>本章导读</strong>：本章从宏观视角介绍 Claude Code 的定位、技术栈和架构全貌。我们会先理解它作为 Agent 与传统编程辅助工具的本质区别（1.1），然后介绍技术选型（1.2）、6 条核心设计原则（1.3）、关键术语（1.3+）、源码目录结构（1.4），最后通过数据流全景（1.5）、启动流程（1.6）和架构总览（1.7）将所有概念串联起来。如果你只想快速了解全貌，可以直接跳到 1.5 数据流全景。</p></blockquote>

<h2>1.1 Claude Code 解决什么问题</h2>

<p>Claude Code 不是一个简单的"CLI 调用大模型"工具。它是 Anthropic 官方推出的 <strong>受控工具循环 Agent（Controlled Tool-Loop Agent）</strong>，专为真实软件工程任务设计。</p>

<h3>从工具到 Agent：三级范式</h3>

<p>要理解 Claude Code 的定位，我们需要先理解 AI 辅助编程的三级范式：</p>

<p><strong>第一级：代码补全</strong>（如 Copilot）。模型的工作是"预测下一行代码"。它看到你的光标位置和上下文，生成一个补全建议。这本质上是一个<strong>单次预测问题</strong>——模型不需要理解整个项目，不需要执行任何操作，只需要根据局部上下文生成合理的代码片段。用户始终是驾驶员，模型只是副驾。</p>

<!--more-->   


<p><strong>第二级：IDE 聊天助手</strong>（如 Cursor Chat、Copilot Chat）。用户可以用自然语言描述需求，模型生成代码片段或修改建议。这比补全强大——模型可以看到更多上下文，可以生成多个文件的修改。但关键限制是：<strong>模型不能执行操作</strong>。它生成一个 diff，由用户决定是否 apply。如果 diff 有问题（比如依赖了一个不存在的函数），用户需要手动发现并反馈，模型无法自行验证。</p>

<p><strong>第三级：自主 Agent</strong>（Claude Code）。模型不仅生成代码，还能<strong>自主执行多步操作</strong>。考虑一个真实场景：你想给项目添加一个新的 REST endpoint。Copilot 会给你一个函数体。IDE 聊天可能会建议一个修改方案。而 Claude Code 的做法是：先用 Grep 搜索现有路由定义理解项目的路由模式，用 FileRead 读取中间件配置，然后创建 handler 文件、注册路由、编写测试，接着运行 <code>npm test</code> 发现测试失败，读取错误信息，修复代码，再次运行测试直到通过，最后提交 git commit。整个过程是一个<strong>自主决策循环</strong>——模型决定下一步做什么，执行后观察结果，再决定下一步。</p>

<p>这种范式跃迁带来了根本性的架构差异。一个 Agent 需要：
- <strong>循环</strong>（Loop）：不是单次调用，而是反复 "思考→执行→观察" 直到任务完成
- <strong>工具</strong>（Tools）：不是只生成文本，而是能读文件、写文件、执行命令
- <strong>记忆</strong>（Memory）：不是每次从零开始，而是记住用户偏好和项目上下文
- <strong>安全控制</strong>（Safety）：因为它在用户机器上执行真实操作，所以需要严格的权限管理</p>

<p>Claude Code 的每一个架构决策都围绕这四个需求展开。</p>

<h3>与其他 Agent 的区别</h3>

<p>市面上不乏其他编程 Agent（如 AutoGPT、OpenDevin、Aider 等），但 Claude Code 有一个独特优势：它由构建 Claude 模型的同一个团队开发。这意味着<strong>系统提示词、工具描述、错误处理策略都与模型的行为特性共同设计和调优</strong>。例如，Claude Code 的 system prompt 不是一个通用的 "你是一个编程助手"——它包含了针对 Claude 模型特性优化的详细行为指令，工具的 <code>description</code> 字段也经过反复调优以匹配模型的理解模式。</p>

<p>此外，Claude Code 在生产级工程质量上远超大多数开源 Agent 项目：5 层纵深防御的安全系统、4 级渐进式上下文压缩、7 种错误恢复策略、流式工具预执行——这些不是学术 demo，而是服务真实用户的工业级实现。</p>

<h3>"Agent-first" 的架构含义</h3>

<p>"Agent-first" 不是营销口号——它有具体的架构含义：<strong>模型是循环中的决策者，而非人类</strong>。人类设定目标（"给这个项目添加用户认证"）并审批危险操作（"确认执行 <code>npm install</code>？"），但在两次人类交互之间，模型自主决定读什么文件、改什么代码、执行什么命令。</p>

<p>这体现在源码中最核心的一行——<code>src/query.ts:307</code> 的 <code>while (true)</code>：</p>

<pre><code class="typescript">// src/query.ts:307
while (true) {
    // ... 压缩 → API 调用 → 工具执行 → 继续/退出
}
</code></pre>

<p>这个循环<strong>只有当模型的响应不包含任何工具调用时才会退出</strong>。换句话说，是模型——而不是代码逻辑——决定任务是否完成。代码只是提供了执行环境，真正的"大脑"是模型本身。</p>

<h2>1.2 技术栈</h2>

<table>
<thead>
<tr>
<th> 层次 </th>
<th> 技术选型 </th>
<th> 说明 </th>
</tr>
</thead>
<tbody>
<tr>
<td> 运行时 </td>
<td> Bun </td>
<td> 高性能 JS/TS 运行时，支持编译时 Feature Flag 消除 </td>
</tr>
<tr>
<td> 语言 </td>
<td> TypeScript </td>
<td> 全量 TypeScript，严格类型检查 </td>
</tr>
<tr>
<td> UI 框架 </td>
<td> React + Ink（自研） </td>
<td> 基于 React 的终端 UI 框架，自研 Ink 渲染器（<code>src/ink/</code>，~1.0MB） </td>
</tr>
<tr>
<td> 布局引擎 </td>
<td> Yoga </td>
<td> Facebook 的 Flexbox 布局引擎，适配终端 </td>
</tr>
<tr>
<td> Schema 验证 </td>
<td> Zod </td>
<td> 运行时类型校验，用于工具输入、Hook 输出、配置验证 </td>
</tr>
<tr>
<td> CLI 框架 </td>
<td> Commander.js </td>
<td> 命令行参数解析，分发到 REPL/headless/SDK 模式 </td>
</tr>
<tr>
<td> API 协议 </td>
<td> Anthropic SDK </td>
<td> 官方 TypeScript SDK，支持流式响应 </td>
</tr>
</tbody>
</table>


<p>技术选型本身不是本文重点，但有两个选择值得一提，因为它们深刻影响了架构设计：</p>

<ul>
<li><strong>Bun 的 <code>feature()</code> 宏</strong>：Claude Code 内部有大量功能（协调器模式、Swarm 团队等）在外部发布版本中需要完全移除。Bun 提供的编译时 Feature Flag 让这些代码在构建时被物理删除，而非运行时隐藏。这在后续"编译时 Feature Gate"设计原则中会详细展开。</li>
<li><strong>自研 React 终端渲染器</strong>：Claude Code 的终端 UI 复杂度远超普通 CLI——权限确认对话框、流式代码高亮、嵌套工具进度指示器都需要组件化的状态管理。团队维护了一个 ~1.0MB 的定制 Ink 渲染器（而非使用上游库），详见 <a href="https://diwang.info/how-claude-code-works/#/./12-user-experience">第 14 章：用户体验设计</a>。</li>
</ul>


<h2>1.3 核心设计原则</h2>

<p>Claude Code 的架构遵循 6 条核心设计原则：</p>

<h3>1. Generator-based 流式架构</h3>

<p>从 API 调用到 UI 渲染，全链路使用 <code>async function*</code> 异步生成器。这不是简单的 callback 或 Promise 链——而是真正的流式处理管道，每个 Token、每个工具结果都能实时流向用户界面。</p>

<p>核心查询循环的签名是：</p>

<pre><code class="typescript">// src/query.ts
export async function* query(
  params: QueryParams,
): AsyncGenerator&lt;StreamEvent | Message | ToolUseSummaryMessage, Terminal&gt;
</code></pre>

<p>这是一个异步生成器——它不是一次性返回结果，而是<strong>边执行边 yield 事件</strong>。调用方（QueryEngine）通过 <code>for await (const msg of query(params))</code> 实时消费每一个事件：模型输出的每个 Token、每个工具调用的结果、压缩事件、错误恢复——所有这些都通过同一个 generator 管道流向 UI 层。</p>

<p>这种设计的好处是<strong>零缓冲延迟</strong>：用户在模型开始生成的瞬间就能看到输出，而不需要等待整个响应完成。</p>

<p><strong>为什么是 Generator 而不是 Callback 或 Promise？</strong> 这个选择不是随意的——三种异步模式各有根本性的局限：</p>

<ul>
<li><strong>Callback 模式</strong>：经典 Node.js 风格，容易陷入 "callback hell"，更重要的是无法优雅地传递 backpressure（当 UI 渲染跟不上数据产生速度时，没有自然的暂停机制）。当用户按 Ctrl+C 中断时，需要手动在每一层 callback 中接线取消逻辑。</li>
<li><strong>Promise/async-await 模式</strong>：解决了 callback hell，但 <code>await</code> 是阻塞式的——一个 <code>await apiCall()</code> 必须等到整个响应完成才能返回。要实现流式，你需要手动缓冲部分结果并轮询，这本质上是在 Promise 之上重新发明 generator。</li>
<li><strong>Generator 模式</strong>：<code>yield</code> 天然就是流式语义——生产者（API 层）产出一个 token 就 yield 一次，消费者（UI 层）按自己的节奏拉取。更关键的是，<code>generator.return()</code> 可以<strong>级联清理整个调用链</strong>：用户按 Ctrl+C → REPL 调用 generator.return() → QueryEngine 的 generator 终止 → query() 的 generator 终止 → API 请求被 abort。不需要手动接线，cleanup 沿着 generator 链自动传播。</li>
</ul>


<p>注意 <code>query()</code> 的返回类型 <code>AsyncGenerator&lt;..., Terminal&gt;</code>——<code>Terminal</code> 是 generator 的 <strong>return type</strong>，代表查询的最终状态，与 yield 出的中间事件流是分离的。这种"双通道"（yield 流式事件 + return 最终结果）只有 generator 能干净地表达。</p>

<p>整个数据流形成了一个嵌套的 generator 管道：<code>REPL.tsx</code> → <code>QueryEngine.submitMessage()</code> → <code>query()</code> → <code>queryModelWithStreaming()</code>（<code>services/api/claude.ts</code>）。每一层 generator 在管道上叠加自己的处理逻辑（压缩、错误恢复、权限检查），但对上层来说，它只是一个统一的 <code>AsyncGenerator</code> 事件流。</p>

<h3>2. 防御性分层安全</h3>

<p>权限系统采用多层防御：</p>

<pre><code>权限规则匹配 (src/hooks/toolPermission/)
    ↓ 通过
Bash AST 分析 (src/utils/bash/, tree-sitter 解析)
    ↓ 通过
23 项静态安全验证器
    ↓ 通过
ML 分类器 (yoloClassifier)
    ↓ 通过
用户确认对话框
</code></pre>

<p><strong>为什么需要这么多层？</strong> 理解威胁模型是关键：Claude Code 在用户的真实机器上执行任意代码。模型不是完美的——它可能因为上下文混淆而生成错误命令，可能被恶意 README 中的 prompt injection 误导，或者只是单纯犯了一个逻辑错误。一条 <code>rm -rf ~</code> 就足以造成不可挽回的损失。</p>

<p>这是经典的<strong>纵深防御</strong>：即使某一层有 bug 或被绕过，其他层仍然可以阻止危险操作。每一层使用不同的技术手段，覆盖不同类别的风险：</p>

<ol>
<li><strong>权限规则匹配</strong>（<code>src/hooks/toolPermission/</code>）：这是<strong>策略层</strong>——用户通过 CLAUDE.md 的 <code>allowedTools</code> 或 <code>--allowedTools</code> 标志声明哪些操作是被允许的。这一层表达的是用户意图："在这个项目中，运行 <code>npm test</code> 总是安全的"。</li>
<li><strong>Bash AST 分析</strong>（<code>src/utils/bash/</code>, tree-sitter）：不是用正则匹配命令字符串，而是用 tree-sitter 将 Bash 命令解析为抽象语法树。为什么不用正则？因为 Bash 语法极其灵活——<code>r"m" -rf /</code>、<code>$(echo rm) -rf /</code>、<code>eval "rm -rf /"</code> 这些变形都能绕过简单的字符串匹配，但 AST 分析能识别出实际执行的命令。</li>
<li><strong>23 项静态安全验证器</strong>：硬编码的已知危险模式检查。这是"白名单/黑名单"层——某些操作（如写入 <code>/etc/passwd</code>、修改 SSH 配置）无论上下文如何都应该被拦截。</li>
<li><strong>ML 分类器</strong>（yoloClassifier）：一个经过训练的分类模型，能根据命令的语义上下文判断安全性。它捕获的是静态规则覆盖不到的"新型"危险模式——比如一条看起来无害但在当前上下文中可能造成问题的命令。</li>
<li><strong>用户确认对话框</strong>：最终的人类审核。即使所有自动化层都放行了，用户仍然可以看到即将执行的操作并选择拒绝。</li>
</ol>


<p>关键设计洞察：<strong>各层使用完全不同的技术</strong>（规则匹配、语法解析、机器学习、人类判断），这意味着单一类别的 bug 无法同时绕过所有层。即使权限规则配置错误地放行了一条命令，tree-sitter AST 分析仍然会检测到 <code>rm -rf /</code> 这样的结构性危险模式。</p>

<h3>3. 编译时 Feature Gate</h3>

<p>通过 Bun bundler 的 <code>feature()</code> 宏实现编译时死代码消除。内部功能（如协调器模式）在外部构建中完全移除——不是运行时隐藏，而是编译时物理删除。</p>

<p>这个模式在整个代码库中反复出现：</p>

<pre><code class="typescript">// src/query.ts 开头 — 6 个 Feature Gate 条件加载
const reactiveCompact = feature('REACTIVE_COMPACT')
  ? (require('./services/compact/reactiveCompact.js') as typeof import('./services/compact/reactiveCompact.js'))
  : null
const contextCollapse = feature('CONTEXT_COLLAPSE')
  ? (require('./services/contextCollapse/index.js') as typeof import('./services/contextCollapse/index.js'))
  : null
const skillPrefetch = feature('EXPERIMENTAL_SKILL_SEARCH')
  ? (require('./services/skillSearch/prefetch.js') as typeof import('./services/skillSearch/prefetch.js'))
  : null
const jobClassifier = feature('TEMPLATES')
  ? (require('./jobs/classifier.js') as typeof import('./jobs/classifier.js'))
  : null
const snipModule = feature('HISTORY_SNIP')
  ? (require('./services/compact/snipCompact.js') as typeof import('./services/compact/snipCompact.js'))
  : null
const taskSummaryModule = feature('BG_SESSIONS')
  ? (require('./utils/taskSummary.js') as typeof import('./utils/taskSummary.js'))
  : null
</code></pre>

<p><code>as typeof import(...)</code> 类型断言让 TypeScript 在编译期获得正确的类型信息，而 <code>feature()</code> 在 Bun bundler 构建时被求值——如果结果为 <code>false</code>，整个 <code>require()</code> 分支和相关代码都被 tree-shaken 移除。使用这些模块的代码总是先检查 <code>if (contextCollapse) { ... }</code>，这个条件判断本身也在编译时被消除。</p>

<h3>4. 状态集中 + 不可变更新</h3>

<p>全局状态集中于 <code>bootstrap/state.ts</code>（1,758 行，150+ 访问器）。为什么不直接用全局变量？</p>

<p>在一个拥有 66+ 工具、多个子 Agent、压缩管道和 React UI 的系统中，共享状态是不可避免的——当前使用的模型名、会话 ID、Feature Flag 缓存、累计成本、文件修改状态等，这些信息需要被多个子系统同时访问和修改。朴素的全局变量方案会带来三个实际问题：</p>

<ol>
<li><strong>import 循环</strong>：模块 A 导入 B 的状态，B 导入 C 的工具，C 又导入 A 的状态——在一个 1,900 文件的项目中，这种循环几乎不可避免</li>
<li><strong>不可追踪的修改</strong>：当某个 bug 导致模型名被意外改变，你无法设断点查看"是谁在什么时候改了这个值"</li>
<li><strong>React 渲染问题</strong>：直接修改全局对象的属性不会触发 React 组件的重新渲染</li>
</ol>


<p><code>bootstrap/state.ts</code> 的解决方案是通过显式的 getter/setter 函数暴露状态（如 <code>getSessionId()</code>、<code>getTotalCost()</code>、<code>setCurrentModel()</code>），而不是导出可变对象。每个模块只导入自己需要的 getter/setter 函数，从而打破 import 循环；每次修改都经过函数调用，可以轻松添加日志或断点追踪。</p>

<p>UI 状态使用 Zustand 模式的不可变更新——<code>setAppState(prev =&gt; ({ ...prev, newField: value }))</code>——保证 React 组件能正确感知状态变化。</p>

<p>值得注意的是，这不是一个"理想"的架构——团队自己也在控制全局状态的增长。但在 Claude Code 这样的复杂系统中，集中管理的 getter/setter 是一个务实的平衡：比全局变量安全，比完整的状态管理框架（如 Redux）轻量。</p>

<h3>5. 渐进式压缩</h3>

<p>Snip → Microcompact → Context Collapse → Autocompact 四级压缩流水线，确保对话永不因上下文溢出而中断。四级压缩按成本从低到高排列，每级解决不同粒度的问题：</p>

<ol>
<li><strong>Snip</strong>（零 API 成本）：移除对话历史中已经不再被引用的旧工具结果。例如，10 轮前的一次 <code>grep</code> 搜索结果可能有 50KB，但模型早已不再关注它。Snip 用一个占位符替换这些内容，纯本地操作，不需要调用 API。（<code>query.ts:401-410</code>）</li>
<li><strong>Microcompact</strong>（近零成本）：压缩单个工具结果的体积。比如一个 Grep 工具返回了 200 行匹配结果，Microcompact 可以将其截断为最相关的前 20 行。同样是本地启发式操作。（<code>query.ts:414-426</code>）</li>
<li><strong>Context Collapse</strong>（中等成本）：将相关的消息序列分组折叠为摘要。关键设计：这是一个<strong>读时投影</strong>——原始完整历史保留在内存中，发送给 API 的是折叠后的视图。这意味着折叠是可逆的，不会丢失原始信息。（<code>query.ts:440-447</code>）</li>
<li><strong>Autocompact</strong>（全量成本）：fork 一个子 Agent 生成整个对话的摘要，用摘要替换原始历史。这是"核选项"——释放最多空间，但不可逆地丢失对话细节。（<code>query.ts:454-467</code>）</li>
</ol>


<p><strong>为什么四级而非只用 Autocompact？</strong> 如果只有 Autocompact，每次上下文接近满就必须调用 API 生成摘要——既有延迟成本（用户等待），又有质量成本（细节丢失）。通过先执行零成本的 Snip 和 Microcompact，系统往往能释放足够的空间避免触发昂贵的 Autocompact。实践中，很多对话自始至终都不需要走到 Autocompact 这一步。</p>

<p>详见 <a href="https://diwang.info/how-claude-code-works/#/./03-context-engineering">第 3 章：上下文工程</a>。</p>

<h3>6. 工具即扩展点</h3>

<p>所有能力——文件操作、搜索、Agent 派生、MCP 桥接——统一为 <code>Tool</code> 接口（<code>src/Tool.ts</code>）。无论是内置的 <code>BashTool</code>、通过 MCP 协议接入的外部工具，还是插件系统注册的第三方工具，它们共享完全相同的执行管道：权限检查 → 输入校验 → 执行 → 结果格式化 → UI 渲染。</p>

<p><code>Tool</code> 接口（<code>src/Tool.ts</code>）拥有约 20 个字段和方法，每一个都在统一管道中扮演角色：
- <code>isReadOnly()</code>：告诉权限系统这个工具是否只读——只读工具（如 Grep、Glob）可以跳过用户确认
- <code>isConcurrencySafe()</code>：告诉 <code>StreamingToolExecutor</code> 这个工具能否与其他工具并行执行——Grep 可以，但 FileEdit 不行（可能产生写冲突）
- <code>shouldDefer</code>：告诉 API 层是否延迟发送完整 schema——66+ 个工具的 schema 加起来占用大量 token，不常用的工具可以按需加载
- <code>inputSchema</code>（Zod）：模型生成的参数在执行前必须通过 Schema 验证，防止畸形输入触达工具执行层
- <code>interruptBehavior()</code>：定义用户中断时工具的行为——有些工具可以立即中断，有些需要清理</p>

<p><code>findToolByName()</code> 函数不区分工具来源——对 query 循环来说，所有工具都是平等的 <code>Tool</code> 对象。这意味着一个通过 MCP 协议接入的外部 Kubernetes 工具，和内置的 BashTool 经历完全相同的权限检查、输入验证、结果格式化流程。扩展 Claude Code 的能力就是实现一个符合 <code>Tool</code> 接口的对象，而不需要修改核心循环——这是经典的开闭原则（Open-Closed Principle）在 Agent 架构中的体现。</p>

<h3>核心术语速查</h3>

<p>在深入后续章节之前，以下是贯穿整个代码库的几个核心概念：</p>

<table>
<thead>
<tr>
<th> 术语 </th>
<th> 定义 </th>
<th> 对应源码 </th>
</tr>
</thead>
<tbody>
<tr>
<td> <strong>State</strong> </td>
<td> 单次 query 循环的可变状态对象，包含消息历史、压缩追踪、output token 恢复计数等 </td>
<td> <code>query.ts:204</code> <code>type State</code> </td>
</tr>
<tr>
<td> <strong>Tool</strong> </td>
<td> 统一的工具接口，所有能力（内置/MCP/插件）都实现此接口，共享同一执行管道 </td>
<td> <code>Tool.ts</code> </td>
</tr>
<tr>
<td> <strong>Message</strong> </td>
<td> 对话中的一条消息，包含 <code>UserMessage</code>、<code>AssistantMessage</code>、<code>ToolUseSummaryMessage</code> 等子类型 </td>
<td> <code>types/message.ts</code> </td>
</tr>
<tr>
<td> <strong>StreamEvent</strong> </td>
<td> generator 管道中 yield 的事件单元，代表一个 token、工具结果或状态变更 </td>
<td> <code>types/message.ts</code> </td>
</tr>
<tr>
<td> <strong>Terminal / Continue</strong> </td>
<td> query 循环的两种转移状态——<code>Terminal</code> 表示循环结束，<code>Continue</code> 表示需要继续下一轮迭代 </td>
<td> <code>query/transitions.ts</code> </td>
</tr>
<tr>
<td> <strong>QueryEngine</strong> </td>
<td> 会话级引擎，管理对话生命周期（持久化、预算、结果组装），是 UI 层与核心循环之间的边界 </td>
<td> <code>QueryEngine.ts</code> </td>
</tr>
</tbody>
</table>


<h2>1.4 源码目录结构</h2>

<p>Claude Code 源码约 1,900 文件、512K+ 行 TypeScript，目录结构如下：</p>

<pre><code>src/
├── main.tsx                 # CLI 主入口（4,683 行）
│                            # Commander.js 解析参数，分发到 REPL/headless/SDK 模式
├── QueryEngine.ts           # 会话引擎（1,295 行）
│                            # 管理对话全生命周期：消息持久化、预算追踪、结果组装
├── query.ts                 # 核心查询循环（1,729 行）
│                            # 单次查询的状态机：压缩→API调用→工具执行→恢复/继续
├── Tool.ts                  # 工具接口定义
│                            # 所有工具（内置/MCP/插件）的统一类型约束
├── tools.ts                 # 工具注册与组装
├── context.ts               # 上下文构建（189 行）
│                            # getSystemContext/getUserContext：Git状态、CLAUDE.md、日期
│
├── bootstrap/               # 全局状态管理
│   └── state.ts             # 集中式状态存储（1,758 行，150+ getter/setter）
│                            # 所有子系统通过访问器读写共享状态，避免 import 循环
│
├── entrypoints/             # 入口点
│   ├── init.ts              # 核心初始化（340 行）：14 步幂等初始化
│   ├── cli.tsx              # 快速路径（--version, MCP server, bridge）
│   └── sdk/                 # SDK 入口与类型
│
├── screens/                 # 主要界面
│   ├── REPL.tsx             # 主对话 UI（875KB）：消息渲染、输入处理、状态管理
│   ├── Doctor.tsx           # 诊断界面
│   └── ResumeConversation.tsx
│
├── tools/                   # 66+ 内置工具
│   ├── BashTool/            # Shell 命令执行（含 AST 安全分析）
│   ├── AgentTool/           # 子 Agent 派生（支持 worktree 隔离）
│   ├── FileReadTool/        # 文件读取（支持图片、PDF、Notebook）
│   ├── FileEditTool/        # 文件编辑（search-and-replace 策略）
│   ├── GrepTool/            # 内容搜索（基于 ripgrep）
│   ├── GlobTool/            # 文件匹配
│   ├── WebFetchTool/        # 网页获取
│   ├── SkillTool/           # 技能调用
│   └── ...                  # 更多工具
│
├── services/
│   ├── api/                 # API 客户端层
│   │   ├── claude.ts        # 核心查询逻辑（3,419 行）
│   │   │                    # HTTP→Claude API 的桥梁：prompt 构建、缓存控制、
│   │   │                    # thinking 配置、task budget 注入、流式响应解析
│   │   ├── withRetry.ts     # 重试策略（指数退避 + 模型降级）
│   │   └── promptCacheBreakDetection.ts  # 缓存断裂检测与自动归因
│   ├── compact/             # 压缩系统
│   │   ├── autoCompact.ts   # 自动压缩触发（阈值计算、条件判断）
│   │   └── compact.ts       # 摘要生成引擎（1,705 行）
│   │                        # fork 子 Agent 生成对话摘要，压缩后恢复最近文件和技能
│   ├── mcp/                 # MCP 协议集成（7 种传输）
│   ├── oauth/               # OAuth 2.0 + PKCE
│   ├── plugins/             # 插件系统
│   └── lsp/                 # 语言服务器协议
│
├── hooks/                   # 权限与 Hook 处理
│   └── toolPermission/      # 工具权限判定
│       └── handlers/        # 3 种权限处理器：规则匹配、Hook、用户确认
│
├── coordinator/             # 多 Agent 协调器（内部功能，Feature-gated）
├── memdir/                  # 记忆系统（~/.claude/memory/ 管理）
├── skills/                  # 技能系统（18+ 内置技能）
├── ink/                     # 自定义终端渲染器（~1.0MB 核心，React→终端输出）
├── vim/                     # Vim 模式
├── schemas/                 # Zod Schema 定义
└── utils/                   # 通用工具库
    ├── hooks.ts             # Hook 执行引擎
    ├── bash/                # Bash AST 解析（tree-sitter）
    ├── messages.ts          # 消息处理（5,512 行）：规范化、压缩边界、格式转换
    └── tokens.ts            # Token 估算与追踪
</code></pre>

<h2>1.5 数据流全景</h2>

<p>理解 Claude Code 的关键是理解<strong>数据如何在各层之间流动</strong>。下面是一次完整的用户交互的数据流：</p>

<center><img src="https://ibillxia.github.io/images/2026/IMAG2026042601.png"></center>


<p></p>

<p>让我们沿着数据流逐步展开，理解每个阶段发生了什么：</p>

<p><strong>Step 1：用户输入进入 REPL</strong>。React 组件 <code>REPL.tsx</code> 捕获用户的文本输入。如果是斜杠命令（如 <code>/clear</code>、<code>/compact</code>），在本地直接处理，永远不会发送到 API。普通消息则传递给 <code>QueryEngine.submitMessage()</code>。</p>

<p><strong>Step 2：QueryEngine 准备查询</strong>。<code>processUserInput()</code> 处理消息中的附件（图片缩放、文件引用解析），构建包含消息历史、系统提示词、工具列表和权限上下文的 <code>QueryParams</code> 对象，然后调用 <code>query()</code> 启动核心循环。</p>

<p><strong>Step 3：4 级压缩管道运行</strong>。注意——压缩不是只在对话开始时运行一次，而是<strong>在每次 API 调用之前都会运行</strong>。循环的每一轮迭代都会依次检查 Snip → Microcompact → Context Collapse → Autocompact，按需触发。大多数迭代中没有任何压缩触发（上下文还没满），但当历史消息累积到接近上下文窗口上限时，压缩管道会自动介入。</p>

<p><strong>Step 4：API 调用</strong>。系统提示词、压缩后的消息历史和工具 schema 被发送到 Claude API（通过 <code>services/api/claude.ts</code> 的 <code>queryModelWithStreaming()</code>）。响应以 token-by-token 的方式流式返回，每个 token 被 yield 为 <code>StreamEvent</code> 沿着 generator 链向上传递到 UI，用户立即看到文字出现。</p>

<p><strong>Step 5：工具在流式过程中即开始执行</strong>。这是 Claude Code 的一个重要性能优化：<code>StreamingToolExecutor</code> <strong>不等待模型的完整响应</strong>。当流式解析器检测到一个 <code>tool_use</code> JSON block 已经完整，工具执行立即开始——此时模型可能还在继续生成后面的文字或其他工具调用。只读且并发安全的工具（如 Grep、Glob）甚至可以<strong>并行执行</strong>，进一步缩短多工具调用的总耗时。</p>

<p><strong>Step 6：结果注入，循环继续</strong>。工具的执行结果被封装为 <code>tool_result</code> 消息追加到对话历史中。循环回到 Step 3——再次检查是否需要压缩，再次调用 API。模型看到工具结果后决定下一步：继续调用更多工具，或者生成最终的文字回复。</p>

<p><strong>Step 7：循环退出，结果组装</strong>。当模型的响应中不包含任何 <code>tool_use</code> block 时，<code>query()</code> 返回一个 <code>Terminal</code> 值。<code>QueryEngine</code> 组装最终结果，持久化对话历史，更新 usage/cost 追踪。</p>

<h3>关于性能：流式工具预执行</h3>

<p>Step 5 中的"流式工具预执行"值得单独强调。在朴素的实现中，流程是串行的：等待模型完整响应 → 解析工具调用 → 顺序执行工具 → 发送结果。Claude Code 的流程是重叠的：模型还在生成文字的同时，已解析完成的工具调用已经在执行。对于一次包含 3-4 个工具调用的响应，这种重叠可以显著减少端到端延迟。</p>

<h3>关于错误恢复</h3>

<p>数据流中隐藏着多层错误恢复机制：
- <strong>API 错误</strong>（429 限速、529 服务过载）：<code>withRetry</code> 层自动进行指数退避重试，严重情况下可以降级到备选模型
- <strong>上下文过长</strong>（<code>prompt_too_long</code>）：触发 reactive compact——紧急执行一轮压缩然后重试 API 调用
- <strong>工具执行失败</strong>：错误信息被包装为 <code>tool_result</code>（标记 <code>is_error: true</code>）返回给模型，模型可以自行决定是重试还是换一种方法。<code>yieldMissingToolResultBlocks()</code>（<code>query.ts:123</code>）确保每个 <code>tool_use</code> 都有对应的 <code>tool_result</code>，即使在中断场景下也不会出现消息配对缺失</p>

<p>关键洞察：<strong>数据通过嵌套的 async generator 流动</strong>。每一层都在 generator 管道上添加自己的处理逻辑（权限检查、压缩、错误恢复），但对上层来说，它只是一个统一的事件流。这使得关注点完全分离——QueryEngine 不需要知道压缩细节，REPL 不需要知道错误恢复逻辑。</p>

<h2>1.6 启动流程</h2>

<p>Claude Code 的启动经过精心优化，将大量工作并行化和延迟化。整个流程分为 9 个阶段，关键路径仅约 <strong>235ms</strong>：</p>

<center><img src="https://ibillxia.github.io/images/2026/IMAG2026042602.png"></center>


<p></p>

<h3>为什么分 9 个阶段？</h3>

<p>这个设计的核心目标是<strong>最小化用户感知的启动时间</strong>。用户关心的是"输入 <code>claude</code> 后多快看到输入提示符"，而不是所有初始化都完成了。所以：</p>

<ul>
<li><strong>Phase 1-2 并行预取</strong>：MDM（移动设备管理）策略读取和 Keychain 凭证预取在模块加载的同时就并行启动，而不是等加载完成后串行执行</li>
<li><strong>Phase 6 幂等初始化</strong>：<code>init()</code> 函数是 memoized 的，重复调用无副作用。这让多个代码路径都可以安全地调用 <code>await init()</code> 而不用担心重复初始化</li>
<li><strong>Phase 8 延迟非关键任务</strong>：用户信息查询、文件计数统计、模型能力检测——这些对首次交互不重要的操作被推迟到首帧渲染之后</li>
<li><strong>Phase 9 懒加载重依赖</strong>：OpenTelemetry（~400KB+）在用户完成 Trust Dialog 之后才加载，避免拖慢启动速度</li>
</ul>


<h2>1.7 架构总览</h2>

<center><img src="https://ibillxia.github.io/images/2026/IMAG2026042603.png"></center>


<p></p>

<p>这张图看起来像一个普通的分层架构，但每一层的设计决策都值得理解：</p>

<p><strong>入口层（main.tsx）</strong>：CLI 入口处理三种截然不同的运行模式——REPL（交互式终端）、Print 模式（<code>-p</code> 标志，单次查询后退出）和 SDK/Bridge 模式（供第三方程序调用）。关键设计是：<strong>三种模式全部汇聚到同一个 QueryEngine</strong>。这意味着核心 Agent 循环是模式无关的——无论 Claude Code 是被人类交互使用、被 CI 脚本以 <code>-p</code> 调用、还是被 IDE 插件通过 SDK 集成，底层执行的都是同一个 <code>query()</code> 函数。这对测试也很有价值：Print 模式本质上就是一个无头测试工具。</p>

<p><strong>会话层（QueryEngine）</strong>：管理一次对话的完整生命周期——消息持久化（每次交互自动保存）、成本追踪（累计 token 和美元开销）、预算执行（task budget 限额）、结构化输出重试。它是"用户交互"和"Agent 执行"之间的边界。当一条新消息到达时，QueryEngine 判断它是斜杠命令、文件附件还是普通 prompt，做相应的预处理后才转交给核心循环。</p>

<p><strong>核心循环（query）</strong>：这是 Claude Code 的心脏。一个 <code>while(true)</code> 循环反复执行：压缩上下文 → 调用 API → 执行工具 → 判断是否继续。循环携带可变的 <code>State</code> 对象（<code>query.ts:204</code>），包括消息历史、压缩追踪状态、输出 token 恢复计数、turn 计数等。循环有 7 个不同的"继续点"（Continue Sites），分别处理正常工具循环、上下文过长恢复、压缩触发重试等场景。</p>

<p><strong>服务层（API + 工具 + 上下文）</strong>：三个独立的子系统，由核心循环编排协调。API 服务处理模型通信（流式传输、重试策略、提示词缓存）。工具系统提供 66+ 种能力（文件操作、搜索、Agent 派生、MCP 桥接）。上下文系统负责构建系统提示词、注入 CLAUDE.md 内容、管理 git 状态信息。三者之间互不依赖。</p>

<p><strong>基础设施层（OAuth、History、Telemetry、Plugins）</strong>：横切关注点，支撑所有其他层但不参与主循环。OAuth 处理认证，History 提供对话持久化和恢复（<code>claude --resume</code>），Telemetry（懒加载，~400KB+）追踪使用数据，Plugins 扩展工具和 Hook。</p>

<h3>模块依赖规则</h3>

<p>这个分层有一条关键的依赖规则：<strong>核心循环（query.ts）依赖服务层，但永远不依赖 UI 层；UI 层（REPL.tsx）依赖 QueryEngine，但永远不直接依赖 query.ts</strong>。这意味着你可以把整个终端 UI 替换为 Web UI，只需要重写 REPL 层——QueryEngine 及其以下的所有模块完全不用改动。SDK 模式就是这个设计的直接体现：它绕过了整个 UI 层，直接与 QueryEngine 交互。</p>

<h2>1.8 代码规模参考</h2>

<table>
<thead>
<tr>
<th> 指标 </th>
<th> 数值 </th>
</tr>
</thead>
<tbody>
<tr>
<td> TypeScript 文件 </td>
<td> ~1,332 </td>
</tr>
<tr>
<td> TSX (React) 文件 </td>
<td> ~552 </td>
</tr>
<tr>
<td> 总行数 </td>
<td> 512,000+ </td>
</tr>
<tr>
<td> 内置工具数 </td>
<td> 66+ </td>
</tr>
<tr>
<td> Hook 事件类型 </td>
<td> 23+ </td>
</tr>
<tr>
<td> 安全验证器 </td>
<td> 23 项 </td>
</tr>
<tr>
<td> MCP 传输类型 </td>
<td> 7 种 </td>
</tr>
<tr>
<td> 权限模式 </td>
<td> 5+2 种 </td>
</tr>
<tr>
<td> 内置技能 </td>
<td> 18+ </td>
</tr>
</tbody>
</table>


<hr />

<p>下一章：<a href="https://diwang.info/how-claude-code-works/#/./02-agent-loop">系统主循环</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Anthropic: 多智能体协作的五种模式及其适用场景]]></title>
    <link href="https://ibillxia.github.io/blog/2026/04/11/multi-agent-coordination-patterns/"/>
    <updated>2026-04-11T22:07:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2026/04/11/multi-agent-coordination-patterns</id>
    <content type="html"><![CDATA[<p>翻译自 Anthropic 官方博客，原文链接：<a href="https://claude.com/blog/multi-agent-coordination-patterns">https://claude.com/blog/multi-agent-coordination-patterns</a></p>

<p><strong>五种多智能体协调模式、各自的权衡取舍，以及何时从一种模式演进到另一种模式。</strong></p>

<p>在此前的一篇文章中，我们探讨了多智能体系统何时能带来价值、何时单个智能体是更好的选择。本文面向已经做出这一判断、现在需要决定哪种协调模式适合其问题的团队。</p>

<p>我们发现，许多团队选择模式时往往基于"听起来复杂高端"，而非"是否契合实际问题"。我们建议从最简单的可行模式出发，观察其局限所在，再逐步演进。本文将分析以下五种模式的机制与局限：</p>

<ul>
<li><strong>生成器-验证器（Generator-Verifier）</strong>：适用于有明确评估标准的质量关键型输出</li>
<li><strong>编排器-子智能体（Orchestrator-Subagent）</strong>：适用于任务分解清晰、子任务边界明确的场景</li>
<li><strong>智能体团队（Agent Teams）</strong>：适用于并行、独立、长时运行的子任务</li>
<li><strong>消息总线（Message Bus）</strong>：适用于具有不断扩展的智能体生态的事件驱动管道</li>
<li><strong>共享状态（Shared State）</strong>：适用于智能体需要在彼此发现的基础上协作推进的场景</li>
</ul>


<!--more-->


<h2>模式一：生成器-验证器（Generator-Verifier）</h2>

<center><img src="https://ibillxia.github.io/images/2026/IMAG2026041101.png"></center>


<p></p>

<p>这是最简单的多智能体模式，也是部署最广泛的模式之一。我们在上一篇文章中将其介绍为"验证子智能体"模式，这里使用更宽泛的"生成器-验证器"表述，因为生成器不一定是编排器。</p>

<h3>工作原理</h3>

<p>生成器接收任务并产生初始输出，将其传递给验证器进行评估。验证器检查输出是否满足所需标准，若满足则接受，若不满足则附带反馈信息拒绝。拒绝后，反馈被路由回生成器，生成器据此生成修订版本。此循环持续进行，直到验证器接受输出或达到最大迭代次数为止。</p>

<h3>适用场景</h3>

<p>以一个针对客户工单自动生成邮件回复的支持系统为例：生成器根据产品文档和工单上下文生成初始回复；验证器对照知识库检查准确性、依据品牌指南评估语气，并确认回复是否涵盖了所有问题。未通过检查的内容会以具体反馈（如将某功能错误归属于某定价层级、或遗漏了某个工单问题）返回给生成器。</p>

<p>当输出质量至关重要且评估标准可以明确定义时，适合使用此模式。它在代码生成（一个智能体写代码，另一个写并运行测试）、事实核查、基于评分标准的评级、合规性验证等领域均有良好表现——凡是"错误输出的代价高于额外生成一次的代价"的领域，都适合此模式。</p>

<h3>局限所在</h3>

<p>验证器的能力上限取决于其评估标准的质量。如果验证器仅被告知"检查输出是否良好"而没有进一步标准，它往往会直接通过生成器的输出。团队最常见的失败之处在于：实现了循环，却没有定义"验证"究竟意味着什么，从而制造了质量控制的假象，而非实质。</p>

<p>此外，该模式假设"生成"与"验证"是可分离的技能。如果评估某种创意方案与生成它一样困难，验证器可能并不能可靠地捕获问题。</p>

<p>最后，迭代循环可能陷入停滞。若生成器无法解决验证器的反馈，系统将在不收敛的情况下反复震荡。设置最大迭代次数并制定回退策略（升级给人工、带注释返回最优结果）可以防止无限循环。</p>

<hr />

<h2>模式二：编排器-子智能体（Orchestrator-Subagent）</h2>

<center><img src="https://ibillxia.github.io/images/2026/IMAG2026041102.png"></center>


<p></p>

<p>这一模式以层级关系为核心。一个智能体充当"团队负责人"，负责规划工作、分配任务、综合结果；子智能体负责处理特定职责并汇报结果。</p>

<h3>工作原理</h3>

<p>主导智能体接收任务后决定如何处理：部分子任务由自身直接完成，其余分派给子智能体。子智能体完成工作后返回结果，编排器将其综合为最终输出。</p>

<p><a href="https://code.claude.com/docs/en/overview">Claude Code</a> 使用的正是这一模式。主智能体自行编写代码、编辑文件、运行命令，并在需要搜索大型代码库或调查独立问题时在后台派遣子智能体——工作在结果回流的同时并行推进。每个子智能体在自己的上下文窗口中运行，返回提炼后的发现，从而使编排器的上下文专注于主要任务，而探索工作在并行中完成。</p>

<h3>适用场景</h3>

<p>以自动化代码审查系统为例：当 Pull Request 到来时，系统需要检查安全漏洞、验证测试覆盖率、评估代码风格、评估架构一致性。每项检查相互独立，需要不同的上下文，输出清晰明确。编排器将每项检查分派给专职子智能体，收集结果后综合为统一的审查报告。</p>

<p>当任务分解清晰、子任务之间相互依赖度低时，适合使用此模式。编排器保持对整体目标的连贯视图，而子智能体则专注于各自职责。</p>

<h3>局限所在</h3>

<p>编排器会成为信息瓶颈。当某个子智能体发现了与另一个子智能体工作相关的信息时，该信息必须通过编排器中转。若安全子智能体发现了影响架构分析的认证漏洞，编排器必须识别这一依赖关系并适当路由信息。经过多次此类交接后，关键细节往往会丢失或被摘要删除。</p>

<p>此外，顺序执行也会限制吞吐量。除非显式并行化，子智能体将依次运行，这意味着系统承担了多智能体的 Token 成本，却未获得速度上的收益。</p>

<hr />

<h2>模式三：智能体团队（Agent Teams）</h2>

<p>当工作可以分解为能够长时间独立并行推进的子任务时，编排器-子智能体模式可能过于局限。</p>

<h3>工作原理</h3>

<center><img src="https://ibillxia.github.io/images/2026/IMAG2026041103.png"></center>


<p></p>

<p>协调者（Coordinator）将多个工作者智能体作为独立进程启动。团队成员从共享队列中认领任务，自主完成多步骤工作，并在完成后发出信号。</p>

<p>与编排器-子智能体的核心区别在于工作者的持久性。编排器为单个有界子任务派遣子智能体，子任务完成后子智能体即终止；而团队成员则跨多次任务持续存活，不断积累上下文和领域专知，随时间推移提升表现。协调者分配工作、收集结果，但不会在任务之间重置工作者。</p>

<h3>适用场景</h3>

<p>以将大型代码库从一个框架迁移到另一个框架为例：每个服务可以由团队成员独立迁移，包含其各自的依赖、测试套件和部署配置。协调者将每个服务分配给某个团队成员，成员自主完成迁移工作：依赖更新、代码修改、测试修复、验证。协调者收集完成的迁移结果并在整个系统上运行集成测试。</p>

<p>当子任务独立、且能从持续多步骤工作中获益时，适合使用此模式。每个团队成员会积累对其负责领域的熟悉程度，而非每次从零开始。</p>

<h3>局限所在</h3>

<p>独立性是关键前提。与编排器-子智能体不同（编排器可以在子智能体之间中转信息），团队成员自主运行，无法轻松共享中间发现，其输出可能因此产生冲突。</p>

<p>完成检测也更加困难。由于团队成员自主工作且时长不固定，协调者必须处理"部分完成"的情况——某个成员两分钟完成，另一个需要二十分钟。</p>

<p>共享资源会加剧上述问题。当多个团队成员操作同一代码库、数据库或文件系统时，可能出现编辑同一文件或做出不兼容更改的情况。此模式需要仔细的任务分区和冲突解决机制。</p>

<hr />

<h2>模式四：消息总线（Message Bus）</h2>

<p>随着智能体数量增加、交互模式日趋复杂，直接协调变得难以管理。消息总线引入了一个共享通信层，智能体通过发布和订阅事件进行交互。</p>

<h3>工作原理</h3>

<center><img src="https://ibillxia.github.io/images/2026/IMAG2026041104.png"></center>


<p></p>

<p>智能体通过两种原语交互：发布（Publish）和订阅（Subscribe）。智能体订阅自己感兴趣的主题，路由器（Router）将匹配的消息分发出去。新增的具备新能力的智能体可以立即开始接收相关工作，无需重新布线现有连接。</p>

<h3>适用场景</h3>

<p>安全运营自动化系统是此模式的典型示例：警报从多个来源涌入，分类智能体按严重程度和类型分类，将高严重性的网络警报路由给网络调查智能体，将凭据相关警报路由给身份分析智能体。每个调查智能体可能发布上下文收集请求，由专门的上下文收集智能体来满足。发现结果流向响应协调智能体，由其确定适当的行动。</p>

<p>此管道适合消息总线，因为事件从一个阶段流向下一阶段，团队可以随着威胁类别演变添加新智能体类型，各智能体可以独立开发和部署。</p>

<p>当工作流程由事件驱动而非预定顺序产生，且智能体生态系统可能持续增长时，适合使用此模式。</p>

<h3>局限所在</h3>

<p>事件驱动通信的灵活性使追踪变得更加困难。当一个警报触发跨五个智能体的事件级联时，理解发生了什么需要仔细的日志记录和关联分析，调试难度远高于跟随编排器的顺序决策。</p>

<p>路由准确性也至关重要。若路由器错误分类或丢弃某个事件，系统会悄无声息地失败——什么都没处理，却也没有崩溃。基于 LLM 的路由器提供了语义灵活性，但也引入了其自身的故障模式。</p>

<hr />

<h2>模式五：共享状态（Shared State）</h2>

<p>前述各模式中的编排器、团队负责人和消息路由器均集中管理信息流。共享状态通过让智能体直接读写持久化存储来协调，从而去除了中间人。</p>

<h3>工作原理</h3>

<center><img src="https://ibillxia.github.io/images/2026/IMAG2026041105.png"></center>


<p></p>

<p>智能体自主运行，从共享数据库、文件系统或文档中读取和写入信息，没有中心协调者。智能体检查存储中的相关信息，基于发现采取行动，并将结果写回。工作通常从初始化步骤开始（在存储中植入问题或数据集），在满足终止条件时结束：时间限制、收敛阈值，或某个指定智能体判断存储中已有足够的答案。</p>

<h3>适用场景</h3>

<p>以研究综合系统为例：多个智能体调查某个复杂问题的不同方面——一个探索学术文献，一个分析行业报告，一个检查专利申请，一个监测新闻报道。每个智能体的发现可能影响其他智能体的调查方向。学术文献智能体发现的某位关键研究者，可能正是行业智能体需要深入研究的公司的创始人。</p>

<p>有了共享状态，发现结果直接写入存储。行业智能体可以立即看到学术智能体的发现，无需等待协调者路由信息。智能体在彼此工作的基础上持续推进，共享存储成为不断演化的知识库。</p>

<p>共享状态还消除了协调者作为单点故障的风险。若任意一个智能体停止运行，其他智能体仍可继续读写；而在编排器和消息总线系统中，协调者或路由器的故障将导致一切停摆。</p>

<h3>局限所在</h3>

<p>没有显式协调，智能体可能重复工作或追求相互矛盾的方向。两个智能体可能独立调查同一条线索。智能体之间的交互产生系统行为，而非自上而下的设计，使结果难以预测。</p>

<p>更难处理的故障模式是反应式循环：智能体 A 写入一个发现，智能体 B 读取并写入跟进内容，智能体 A 看到跟进并再次响应——系统持续消耗 Token，却没有收敛。重复工作和并发写入有成熟的工程解决方案（锁、版本控制、分区），而反应式循环是行为问题，需要一流的终止条件：时间预算、收敛阈值（N 个周期内无新发现），或某个专门负责判断存储答案是否充分的指定智能体。将终止条件视为事后补充的系统往往会无限循环，或在某个智能体上下文填满时随机停止。</p>

<hr />

<h2>模式的选择与演进</h2>

<p>正确的模式取决于系统的几个结构性问题。在前一篇文章中，我们倡导以上下文为中心的分解方式——按每个智能体所需的上下文来划分工作，而非按工作类型划分。这一原则同样适用于此。各模式的区别在于它们如何管理上下文边界和信息流动。</p>

<h3>编排器-子智能体 vs. 智能体团队</h3>

<center><img src="https://ibillxia.github.io/images/2026/IMAG2026041106.png"></center>


<p></p>

<p>两者都涉及协调者将工作分派给其他智能体，区别在于工作者需要维持上下文多长时间。</p>

<ul>
<li><strong>选择编排器-子智能体</strong>：子任务短暂、聚焦、输出清晰。代码审查系统适用于此，因为每项检查完成分析、生成报告，并在单次有界调用内返回，子智能体无需跨多个周期持有上下文。</li>
<li><strong>选择智能体团队</strong>：子任务从持续多步骤工作中获益。代码库迁移适用于此，因为每个团队成员会对其负责的服务形成真正的熟悉程度——依赖图谱、测试模式、部署配置，这种积累的上下文能带来一次性派遣无法复制的效果。</li>
</ul>


<p>当子智能体需要跨调用保留状态时，智能体团队是更好的选择。</p>

<h3>编排器-子智能体 vs. 消息总线</h3>

<center><img src="https://ibillxia.github.io/images/2026/IMAG2026041107.png"></center>


<p></p>

<p>两者都能处理多步骤工作流，区别在于工作流结构的可预测性。</p>

<ul>
<li><strong>选择编排器-子智能体</strong>：步骤顺序提前已知。代码审查系统遵循固定管道：接收 PR、运行检查、综合结果。</li>
<li><strong>选择消息总线</strong>：工作流由事件产生，可能因发现的内容而变化。安全运营系统无法预测会到来哪些警报或需要什么调查路径，也可能出现需要新处理方式的新警报类型。消息总线通过将事件路由给有能力的智能体来适应这种可变性，而非遵循预定顺序。</li>
</ul>


<p>当编排器中为处理不断扩展的案例类型而积累了大量条件逻辑时，消息总线能让这种路由变得显式且可扩展。</p>

<h3>智能体团队 vs. 共享状态</h3>

<center><img src="https://ibillxia.github.io/images/2026/IMAG2026041108.png"></center>


<p></p>

<p>两者都涉及自主工作的智能体，区别在于智能体是否需要彼此的发现。</p>

<ul>
<li><strong>选择智能体团队</strong>：智能体在互不交互的独立分区上工作。代码库迁移适用于此，每个团队成员负责自己的服务，协调者在最后合并结果。</li>
<li><strong>选择共享状态</strong>：智能体的工作是协作性的，发现需要实时在它们之间流动。研究综合系统更适合此模式，因为学术智能体发现的关键研究者，立即与行业智能体的调查相关。</li>
</ul>


<p>一旦团队成员需要相互通信，而不仅仅是共享最终结果，共享状态更加自然。</p>

<h3>消息总线 vs. 共享状态</h3>

<center><img src="https://ibillxia.github.io/images/2026/IMAG2026041109.png"></center>


<p></p>

<p>两者都支持复杂的多智能体协调，区别在于工作是以离散事件的形式流动，还是以共享知识库的形式积累。</p>

<ul>
<li><strong>选择消息总线</strong>：智能体在管道中对事件做出反应。安全运营系统逐阶段处理警报，每个事件在完成前触发下一个事件，该模式擅长将事件路由给有能力的智能体。</li>
<li><strong>选择共享状态</strong>：智能体随时间推移在积累的发现上持续构建。研究综合系统持续积累知识，智能体反复返回存储，查看他人的发现并调整自己的调查方向。</li>
</ul>


<p>消息总线仍有路由器这个中心组件来决定事件走向；共享状态则是去中心化的。若消除单点故障是优先级，共享状态能更完整地做到这一点。</p>

<p>如果消息总线中的智能体正在发布事件来共享发现而非触发行动，共享状态是更好的选择。</p>

<hr />

<h2>快速入门</h2>

<p>生产系统通常会组合使用多种模式。常见的混合方式是：用编排器-子智能体处理整体工作流，同时在协作密集的子任务中使用共享状态；或使用消息总线进行事件路由，用智能体团队风格的工作者处理每种事件类型。这些模式是构建模块，而非互斥的选择。</p>

<p>下表总结了各模式的适用情境：</p>

<table>
<thead>
<tr>
<th> 情境 </th>
<th> 模式 </th>
</tr>
</thead>
<tbody>
<tr>
<td> 质量关键型输出，有明确评估标准 </td>
<td> 生成器-验证器 </td>
</tr>
<tr>
<td> 任务分解清晰，子任务边界明确 </td>
<td> 编排器-子智能体 </td>
</tr>
<tr>
<td> 并行工作负载，独立的长时运行子任务 </td>
<td> 智能体团队 </td>
</tr>
<tr>
<td> 事件驱动管道，智能体生态持续增长 </td>
<td> 消息总线 </td>
</tr>
<tr>
<td> 协作性研究，智能体共享发现 </td>
<td> 共享状态 </td>
</tr>
<tr>
<td> 不允许单点故障 </td>
<td> 共享状态 </td>
</tr>
</tbody>
</table>


<p>对于大多数用例，我们建议从编排器-子智能体开始。它以最低的协调开销处理最广泛的问题。观察其局限所在，再根据具体需求向其他模式演进。</p>

<hr />

<p><em>在即将发布的文章中，我们将结合生产实现和案例研究深入探讨每种模式。关于多智能体系统何时值得投入的背景，请参阅<a href="https://claude.com/blog/building-multi-agent-systems-when-and-how-to-use-them">《构建多智能体系统：何时以及如何使用》</a>。</em></p>

<hr />

<p><strong>致谢</strong>：作者 Cara Phillips，贡献者 Eugene Yan、Jiri De Jonghe、Samuel Weller 和 Erik S.</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[OpenClaw中的Agent Trigger解读]]></title>
    <link href="https://ibillxia.github.io/blog/2026/03/05/openclaw-trigger-interpretation/"/>
    <updated>2026-03-05T10:08:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2026/03/05/openclaw-trigger-interpretation</id>
    <content type="html"><![CDATA[<h1>1 引言</h1>

<p>自动化非常强大，但如果没有触发器，自动化系统将不会发生任何事情。</p>

<p>在 OpenClaw 中，AI Agent Trigger（AI 代理触发器）是告诉代理什么时候开始执行任务的事件。 <br/>
它定义了执行的时间、原因以及条件。如果没有触发器，系统会一直处于等待状态； <br/>
一旦触发器被激活，Agent 工作流就会自动运行。</p>

<center><img src="https://ibillxia.github.io/images/2026/IMAG2026030501.png"></center>


<p></p>

<!--more-->   


<p>总结：Trigger 是 Agent 自动化系统的入口。 <br/>
Trigger 是 Agent 系统区别于传统 Chatbot 的关键组件。 <br/>
Chatbot 通常是被动响应用户输入，而 Agent 通过 Trigger 可以主动执行任务。</p>

<h1>2 什么是 AI Agent Trigger</h1>

<p>AI Agent Trigger 是一种规则或事件，用于激活 OpenClaw 中的工作流。</p>

<p>常见触发条件包括： <br/>
- 收到新的电子邮件 <br/>
- Webhook 请求 <br/>
- 定时任务 <br/>
- 文件上传 <br/>
- 聊天频道消息 <br/>
- 数据库更新 <br/>
- 系统告警</p>

<p>典型流程：</p>

<blockquote><p>事件 → Trigger → Agent → Action</p></blockquote>

<p>总结：Trigger 让 AI 从“被动回答问题”升级为“主动执行任务”。 <br/>
Trigger 本质上是 Agent 系统中的事件驱动机制。 <br/>
它类似于分布式系统中的 Event Bus。</p>

<h1>3 为什么 Trigger 很重要</h1>

<p>OpenClaw 设计用于任务自动化、多步骤工作流以及 API 编排。</p>

<p>Trigger 的存在让系统能够在后台持续运行，而不需要人工启动。</p>

<p>典型场景包括： <br/>
- 7×24 自动化 <br/>
- 客户流程管理 <br/>
- 系统监控 <br/>
- AI 规模化运营</p>

<p>总结：Trigger 让 AI 从工具变成系统。 <br/>
企业级 Agent 系统通常由 Trigger、Workflow Engine、LLM 推理模块、工具调用和监控系统组成。</p>

<h1>4 Trigger 类型</h1>

<p>常见 Trigger 类型包括：</p>

<h2>1）时间触发（Time-based）</h2>

<p>例如：每天 9 点生成报告。</p>

<h2>2）事件触发（Event-based）</h2>

<p>例如：新用户注册后启动 onboarding 流程。</p>

<h2>3）Webhook Trigger</h2>

<p>外部系统通过 HTTP 请求触发 Agent。</p>

<h2>4）手动触发</h2>

<p>用户点击按钮或执行命令。</p>

<p>总结：Trigger 可以分为周期型和事件驱动型两大类。不同 Trigger 类型适用于不同自动化场景。</p>

<h1>5 Trigger 的内部工作流程</h1>

<p>典型流程：</p>

<ul>
<li>1 触发条件检测</li>
<li>2 权限验证</li>
<li>3 Agent 接收任务</li>
<li>4 LLM 推理</li>
<li>5 工具调用</li>
<li>6 输出结果</li>
</ul>


<p>总结：Trigger 只是入口，真正执行任务的是 Agent Runtime 与工具系统。典型的执行流程：Trigger 层 → Agent Runtime → LLM Reasoning → Tool Execution → Result</p>

<h1>6 Trigger 与 Token 成本</h1>

<p>Trigger 频率直接影响 Token 成本。</p>

<p>常见问题包括： <br/>
- 触发频率过高 <br/>
- 小任务使用昂贵模型 <br/>
- 上下文过长</p>

<p>总结：AI 系统成本 ≈ Trigger 频率 × 模型价格 × 上下文长度。 <br/>
合理设计 Trigger 可以显著降低 AI 成本。</p>

<h1>7 真实案例：自动客服</h1>

<p>Trigger：收到客服邮件。</p>

<p>Agent 执行流程： <br/>
- 1 提取问题 <br/>
- 2 分类 <br/>
- 3 生成回复 <br/>
- 4 记录系统</p>

<p>总结：Trigger 可以驱动完整客服自动化流程。客服 Agent 是目前最成熟的企业 Agent 应用之一。</p>

<h1>8 Multi-Agent 系统中的 Trigger</h1>

<p>在复杂系统中，一个 Trigger 可以启动多个 Agent。</p>

<p>例如： <br/>
产品发布 → <br/>
Marketing Agent 写公告 <br/>
Social Agent 发布社媒 <br/>
Analytics Agent 监控数据</p>

<p>总结：Trigger 是 Multi-Agent Orchestration 的入口。 <br/>
Trigger 可以协调多个 Agent 共同工作。</p>

<h1>9 常见问题</h1>

<p>常见问题包括：</p>

<p>Trigger Loop（循环触发） <br/>
触发过于频繁 <br/>
模型分配不合理</p>

<p>总结：Trigger 设计必须考虑安全性。需要加入限流、条件判断和监控机制。</p>

<h1>10 什么时候应该使用 Trigger</h1>

<p>适用于： <br/>
自动化流程 <br/>
周期任务 <br/>
系统监控 <br/>
数据处理</p>

<p>不适用于： <br/>
需要人工审核 <br/>
实验流程</p>

<p>总结：Trigger 适用于稳定、可重复的流程。 <br/>
自动化前提是流程稳定。</p>

<h1>11 总结</h1>

<p>AI Agent Trigger 是自动化 Agent 系统的核心组件。</p>

<p>它定义了： <br/>
什么时候执行 <br/>
什么事件启动流程 <br/>
系统如何扩展</p>

<p>架构总结：</p>

<pre><code>Trigger   
↓   
Workflow Engine   
↓   
Agent (LLM)   
↓   
Tools   
↓   
Action   
</code></pre>

<p>最终意义： <br/>
Trigger 让 AI 从被动助手变成主动自动化系统。</p>

<p>原文链接： <a href="https://openclawdirectory.co.uk/blog/what-is-an-ai-agent-trigger-in-openclaw/">https://openclawdirectory.co.uk/blog/what-is-an-ai-agent-trigger-in-openclaw/</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Anthropic Agentic Coding最新报告：程序员的核心能力将彻底改变]]></title>
    <link href="https://ibillxia.github.io/blog/2026/02/23/anthropic-agentic-coding-report/"/>
    <updated>2026-02-23T19:07:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2026/02/23/anthropic-agentic-coding-report</id>
    <content type="html"><![CDATA[<center><img src="https://ibillxia.github.io/images/2026/IMAG2026022301.png"></center>


<p>原始报告：<a href="https://resources.anthropic.com/hubfs/2026%20Agentic%20Coding%20Trends%20Report.pdf">2026 Agentic Coding Trends Report</a></p>

<!--more-->


<h1>开篇：一个正在发生的残酷现实</h1>

<p>2025年，Anthropic的研究团队发现了一个令人不安的数据：工程师在使用AI完成工作时，虽然平均生产力提升显著，但他们真正能够"完全委托"的任务，仅仅占所有工作的0-20%。</p>

<p>这个数字刺痛了多少人？</p>

<p>我们总在讨论AI会不会替代程序员，但真正的答案可能更可怕：AI不会让你失业，但会使用AI的人会让你失业。</p>

<p>Anthropic刚刚发布的《2026 Agentic Coding Trends Report》，用八大趋势向我们宣告了一个新时代的到来——在这个时代里，编程的核心能力正在发生根本性迁移：从"写代码"转向"编排AI"。</p>

<p>谁掌握了编排AI的能力，谁就掌握了未来。</p>

<p>这份报告基于Anthropic与客户的深度合作与观察，总结了8大趋势，并将它们归纳为三个维度：基础趋势（Foundation）、能力趋势（Capability）和影响趋势（Impact）。让我们一一看清这些趋势如何重塑软件开发。</p>

<h1>第一类：基础趋势——底层的地壳轮动</h1>

<h2>趋势1：软件开发生命周期正在被重构</h2>

<p>从机器码到汇编，从C到高级语言，每一次抽象都缩小了人类思维与机器执行之间的鸿沟。2026年，我们将迎来最大的一次跨越：AI正在把"写代码"这个核心活动，从人类手中逐步剥离。</p>

<center><img src="https://ibillxia.github.io/images/2026/IMAG2026022302.png"></center>


<p>传统的软件开发生命周期(SDLC)阶段依然存在，但节奏已经天翻地覆：</p>

<p>• 曾经需要数周的需求-开发-测试-部署周期，正在被压缩到小时级
• 实施、测试、文档生成等工作已经可以自动化
• 监控直接反馈到快速迭代
核心预测：</p>

<p>• 工程师的工作将从"实现"转向"架构设计和战略决策"
• 角色从"写代码的人"转变为"编排AI代理、评估输出、提供战略指挥的人"
• 入职新项目的时间从"几周"缩短到"几小时"——这意味着什么？人才部署将彻底改变
案例：Augment Code帮助某企业将原本需要4-8个月的项目，两周内完成。</p>

<h1>第二类：能力趋势——AI能做什么</h1>

<h2>趋势2：单代理→多代理协调系统</h2>

<p>2025年我们还局限在单代理工作流，2026年，多代理团队将成为标配。</p>

<p>想象一下：你不是和一个AI对话，而是有一个"总指挥代理"，下面协调着专门的"测试代理"、“前端代理”、“后端代理”、“文档代理”……它们并行工作，各自有专属的上下文窗口，最后合成完整的输出。</p>

<center><img src="https://ibillxia.github.io/images/2026/IMAG2026022303.png"></center>


<p>这需要什么新技能？</p>

<p>• 任务分解能力
• 代理专业化配置
• 协调协议设计
• 多并发生成版本控制
案例： Fountain公司使用Claude的多层代理编排，实现筛选提速50%、入职快40%、转化率翻倍。一个物流客户 staffing 新配送中心的时间从数周缩短到72小时以内。</p>

<h2>趋势3：长效代理——能连续工作数天</h2>

<p>早期的代理只能处理几分钟内的任务：“修这个bug”、“写这个函数”。</p>

<p>2026年的代理能连续工作数天甚至数周，自主构建完整的应用和系统。它们会：</p>

<p>• 跨数十个工作会话进行规划、迭代、精炼
• 适应意外发现，从失败中恢复
• 在整个复杂项目中保持连贯的状态
案例： Rakuten工程师测试Claude Code在vLLM（一个拥有1250万行跨多语言代码的巨无霸开源库）中实现特定的激活向量提取方法。7小时自主工作完成整个任务，精度达到99.9%。</p>

<p>这意味着什么？ formerly non-viable projects become feasible（曾经不划算的项目现在变得可行）。积压多年的技术债务可以被系统性地消除。</p>

<h2>趋势4：智能协作——人类监督的新范式</h2>

<p>2026年最有价值的突破可能是：代理学会何时求助，人类只在必要时介入。</p>

<p>这不是"去除人类"，而是让人类的注意力在最关键的地方发挥作用。</p>

<p>三大预测：</p>

<ol>
<li>代理质量审查成为标准：用AI代理审查AI生成的代码（安全漏洞、架构一致性、质量问题）</li>
<li>代理学会何时求助：识别需要人类判断的场景，标记不确定性，升级具有业务影响的决策</li>
<li>人机监督从"审查所有"转向"审查关键"：系统自动处理常规验证，真正新颖的情况、边界案例、战略决策才需要人工干预</li>
</ol>


<h1>第三类：影响趋势——商业与组织的变革</h1>

<h2>趋势5：编程普及化——从工程师到每个人</h2>

<p>2026年，编码代理将突破工程团队的边界，进入以下领域：</p>

<p>• 遗留语言（COBOL、Fortran、领域特定语言）
• 非传统开发者：网络安全、运维、设计师、数据科学家
• 非技术业务人员
"人人是开发者"的时代真的来了。</p>

<p>案例： Legora（AI法律平台）让律师也能创建复杂的自动化工作流，无需工程专业知识。Zapier内部89%员工采用AI，部署800+个AI代理；设计团队在客户访谈时用Claude实时生成原型原型展示。</p>

<h2>趋势6：生产力重新定义软件开发经济学</h2>

<p>生产力不是"做得更快"，而是 “做得更多”。</p>

<p>Anthropic内部研究发现：</p>

<p>• 工程师每类任务耗时减少，但输出量大幅增加
• 27%的AI辅助任务是"本来不会做"的：扩展项目规模、构建锦上添花工具（交互式仪表板）、探索性工作
• “papercuts”（小麻烦）被大量修复——因为AI让修复它们变得划算
TELUS公司创建超过13,000个定制AI方案，代码交付速度提升30%，节省500,000+小时，每次AI交互平均节省40分钟。</p>

<h2>趋势7：非技术用例爆发——全员生产力提升</h2>

<p>2026年最大的增长点之一：业务职能部门使用代理为自己造工具。</p>

<p>销售、市场、法务、运营团队将有能力：</p>

<p>• 自动化工作流
• 构建内部工具
• 无需工程介入解决业务问题
案例： Anthropic自身法务团队将营销审核从2-3天压缩到24小时内完成，通过Claude驱动的自动化工作流，处理合同修订、内容审查等重复性任务。</p>

<h2>趋势8：安全双重刃——攻防两端都在AI化</h2>

<p>AI既帮助防御者，也帮助攻击者。</p>

<p>预测：</p>

<p>• 安全知识民主化：任何工程师都能进行深度安全审查、加固、监控
• 威胁行为者规模化攻击
• 自主安全防御系统兴起：用AI对抗AI，在机器速度上响应
最终，有准备的团队将占优势——那些从一开始就把安全内建到代理系统中的团队，才能对抗使用相同技术的对手。</p>

<h1>核心洞察：协作悖论与人类角色的根本转变</h1>

<p>报告中最发人深省的发现是 “协作悖论”：</p>

<p>工程师在60%的工作中使用AI，却只能"完全委托"0-20%的任务。</p>

<p>这不是矛盾，而是揭示了AI协作的本质：有效使用AI需要积极的人类参与。</p>

<p>工程师们逐渐发展出"AI委托直觉"：</p>

<p>• 容易验证的任务 → 完全交给AI
• 低风险任务 → 交给AI（比如快速脚本追踪bug）
• 概念性强、依赖设计、需要组织背景或"品味"的任务 → 自己处理或与AI协作
如Anthropic一位工程师所说：</p>

<blockquote><p>我主要在用AI的场合是我已经知道答案应该是什么样的。这种能力是我通过’hard way’的软件工程练出来的。</p></blockquote>

<p>CRED（印度最大金融科技平台之一）的答案是：工程师与AI协作而非被替代，他们用Claude Code覆盖整个开发生命周期，执行速度翻倍——不是通过削减人力，而是通过把开发者推向更高价值的工作。</p>

<h1>2026开发者生存指南：掌握"编排"能力</h1>

<p>基于八大趋势，Anthropic给出2026年四大优先事项：</p>

<ol>
<li><p>掌握多代理协调
单代理搞不定的复杂任务，需要用多代理架构处理。这需要你：</p>

<p>  • 学会任务分解（problem decomposition） <br/>
  • 设计代理专业化配置 <br/>
  • 建立协调协议（coordination protocols）  <br/>
  • 管理多并发生成和版本控制工作流</p></li>
<li><p>扩展人机监督规模
不是让AI做所有事情然后你review，而是构建智能系统自己处理常规验证，只在必要时介入。</p></li>
<li><p>把代理能力扩展到工程之外
不要只让自己会用。去教你的法务、市场、运营同事怎么用代理解决问题。谁先吃到螃蟹，谁就先获得生产力红利。</p></li>
<li><p>将安全内建到代理系统设计
从第一天就考虑安全，而不是事后补救。</p></li>
</ol>


<h1>写在最后：人不会被AI替代，但会被会用AI的人替代</h1>

<p>Anthropic报告中有一句让我醍醐灌顶的话：</p>

<blockquote><p>The goal isn’t to remove humans from the loop—it’s to make human expertise count where it matters most.</p></blockquote>

<p>这不是"AI帮你写代码"的级别，而是 “AI帮你构建系统，你在更高维度思考” 的级别。</p>

<p>那些还在埋头苦写每一行代码的人，可能会发现自己正在重复造轮子。而掌握"指挥AI"能力的人，已经在用数倍的速度前进。</p>

<p>2026年的开发者画像已经清晰：</p>

<pre><code>• 你不再需要成为一个全栈专家（AI会填补知识空白）
• 但你必须成为一个优秀的"总指挥"（学会编排、批判性评估、战略方向）
• 你的价值 = 你定义正确问题的能力 × AI实现的速度
</code></pre>

<p>Anthropic的报告是写给行业的预警，也是写给每个开发者的通知书：</p>

<blockquote><p>要么提升自己的游戏指挥能力，要么被时代的游戏淘汰。</p></blockquote>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Google&Kaggle五天Agent强化课程]]></title>
    <link href="https://ibillxia.github.io/blog/2026/01/05/5d-agent-course-by-google/"/>
    <updated>2026-01-05T09:51:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2026/01/05/5d-agent-course-by-google</id>
    <content type="html"><![CDATA[<p>Kaggle链接：<a href="https://www.kaggle.com/learn-guide/5-day-agents">https://www.kaggle.com/learn-guide/5-day-agents</a></p>

<p>推荐B站up主<a href="https://space.bilibili.com/85645193">碳硅同传</a>的讲解视频：</p>

<p>第一集：</p>

<iframe src="//player.bilibili.com/player.html?isOutside=true&aid=115769501356144&bvid=BV1JSBTB2EMT&cid=34935080758&p=1" 
scrolling="no" border="0" frameborder="no" framespacing="0" allowfullscreen="true"
style="width: 100%; height: 600px; max-width: 900px;"></iframe>




<!--more-->


<p>第二集：<a href="https://www.bilibili.com/video/BV19uB2BjEiZ">https://www.bilibili.com/video/BV19uB2BjEiZ</a></p>

<p>第三集：<a href="https://www.bilibili.com/video/BV1fiBYBnEmy">https://www.bilibili.com/video/BV1fiBYBnEmy</a></p>

<p>第四集：<a href="https://www.bilibili.com/video/BV1NEBkBREUF">https://www.bilibili.com/video/BV1NEBkBREUF</a></p>

<p>第五集：<a href="https://www.bilibili.com/video/BV1G6vkBfEjF">https://www.bilibili.com/video/BV1G6vkBfEjF</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[数据科学与机器学习2026年趋势展望]]></title>
    <link href="https://ibillxia.github.io/blog/2025/12/12/data-science-trends-2026/"/>
    <updated>2025-12-12T10:27:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2025/12/12/data-science-trends-2026</id>
    <content type="html"><![CDATA[<p>分享两篇关于数据科学与机器学习趋势的文章，供大家参考。总结下来，最最重要的两大趋势：</p>

<ul>
<li><p>实际应用中，<strong>小模型</strong>（Qwen-8B etc）将越来越受到关注</p></li>
<li><p><strong>Agent和AutoML</strong>将越来越受到关注</p></li>
</ul>


<h1><a href="https://towardsdatascience.com/data-science-in-2026-is-it-still-worth-it/">1. Data Science in 2026: Is It Still Worth It?</a></h1>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025121201.png"></center>


<p>Are you thinking about switching to Data Science in 2026?</p>

<p>If the answer is “yes,” this article is for you.</p>

<!--more-->


<p>I’m Sabrine. I have spent the last 10 years working in the AI field across Europe—from big companies and startups to research labs. And if I had to start over again today, I would honestly still choose this field. Why?</p>

<p>For the same reasons that brought many of us here: the intellectual challenge, the impact you can have, the love for mathematics and code, and the possibility to solve real-life problems.</p>

<p>But looking toward 2026… is it still worth it?</p>

<p>If you scroll through LinkedIn, you will see two teams fighting: one saying “Data Science is dead,” and the other saying it is growing thanks to the AI trend.</p>

<p>When I look around me, I personally think we will always need computational skills. We will always need people who can understand data and help make decisions. Numbers have always been everywhere, and why would they disappear in 2026?</p>

<p>However, the market has changed. And to navigate it now, you need good guidance and clear information.</p>

<p>In this article, I’ll share my own experience from working in research and industry, and from mentoring more than 200 Data Scientists over the last few years.</p>

<h2>So what is happening in the market now?</h2>

<p>I will be honest and not sell you any dream about it.</p>

<p>The goal is not to introduce biases, but to give you enough information to make your own decision.</p>

<h2>Is the Data Science job family broader than ever?</h2>

<p>Source: <a href="https://pixabay.com/photos/sea-wave-splash-big-wave-ocean-5499649/">pixabay</a> (Kanenori)</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025121201.jpeg"></center>


<p>One of the biggest mistakes of junior Data Scientists is thinking Data Science is one single job.</p>

<p>In 2026, Data Science is a large family of roles. Before writing a single line of code, you need to understand where you fit.</p>

<p>People are fascinated by AI: how ChatGPT talks, how Neuralink stimulates brains, and how algorithms affect health and security. But let’s be honest: not all aspiring Data Scientists will build these types of projects.</p>

<p>These roles need strong applied math and advanced coding skills. Does that mean you will never reach them? No. But they are often for people with PhDs, computational scientists, and engineers trained exactly for these niche jobs.</p>

<p>Let’s take a real example: a Machine Learning/Data Scientist job offer I saw today (Nov 27) at a GAFAM company.</p>

<p><em>Screenshot taken by the author</em></p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025121202.png"></center>


<p>If you look at the description, they ask for:</p>

<ul>
<li><p>Patents</p></li>
<li><p>First-author publications</p></li>
<li><p>Research contributions</p></li>
</ul>


<p>Does everyone interested in Data Science have a patent or a publication? Of course not.</p>

<p>This is why you must avoid moving blindly.</p>

<p>If you just finished a bootcamp or are early in your studies, applying for jobs that explicitly require research publications will only bring frustration. These very specialized jobs are usually for people with advanced academic backgrounds (PhD, post-doc, or computational engineering).</p>

<p>My advice: be strategic. Focus on roles that match your skills.</p>

<p>Don’t waste time applying everywhere.</p>

<p>Use your energy to build a portfolio that aligns with your goals.</p>

<p>You must understand the different sub-fields inside Data Science and choose what fits your background. For example:</p>

<ul>
<li><p><strong>Product Data Analyst / Scientist:</strong> product lifecycle and user needs</p></li>
<li><p><strong>Machine Learning Engineer:</strong> deploying models</p></li>
<li><p><strong>GenAI Engineer:</strong> works on LLMs</p></li>
<li><p><strong>Classic Data Scientist:</strong> inference and prediction</p></li>
</ul>


<p>If you look at a Product Data Scientist role at Meta, the technical level is often more adapted to most Data Scientists on the market compared to a Core AI Research Engineer or Senior Data Scientist role.</p>

<p>These roles are more realistic for someone without a PhD.</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025121203.png"></center>


<p><em>Screenshot taken by the author</em></p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025121204.png"></center>


<p>Even if you don’t want to work at GAFAM, keep in mind:</p>

<p><strong>They set the direction. What they require today becomes the norm everywhere else tomorrow.</strong></p>

<h2>Now, how about coding and math in 2026?</h2>

<p>Source: <a href="https://pixabay.com/photos/heart-couple-together-1731755/">pixabay</a> (NoName_13)</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025121205.jpeg"></center>


<p>Here is a controversial but honest truth for 2026: <strong>Analytical and mathematical skills matter more than just coding.</strong></p>

<p>Why? Almost every company now uses AI tools to help write code. But AI cannot replace your ability to:</p>

<ul>
<li><p>understand trends</p></li>
<li><p>explain where the value comes from</p></li>
<li><p>design a valid experiment</p></li>
<li><p>interpret a model in a real context</p></li>
</ul>


<p>Coding is still important, but you cannot be a “General Importer”—someone who only imports sklearn and runs .fit() and .predict().</p>

<p>Very soon, an AI agent may do that part for us.</p>

<p>But your math and analytical skills are still important, and will always be.</p>

<p>A simple example:</p>

<p>You can ask an AI: <em>“Explain PCA like I’m 2 years old.”</em></p>

<p>But your real value as a Data Scientist comes when you ask something like:</p>

<p>“I need to optimize the water production of my company in a specific region. This region is facing issues that make the network unavailable in specific patterns. I have hundreds of features about this state of the network. How can I use PCA and be sure the most important variables are represented in the PC I’m using?”</p>

<p>-> This human context is your value.</p>

<p>-> AI writes the code.</p>

<p>-> You bring the logic.</p>

<h2>And how about the Data Science toolbox?</h2>

<p>Let’s start with Python. As a programming language with a large data community, Python is still essential and probably the first language to learn as a future Data Scientist.</p>

<p>The same for Scikit-learn, a classic library for machine learning tasks.</p>

<p><em>Screenshot taken by the author</em></p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025121206.png"></center>


<p>We can also see on Google Trends (late 2025) that:</p>

<ul>
<li><p>PyTorch is now more popular than TensorFlow</p></li>
<li><p>GenAI integration is growing much faster than classical libraries</p></li>
<li><p>Data Analyst interest stays stable</p></li>
<li><p>Data Engineer and AI Specialist roles interested more people than general Data Scientist roles</p></li>
</ul>


<p>Don’t ignore these patterns; they are very helpful for making decisions.</p>

<p>You need to stay flexible.</p>

<p><em>If the market wants PyTorch and GenAI, don’t stay stuck with only Keras and old NLP.</em></p>

<h2>And what about the new stack for 2026?</h2>

<p>This is where the 2026 roadmap is different from 2020.</p>

<p>To get hired today, you need to be <strong>production-ready</strong>.</p>

<p><strong>Version Control (Git):</strong> You will use it daily. And to be honest, this is one of the first skills you need to learn at the beginning. It helps you organize your projects and everything you learn.</p>

<p>Whether you are starting a Master’s program or beginning a bootcamp, please don’t forget to create your first GitHub repository and learn a few basic commands before going further.</p>

<p><strong>AutoML:</strong> Understand how it works and when to use it. Some companies use AutoML tools, especially for Data Scientists who are more product-oriented.</p>

<p>The tool I have in mind, and that you can access for free, is <strong>Dataiku</strong>. They have a great academy with free certifications. It is one of the AutoML tools that has exploded in the market in the last two years.</p>

<p>If you don’t know what AutoML is: it is a tool that lets you build ML models <em>without coding</em>. Yes, it exists.</p>

<p>Remember what I said earlier about coding? This is one of the reasons why other skills are becoming more important, especially if you are a product-oriented Data Scientist.</p>

<p><strong>MLOps:</strong> Notebooks are not enough anymore. This applies to everyone. Notebooks are good for exploration, but if at some point you need to deploy your model in production, you must learn other tools.</p>

<p>And even if you don’t like data engineering, you still need to understand these tools so you can communicate with data engineers and work together.</p>

<p>When I talk about this, I think about tools like <strong>Docker</strong> (<a href="https://medium.com/code-like-a-girl/mlops-fundamentals-a-beginners-guide-to-docker-containerization-4f5a984e7117">check out my article</a>), <strong>MLflow</strong> (<a href="https://towardsdatascience.com/model-management-with-mlflow-azure-and-docker-2920b51a5bdd/">link here</a>), and <strong>FastAPI</strong>.</p>

<p><strong>LLMs and RAG:</strong> You don’t need to be an expert, but you should know the basics: how the LangChain API works, how to train a small language model, what RAG means, and how to implement it. This will really help you stand out in the market and maybe move further if you need to build a project that involves an AI Agent.</p>

<h2>Portfolio: Quality over quantity</h2>

<p>In this fast and competitive market, how can you prove you can do the job? I remember I’ve written an article about how to create a portfolio 2 years ago and what I’m going to say here can look a bit contradictory, but let me explain. Before ChatGPT and AI tools flooded the market, having a portfolio with a bunch of projects to show your different skills like data cleaning and data processing was very important, but today all these basic steps are often done using AI tools that are ready for that, so we will focus more on building something that will make you different and make the recruiter want to meet you.</p>

<p><strong><em>I would say: “Avoid burnout. Build smart.”</em></strong></p>

<p>Don’t think you need 10 projects. If you’re a student or a junior, one or two good projects are enough.</p>

<p>Take advantage of the time you have during your internship or your final bootcamp project to build it. Please don’t use simple Kaggle datasets. Look online: you can find a huge amount of real use-case data, or research datasets that are more often used in industry and labs to build new architectures.</p>

<p>If your goal is not to go deep into the technical side, you can still show other skills in your portfolio: slides, articles, explanations of how you thought about the business value, what results you got, and how these results can be used in reality. Your portfolio depends on the job you want.</p>

<ul>
<li><p>If your goal is more math-oriented, the recruiter will probably want to see your literature review and how you implemented the latest architecture on your data.</p></li>
<li><p>If you are more product-oriented, I would be more interested in your slides and how you interpret your ML results than in the quality of your code.</p></li>
<li><p>If you are more MLOps-oriented, the recruiter will look at how you deployed, monitored, and tracked your model in production.</p></li>
</ul>


<p>To finish, I want to remind you that the market is changing fast, but it is not the end of Data Science. It just means you need to be more aware of where you fit, what skills you want to grow, and how you present yourself.</p>

<p>Keep learning, and build a portfolio that truly reflects who you are. You will find your place</p>

<p>If you enjoyed this article, feel free to follow me on LinkedIn for more honest insights about AI, Data Science, and careers.</p>

<p> <strong>LinkedIn:</strong> <em>Sabrine Bendimerad</em></p>

<p> <strong>Medium:</strong> <a href="https://medium.com/@sabrine.bendimerad1">https://medium.com/@sabrine.bendimerad1</a></p>

<hr />

<h1><a href="https://medium.com/data-science-collective/whats-trending-in-data-science-and-ml-preparing-for-2026-7b6983b2866d">2. What's Trending in Data Science and ML Preparing for 2026</a></h1>

<p><em>Was it time well spent?</em></p>

<p>Last week, I attended the <em>Nordic DS/ML Summit 2025</em> in Stockholm 🇸🇪, a two-day event bringing together industry leaders, researchers, and practitioners to share how data science and machine learning are evolving.</p>

<p>The lineup included big names like Meta, NVIDIA, and Google DeepMind.</p>

<p>And of course, the hottest topic was….🥁… <strong>Agentic AI</strong> (no surprise there)<a href="https://substackcdn.com/image/fetch/$s_!SSnz!,f_auto,q_auto:good,fl_progressive:steep/https%3A%2F%2Fsubstack-post-media.s3.amazonaws.com%2Fpublic%2Fimages%2Fca699d07-a7ec-4eaa-9fc7-1a36f9bd3faf_3200x2408.jpeg"></a></p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025121210.jpeg"></center>


<p>Panel discussion on the topic of “Building the Future of AI: Scaling Foundation Models and Agents for Real-World Applications“. Image by NDSML.</p>

<p>This photo is from an interesting panel discussion featuring:</p>

<ul>
<li><p>Chi Wang, Senior Staff Research Scientist at Google DeepMind and Founder of AutoGen (now AG2)</p></li>
<li><p>Rohit Patel, Director at Meta Superintelligence Labs</p></li>
<li><p>Sepideh Pashami, Acting Director of Data Analysis at RISE Research Institutes of Sweden</p></li>
</ul>


<p><em>So I would say yes, it was definitely time well spent!</em></p>

<p>Today, I’m going to break down my learnings, the key trends I observed, and,<strong> towards the end,</strong> tie it all back to what we, Data Scientists, should pay attention to as we enter 2026.</p>

<p>Let’s get to it!</p>

<h1>Three key trends that stood out (to me) the most</h1>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025121211.jpeg"></center>


<p>Cartoon depicting the motivation behind AutoGen. Image by AutoGen.</p>

<p>These are the trends that stuck with me during the event, and that, in some way or another, have gained enough momentum to be worth following closely.</p>

<h3>1）. From traditional analytics to <em>agentic analytics</em></h3>

<p>We’re entering a new phase of analytics. One where dashboards and static reports aren’t enough anymore.</p>

<p><em>I foresee this will become an even bigger topic by the beginning of 2026</em>.</p>

<p>The focus will be on creating more dynamic systems that can speed up the path from data to insight, making analytics more adaptive and less dependent on manual exploration, which many are starting to refer to as Agentic Analytics.</p>

<blockquote><p>💡 One thing is clear, the companies who will lead the way in this Agentic Analytics shift, will be those who have the foresight to build a strong data engineering foundation and invest in their semantic modeling. That’s ultimately what will enable AI agents to interact with their data in meaningful ways. <em>More on this later in the article.</em></p></blockquote>

<h3>2）. Small language models are the next big thing</h3>

<p>Small language models (sLMs) are becoming surprisingly capable.</p>

<p>Models like <strong>Phi-3</strong>, <strong>Mistral</strong>, and <strong>Llama 3 8B</strong> show that you don’t need massive infrastructure to get strong performance. With some fine-tuning, they can even outperform larger models on focused tasks.</p>

<blockquote><p>💡 For developers and smaller teams, this also means we can now run fast, private, and low-cost models, on a regular laptop or even a phone.</p></blockquote>

<h3>3）. The rise of specialized multi-agent systems</h3>

<p>A big theme that kept coming up was the move toward <strong>hierarchical multi-agent systems</strong>. Instead of relying on a single agent to handle an entire workflow, newer architectures now use an <strong>orchestrator agent</strong> that breaks tasks into smaller pieces and delegates them to specialized sub-agents.</p>

<p>Each sub-agent focuses on one tiny, well-defined task, like cleaning data, summarizing findings, or generating code, and becomes extremely good at that single thing. Together, they form a coordinated system that’s faster, cheaper, and more reliable than one general-purpose agent working alone.</p>

<blockquote><p>💡 This “divide and conquer” approach also opens the door for <strong>sLMs</strong> to play a bigger role. Since each sub-agent only needs to handle a narrow task, even lightweight models can perform well when combined in a well-orchestrated system.</p></blockquote>

<p>It’s a design pattern we’ll likely see more of as agentic systems mature and move into production use.</p>

<h2>What should Data Scientists pay attention to?</h2>

<p>My recommendation, especially to mid-senior data scientists who want to take the next leap in their career growth: <strong>Lead the way in the Agentic Analytics shift at your company.</strong></p>

<p>From what I’ve seen, most organizations are still just waking up to this change.</p>

<p>That means you have a real opportunity to lead, whether it’s by championing modern enterprise tools that enable agentic analytics or by building your own agents that make analysis faster, more interactive, and closer to decision-making.</p>

<p>The people who learn to bridge that gap early, between AI agents and analytics, will be the ones shaping how Data Science is practiced in the next decade.</p>

<h3>Five real-world agentic AI use cases in Data Science</h3>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025121212.jpeg"></center>


<p>Diagram of how TimeGPT takes the historical values of the target values and additional exogenous variables as inputs to produce the forecasts. Image by TimeGPT.</p>

<p>These are some use cases where GenAI and Agentic AI are already starting to make an impact:</p>

<ol>
<li><p><strong>Conversational dashboards for fast insights</strong>Think dashboards you can talk to, ask questions in plain English, and get instant summaries or visualizations. Power BI Copilot and Tableau Pulse are early examples, but the concept applies to any workflow where non-technical users need fast answers from data.</p></li>
<li><p><strong>EDA and data cleaning agents</strong>AI agents are starting to automate the time-consuming work of detecting outliers, normalizing data, and generating initial visualizations during EDA. Tools like Tableau’s Data Pro hint at how you can build agents to accelerate your data prep process.</p></li>
<li><p><strong>Foundation models for analytics</strong>Instead of training a new model for every metric or product, foundation models like TimeGPT are starting to handle forecasting, anomaly detection, and other analytic tasks straight from raw data. This makes advanced analytics more accessible, even for teams without deep expertise in time series or model building.</p></li>
<li><p><strong>Agentic monitoring and proactive analytics</strong>Rather than waiting for a human to check a dashboard, agentic systems can watch KPIs, detect shifts, and trigger alerts or recommendations. Tableau Inspector and Adverity are pushing this, but it’s a pattern any analyst can explore with the right setup.</p></li>
<li><p><strong>Multi-agent orchestration of the ML workflow</strong>Platforms like causaLens are an example of what’s possible with AI agents that collaborate, some clean the data, others build models, and others explain results. It’s not just automation, it’s coordination, and it’s a glimpse of how future ML workflows could run.</p></li>
</ol>


<h3>🔑 Don’t ignore this: The Semantic Layer</h3>

<p>I wanted to mention this concept again since I feel it might get overlooked by a lot of you reading this article, and that would be a BIG mistake.<a href="https://substackcdn.com/image/fetch/$s_!mQOI!,f_auto,q_auto:good,fl_progressive:steep/https%3A%2F%2Fsubstack-post-media.s3.amazonaws.com%2Fpublic%2Fimages%2F6ed5744b-a2a4-4fc4-826f-e455a91c5818_960x498.png"></a></p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025121213.png"></center>


<p>Semantic Layer architecture. Image by Tallius.</p>

<p>Over the past 6 months or so, I’ve been spending more time building my own AI workflows to optimise and automate much of my data science work. And recently, I deployed a talk-to-your-data Slackbot that is slowly redefining the meaning of self-serve analytics at my company.</p>

<p>One of the keys to the success of these tools has been defining the semantic layer.</p>

<blockquote><p><em>📌 </em>This is also why I’m teaching it to the 22 Data Scientists currently enrolled in my</p></blockquote>

<p>The idea is simple: the semantic layer creates a shared definition of metrics and business logic so data scientists, stakeholders, and most importantly, AI agents, all work from the same source of truth.<a href="https://substackcdn.com/image/fetch/$s_!KeLe!,f_auto,q_auto:good,fl_progressive:steep/https%3A%2F%2Fsubstack-post-media.s3.amazonaws.com%2Fpublic%2Fimages%2Fc2b78cee-94e2-45fc-a05f-8803027a9b2d_1072x814.webp"></a></p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025121214.jpeg"></center>


<p>Example of a semantic layer YAML file. Image by dbt.</p>

<p>And believe me, you don’t need to be a data engineer to start building a semantic layer to enhance your AI agents.</p>

<p><a href="https://read.futureproofds.com/p/whats-trending-in-data-science-and-ml-2026#poll-395619"></a></p>

<p>POLL</p>

<h3>Should I write an entire article on this topic?</h3>

<p>Yes</p>

<p>96%</p>

<p>No, I’m good</p>

<p>4%</p>

<p>45 VOTES · POLL CLOSED</p>

<h2>Honorable mentions</h2>

<p>Here are some cool tools and products I didn’t get to cover, but thought deserved at least a quick mention:</p>

<ul>
<li><p>Orion: An on-device autonomous agent that orchestrates your AI tools and runs locally for cross-platform automation.</p></li>
<li><p>AG2: Open-source framework (formerly AutoGen) for building and orchestrating multi-agent systems.</p></li>
<li><p>HP ZGX Nan AI Station: A nano AI workstation for prototyping and fine-tuning inference models of up to 200B parameters locally.</p></li>
</ul>


<h1>Closing thoughts</h1>

<p>If there’s one thing this year’s summit made clear, it’s that the gap between <em>AI research</em> and <em>real-world data science</em> is closing faster than ever.</p>

<p>Agentic systems, small models, and better data foundations aren’t just trends; they’re signals that our field is evolving into something more intelligent.</p>

<p>For data scientists, this next phase isn’t about chasing every new tool that comes out. It’s about understanding the direction we’re heading and learning how to apply these ideas where they actually create impact.</p>

<p>If you do that, if you stay curious, experiment with these concepts, and think beyond your notebook, you won’t just keep up with the changes. You’ll help shape what data science looks like in 2026 and beyond.</p>

<h2>A couple of other great resources:</h2>

<ul>
<li><p><strong>🚀 Ready to take the next step? </strong>Build real</p></li>
<li><p>💼 <strong>Job searching?</strong></p></li>
<li><p><strong>🤖 Struggling to keep up with AI/ML?</strong></p></li>
</ul>


<p><em>Thank you for reading! I hope these insights give you a competitive advantage as we prepare to enter 2026.</em></p>

<p><em>- Andres Vourakis</em></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[GenRec综述论文笔记]]></title>
    <link href="https://ibillxia.github.io/blog/2025/06/18/gen-rec-servey-notes/"/>
    <updated>2025-06-18T09:51:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2025/06/18/gen-rec-servey-notes</id>
    <content type="html"><![CDATA[<p>论文链接：<a href="https://arxiv.org/abs/2409.15173">https://arxiv.org/abs/2409.15173</a></p>

<h1>book structure</h1>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025061801.webp"></center>




<!--more-->


<p>Advantages of recommender systems with generative models</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025061802.webp"></center>


<p>Outline of key techniques and publications in LLM-driven RSs</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025061803.webp"></center>


<h1>Chap4-LLM Rec</h1>

<h2>4.3 Encoder-only LLM Rec</h2>

<p><strong>encoder-only LLMs</strong> can be used in two main architectures: as dense retrievers (c.f. Sec 4.3.1) or as cross-encoders (c.f. Sec 4.3.2).</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025061804.webp"></center>


<p>Autoregressive LLM inputs are called prompts, which are sequences of tokens expressing a task such as top-k recommendation, rating prediction, or explanation generation.</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025061805.webp"></center>


<h2>4.5 Retrieval Augmented Rec</h2>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025061806.webp"></center>


<h2>4.6 LLMs Representation Generation</h2>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025061807.webp"></center>


<h2>4.7 Conversational Rec</h2>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025061808.webp"></center>


<h1>Chap5-Multi-modal GMs for Rec</h1>

<h2>5.1 Introduction</h2>

<p>Why?——item cold start problem, user request understanding, complex recsys scenes(virtual try-on, conversational shopping assistans)</p>

<p>Challenges——alignment，latent space learning</p>

<h2>5.2 Contrastive Multimodal Rec</h2>

<p>CLIP</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025061809.webp"></center>


<p>Align BEfore Fuse (ALBEF)</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025061810.webp"></center>


<h2>5.3 Generative Multimodal Rec</h2>

<p>GAN</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025061811.webp"></center>


<p>VAE</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025061812.webp"></center>


<p>Diffusion</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025061813.webp"></center>


<h2>5.4 Applications of Multimodal Rec</h2>

<ul>
<li><p>E-commerce</p></li>
<li><p>product visualization-virtual try on</p></li>
<li><p>Marketing-create personalized ad images and videos</p></li>
<li><p>Streaming services-Long and short-form video,music,audiobooks,podcasts,radio</p></li>
<li><p>Travel</p></li>
</ul>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[基于so-vits-svc的AI翻唱歌曲教程]]></title>
    <link href="https://ibillxia.github.io/blog/2025/03/16/music-cover-by-so-vits-svc/"/>
    <updated>2025-03-16T09:51:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2025/03/16/music-cover-by-so-vits-svc</id>
    <content type="html"><![CDATA[<p>原文链接：<a href="https://zhuanlan.zhihu.com/p/635429751">https://zhuanlan.zhihu.com/p/635429751</a></p>

<p>So-vits-svc(SoftVC VITS Singing Voice Conversion)是一款开源免费AI语音转换软件，最近大火的AI孙燕姿利用的也是这一技术：</p>

<p><a href="https://github.com/svc-develop-team/so-vits-svc">github.com/svc-develop-team/so-vits-svc</a></p>

<p>so-vits-svc可以通过学习一个人的声音，对另一首歌做音色替换。所需的样本量较少，且少量的训练时间就可以得到不错的效果。</p>

<p>但如果要真的跑so-vits-svc，步骤非常多，目前的门槛还是很高的。B站上有大神做了整合包，这个门槛会稍微低一点：</p>

<iframe src="//player.bilibili.com/player.html?isOutside=true&aid=695334884&bvid=BV1H24y187Ko&cid=1110252582&p=1" 
scrolling="no" border="0" frameborder="no" framespacing="0" allowfullscreen="true"
style="width: 100%; height: 500px; max-width: 900px;"></iframe>




<!--more-->


<p>但目前网络上关于so-vits-svc的教程，大多没有把从下载、数据准备、模型训练到最终歌曲生成的整个流程讲清楚。</p>

<p>这篇文章会讲述在Windows系统中，利用so-vits-svc进行声音克隆的完整过程。</p>

<p>在进入教程之前你需要满足几个条件：</p>

<ul>
<li>有N卡，且内存在6G以上，并安装了对应的CUDA环境</li>
<li>可以登录外网</li>
</ul>


<h1>一、下载so-vits-svc</h1>

<h2>1. 创建新的conda环境</h2>

<p>首先你需要在电脑上安装Anaconda，生成一个新的环境，方便不同Python包的版本管理：</p>

<pre><code>conda create --name so-vits-svc
</code></pre>

<p>激活该环境：</p>

<pre><code>conda activate so-vits-svc
</code></pre>

<h2>2. Git clone项目</h2>

<p>首先把该项目下载到本地</p>

<pre><code>git clone https://github.com/svc-develop-team/so-vits-svc.git
</code></pre>

<p>进入项目文件夹</p>

<pre><code>cd .\so-vits-svc\
</code></pre>

<h2>3. 安装所需的Python环境</h2>

<pre><code>pip install -r requirements_win.txt
</code></pre>

<p>注意：安装过程中，可能会有个别包安装失败。安装失败后，可以手动安装，例如我遇到了faiss, edge-tts, fairseq这三个包安装识别，然后自己手动通过pip安装：</p>

<pre><code>pip install faiss-cpu
pip install edge-tts
pip install fairseq
</code></pre>

<h2>4. 下载预训练模型文件</h2>

<p>下载地址：<a href="https://ibm.ent.box.com/s/z1wgl1stco8ffooyatzdwsqn2psd9lrr">checkpoint_best_legacy_500.pt | Powered by Box</a></p>

<p>下载得到checkpoint_best_legacy_500.pt文件，放到.\so-vits-svc\pretrain文件夹中。</p>

<h1>二、准备训练集</h1>

<p>so-vits-svc需要唱歌或者说话的音频文件作为训练集，这里以唱歌的音频为例。</p>

<p>准备训练集时，需要把人声提取出来，去掉和声和伴奏。然后把人声切成30秒以内的片段。</p>

<h2>1. 歌曲下载</h2>

<p>首先你需要下载一个人唱歌的音频，尽量下载无损音质(flac., wav.)，音频的数量建议在20首以上。</p>

<h2>2. 提取人声</h2>

<p>人声的提取需要用到Ultimate Vocal Remover，这是一个基于深度学习的人声、伴奏分离工具:</p>

<p><a href="https://github.com/Anjok07/ultimatevocalremovergui">github.com/Anjok07/ultimatevocalremovergui</a></p>

<p>GitHub上有下载地址：</p>

<p><a href="https://github.com/Anjok07/ultimatevocalremovergui/releases/download/v5.5.0/UVR_v5.5.1_setup.exe">https://github.com/Anjok07/ultimatevocalremovergui/releases/download/v5.5.0/UVR_v5.5.1_setup.exe</a></p>

<p>下载好Ultimate Vocal Remover，需要分别去除伴奏、和声，才能得到干净的人声：</p>

<h3>（1）去除伴奏</h3>

<p>去除伴奏利用MDX-Net：</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025031601.jpg"></center>


<h3>（2）去除和声</h3>

<p>把去除伴奏了的歌曲文件，利用VR Architecture去除和声：</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025031602.jpg"></center>


<p>如果左下角Choose VR Model中只有1_HP-UVR，可以通过Download More Models下载6_HP-Karaoke-UVR这个模型。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025031603.jpg"></center>


<p>最后就可以得到干净的人声了。</p>

<h2>3. 音频切片</h2>

<p>得到了干净的人声后，需要把音频切成30s以内的片段，且去除没有人声的片段，只保留有人声的片段。</p>

<p>音频切片需要用到开源的Audio Slicer工具：</p>

<p><a href="https://github.com/flutydeer/audio-slicer">github.com/flutydeer/audio-slicer</a></p>

<p>下载地址：<a href="https://github.com/flutydeer/audio-slicer/releases">Releases · flutydeer/audio-slicer</a></p>

<p>添加人声文件，设置好输出文件夹，采用默认设置就可以了：</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025031604.jpg"></center>


<p>最终得到的音频片段：</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025031605.jpg"></center>


<p>数据集准备好后，在.\so-vits-svc\dataset_raw文件夹中新建一个文件夹，命名为该音色的名字，例如：</p>

<blockquote><p>.\so-vits-svc\dataset_raw\jayzhou</p></blockquote>

<p>把所有声音片段放在都放在这个文件夹下。</p>

<h1>三、模型训练</h1>

<h2>1. 导入训练集</h2>

<p>确保激活了conda环境，且当前目录为.\so-vits-svc\，在command window中依次输入</p>

<pre><code>python resample.py
python preprocess_flist_config.py
python preprocess_hubert_f0.py
</code></pre>

<h2>2. 开始训练</h2>

<p>导入训练集后，可以开始训练</p>

<pre><code>python train.py -c configs/config.json -m 44k
</code></pre>

<p>如果成功开始训练，可以得到每个Epoch的时间、loss以及模型保存的信息：</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025031606.jpg"></center>


<p>建议至少训练1000个Epoch。</p>

<p>可以通过Ctrl+C停止训练。</p>

<h1>四、音色替换</h1>

<h2>1. 准备干净的人声以及伴奏</h2>

<p>so-vits-svc本质是对干净的人声进行音色替换，所以需要做替换的音频，也应该是干净的人声。</p>

<p>下载一首新的歌曲，重复提取人声那一部分，去除和声和伴奏。</p>

<p>去除伴奏的时候，不用勾选Vocal Only，这样就能得到歌曲的伴奏。在后面人声、伴奏混合时可以用到。</p>

<h2>2. 打开WebUI</h2>

<pre><code>python .\webUI.py
</code></pre>

<p>Running on local URL: http://127.0.0.1:7860</p>

<p>在浏览器中输入http://127.0.0.1:7860，可以打开WebUI：</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025031607.jpg"></center>


<h3>（1）加载模型</h3>

<p>首先导入模型文件，模型文件所在位置：.\so-vits-svc\logs\44k\G_xxxx.pth</p>

<p>然后导入配置文件，配置文件所在位置：.\so-vits-svc\configs\config.json</p>

<p>导入这两个文件后，加载模型，如果成功加载，可以得到音色名称：</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025031608.jpg"></center>


<h3>（2）导入歌曲并替换音色</h3>

<p>将刚才准备的干净人声导入，点击音频转换即可得到转换后的音频：</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025031609.jpg"></center>


<h1>五、人声伴奏混合</h1>

<p>得到转换后的人声音频后，需要把人声和伴奏混合，最简单的方法是利用格式工厂的音频混合功能：</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025031610.jpg"></center>


<p>这种方法不能条件人声和伴奏的比例，如果想要更好地去混音，可以利用AU等专业的音乐处理软件。</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[大模型入门指南：Transformer与Attention机制]]></title>
    <link href="https://ibillxia.github.io/blog/2025/01/22/transformer-attention-guide/"/>
    <updated>2025-01-22T16:51:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2025/01/22/transformer-attention-guide</id>
    <content type="html"><![CDATA[<p>原文链接：<a href="https://developer.aliyun.com/article/1655110">https://developer.aliyun.com/article/1655110</a></p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025012201.octet-stream"></center>


<p>阿里妹导读</p>

<p>本文档旨在详细阐述当前主流的大模型技术架构如Transformer架构。我们将从技术概述、架构介绍到具体模型实现等多个角度进行讲解。通过本文档，我们期望为读者提供一个全面的理解，帮助大家掌握大模型的工作原理，增强与客户沟通的技术基础。本文档适合对大模型感兴趣的人员阅读。</p>

<!--more-->


<h1>论文介绍</h1>

<p>论文名称：《Attention is all you need》</p>

<p>发布时间：2017/06/12</p>

<p>发布单位：Google、多伦多大学</p>

<p>简单摘要：所有LLM的始祖，迈向NLP新时代的基础架构</p>

<p>中文摘要：传统的序列转换模型使用复杂的循环或卷积神经网络，包括编码器和解码器。表现最好的模型会透过注意力机制连接编码器和解码器。</p>

<p>作者团队提出了一种新的简单网络结构，Transformer，完全基于注意力机制，不再使用循环和卷积。</p>

<p>在两个机器翻译任务上进行实验，发现这些模型在质量上的表现优越，并且更容易进行平行运算，训练所需时间明显减少。</p>

<p>该模型在WMT 2014年英德翻译任务上达到了28.4 BLEU，比现有最佳结果（包括整体模型）提高了超过2 BLEU。在WMT 2014年英法翻译任务中，模型在八个GPU上训练3.5天后，取得了新的单模型最佳BLEU分数41.8，训练成本仅为文献中最佳模型的一小部分。</p>

<p>展示出无论是在大量或有限的训练数据下，Transformer在其他任务中的泛化能力，成功应用于英语组成句分析。</p>

<p>论文链接：https://arxiv.org/pdf/1706.03762.pdf</p>

<p>核心技术：模型架构（此处先留下大体印象 encode+decode）</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025012202.webp"></center>


<h1>LLM 浅谈</h1>

<p>很多人认为大模型可以直接回答问题或参与对话，但实际上，它们的核心功能是根据输入的文本预测下一个可能出现的词汇，即“Token”。这种预测能力使得LLM在各种应用中表现出色，包括但不限于：</p>

<p>文本生成：LLM可以生成连贯且有意义的文本段落，用于写作辅助、内容创作等。</p>

<p>问答系统：通过理解问题的上下文，LLM能够生成准确的回答，广泛应用于智能客服和信息检索。</p>

<p>翻译：LLM可以根据上下文进行高质量的语言翻译，支持多语言交流。</p>

<p>文本摘要：LLM能够提取长文档的关键内容，生成简洁的摘要，方便快速理解。</p>

<p>对话系统：LLM可以模拟人类对话，提供自然流畅的互动体验，应用于聊天机器人和虚拟助手。</p>

<p>通过理解Token的概念，我们可以更好地掌握LLM的工作原理及其在实际应用中的强大能力。</p>

<h2>Token</h2>

<h3>样例</h3>

<p>谈到 token，不得不提到近期一个大模型被揪出来的低级错误“Strawberry里有几个 r”。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025012203.webp"></center>


<p>嘲笑之后，大家也冷静了下来，开始思考：低级错误背后的本质是什么？</p>

<p>大家普遍认为，是 Token 化（Tokenization）的锅。</p>

<p>在国内，Tokenization 经常被翻译成「分词」。这个翻译有一定的误导性，因为 Tokenization 里的 token 指的未必是词，也可以是标点符号、数字或者某个单词的一部分。比如，在 OpenAI 提供的一个工具中，我们可以看到，Strawberry 这个单词就被分为了 Str-aw-berry 三个 token。在这种情况下，你让 AI 大模型数单词里有几个 r，属实是为难它。</p>

<p>为了让大家直观地看到大模型眼里的文字世界，Karpathy特地写了一个小程序，用表情符号（emoji）来表示 token。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025012204.webp"></center>


<h3>尝试</h3>

<p>Token 是 LLM 理解的文本基本单位。虽然将 Token 看作单词很方便，但对 LLM 来说，目标是尽可能高效地编码文本。所以在许多情况下，Token 代表的字符序列比整个单词都要短或长。标点符号和空格也被表示为 Token，可能是单独或与其他字符组合表示。LLM 词汇中的每个 Token 都有一个唯一的标识符，通常是一个数字。LLM 使用分词器在常规文本字符串和等效的 Token 数列表之间进行转换。</p>

<pre><code>import tiktoken
# 获取适用于 GPT-2 模型的编码器
encoding = tiktoken.encoding_for_model("gpt-2")
# 编码示例句子
encoded_text = encoding.encode("A journey of a thousand miles begins with a single step.")print(encoded_text)
# 解码回原始文本
decoded_text = encoding.decode(encoded_text)print(decoded_text)
# 单个 token 的编码与解码
print(encoding.decode([32]))  # 'A'
print(encoding.decode([7002])) # ' journey'
print(encoding.decode([286]))   # 'of'
# 编码单词 "Payment"
payment_encoded = encoding.encode("thousand")
print(payment_encoded)
# 解码 "Payment" 的编码结果
print(encoding.decode([400])) # 'th'
print(encoding.decode([29910]))   # 'ousand'
[32, 7002, 286, 257, 7319, 4608, 6140, 351, 257, 2060, 2239, 13]
A journey of a thousand miles begins with a single step.
A 
journey 
of
[400, 29910]
th
ousand
</code></pre>

<h3>预测下一个 Token</h3>

<p>如上所述，给定一段文本，语言模型的任务是预测接下来的一个 Token。如果用 Python 伪代码来表示，这看起来就像这样：</p>

<pre><code>predictions = predict_next_token(['A', 'journey', 'of', 'a'])
</code></pre>

<p>这里predict_next_token 函数接收一个由用户提供的提示词转换成的一系列输入 Token。在本例中，我们假设每个单词都构成一个单独的 Token。实际上，每个 Token 都会被编码为一个数字，而非直接以文本形式传入模型。函数的输出是一个数据结构，其中包含了词汇表中每一个可能的 Token 出现在当前输入序列之后的概率值。</p>

<p>语言模型需要通过一个训练过程来学会做出这样的预测。训练过程中，模型会接触到大量的文本数据，从中学习语言模式和规则。训练完成后，模型就能够利用所学知识来估计任何给定 Token 序列之后可能出现的下一个 Token 的概率。</p>

<p>要生成连续的文本，模型需要反复调用自身，每次生成一个新的 Token 并将其加入到已有的序列中，直至达到预设的长度。下面是一段更详尽的 Python 伪代码，展示了这一过程：</p>

<pre><code>def generate_text(prompt, num_tokens, hyperparameters):   
tokens = tokenize(prompt)  # 将提示转换为 Token 列表    
for _ in range(num_tokens):  # 根据指定的 Token 数量重复执行      
predictions = predict_next_token(tokens)  # 获取下一个 Token 的预测       
next_token = select_next_token(predictions, hyperparameters)  # 根据概率选择下一个 Token       
tokens.append(next_token)  # 将选中的 Token 添加到列表中   
return ''.join(tokens)  # 将 Token 列表合并为字符串
# 辅助函数用于选择下一个 Token
def select_next_token(predictions, hyperparameters):  
# 使用温度（temperature）、top-k 或 top-p 等策略调整 Token 选择 
# 实现细节取决于具体的超参数设置    
pass
</code></pre>

<p>在这段代码中，<code>generate_text</code> 函数接受一个提示字符串、生成 Token 的数量以及一组超参数作为输入。<code>tokenize</code> 函数负责将提示转换成 Token 列表，而 <code>select_next_token</code> 函数则根据预测的概率分布选择下一个 Token。通过调整 <code>select_next_token</code> 中的超参数，比如温度（temperature）、top-k 和 top-p，可以控制生成文本的多样性和创造性。随着循环的不断迭代，新的 Token 不断被添加到序列中，最终形成了连贯的文本输出。</p>

<h2>模型训练</h2>

<p>想象一下，我们要培养一个模型来预测句子中的下一个词。这就像是玩一个猜词游戏，模型需要根据已经出现的词来猜测下一个词。简化的词汇表,我们假设只有五个词：</p>

<p>['I','you','love','oranges','grapes']</p>

<p>我们不考虑空格和标点，只关注这些词。</p>

<p>我们有三句话作为训练数据：</p>

<ul>
<li><p>I love oranges</p></li>
<li><p>I love grapes</p></li>
<li><p>you love oranges</p></li>
</ul>


<p>我们可以想象一个5x5的表格，表格中的每个格子代表一个词后面跟着另一个词的次数。这个表格可能看起来像这样：</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025012205.webp"></center>


<p>在这个例子中，“I”后面跟着“love”两次，而“love”后面跟着“oranges”一次，跟着“grapes”两次。为了使这个表格有用，我们需要将这些次数转换成概率，这样模型就可以预测下一个词的可能性。例如，“love”后面跟着“oranges”的概率是 1/3（大约33.3%），而跟着“grapes”的概率是 2/3（大约66.7%）。</p>

<p>但是，我们遇到了一个问题：“oranges”和“grapes”没有出现在其他词后面。这意味着，如果没有其他信息，模型将无法预测这两个词后面可能是什么。为了解决这个问题，我们可以假设每个词后面都可能跟着词汇表中的任何其他词，尽管这并不完美，但它确保了模型即使在训练数据有限的情况下也能做出预测。</p>

<p>在现实世界中，大型语言模型（LLM）使用了大量的训练数据，这减少了这种“漏洞”出现的机会。然而，由于某些词的组合在训练数据中出现得较少，LLM可能在某些情况下表现不佳，导致生成的文本虽然语法上正确，但逻辑上可能存在错误或不一致。这种现象有时被称为“模型幻觉”。</p>

<h2>上下文窗口</h2>

<p>在之前的讨论中，我们提到使用马尔可夫链训练小模型的方法，这种方法的局限在于它只依赖最后一个 Token 来预测下一个 Token。这意味着，任何出现在最后一个 Token 之前的文本都不会影响预测结果，因此这种解决方案的上下文窗口非常小，只有一个 Token。由于上下文窗口过小，模型容易“忘记”先前的信息，导致生成的文本缺乏一致性，从一个词跳跃到另一个词。</p>

<h2>改进上下文窗口</h2>

<p>为了提高模型的预测质量，可以尝试增加上下文窗口的大小。例如，如果使用两个 Token 的上下文窗口，就需要构建一个更大的概率矩阵，其中每一行代表所有可能的两个 Token 序列。对于五个 Token 的词汇表，这将新增 25 行（5<sup>2</sup>）。在每次调用 predict_next_token() 函数时，将使用输入的最后两个 Token 来查找概率表中的对应行。</p>

<p>然而，即使使用两个 Token 的上下文窗口，生成的文本仍可能缺乏连贯性。为了生成更加一致且有意义的文本，需要进一步增加上下文窗口的大小。例如，将上下文窗口增加到三个 Token 将使概率表的行数增加到 125 行（5<sup>3</sup>），但这仍然不足以生成高质量的文本。</p>

<p>随着上下文窗口的增大，概率表的大小呈指数级增长。以 GPT-2 模型为例，它使用了 1024 个 Token 的上下文窗口。如果按上文中使用马尔可夫链来实现这样一个大的上下文窗口，每行概率表都需要代表一个长度在 1 到 1024 个 Token 之间的序列。对于一个包含 5 个 Token 的词汇表，可能的序列数量为 5<sup>1024</sup>，这是一个天文数字。这个数字太大了，以至于无法实际存储和处理如此庞大的概率表。因此，马尔可夫链在处理大规模上下文窗口时存在严重的可扩展性问题。</p>

<h2>从马尔可夫链到神经网络</h2>

<p>显然，使用概率表的方法在处理大规模上下文窗口时不可行。我们需要一种更高效的方法来预测下一个 Token。这就是神经网络发挥作用的地方。神经网络是一种特殊的函数，它可以接受输入数据，对其进行一系列计算，然后输出预测结果。对于语言模型而言，输入是一系列 Token，输出是下一个 Token 的概率分布。</p>

<p>神经网络的关键在于其参数。这些参数在训练过程中逐渐调整，以优化模型的预测性能。训练过程涉及大量的数学运算，包括前向传播和反向传播。前向传播是指输入数据通过网络的各个层进行计算，生成预测结果；反向传播则是根据预测结果与真实标签之间的差异，调整网络的参数，以减小误差。</p>

<p>现代语言模型，如 GPT-2、GPT-3 和 GPT-4，使用了非常深的神经网络，拥有数亿甚至数万亿的参数。这些模型的训练过程非常复杂，通常需要数周甚至数月的时间。尽管如此，训练有素的 LLM 能够在生成文本时保持较高的连贯性和一致性，这得益于其强大的上下文理解和生成能力。</p>

<h2>Transformer 和注意力机制</h2>

<p>Transformer 是目前最流行的神经网络架构之一，特别适用于自然语言处理任务。Transformer 模型的核心特点是其注意力机制。注意力机制允许模型在处理输入序列时，动态地关注序列中的不同部分，从而更好地捕捉上下文信息。</p>

<p>注意力机制最初应用于机器翻译任务，目的是帮助模型识别输入序列中的关键信息。通过注意力机制，模型可以“关注”输入序列中的重要 Token，从而生成更准确的翻译结果。在语言生成任务中，注意力机制同样发挥了重要作用，使得模型能够在生成下一个 Token 时，综合考虑上下文窗口中的多个 Token，从而生成更加连贯和有意义的文本。</p>

<p>总结来说，虽然马尔可夫链提供了一种简单的文本生成方法，但其在处理大规模上下文窗口时存在明显的局限性。现代语言模型通过使用神经网络和注意力机制，克服了这些局限性，实现了高效且高质量的文本生成。</p>

<h1>Transformer 的输入</h1>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025012202.webp"></center>


<p>回到框架图，Transformer中单词的输入表示x由单词Embedding和位置Embedding （Positional Encoding）相加得到。</p>

<p>单词的 Embedding 有很多种方式可以获取，例如可以采用 Word2Vec、Glove 等算法预训练得到，也可以在 Transformer 中训练得到。</p>

<p>Transformer 中除了单词的 Embedding，还需要使用位置 Embedding 表示单词出现在句子中的位置。因为 Transformer 不采用 RNN 的结构，而是使用全局信息，不能利用单词的顺序信息，而这部分信息对于 NLP 来说非常重要。所以 Transformer 中使用位置 Embedding 保存单词在序列中的相对或绝对位置。</p>

<p>简而言之：以苹果为例</p>

<p>“水果店里有苹果，香蕉”中苹果代指水果，“商店里最新推出了苹果 16”中苹果代表品牌</p>

<h1>Self-Attention（自注意力机制）</h1>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025012206.webp"></center>


<p>上图是论文中 Transformer 的内部结构图，左侧为 Encoder block，右侧为 Decoder block。红色圈中的部分为 Multi-Head Attention，是由多个 Self-Attention组成的，可以看到 Encoder block 包含一个 Multi-Head Attention，而 Decoder block 包含两个 Multi-Head Attention (其中有一个用到 Masked)。Multi-Head Attention 上方还包括一个 Add &amp; Norm 层，Add 表示残差连接 (Residual Connection) 用于防止网络退化，Norm 表示 Layer Normalization，用于对每一层的激活值进行归一化。</p>

<p>因为 Self-Attention是 Transformer 的重点，所以我们重点关注 Multi-Head Attention 以及 Self-Attention，首先详细了解一下 Self-Attention 的内部逻辑。</p>

<p>当然，我们可以用更简洁的方式来理解Self-Attention机制。</p>

<h2>Self-Attention 简介</h2>

<p>Self-Attention 是一种允许模型在处理序列数据时关注不同部分的方法，特别适用于处理长文本。它通过计算序列中每个元素与其他所有元素之间的相关性来实现这一点。</p>

<h3>工作流程</h3>

<ol>
<li><p>转换：每个输入元素（比如一个词）都会被转换成三个向量：Query (查询)、Key (键) 和 Value (值)。这些向量是通过将输入向量分别乘以三个不同的权重矩阵WQ、WK 和 WV 得到的。</p></li>
<li><p>计算注意力分数：对于每个元素，使用它的 Query 向量与所有其他元素的 Key 向量进行点积运算，得到一个分数列表。这个分数表示当前元素与其他所有元素的相关性。</p></li>
<li><p>归一化：将这些分数通过 softmax 函数进行归一化，得到一个概率分布，表示当前元素对其他所有元素的注意力权重。</p></li>
<li><p>加权求和：使用这些注意力权重对所有元素的 Value 向量进行加权求和，得到最终的输出向量。</p></li>
</ol>


<h3>公式</h3>

<p>假设输入序列为 X=[x1,x2,...,xn]X，每个 xi 都是一个向量。</p>

<ul>
<li>转换：</li>
</ul>


<center><img src="https://ibillxia.github.io/images/2025/IMAG2025012207.webp"></center>


<ul>
<li>计算注意力分数：   </li>
</ul>


<center><img src="https://ibillxia.github.io/images/2025/IMAG2025012208.webp"></center>


<p>其中dk是Key向量的维度。</p>

<ul>
<li><p>归一化：Attention Weights=softmax(Scores)</p></li>
<li><p>加权求和：Output=Attention Weights⋅V</p></li>
</ul>


<h2>Multi-Head Attention</h2>

<ul>
<li>多头注意力（Multi-Head Attention）是为了让模型从多个不同的角度捕捉信息。具体做法是并行运行多个Self-Attention层（每个称为一个“头”），然后将所有头的输出拼接在一起，再通过一个线性变换。</li>
</ul>


<h2>总结</h2>

<ul>
<li><p>Self-Attention 让模型能够关注序列中的不同部分，从而更好地捕捉长距离依赖关系。</p></li>
<li><p>Multi-Head Attention 通过多个Self-Attention层增强模型的表达能力，使其能够从多个角度综合考虑信息。</p></li>
</ul>


<h1>Add &amp; Norm 和 Feed Forward</h1>

<h2>Add &amp; Norm (残差连接与层归一化)</h2>

<h3>残差连接 (Residual Connection)</h3>

<ul>
<li><p>作用：帮助模型更好地学习，防止训练过程中信息丢失。</p></li>
<li><p>方法：把输入直接加到输出上。</p></li>
</ul>


<h3>层归一化 (Layer Normalization)</h3>

<ul>
<li><p>作用：让数据更稳定，加快训练速度。</p></li>
<li><p>方法：把每个样本的特征值调整到一个标准范围内，通常是平均值为0，标准差为1。</p></li>
</ul>


<p>结合使用</p>

<ul>
<li><p>步骤：</p></li>
<li><p>先计算某一层的输出 F(x)F(x)。</p></li>
<li><p>把输入 xx 加到 F(x)F(x) 上，得到 y=F(x)+xy=F(x)+x。</p></li>
<li><p>对 yy 进行层归一化，得到最终的输出。</p></li>
</ul>


<h3>Feed Forward (前馈神经网络)</h3>

<p>作用</p>

<ul>
<li>增加非线性：让模型更灵活，能处理更复杂的数据。</li>
</ul>


<p>结构</p>

<ul>
<li><p>两层全连接网络：</p></li>
<li><p>第一层：把输入通过一个线性变换（乘以一个矩阵），然后用 ReLU 激活函数处理。</p></li>
<li><p>第二层：再通过一个线性变换（乘以另一个矩阵）。</p></li>
</ul>


<h3>总结</h3>

<ul>
<li><p>Add &amp; Norm：通过残差连接和层归一化，让模型更稳定，训练更快。</p></li>
<li><p>Feed Forward：通过两层全连接网络增加模型的灵活性，使其能处理更复杂的数据。</p></li>
</ul>


<h2>Decoder 结构</h2>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025012209.webp"></center>


<p>上图红色部分为 Transformer 的 Decoder block 结构，与 Encoder block 相似，但是存在一些区别：</p>

<ul>
<li><p>包含两个 Multi-Head Attention 层。</p></li>
<li><p>第一个 Multi-Head Attention 层采用了 Masked 操作。</p></li>
<li><p>第二个 Multi-Head Attention 层的K, V矩阵使用 Encoder 的编码信息矩阵C进行计算，而Q使用上一个 Decoder block 的输出计算。</p></li>
<li><p>最后由一个 Softmax 层计算下一个翻译单词的概率。</p></li>
</ul>


<h1>附</h1>

<pre><code># 导入必要的库
import torch
import torch.nn as nn
import torch.optim as optim
import math
# 定义位置编码层，用于为输入序列添加位置信息
class PositionalEncoding(nn.Module):   
def __init__(self, d_model, max_len=5000):       
super(PositionalEncoding, self).__init__()       
# 初始化位置编码张量       
pe = torch.zeros(max_len, d_model)       
position = torch.arange(0, max_len, dtype=torch.float).unsqueeze(1)      
div_term = torch.exp(torch.arange(0, d_model, 2).float() * (-math.log(10000.0) / d_model))        # 计算正弦和余弦值        pe[:, 0::2] = torch.sin(position * div_term)        pe[:, 1::2] = torch.cos(position * div_term)        pe = pe.unsqueeze(0).transpose(0, 1)        # 将位置编码注册为缓冲区        self.register_buffer('pe', pe)
    def forward(self, x):      
    # 将位置编码与输入相加      
    x = x + self.pe[:x.size(0), :]      
    return x
# 定义基于Transformer的模型类
class TransformerModel(nn.Module):   
def __init__(self, input_dim, output_dim, d_model=512, nhead=8, num_encoder_layers=6, dim_feedforward=2048, dropout=0.1):     
super(TransformerModel, self).__init__()       
self.model_type = 'Transformer'        
# 定义嵌入层        
self.embedding = nn.Embedding(input_dim, d_model)       
# 定义位置编码层       
self.pos_encoder = PositionalEncoding(d_model)       
# 定义编码器层      
encoder_layers = nn.TransformerEncoderLayer(d_model, nhead, dim_feedforward, dropout)     
self.transformer_encoder = nn.TransformerEncoder(encoder_layers, num_encoder_layers)     
self.d_model = d_model     
# 定义解码器层      
self.decoder = nn.Linear(d_model, output_dim)     
# 初始化权重       
self.init_weights()
    def init_weights(self):       
    initrange = 0.1       
    self.embedding.weight.data.uniform_(-initrange, initrange)     
    self.decoder.bias.data.zero_()      
    self.decoder.weight.data.uniform_(-initrange, initrange)
    def forward(self, src, src_mask):       
    # 嵌入输入并乘以根号下d_model       
    src = self.embedding(src) * math.sqrt(self.d_model)     
    src = self.pos_encoder(src)      
    # 编码输入       
    output = self.transformer_encoder(src, src_mask)    
    # 解码输出       
    output = self.decoder(output)      
    return output
# 生成一个上三角矩阵作为掩码def generate_square_subsequent_mask(sz):  
mask = (torch.triu(torch.ones(sz, sz)) == 1).transpose(0, 1)  
mask = mask.float().masked_fill(mask == 0, float('-inf')).masked_fill(mask == 1, float(0.0))   
return mask
# 示例用法
input_dim = 1000 # 词汇表大小
output_dim = 1000 # 输出维度
seq_length = 10  # 序列长度
# 创建模型实例
model = TransformerModel(input_dim=input_dim, output_dim=output_dim)
# 示例数据
src = torch.randint(0, input_dim, (seq_length, 32))  # (序列长度, 批次大小)
src_mask = generate_square_subsequent_mask(seq_length)
# 前向传播
output = model(src, src_mask)
print(output.shape)  # 预期输出: [序列长度, 批次大小, 输出维度]
# 定义简单的损失函数和优化器用于训练
criterion = nn.CrossEntropyLoss()
optimizer = optim.Adam(model.parameters(), lr=0.001)
# 示例训练循环
for epoch in range(10):  # 迭代次数  
optimizer.zero_grad()   
output = model(src, src_mask)   
loss = criterion(output.view(-1, output_dim), src.view(-1)) 
loss.backward()   
optimizer.step()
print(f"Epoch {epoch+1}, Loss: {loss.item()}")
</code></pre>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[【凉夏】推荐模型中的门控机制与动态权重]]></title>
    <link href="https://ibillxia.github.io/blog/2025/01/03/gating-mechanism-in-recommendation/"/>
    <updated>2025-01-03T10:33:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2025/01/03/gating-mechanism-in-recommendation</id>
    <content type="html"><![CDATA[<p>原文链接：<a href="https://zhuanlan.zhihu.com/p/701181424">https://zhuanlan.zhihu.com/p/701181424</a></p>

<p>大家好，我是凉夏同学。今天是我第一次在知乎分享我对算法模型的理解，有些生疏，还请大家多多指教。感兴趣的话，敬请关注公众号：凉夏的机器学习笔记。感恩~</p>

<p>今天要讲的是最近比较火的一种网络结构，可以近乎“无脑地”加入到网络结构当中，基本上都可以带来或多或少的效果提升。这个网络结构有人叫他门控网络、门控结构，也有人叫他的英文名“gating”，也有人叫他动态权重。</p>

<p>这篇文章，我会先讲一下我对门控机制的理解，然后会简单介绍几个使用门控机制的论文。</p>

<h1>门控的本质</h1>

<p>一直都觉得深度学习模型架构里，很多东西都是相同的，虽然很多时候他们有不同的名称、亦或是用在了不同的模型位置，但底层的思想和逻辑很可能是一致的。而我认为，门控网络结构的本质就是注意力机制。说到注意力机制，大家应该都不陌生，该结构出现在以DNN模型为基础的各种应用结构中，例如：transformer里面使用了multi-head attention（MHA）这种多头自注意力机制；用户行为序列建模DIN结构使用了target attention结构。</p>

<!--more-->


<p>那么注意力机制的本质又是什么呢？我的理解是：注意力机制就是自适应学习得到权重。所以相关结构可以再进行细分，得到三种关键元素： <br/>
- ①输入：使用哪些信息作为注意力的输入；
- ②结构：如何计算注意力权重；
- ③输出：得到了权重之后，我们要作用在哪里。</p>

<p>基本上，我们见到的任何注意力机制都可以归因到上面三个问题上。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010331.jpeg"></center>


<center>transformer中的MHA结构</center>


<p></br></p>

<p>拿我们举例子的transformer中的多头注意力机制来说。如果是encoder那侧，那么则是自注意力机制，输入是同一个序列的token经过矩阵转换之后的向量；注意力计算方式是Scaled（除以根号d）的内积再softmax；输出依旧作用在当前序列上。如果是DIN结构，那输入是target poi的embedding，计算方式则是简单的内积，输出在用户序列的每个seq poi的embedding上。</p>

<p>简单介绍完了注意力机制，说回来我们的门控。既然门控本质也是注意力机制，那么其三元素也是包括输入、结构、输出。只是相应的内容有一些变化，从而衍生出不同的网络结构和应用。下面介绍一些常见的。</p>

<h1>LHUC结构</h1>

<p>LHUC的全称是Learning Hidden Unit Contributions，最早语音识别领域所提出的，其核心的思想是：为每一个 speaker 定制一个振幅函数 (amplitude function)，作用于 DNN 的隐层之上。从公式上看非常直观：</p>

<p>其中 代表哈达玛积，也就是两个向量对应位置的元素相乘。其右边代表DNN某个隐藏层输出（因为大概形式就是f(w·x+b)嘛），其左边代表振幅函数的输出。虽说名字叫做振幅函数，但整个公式的形式看起来非常符合我们上文讨论的注意力机制的形式——生成了权重乘在隐藏层的输出上。其输入就是每个speaker的信息，结构就是浅层的DNN，输出作用在DNN的多个隐藏层上。其中有一个细节是，一般来说作为权重的DNN子结构会使用sigmoid作为激活函数，使得权重在0~1之间。而LHUC在sigmoid前面乘了个固定数字“2”，使得权重在0~2之间。带来的效果是：若权重在0~1之间，则是对原有隐藏层数值的抑制；若权重在1~2之间，则是对原有数值的放大。</p>

<h1>PPNet</h1>

<p>PPNet的全称是Parameter Personalized Net，是快手提出的模型架构，将上述语音领域的LHUC架构搬到了推荐领域，将每一个“speaker”当做是每一个用户，从而提升模型个性化效果。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010332.jpg"></center>


<center>快手PPNet网络架构</center>


<p></br></p>

<p>我们看下PPNet的门控结构的输入输出和网络结构。其输入的特征uid，pid，aid 分别表示 user id，photo id，author id，将这三个id输入门控网络得到全部其他隐藏层元素维度的权重，具体要作用在哪一层的时候，就直接将门控网络的输出维度设置成相同的即可。而在图的右上角我们可以看到门控网络的具体结构，是两层的MLP网络——第一层NN是以ReLU作为激活函数，第二层是sigmoid作为激活函数同时在输出之后和LHUC一样乘了“2”。PPNet 中的门控结构比较轻量化，而且很容易移植到工业界各个大厂的排序模型当中，因此在最近几年是一个比较火的网络子结构。</p>

<p>这里我们简单提一下两层MLP的好处。因为要输出全部隐藏层（尤其是第一层是全部特征embedding拼接之后的维度）元素维度的权重，因此参数量一般会比较大。而两层的MLP可以通过降低第一层NN的输出维度，使得门控结构“先降维、后升维”从而降低参数量。类似的思想在SENet，或者大模型SFT的lora结构都有所体现。</p>

<p>以上是门控机制在推荐模型中的一个应用实例。如前文所说，深度学习模型架构里很多东西都是相同的，后续会介绍更多使用了门控机制的网络架构，但本质上都和PPNet或者LHUC非常类似，其核心思想都是注意力机制，区别基本在于输入、输出和网络架构。</p>

<h1>PEPNet</h1>

<p>PEPNet全称是Parameter and Embedding Personalized Network，可以看到只在PPNet的基础上多了一个Embedding Personalized Network。体现在结构细节上，就是除了原先使用三个id特征做模型结构中间隐藏层的个性化之外，新增了使用场景特征对全部特征提前做一次个性化。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010333.jpg"></center>


<center>PEPNet</center>


<p></br></p>

<p>如图所示，右侧依旧是PPNet，只是左下角新增了使用场景特征计算全部特征的个性化。有一个小细节就是场景特征同时作为门控的输入和主网络的输入，也就是二者共享embedding的。在其作为门控输入的时候进行stop gradient，防止其梯度影响主网络。</p>

<h1>AdaSpase</h1>

<p>AdaSparse，阿里2022年的论文《Learning Adaptively Sparse Structures for Multi-Domain Click-Through Rate Prediction》。文章也是使用到了门控机制，只是输入输出有一些差异，以及其中结合“稀疏”的思想体现了一些创新的出发点。模型如图所示，右侧②是门控网络的结构，可以看到输入使用了整个隐藏层的向量拼接上场景特征向量一起作为输入；网络结构结合了不同稀疏效果的设计了Binarization、Scaling、Fusion三种方法；最终也是作为权重与当前隐藏层进行元素乘，只是由于不同方法的稀疏性不同，所产生的效果不同。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010334.jpg"></center>


<center>AdaSparse</center>


<p></br></p>

<p>AdaSparse将门控网络称之为”场景自适应的裁剪器“，其主要结构也是DNN，公式如下图：</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010335.webp"></center>


<p>第l层的隐藏层和相应的权重进行元素乘</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010336.png"></center>


<p>权重的输入是向量拼接后经过一层MLP和sigmoid，之后再输入到另一个函数S</p>

<p>其中S则代表了Binarization、Scaling、Fusion三种方法。Binarization是将输入的绝对值和阈值ε输入符号函数sign中，如果输入的绝对值大于ε，那么经符号函数输出1，反之输出0。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010337.jpeg"></center>


<p>Scaling方法则是：sigmoid之后乘上缩放因子β（类似于LHUC里面的”2“），再将符号函数的输出再乘上原来的值。大概思路是：经过符号函数变为1或者0，会损失信息，如果再乘上原来的值，不仅可以知道隐藏层对应位置的信息是否要保留（对应裁剪器同位置权重是否为1），还能知道权重的大小幅度。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010338.jpeg"></center>


<p>Fusion方法则是将上述两种思路结合起来，我们从公式上可以看出来。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010339.png"></center>


<p>以上是AdaSparse门控网络的几种形式，具体哪种效果好，我们可以通过实验来确定。那么其中的稀疏是如何得到的呢？论文中将是通过将L1正则项加入到loss中得到的，而这个loss会随着训练而慢慢增大，带来的效果就是训练初期参数比较稠密，最大程度保留信息；随着训练的进行，loss逐渐变大，从而得到稀疏权重。而稀疏的优点，论文中大概是说，可以提升泛化性——因为我们是将场景特征作为门控的输入的，也就是场景特征来控制哪些特征要保留——从而可以减少不同场景的互相影响，而提升泛化性吧。我个人是这么理解的。</p>

<h1>M2M</h1>

<p>出自阿里的论文《Leaving No One Behind: A Multi-Scenario Multi-Task Meta Learning Approach for Advertiser Modeling》，两个”M“，分别对应了多场景和多目标，其中多场景是使用门控机制来解决的，多目标使用的是传统的MMOE。而这里的门控网络并不是在输出权重作用在隐藏层上，而是通过Meta Unit模块，根据“场景”特征，动态生成权重，再reshape成一个MLP。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010340.jpg"></center>




<center>M2M的Meta Unit结构</center>


<p></br></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[【蘑菇先生】工业界大模型推荐实战经验梳理]]></title>
    <link href="https://ibillxia.github.io/blog/2025/01/03/llm-recommendation-industry-practice/"/>
    <updated>2025-01-03T10:31:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2025/01/03/llm-recommendation-industry-practice</id>
    <content type="html"><![CDATA[<p>原文链接：<a href="https://zhuanlan.zhihu.com/p/7387477310">https://zhuanlan.zhihu.com/p/7387477310</a></p>

<p>大家好，我是蘑菇先生。好久不见，今天盘点下2024年工业界大模型在搜广推上的实战落地型工作。业界工作大体分为两类：</p>

<ul>
<li><p>用大模型做数据和知识增强、提取表征、通过prompt将推荐转成对话驱动的任务等，本质上没有修改LLM，属于信息增强和补充方法，无法直接建模海量协同信号。</p></li>
<li><p>修改LLM直接建模搜广推海量数据中的协同信号，对输入输出范式改造，通过预训练/微调等过程建模海量数据，让模型同时拥有通用的世界知识和垂直领域海量协同信息。是能实现搜广推大模型scaling的前提，需要更复杂的工程架构支持。</p></li>
</ul>


<p>第一类工作层出不穷，大部分在工业界无法惊起水花，属于学术性的探索居多。第二类工作寥寥无几，需要资源算力和业务场景等支撑，但也是搜广推值得探索的前沿方向之一。本文重点分享第二类工作。本次分享4篇文章：GRs(Meta)、HLLMs(字节)、NoteLLM(小红书)、NoteLLM-2(小红书)等工作，基本都在各自场景验证了收益，最近又重读了下，常读常新，做一些梳理分享给大家。涵盖目前推荐系统两类主流范式：</p>

<ul>
<li><p>表征学习：NoteLLM\NoteLLM-2提供了一种使用LLMs训练item表征的范式，通过海量数据中的“共现”协同信号，应用对比学习目标来微调LLMs并提取多模态的item表征。学习的item表征可以用于I2I召回、排序特征提取器等。</p></li>
<li><p>模型预估：GRs和HLLMs通过改造输入数据为行为序列，在自回归设定下，可以建模海量用户行为，从而达到推荐的目的。二者既可以作为召回模型、也可以用作排序模型，用于CXR预估。但二者在输入、架构、目标方面存在较大差异，下文会介绍。</p></li>
</ul>


<!--more-->


<h1>Meta | GRs</h1>

<p>全称：Actions speak louder than words: Trillion-parameter sequential transducers for generative recommendations.</p>

<p>意义：为工业级别的搜广推应用中如何借鉴LLMs的能力指明了一条新的道路。</p>

<p>整体方案一言以蔽之，通过生成式框架来重新定义召回和排序任务。在该生成式框架下，改造召排任务对应的输入输出范式、损失目标建模范式，在Transfomers架构上融入推荐系统DIN/MoE/SENet等主流设计思路，并通过算力优化达到线上推理条件。模型的输入不是文本，而是推荐系统标准的ID/结构化稀疏型特征构成的序列，借鉴LLMs模型架构，但没有用到预训练权重，意味着需要海量推荐系统数据进行训练才可能达到传统推荐模型的效果。</p>

<p>更多技术细节参见：行动胜过言语: Meta落地工业界首个万亿级别参数的生成式推荐系统。本文主要补充更高阶的一些问题，也可参见：Meta生成式推荐模型GRs关键问题探讨。</p>

<p>有2类关键问题想讨论：</p>

<ul>
<li><p>如何用生成式框架来重新定义召回和排序任务，在该生成式框架下，如何改造输入输出范式、损失目标建模范式；</p></li>
<li><p>离线训练和在线推理阶段，输入输出范式存在什么差异？如何跨候选复用算力，做到更低时延？</p></li>
</ul>


<h2>关键问题1：如何用生成式框架来重新定义召回和排序任务？</h2>

<p>从传统sequential recommender问题出发，探讨GRs如何改造传统建模范式。</p>

<ul>
<li><p>Ignorance of features other than user-interacted items： 传统的序列推荐，通常只使用用户交互过的正样本item来构造序列。这里面包含两层含义，仅使用itemID来表征item、仅使用正反馈来构造序列。前者会忽略item的各类category语义信息、后者无法捕捉一些真实后验行为(如ctr)。GR通过两方面来分别解决这两个问题。a. 通过引入辅助信息并合并到主序列实现对稀疏类别特征的建模。b. 对全正负反馈超长序列的target attention来间接捕获数值统计型特征，各类后验行为均保留着，就能够通过attention近似捕捉真实后验，间接体现数值型统计特征的作用，泛化性也更好。通过实验，我们可以发现如果GRs模型仅在有交互的正样本itemID上学习，指标会下降非常多，即：2.6% NE的下降、1.3%的HitRate@100的下降。</p></li>
<li><p>User representations are computed in a target-independent setting： 用户的表征和待预测目标item之间互相独立，表达能力弱，这个主要是传统序列模型的问题，比如GRU4Rec、SASRec、BERT4Rec等，无法在输入侧进行交互，仅仅能通过输出侧的softmax来实现和目标item的弱交互。DLRMs中的DIN等模型，引入了target-attention机制来缓解该问题。GRs通过将action看做新的模态，交错插入item和action，能够实现在causal autoregressive的设定下，使用target-aware attention实现更强的交互能力。</p></li>
<li><p>Discriminative formulations restrict applicability of prior sequential recommender work to pointwise settings: 传统的序列推荐是判别式的，只能在pointwise的设定下发挥作用。这类工作本质上学习的是或者在给定用户状态下推荐下一个item的条件概率。但在真实的推荐系统中，实际上可以拆解为两类概率过程，a. 推荐next content (召回)，b. 用户对该content的反馈action (排序)。生成式方法是联合学习content和action，即：。个人认为这是生成式范式有可能表现更好的本质原因，能更加无损地建模原始数据分布，配合LLM的架构，可以容纳更多数据和信息。</p></li>
</ul>


<p>如下图所示：Next Action Token prediction对应ranking任务，自回归预测的是前一个item在action上的概率分布，也即该item的行为类型。Next content token prediction对应retrieval任务。自回归预测的是next item，即在item物料上的概率分布。还是非常优雅的，将召排任务统一成“生成任务”视角。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010301.jpg"></center>


<center>召排范式</center>


<p></br></p>

<p>模型对比图如下，左侧是传统的序列推荐模型，action只会在输入侧构建item表征时，作为额外的信息融入；输出端自回归的仍然是item本身；右侧是GR模型，输入端显式地将action也作为1种序列组成类型，输出端item和action均能够作为监督信号，来实现自回归预测。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010302.jpeg"></center>


<center>模型架构</center>


<p></br></p>

<p>此处召排本质上可以是统一的模型，如图中所示，在1个任务中联合建模和，此处拆成两个任务有一定取舍，可能是为了适配现有推荐系统链路。</p>

<p>除此之外，这一范式不仅能更好的建模数据分布，同时在可以通过beam search来生成item序列结果。这个相比于传统listwise的重排(如DPP和RL)，会更加有优势。也就是说，该方案还能充当重排层的角色，实现listwise的预测，这样就统一了召回、排序、重排，真正实现生成式推荐。</p>

<h2>关键问题2：排序任务训练和推理阶段的差异在哪，如何跨候选复用算力？</h2>

<p>训练： 下图是训练示意图，看示意图是个召排统一的GR模型，输出侧的监督信号包括了item和action。这个地方和前文描述的稍有出路。前文描述的排序任务输出侧监督信号主要是action，召回任务输出侧监督信号主要是item。但不影响理解。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010303.jpeg"></center>


<center>训练</center>


<p></br></p>

<p>推理：</p>

<p>排序在推理时，正常要对所有个候选都推理一遍。假设序列长度为，复杂度为。此处作者可以复用多个候选间的算力，一次性推理多个候选的打分(分batch)。</p>

<p>如下图所示：将batch为大小的候选items拼接到输入行为序列末尾，可一次性推理出每个item的预测action概率分布。多个target之间复用历史行为序列，因此历史行为序列理论上只需要1次推理即可，不同target item之间通过mask机制，阻断互相attention，这样就能够实现b_{m}个候选同时预估，同时实现候选item和历史行为item的self-attention。即时叠加多层self-attention注意力机制，由于是causal self-attention，历史行为items表征中不会耦合待预测的target items表征信息。上述操作后，复杂度为O((n+m)<sup>2</sup> d)=O(n<sup>2d</sup>)。</p>

<p>这样实现的效果等价于对b_m个候选分别拼接到历史行为序列中，各自做1次前向传播，一共做b_m次，这样会导致历史行为序列的计算冗余。改进后，在1次前向传播推理中同时预估b_m个候选，最大程度复用算力。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010304.jpeg"></center>


<center>推理</center>


<p></br></p>

<p>除了跨候选复用算力，在跨请求之间，通过KV caching机制，比如HSTU中的K(X)和V(X)可以在跨batch和跨请求之间复用。</p>

<p>总结一下：GRs将用户行为组织成序列行为，纳入了曝光/点击/交互等各类行为，将item和action都视作相同模态，作为序列的组成部分，每个item使用ID和结构化的稀疏特征来表示，抛弃了LLMs的预训练权重，通过海量数据自回归训练改造的Transformer架构HSTU。最终通过M-FALCON等算力优化手段实现线上推理。</p>

<p>更多技术细节参见：行动胜过言语: Meta落地工业界首个万亿级别参数的生成式推荐系统。 ‍</p>

<h1>字节 | HLLM</h1>

<p>全称：Enhancing Sequential Recommendations via Hierarchical Large Language Models for Item and User Modeling</p>

<p>意义：HLLM是国内第一个对标Meta的GRs工作，也是今年大模型推荐的大作。</p>

<p>文章主要探讨了3个方面的问题：① 发挥LLM的预训练权重价值：研究预训练大型语言模型（LLMs）中包含的世界知识和推理能力在推荐系统任务中的具体价值，这一点与GRs的不同，GRs用了LLMs的模型架构，但没有使用预训练的权重，意味着HLLMs的数据效率可能更高。② 推荐任务微调：探讨如何为推荐任务进行特定微调（fine-tuning）。③ 模型的可扩展性：验证大语言模型在推荐系统中是否同样具备scaling能力，即模型性能是否随着模型参数量的增加而提升。</p>

<p>为了解决这一难题，文章也同Meta GRs一样，将推荐任务形式化为“序列推荐”。并提出了层次化大语言模型，第一级是Item LLM，使用LLM从item的文本表述中提取丰富的语义信息，此处没有使用ID信息； 第二级是User LLM，利用第一级的item表征来建模用户行为序列，预测用户的未来兴趣，此处大概率只使用用户交互的正样本。思路比较清晰。</p>

<p>模型整体结构如下：</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010305.jpeg"></center>




<center>模型结构</center>


<p></br></p>

<h2>Item LLM</h2>

<p>本质上是使用LLM作为item侧的特征提取器，将丰富的文本信息压缩成1个item表征。主要改进就是在文本描述末尾新增特殊标记[ITEM]。然后过LLMs模型，将最后1层[ITEM]对应位置的hidden states作为item的表征。属于基本操作，在NoteLLM中也能见到。这个[ITEM]表征联同itemLLM的参数会在下游推荐任务中进行端到端微调。</p>

<h2>User LLM</h2>

<p>通过item表征，可以将用户行为ID序列U={I_1, I_2,...,I_n}转成表征序列 {E_1, E_2,...,E_n}，然后套一个LLMs，就能让item序列进行充分交叉。该LLMs的输入中把LLM预训练的word embedding丢掉，使用item LLM输出的item embedding替代，然后后续的Transformers建模序列结构保持一致。</p>

<h2>训练目标</h2>

<ul>
<li>生成式目标：和GRs或传统的序列推荐任务都比较像，自回归预测下一个item。和GRs的差异点包括两个方面：① 使用了预训练模型的权重。② User LLMs的输入是item LLM输出的表征(也即根源输入是text-input features)，而GRs主要是ID和结构化的稀疏特征。具体自回归预测时，使用了InfoNCE。</li>
</ul>


<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010306.png"></center>


<p>j是batch内的user编号下标，i是第j个user的序列中的第i个item，E<em>{j,i}是Item LLM输出的表征，\hat{E}</em>{j,i}是User LLM预测的表征。正样本是next item，负样本从batch内非该用户序列的其他样本中采样。</p>

<ul>
<li>判别式目标：判别式目标是给定用户表征、待预测的item表征，判断用户是否对该item感兴趣。这里头有2种建模方式，1种是把用户行为序列和item拼接起来过Transformers，这样能提前交互；另一种是使用LLMs作为User序列提取器，在末尾新增[USER]作为user表征，在输出端进行弱交互，可以复用行为序列表征计算过程，在线上推理时可跨候选间复用user表征，性能好很多。前者由于过早交互，行为序列和待预测的item信息耦合在一起，导致无法跨候选复用算力。此处采用logloss，应该也是基于用户序列来构造正负样本。</li>
</ul>


<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010307.png"></center>


<p>最后的损失如下，可以看到生成式目标实际是作为辅助损失的，主目标是LogLoss分类目标，和DIEN很像。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010308.png"></center>


<p>该目标训练的过程中，能够端到端联合训练Item LLM和user LLM。item LLM的产物[ITEM]对应的表征实际上还能用于其他推荐场景：如作为item表征输入到精排建模、用于召回等。</p>

<p>实验部分也是做的比较完备的，值得一看。包括几个部分：</p>

<ul>
<li>LLMs预训练权重的重要性。“使用预训练”意味着在这些预训练LLMs权重基础上进一步微调、"不使用预训练(Scratch)"意味着丢弃预训练权重，复用架构从头开始训练，就像GRs中的一样。因为HLLMs的输入是item侧文本，预训练权重信息肯定是有用的，毋庸置疑。另外文中也说明了预训练的tokens数量越大，重要性越高。</li>
</ul>


<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010309.jpeg"></center>


<ul>
<li>LLMs微调的重要性。使用生成式目标和判别式目标微调[ITEM]，[User]以及对应LLM的参数，也非常重要。例如：item侧不微调，固定参数，使用LLM输出的最后1层hidden states进行pooling作为item表征，结果非常差。user侧同理。这说明基于next token预测训练的LLMs如果作为特征提取器是非常糟糕的。</li>
</ul>


<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010310.jpeg"></center>


<ul>
<li>对比实验：SASRec和HSTU(GRs)是ID-based模型，HLLMs、LEARN是text-based模型。HLLMs效果上好不少。但是这个地方由于输入数据和建模方式差异，个人认为这么对比可能不是公平的，不能发挥出GRs的优势。下面总结的时候会再说明下原因。</li>
</ul>


<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010311.jpeg"></center>


<p>最后我们再聊聊HLLMs的A/B Test部分，也是满满细节：基座是TinyLlama-1.1B，训练了HLLM-1B。由于性能的考虑，总共分为了3个部分：</p>

<ul>
<li><p>Stage I: 端到端训练所有HLLMs参数：使用判别式损失训练Item LLM和User LLM。用户行为序列长度是150。</p></li>
<li><p>Stage II: 先用Stage I训练Item LLM来编码和存储item表征。通过检索item表征来进一步微调User LLM，只训练User LLMs，因此可以将序列长度从150拓到1000。</p></li>
<li><p>Stage III: HLLM参数固定，基于User LLM抽取user表征，联同Item LLM的表征和已经有的线上特征，喂给精排走流式学习。</p></li>
</ul>


<p>线上serving的时候，item表征在新item发布的时候就会抽取表征；user表征T+1例行化更新；U/I的embedding提前存储好，用于流式训练和serving，这种情况下线上耗时几乎没变。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010312.jpeg"></center>


<p>到此我们可以总结下HLLM和GRs的差异点：</p>

<ul>
<li><p>输入数据：GRs输入用户全周期正负反馈行为序列，数据量更大，主要基于item侧的ID和结构化稀疏特征进行学习；HLLMs输入用户正反馈行为序列，数据量小很多，主要基于item侧的文本描述，没有使用ID特征进行学习。</p></li>
<li><p>模型架构：由于输入的差异和数据量的差异，GRs可以使用LLMs架构从头预训练，将文本模态替换为ID/action进行端到端训练。也是真正意义上的端到端模型。HLLMs主要基于预训练LLMs，能够复用LLMs预训练权重蕴含的世界知识，需要遵循LLMs的文本模态输入，无法直接建模ID，转而添加【ITEM】，【User】来适配预训练LLMs并提炼关键信息，也就意味着微调非常重要。</p></li>
<li><p>训练目标：由于海量数据，GRs可以以自回归为主要目标进行训练，使用更大参数量的模型，其scaling的潜力更大，是能真正替代现有的DNN搜广推模型。HLLMs在传统判别式目标之上新增自回归目标，个人认为scaling的天花板是有限的，也无法真正替代现有的DNN，文中实验部分关于这块的介绍也是比较简单，说服力不足。</p></li>
<li><p>在线推理：late fusion的HLLMs类似双塔架构，通过解耦合item和user表征，复用user侧表征，但是交叉能力比较弱；GRs在双塔和完全体Transformers之间做了个折中，引入了action和item交错编排和target-attention机制，在交叉能力上是更好的，通过causal attention和候选之间mask机制也能实现跨候选间复用“用户历史行为表征”算力，但能达到更好的交叉能力。GRs的上线难度肯定是更大的。</p></li>
</ul>


<p>正是因为上述的差异，在相同数据集上对比HLLMs和HSTU可能不够公平，无法达到各自最优水平进行对比。</p>

<h1>小红书 | NoteLLM</h1>

<p>全称：A Retrievable Large Language Model for Note Recommendation。</p>

<p><strong>意义：第一个使用大模型做I2I推荐的工作。也是使用LLM兼容语义信息和协同信息，提升item embedding学习的代表性工作之一。</strong></p>

<p>小红书发表在WWW 2024上的工作，基于LLM的I2I推荐。本质上是通过LLM来学习笔记的表征，该表征能同时拥有大模型的语义世界知识，还能将小红书用户行为中的协同信号注入其中。其建模的关键假设是：notes frequently read together are likely related. 利用了共现笔记通常比较相似的假设，通过I2I相关笔记对比学习任务来提升item embedding的学习。除此之外，还引入了“标签/类目”生成任务来多目标学习。</p>

<p>整体架构图如下，自下而上：包含了笔记压缩prompt、相关笔记对构建、生成对比学习和标签/类目生成多目标学习、最后应用于I2I推荐、笔记hashtag生成、笔记类目打标等任务。整体工作流大致是：先通过prompt来构造LLMs的输入，同时预留着[EMB]占位符节点，用于I2I协同信号对比学习和标签/类目生成学习，这样就能让共现高的笔记的[EMB]相近，同时还能保留LLMs的语义信息。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010313.jpeg"></center>




<center>NoteLLM模型结构</center>


<p></br></p>

<h2>Note Compression Prompt</h2>

<p>首先是Note Compression Prompt：为每个笔记样本构建一个统一的笔记压缩prompt，然后通过预训练LLMs进行解码学习item表征。prompt格式如下，[EMB]占位符就是学习的目标笔记表征。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010314.png"></center>


<p>对于类目生成任务，引入了类目$c_i$等监督信号。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010315.jpeg"></center>


<p>对于hashtag标签生成任务，引入了hashtags $tp_i$的监督信号。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010316.jpeg"></center>


<p>通过上述步骤装配模版成文本tokens，通过LLMs得到上述tokens的表征序列，[EMB]处的表征会作为笔记表征，通过下面的GCL和CSFT进行预训练。</p>

<h2>GCL</h2>

<p>全称生成对比学习。为了引入海量数据协同信号，作者构建了相关笔记对，计算所有笔记对的用户行为共现得分，并为每个笔记形成共现得分集合。并选择集合中共现得分最高的笔记作为给定笔记的相关笔记。计算相似笔记的公式借鉴了类似swing的思路：即：</p>

<p>$s<em>{n_A→n_B}=\sum</em>{i}^N\frac{1}{N_i}$</p>

<p>接着通过生成对比学习（GCL）使用上述压缩标记作为每个笔记的表征，并训练LLMs从in-batch的负样本中识别出相关的正样本笔记。本质是利用共现笔记中的“协同信号”，来提升这个[EMB]的学习质量，让相似的note的EMB相近。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010317.png"></center>


<h2>CSFT</h2>

<p>全称协同监督微调。笔记的category和hashtags有一些监督信号，可以用于多任务学习，使用自回归生成损失建模即可。具体而言，作者采用了一种协同监督微调（CSFT）方法来训练模型为每个笔记生成标签/分类。由于I2I笔记推荐任务和标签/分类生成任务中学习到的压缩标记都旨在提取笔记内容的关键概念，CSFT可以有效增强笔记嵌入。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010318.jpeg"></center>


<p>最后，训练好的模型可以同时做I2I向量检索、类目打标、话题生成等任务。</p>

<p>从实验部分来看，</p>

<ul>
<li><p>LLaMA 2 zero-shot：直接提取item的表征，不使用任何prompt；</p></li>
<li><p>PromptEOL zero-shot：zero-shot的sentence embedding方法，使用了one-word limitation prompt.</p></li>
<li><p>SentenceBERT：基于共现笔记对比学习来学习笔记相似性，也是小红书线上的基线。</p></li>
<li><p>PromptEOL+CSE：PromptEOL基础上基于共现笔记对比学习来训练LLM。</p></li>
<li><p>RepLLaMA：使用LLM作为双编码器的向量检索模型，类似双塔模型训练，不使用任何any prompts.</p></li>
<li><p>NoteLLM：本文方法。</p></li>
</ul>


<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010319.jpeg"></center>


<h1>小红书 | NoteLLM-2</h1>

<p>全称：Multimodal Large Representation Models for Recommendation。</p>

<p>意义：第一个<strong>直接使用LLM来提升推荐系统多模态表征质量</strong>的工作，发挥LLMs和视觉Encoder各自的优势。</p>

<p>同样出自小红书的工作，提出了一个端到端训练方法，能够方便地定制集成任何现有的LLMs和视觉Encoder，以构建高效的多模态表示模型（Multimodal Large Representation Models, MLRMs）。</p>

<p>NoteLLM的Item表征主要建模文本信息，这篇工作在文本表征之外，重点建模了笔记的多模态信息，并用于多模态的I2I推荐。文章重点探讨了如何充分发挥LLM的文本理解能力，来弥补传统多模态模型对文本信息的忽视，取长补短。</p>

<p>具体而言，在I2I推荐任务上，提出了一种端到端训练方法，和NoteLLM一样，使用I2I共现笔记对的对比学习，来增强多模态表征的学习。所以乍一看就是在note compression prompt设计上如何引入图像表征来形成多模态表征。方法也很简单，</p>

<p>在NoteLLM的prompt基础上引入了图像表征占位。是placeholder, 后面会使用从原始图像中提取的vision embedding代替。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010320.png"></center>


<p>具体而言，先使用上述prompt过tokenizer后得到text embedding。然后text embedding中占位符对应的word embedding使用vision encoder(例如CLIP/BLIP等)经过Connector(MLP层)得到的visual embedding来替换，形成多模态表征序列。然后使用LLMs处理该多模态表征序列，得到最后的last hidden states作为note的多模态表征。然后使用和NoteLLM一样的共现item pairs进行对比学习预训练。如下所示，和NoteLLM是一样的。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010321.jpeg"></center>


<p>但这个简单的改造会面临的问题在于：LLMs在端到端训练中倾向于忽视图像信息，被文本信息主导。为此，文章提出2种方法：包括：① 从prompt的视角区分“文本信息”和“图像信息”，其实就是优化prompt，称为mICL(multimodal in-context learning)；② 在LLM接入下游I2I任务的输出端引入late fusion机制，在结构上显式地让原始图像信息发挥更大作用。整体架构图如下：</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010322.jpeg"></center>


<h2>mICL</h2>

<p>首先是mICL，将prompt改造成如下格式，和NoteLLM的主要差异点是接入下游的时候有2个表征节点，新增的，因为Causal attention机制，这个节点仅提炼了前面的图像表征信息n_i<sup>v</sup>；另一个是整个prompt节点，即prompt尾巴处，提取了文本和图像多模态表征n_i<sup>m</sup>。这两个表征在下游I2I建模时都会发挥作用，以保留图像的信息。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010323.png"></center>


<h2>Late Fusion</h2>

<p>接着是Late Fusion机制：为图像表征和多模态表征都设置了门控融合机制，来自适应融合原始vision encoder的输出v和上述图像表征和多模态表征。例如图像表征如下，其中v是visual encoder(如BLIP/CLIP等)的原始表征，n_i<sup>v</sup>是上述经过LLM后的图像表征。二者通过门控机制融合得到\hat{n}_i^{v}。同理，多模态表征节点n_i<sup>m</sup>，也和v进行门控融合。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010324.jpeg"></center>


<p>图像表征节点和多模态表征节点，分别过对比学习损失来建模。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010325.jpeg"></center>




<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010326.png"></center>




<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010327.png"></center>


<p>从实验部分，</p>

<ul>
<li><p>MTomato-Base：MTomato是小红书自研的基于LLaMA 2的LLM，配合vision encoder CLIP ViT-B进行实验。</p></li>
<li><p>MQwen-Base：使用Qwen Chat作为LLM，vision encoder也是用的CLIP ViT-B进行实验。</p></li>
<li><p>MQwen-bigG：使用Qwen Chat作为LLM，vision encoder替换为ViT-bigG。</p></li>
</ul>


<p>LLM大部分都是采用7B左右的，效果来看 MQwen-bigG>MQwen-Base>MTomato-Base，说明LLM和vision encoder都很重要。mICL和late fusion通过放大vision encoder的作用，都能起到一定作用。</p>

<center><img src="https://ibillxia.github.io/images/2025/IMAG2025010328.jpeg"></center>


<p>NoteLLM-2和NoteLLM的差异：</p>

<ul>
<li><p>prompt组织微调任务的输入：NoteLLM引入了[EMB]特殊标记来提炼文本模态表征、NoteLLM-2引入了[IMG-EMB]图像模态和整体多模态特殊标记来提炼图像表征和多模态表征，这些特殊标记会用于下游推荐系统任务微调；</p></li>
<li><p>协同信号微调：微调任务都使用了共现笔记对，配合生成对比学习目标进行训练。此外，NoteLLM引入了话题标签和类目生成辅助目标共同引导[EMB]的学习；NoteLLM-2专注LLM和视觉编码器的协同，基于上述两个特殊标记引入两个Loss，通过late fusion机制优化LLMs对视觉信息的关注。</p></li>
</ul>


<h1>总结</h1>

<p>基于上述工作，我们可以大致窥探出LLMs在搜广推上应用的范式：</p>

<ul>
<li><p>借鉴LLMs的架构，不用LLMs的预训练权重，此时尽可能保留原始海量的曝光日志数据，通过组织输入样式和ID类特征，自回归的训练大模型，有可能达到推荐系统领域的scaling，这种方法个人认为短期来说上限更高，但难度更大，对应用场景、用户行为频次、供给丰富性等，都有很高的要求，现阶段能真正能落地的很少。代表性工作是GRs。</p></li>
<li><p>借鉴LLMs的架构和预训练权重，输入模态以文本为主，无法直接输入ID等，需要下游推荐任务的微调才能真正发挥价值。由于有预训练权重的存在，数据效率更高，如何设计好的下游微调任务成为了成败的关键。为了达到更好的微调，这里面需要做两个方面工作：① 输入数据如何适配LLMs；② 用什么目标来微调。借鉴字节和小红书等内容化平台的经验，输入数据适配通常配合prompt+特殊标记，通过特殊标记作为预训练LLMs和下游推荐任务的桥梁；在微调目标选择上，要么有显式的共现关系，类似NoteLLM和NoteLLM-2中的共现item对，通过对比学习微调；要么直接基于自回归目标，如HLLMs中结合判别式和生成式自回归目标。i2i方法有一定信息损失，在I2I推荐任务中可能表现不错，但非通用方法。在数据规模足够的场景下，自回归方法更加通用一些，适配的任务可能更多。</p></li>
</ul>


<h1>References</h1>

<p>GRs | Zhai J, Liao L, Liu X, et al. Actions speak louder than words: Trillion-parameter sequential transducers for generative recommendations[J]. arXiv preprint arXiv:2402.17152, 2024.</p>

<p>HLLMs | Chen J, Chi L, Peng B, et al. HLLM: Enhancing Sequential Recommendations via Hierarchical Large Language Models for Item and User Modeling[J]. arXiv preprint arXiv:2409.12740, 2024.</p>

<p>NoteLLM | Zhang C, Wu S, Zhang H, et al. NoteLLM: A Retrievable Large Language Model for Note Recommendation[C]//Companion Proceedings of the ACM on Web Conference 2024. 2024: 170-179.</p>

<p>NoteLLM-2 | Zhang C, Zhang H, Wu S, et al. NoteLLM-2: Multimodal Large Representation Models for Recommendation[J]. arXiv preprint arXiv:2405.16789, 2024. ‍</p>

<p>‍</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Hulu视频广告系统中的算法实践]]></title>
    <link href="https://ibillxia.github.io/blog/2024/07/19/hulu-video-ads-algorithms/"/>
    <updated>2024-07-19T14:40:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2024/07/19/hulu-video-ads-algorithms</id>
    <content type="html"><![CDATA[<p>原文链接：<a href="https://www.infoq.cn/article/ko9yxouspj4kuoh9jut4">https://www.infoq.cn/article/ko9yxouspj4kuoh9jut4</a></p>

<h1>概述</h1>

<h2>Hulu 的广告产品</h2>

<p>首先介绍一下 Hulu 以及 Hulu 的广告产品形态。</p>

<p>Hulu 是一家美国的提供专业视频点播与直播的服务平台，成立于 2006 年，目前由迪士尼控股，拥有近 3000 万付费订阅用户，是全美用户数量增长最快的流媒体平台。</p>

<p>① 产品形态：点播，直播，回看等；</p>

<p>② 广告形态：以视频流中的 15 秒、30 秒的视频广告短片为主；</p>

<p>③ 广告特点：</p>

<ul>
<li><p>以品牌类广告为主，品牌类广告看重长期效果，品牌的曝光度，用户认知等等；</p></li>
<li><p>采用 CPM ( Cost Per Mille，千次展示 ) 方式计费，即无论用户是否点击，按照展示次数计价；</p></li>
<li><p>以担保式广告订单为主，广告主希望其品牌曝光有一定量的保障，例如，某品牌希望在指定地区给指定用户群投放指定的次数；</p></li>
<li><p>质量要求非常高，包括清晰度、创意、制作的精良程度等等。</p></li>
</ul>


<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071901.png"></center>




<!--more-->


<h2>核心算法问题</h2>

<p>上面简单介绍了 Hulu 和 Hulu 的广告产品形态，接下来介绍下广告系统中我们主要面对的核心算法问题有哪些，以及算法发挥的主要作用。</p>

<p><strong>广告生态系统有三个核心要素：广告主，用户，媒体方 ( Hulu )。每个要素都有自己的优化目标。</strong></p>

<h3>广告主：</h3>

<p>广告主的核心优化目标是广告的投放效率、有效性，以及 ROAS ( Return-On-Ad-Spend，广告支出回报率 )，算法的主要应用为：</p>

<p>① 广告定向：使用机器学习算法进行受众定向、基于上下文的定向、lookalike 定向等；</p>

<p>② 订单及价格优化：给广告主一些订单定向条件及价格的建议，使其在 Hulu 上更好的进行订单的购买和投放；</p>

<p>③ 转化率优化：越来越多的品牌类的广告主也开始在意短期或者长期的转化率，使用机器学习方法提高转化率，把正确的广告投给正确的用户；</p>

<h3>用户：</h3>

<p>对于用户的核心优化目标是优化用户体验，尽可能减少广告对用户的干扰，给用户更有连贯性的观看体验，算法的主要应用为：</p>

<p>① 个性化广告：给用户推荐更感兴趣，更相关的广告</p>

<p>② 情景式广告：使用 AI 技术进行图像音频的识别、广告创意的生成，为用户带来浸入式广告体验</p>

<p>③ 个性化频次控制：为用户提供个性化频次控制，避免重复及过量广告为用户带来的反感</p>

<h3>媒体方（Hulu）：</h3>

<p>最终的优化目标：广告收入最大化，吸引更多的广告商和用户；与此同时我们也会进行生产环境的工作效率优化，算法的主要应用为：</p>

<p>① 库存预估：这是在担保式广告里面是一个必不可少的环节，在售卖广告以及做广告预算时发挥重要作用</p>

<p>② 广告流量匹配：对广告订单与用户流量进行匹配，保证担保式广告能够按时按量且均匀的进行投放</p>

<p>③ 定价策略优化：根据流量的热门程度及售卖压力，进行动态的定价，让整个系统得到一个收入的最大化。</p>

<p>下图总结了在 Hulu 的视频广告系统中的 <strong>核心算法问题</strong>，也是我们研究员每天在做的事情：</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071902.png"></center>


<p>① 广告定向，涉及到：</p>

<ul>
<li><p>预估模型：例如用户标签的补全，构建 lookalike 模型等等</p></li>
<li><p>主题模型：使用无监督方式挖掘用户属性</p></li>
<li><p>图像识别技术：识别视频内容中物体、场景、氛围等等，投放上下文相关视频广告</p></li>
</ul>


<p>② 库存预估，涉及到：</p>

<ul>
<li>时序预测算法：库存预估一般被建模成时序预测问题</li>
</ul>


<p>③ 流量匹配，涉及到：</p>

<ul>
<li><p>凸优化：使用凸优化进行离线流量匹配</p></li>
<li><p>自动化控制理论 ( PID Controller )：使用自动化控制理论进行线上动态调整</p></li>
</ul>


<p>④ 转化率优化，涉及到：</p>

<ul>
<li><p>CVR 预估模型</p></li>
<li><p>Casual Inference ( 因果推断 )：使用因果推断，得到一个更加公正、有效的指标来评估转化是否有效。</p></li>
<li><p>Bandit/增强学习：使用 Bandit 以及增强学习的方式来解决广告冷启动的问题</p></li>
</ul>


<p>⑤ 程序化交易广告，涉及到：</p>

<ul>
<li><p>RTB ( Real time bidding ) 策略研究</p></li>
<li><p>流量预估/CVR 预估/投放节奏控制</p></li>
</ul>


<p>⑥ 用户/广告体验分析，涉及到：</p>

<ul>
<li><p>统计理论</p></li>
<li><p>Casual Inference ( 因果推断 )</p></li>
</ul>


<p>⑦ 价格机制设计，涉及到：</p>

<ul>
<li><p>博弈问题</p></li>
<li><p>增强学习</p></li>
</ul>


<h1>核心业务场景中的算法实践</h1>

<p>由于篇幅有限，这里着重讲一下在三个比较核心的业务场景下，算法在 Hulu 广告系统中的实践与落地。</p>

<h2>1. Ad Targeting 广告定向</h2>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071903.png"></center>


<p>广告定向在品牌类广告中，是最最重要的一个环节。</p>

<p>在 Hulu 的场景下，主要有三种类型的广告定向：</p>

<p>① 上下文相关：例如用户用的设备，在什么位置，在哪个频道，看什么样的视频内容，插播广告点前后的视频内容是什么情景等等。</p>

<p>② 用户相关：例如用户年龄性别、用户基本属性以及根据用户历史行为挖掘出来的属性等。</p>

<p>③ 用户广告交互相关：</p>

<ul>
<li><p>Remarketing ( 再营销 )：例如投放广告给最近访问过广告商网站的用户，为他加深品牌印象；</p></li>
<li><p>Look-alike Targeting ( 相似访客定向 )：投放广告给与种子用户相似的受众，扩大受众人数，以及挖掘具有潜能的用户。</p></li>
</ul>


<p>下面分别介绍一下针对这三种类型的广告定向使用到的核心算法：</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071904.png"></center>


<h3>a. 用户相关定向：</h3>

<p>我们构建了用户画像系统作为用户定向的基础。底层是一个核心用户平台，负责用户数据的管理，版本控制，每天的 ETL 等等。注入的数据来自三种数据源：</p>

<p>① 来自第三方数据平台的用户标签、属性，</p>

<p>② Hulu 自己本身的一些标签，这里有两类工作：</p>

<ul>
<li><p>第一类是工作是标签的补全，我们会用一些例如 XGBoost、DNN 的预测模型来对这些缺失标签的用户进行一个预估，得到用户具有哪些标签，以及概率是多少。</p></li>
<li><p>第二类是无监督模式，我们从用户的历史行为以及其他属性中进行挖掘，挖掘出一些新的属性出来，会涉及聚类、主题模型、user2vec 的方式来生成用户的标签。</p></li>
</ul>


<p>③ 广告商提供的用户标签，针对这种标签最常用的就是进行 Lookalike 定向，我们会用机器学习的方式来进行预测，找到与种子用户相似的用户群体。</p>

<h3>b. 上下文相关定向：</h3>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071905.png"></center>


<p>这里主要介绍一下 AI 在视频内容上下文情景广告中的应用。视频广告会插播在视频内容播放前以及播放中， 投放与视频上下文情节比较相关的广告，会减少广告对用户的干扰，举个例子：用户当前所看的是婚礼视频，如果此时投放的广告是珠宝广告，那就毫无违和感，甚至会激起用户的购买欲望。另外，我们需要探测视频中的的一些暴力血腥画面，在这些地方不适合投放广告。</p>

<p>这个问题的核心是使用图像识别技术检测出视频每帧都出现了什么样的实体、情景以及氛围等。Hulu 结合业界的标准构建了自己的 Taxonomy 体系，分为情绪、物体、场景、位置以及声音几个大的分类。</p>

<p>简要介绍一下图像检测大致流程：考虑到获取大量数据样本的人力物力成本较大，我们最开始会借助于公开数据集 ( 比如 Open Iamge、Places 365 等 )，使用 Inception V3、VGG 等模型去生成一些标签，接下来会根据 Hulu 的场景进行少量的人工标注，对模型进行 Finetune 以及使用多模型融合决定最终的标签。</p>

<h3>c. 转化率优化：</h3>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071906.png"></center>


<p>虽然品牌类的广告点击率没有那么重要，但是越来越多的广告商开始和 Hulu 谈，我把我的转化数据给你，你帮我计算下我这次投放的转化率是多少。这就带来了另外一个问题，广告商最终还是希望优化自己广告的转化率，所以我们最近开始了这方面的研究。目前开展的研究是基于 Hulu 自己的广告的转化率优化，Hulu 在自己的视频里面也会放一些推广新剧的广告内容，例如一个 15 秒的预告，希望这些广告能投放给正确的用户，得到比较高的转化率。这个问题被建模成为 CVR 预估的问题，根据投放数据以及转化数据，通过机器学习来建模用户看到广告后的 7 天内、30 天内的转化率，再投放的时候把正确的广告投放给正确的用户。</p>

<ul>
<li>转化率优化中的特征 &amp; 模型</li>
</ul>


<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071907.png"></center>


<p>特征主要分为三块：</p>

<ul>
<li><p>User related：基本的用户属性，如位置，活跃度，留存时间等，以及一些重要的历史行为，比如曾经看过哪些内容，搜索过哪些内容，收藏过哪些内容。</p></li>
<li><p>Ad related：广告本身的属性，如行业、剧目本身的信息等等。</p></li>
<li><p>User-Ad：用户这个广告看过多少次了，通过多次投放使用户加深印象。</p></li>
</ul>


<p>模型方面我们目前使用了 DIN + FM 模型进行建模，同时也在探索更好的模型优化。</p>

<h3>d. 广告定向中不可忽视的问题：因果推断和共享账号</h3>

<ul>
<li>因果推断</li>
</ul>


<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071908.png"></center>


<p>我们在评估广告转化率的时候，会考虑评估是否正确，广告是否有效，举个例子：假如有一群用户，不论你对不对他投放广告，他都会去买这辆车，如果把广告投放给这样的用户，最后评估广告的转化率的时候会评估的非常高，其实广告效果一般，因为你投不投他都会去买，其实是白白消耗了这次投放。这就需要引入 Casual Inference 作为评估指标，其核心思想是：评估这个用户看这个广告和不看这个广告的收益分别是怎么样，最后将两者之间的差作为评估指标，使用 Doubly Robust Estimator 进行建模。除此，更加重要的问题是怎么根据 Casual Inference 作为目标去进行转化率的优化，怎么找到这些广告增益最大的用户，然后把广告投放给这些用户。</p>

<ul>
<li>共享账号</li>
</ul>


<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071909.png"></center>


<p>接下来是用户共享账号的问题，例如：一个账号，孩子晚上 8 点左右看动画片，妈妈晚上 10 点左右看综艺节目，爸爸周末看体育视频，妈妈使用该账号的时候，发现推荐的都是动画片，找不到自己喜欢看的内容。这个在推荐以及广告定向中都是非常重要的问题，例如有的广告商，希望这个体育类广告只推荐给家庭中的爸爸，如果是小孩和妈妈看是没有效果的。</p>

<p>这个问题规划到三个方面来解决：</p>

<p>① 探测这个账号背后有多少个虚拟用户</p>

<p>② 给每个虚拟用户打上标签，预测他的行为模式</p>

<p>③ 用户来到 app 的时候预估是哪个虚拟用户，推荐对应的广告和内容</p>

<p>这个问题核心是一些聚类的算法和预估模型的问题，这也是我们目前正在解决的问题。</p>

<h2>2. Inventory Prediction 流量预估</h2>

<p>流量预估在担保式广告中是非常重要的环节，广告在售卖之前都要检查是否有足够多的库存余量卖给广告主，以保证售出的广告都能够保量完成投放。</p>

<p>问题简单描述为：给定未来的一段时间，一些定向条件的组合，预估有多少广告流量库存。</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071910.png"></center>


<p>这个问题可以建模成时序预估的问题，通常有以下两个步骤：</p>

<p>第一步：用时序预估模型来预估总的流量是多少；</p>

<p>第二步：根据历史分布把流量分配到不同的排列组合，不同的维度上。</p>

<ul>
<li>Time Series Model</li>
</ul>


<p>我们目前尝试过以下三类模型：</p>

<p>第一个是 ARIMA ( Autoregressive Integrated Moving Average model )，稳定时序预估中最常用的模型，第二个是来自 Facebook 的 Prophet 模型，第三个是 LSTM。</p>

<p>ARIMA 作为最为直接简单的稳定时序预估模型，在我们的实验中，在短期时序的预估准确率很不错，但是对长期的预估相对来说就不那么稳定了。原因在于，以下一些因素都会影响到长期的广告流量时序数据：</p>

<ul>
<li><p>用户增长的因素</p></li>
<li><p>季节性的因素</p></li>
<li><p>每周的波动</p></li>
<li><p>节假日和特殊事件</p></li>
</ul>


<p>Prophe 模型是专门为具有这几种特性的时序数据而设计的时序预估模型。</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071911.png"></center>


<p>Prophet 把时序数据拆成了三部分：趋势部分，周期性和季节性部分，节假日部分，每一部分会单独做预测模型，最后合到一起得到最终的预测结果。</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071912.png"></center>


<p>例如，上图为 Prophet 模型产生的几种因素的分解：左上角为趋势，左下角为节假日，右边为季度性，分别为每个月和每周的变化，可以观察到淡季和旺季的变化。目前，我们采用的是 Prophet 模型，在 Hulu 的广告流量预估问题上表现稳健。</p>

<h2>3. Inventory Allocation 流量匹配</h2>

<p>流量匹配在担保式广告的投放环节发挥着重要的作用，保证所有广告订单都可以按时按量并且节奏均匀的投放完成。</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071913.png"></center>


<p>流量匹配算法会分为以下几个步骤：</p>

<p>第一步，把广告订单的投放目标拆分到每个小时，这个拆分会根据我们预估的流量和紧俏程度来进行分配；</p>

<p>第二步，进行 Offline Solving，把问题建模成一个二步图匹配算法，然后使用优化算法进行压缩解的求解；</p>

<p>第三步，进行线上调控，采用 PID ( Proportion Integration Differentiation ) 控制，根据真实的流量实时进行调控。</p>

<p>第一个步骤比较简单，下面简单介绍一下第二、三步中具体用到的算法。</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071914.png"></center>


<p>在离线匹配中，我们把问题建模成一个二部图匹配问题。</p>

<p>在广告中最主要有 SUPPLY 与 DEMAND 端的两个限制条件：上图中左边 SUPPLY 表示用户带来的流量，流量上会有一些标签，表示用户及广告位的属性，右边 DEMAND 表示广告商的订单，订单上也会有些属性，比如投给男性，或者某个地区的；SUPPLY 与 DEMAND 之间的连线表示这个广告位可以投放的广告。SUPPLY 端的限制条件为最多只有这么多的库存可以使用，DEMAND 端的限制条件为需要将每个广告订单都保量投放完成。</p>

<p>之后可以根据二部图建模优化问题，求解目标是每个边的权重，表示该流量以多大的比例分配给某个广告订单。</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071915.png"></center>


<p>首先会将原始优化问题转化为拉格朗日对偶问题，求解得到对偶变量 αj​ 和 betaj​，表示的分别是广告的对偶变量和流量的对偶变量。根据最优解满足 KKT 条件，我们可以只将相对数量级较低的广告对偶变量 αj​ 记下来，提高存储效率，线上再根据 KKT 条件计算 betaj​ 和 xij​。αj​ 可以被看做各个订单重要程度影响因子，作为线上选择订单的权重。</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071916.png"></center>


<p>但是这里还是存在一个问题，这里全部都是用的预估的流量和订单来进行匹配，但实际上会发生一些变化。所以需要第三步，线上实时调控。第一可以保证适应真实的线上流量， 第二也可以进一步保证投放节奏的稳定性。一般使用 PID ( Proportion Integration Differentiation ) 控制技术，这是常用于机械、电气系统中的一种应用非常广泛的自动控制技术，分为比例调节、积分调节、微分调节这 3 项的调节，根据线上广告真实投放的快慢来调控接下来应该投放的速度。</p>

<h1>作者介绍：</h1>

<p>韦春阳，2012 年从北京大学硕士毕业后加入 Hulu，目前担任智能广告算法团队负责人。有 7 年的计算广告领域的从业经验，致力于利用机器学习、人工智能技术来推动 Hulu 广告业务的发展，负责包括精准广告定向、广告流量匹配、广告库存预估、广告转化率优化等项目。</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[使用TensorFlow、ONNX和TensorRT加速深度学习推理]]></title>
    <link href="https://ibillxia.github.io/blog/2024/07/15/tensorflow-onnx-tensorrt-inference/"/>
    <updated>2024-07-15T15:17:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2024/07/15/tensorflow-onnx-tensorrt-inference</id>
    <content type="html"><![CDATA[<p>原文链接：<a href="https://developer.nvidia.cn/blog/speeding-up-deep-learning-inference-using-tensorflow-onnx-and-tensorrt/">https://developer.nvidia.cn/blog/speeding-up-deep-learning-inference-using-tensorflow-onnx-and-tensorrt/</a></p>

<p>从 TensorRT 7 . 0 开始， Universal Framework Format（ UFF ）被弃用。在本文中，您将学习如何使用新的 TensorFlow -ONNX- TensorRT 工作流部署经过 TensorFlow 培训的深度学习模型。图 1 显示了 TensorRT 的高级工作流。</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071501.png"></center>


<p>图 1 。 TensorRT 是一种推理加速器。</p>

<!--more-->


<p>首先，使用任何框架训练网络。网络训练后，批量大小和精度是固定的（精度为 FP32 、 FP16 或 INT8 ）。训练好的模型被传递给 TensorRT 优化器，优化器输出一个优化的运行时（也称为计划）。. plan 文件是 TensorRT 引擎的序列化文件格式。计划文件需要反序列化才能使用 TensorRT 运行时运行推断。</p>

<p>要优化在 TensorFlow 中实现的模型，只需将模型转换为 ONNX 格式，并使用 TensorRT 中的 ONNX 解析器解析模型并构建 TensorRT 引擎。图 2 显示了高级 ONNX 工作流。</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071502.jpeg"></center>


<p>图 2 。 ONNX 工作流。</p>

<p>在本文中，我们将讨论如何使用 ONNX 工作流创建一个 TensorRT 引擎，以及如何从 TensorRT 引擎运行推理。更具体地说，我们演示了从 Keras 或 TensorFlow 中的模型到 ONNX 的端到端推理，以及使用 ResNet-50 、语义分段和 U-Net 网络的 TensorRT 引擎。最后，我们将解释如何在其他网络上使用此工作流。</p>

<p><a href="https://developer.download.nvidia.com/devblogs/tensorflow-onnx-tensorrt-code.tar.gz">下载本文中的代码示例</a>。 下载 TensorFlow -onnx- TensorRT 后 – 代码 tar . gz 文件，您还应该从 Cityscapes dataset scripts repo <a href="https://github.com/mcordts/cityscapesScripts/blob/master/cityscapesscripts/helpers/labels.py">下载 labels.py</a> ，并将其与其他脚本放在同一个文件夹中。</p>

<h1>ONNX 概述</h1>

<p>ONNX 是机器学习和深度学习模型的开放格式。它允许您将不同框架（如 TensorFlow 、 PyTorch 、 MATLAB 、 Caffe 和 Keras ）的深度学习和机器学习模型转换为单一格式。</p>

<p>它定义了一组通用的运算符、深入学习的通用构建块集和通用文件格式。它提供计算图的定义以及内置运算符。可能有一个或多个输入或输出的 ONNX 节点列表形成一个无环图。</p>

<h1>ResNet ONNX 工作流示例</h1>

<p>在这个例子中，我们展示了如何在两个不同的网络上使用 ONNX 工作流并创建一个 TensorRT 引擎。第一个网络是 ResNet-50 。</p>

<p>工作流包括以下步骤：</p>

<ol>
<li><p>将 TensorFlow / Keras 模型转换为. pb 文件。</p></li>
<li><p>将. pb 文件转换为 ONNX 格式。</p></li>
<li><p>创建 TensorRT 引擎。</p></li>
<li><p>从 TensorRT 引擎运行推断。</p></li>
</ol>


<h2>将模型转换为. pb</h2>

<p>第一步是将模型转换为. pb 文件。以下代码示例将 ResNet-50 模型转换为. pb 文件：</p>

<pre><code>import tensorflow as tf
import keras
from tensorflow.keras.models import Model
import keras.backend as K
K.set_learning_phase(0)

def keras_to_pb(model, output_filename, output_node_names):

   """
   This is the function to convert the Keras model to pb.

   Args:
      model: The Keras model.
      output_filename: The output .pb file name.
      output_node_names: The output nodes of the network. If None, then
      the function gets the last layer name as the output node.
   """

   # Get the names of the input and output nodes.
   in_name = model.layers[0].get_output_at(0).name.split(':')[0]

   if output_node_names is None:
       output_node_names = [model.layers[-1].get_output_at(0).name.split(':')[0]]

   sess = keras.backend.get_session()

   # The TensorFlow freeze_graph expects a comma-separated string of output node names.
   output_node_names_tf = ','.join(output_node_names)

   frozen_graph_def = tf.graph_util.convert_variables_to_constants(
       sess,
       sess.graph_def,
       output_node_names)

   sess.close()
   wkdir = ''
   tf.train.write_graph(frozen_graph_def, wkdir, output_filename, as_text=False)

   return in_name, output_node_names

# load the ResNet-50 model pretrained on imagenet
model = keras.applications.resnet.ResNet50(include_top=True, weights='imagenet', input_tensor=None, input_shape=None, pooling=None, classes=1000)

# Convert the Keras ResNet-50 model to a .pb file
in_tensor_name, out_tensor_names = keras_to_pb(model, "models/resnet50.pb", None) 
</code></pre>

<p>除了 Keras ，您还可以从以下位置下载 ResNet-50 ：</p>

<ul>
<li><p><a href="https://github.com/NVIDIA/DeepLearningExamples">深度学习示例 GitHub 存储库</a>：提供最新的深度学习示例网络。您还可以看到 <a href="https://github.com/NVIDIA/DeepLearningExamples/tree/master/TensorFlow/Classification/RN50v1.5">ResNet-50</a> 分支，它包含一个脚本和方法来训练 ResNet-50v1 . 5 模型。</p></li>
<li><p><a href="https://ngc.nvidia.com/catalog/models?orderBy=modifiedDESC&amp;pageNumber=1&amp;query=&amp;quickFilter=models&amp;filters=">NVIDIA NGC 型号</a> ：它有预训练模型的检查点列表。例如，在 ResNet-50v1 . 5 上搜索 TensorFlow ，并从 <a href="https://ngc.nvidia.com/catalog/models/nvidia:rntf_fp32">Download</a> 页面获取最新的检查点。</p></li>
</ul>


<h2>将. pb 文件转换为 ONNX</h2>

<p>第二步是将. pb 模型转换为 ONNX 格式。为此，首先安装 <a href="https://github.com/onnx/tensorflow-onnx">tf2onnx</a> 。</p>

<p>安装 tf2onnx 后，有两种方法可以将模型从. pb 文件转换为 ONNX 格式。第二种方法是使用命令行。运行以下命令：</p>

<pre><code>python -m tf2onnx.convert  --input /Path/to/resnet50.pb --inputs input_1:0 --outputs probs/Softmax:0 --output resnet50.onnx
</code></pre>

<h2>从 ONNX 创建 TensorRT 引擎</h2>

<p>要从 ONNX 文件创建 TensorRT 引擎，请运行以下命令：</p>

<pre><code>import tensorrt as trt

TRT_LOGGER = trt.Logger(trt.Logger.WARNING)
trt_runtime = trt.Runtime(TRT_LOGGER)
def build_engine(onnx_path, shape = [1,224,224,3]):

   """
   This is the function to create the TensorRT engine
   Args:
      onnx_path : Path to onnx_file. 
      shape : Shape of the input of the ONNX file. 
  """
   with trt.Builder(TRT_LOGGER) as builder, builder.create_network(1) as network, trt.OnnxParser(network, TRT_LOGGER) as parser:
       builder.max_workspace_size = (256 &lt;&lt; 20)
       with open(onnx_path, 'rb') as model:
           parser.parse(model.read())
       network.get_input(0).shape = shape
       engine = builder.build_cuda_engine(network)
       return engine

def save_engine(engine, file_name):
   buf = engine.serialize()
   with open(file_name, 'wb') as f:
       f.write(buf)
def load_engine(trt_runtime, plan_path):
   with open(engine_path, 'rb') as f:
       engine_data = f.read()
   engine = trt_runtime.deserialize_cuda_engine(engine_data)
   return engine
</code></pre>

<p>此代码应保存在引擎. py 文件，稍后在文章中使用。</p>

<p>此代码示例包含以下变量：</p>

<ul>
<li>最大工作区大小： 在执行时 ICudaEngine 可以使用的最大 GPU 临时内存。</li>
</ul>


<p>构建器创建一个空网络（ builder.create_network() ）， ONNX 解析器将 ONNX 文件解析到网络（ parser.parse(model.read()) ）。您可以为网络（ network.get_input(0).shape = shape ）设置输入形状，然后生成器将创建引擎（ engine = builder.build_cuda_engine(network) ）。要创建引擎，请运行以下代码示例：</p>

<pre><code>import engine as eng
import argparse
from onnx import ModelProto
import tensorrt as trt

engine_name = “resnet50.plan”
onnx_path = "/path/to/onnx/result/file/"
batch_size = 1 

model = ModelProto()
with open(onnx_path, "rb") as f:
model.ParseFromString(f.read())

d0 = model.graph.input[0].type.tensor_type.shape.dim[1].dim_value
d1 = model.graph.input[0].type.tensor_type.shape.dim[2].dim_value
d2 = model.graph.input[0].type.tensor_type.shape.dim[3].dim_value
shape = [batch_size , d0, d1 ,d2]
engine = eng.build_engine(onnx_path, shape= shape)
eng.save_engine(engine, engine_name) 
</code></pre>

<p>在这个代码示例中，首先从 ONNX 模型获取输入形状。接下来，创建引擎，然后将引擎保存在. plan 文件中。</p>

<h2>运行来自 TensorRT 引擎的推理：</h2>

<p>TensorRT 引擎在以下工作流中运行推理：</p>

<ol>
<li><p>为 GPU 中的输入和输出分配缓冲区。</p></li>
<li><p>将数据从主机复制到 GPU 中分配的输入缓冲区。</p></li>
<li><p>在 GPU 中运行推理。</p></li>
<li><p>将结果从 GPU 复制到主机。</p></li>
<li><p>根据需要重塑结果。</p></li>
</ol>


<p>下面的代码示例详细解释了这些步骤。此代码应保存在推理. py 文件，稍后将在本文中使用。</p>

<pre><code>import tensorrt as trt
import pycuda.driver as cuda
import numpy as np
import pycuda.autoinit

def allocate_buffers(engine, batch_size, data_type):

   """
   This is the function to allocate buffers for input and output in the device
   Args:
      engine : The path to the TensorRT engine. 
      batch_size : The batch size for execution time.
      data_type: The type of the data for input and output, for example trt.float32. 

   Output:
      h_input_1: Input in the host.
      d_input_1: Input in the device. 
      h_output_1: Output in the host. 
      d_output_1: Output in the device. 
      stream: CUDA stream.

   """

   # Determine dimensions and create page-locked memory buffers (which won't be swapped to disk) to hold host inputs/outputs.
   h_input_1 = cuda.pagelocked_empty(batch_size * trt.volume(engine.get_binding_shape(0)), dtype=trt.nptype(data_type))
   h_output = cuda.pagelocked_empty(batch_size * trt.volume(engine.get_binding_shape(1)), dtype=trt.nptype(data_type))
   # Allocate device memory for inputs and outputs.
   d_input_1 = cuda.mem_alloc(h_input_1.nbytes)

   d_output = cuda.mem_alloc(h_output.nbytes)
   # Create a stream in which to copy inputs/outputs and run inference.
   stream = cuda.Stream()
   return h_input_1, d_input_1, h_output, d_output, stream

def load_images_to_buffer(pics, pagelocked_buffer):
   preprocessed = np.asarray(pics).ravel()
   np.copyto(pagelocked_buffer, preprocessed)

def do_inference(engine, pics_1, h_input_1, d_input_1, h_output, d_output, stream, batch_size, height, width):
   """
   This is the function to run the inference
   Args:
      engine : Path to the TensorRT engine 
      pics_1 : Input images to the model.  
      h_input_1: Input in the host         
      d_input_1: Input in the device 
      h_output_1: Output in the host 
      d_output_1: Output in the device 
      stream: CUDA stream
      batch_size : Batch size for execution time
      height: Height of the output image
      width: Width of the output image

   Output:
      The list of output images

   """

   load_images_to_buffer(pics_1, h_input_1)

   with engine.create_execution_context() as context:
       # Transfer input data to the GPU.
       cuda.memcpy_htod_async(d_input_1, h_input_1, stream)

       # Run inference.

       context.profiler = trt.Profiler()
       context.execute(batch_size=1, bindings=[int(d_input_1), int(d_output)])

       # Transfer predictions back from the GPU.
       cuda.memcpy_dtoh_async(h_output, d_output, stream)
       # Synchronize the stream
       stream.synchronize()
       # Return the host output.
       out = h_output.reshape((batch_size,-1, height, width))
       return out 
</code></pre>

<p>为第一个输入行和输出行确定两个维度。您可以在主机（ h_input_1 、 h_output ）中创建页锁定内存缓冲区。然后，为输入和输出分配与主机输入和输出相同大小的设备内存（ d_input_1 ， d_output ）。下一步是创建 CUDA 流，用于在设备和主机分配的内存之间复制数据。</p>

<p>在这个代码示例中，在 do_inference 函数中，第一步是使用 load_images_to_buffer 函数将图像加载到主机中的缓冲区。然后将输入数据传输到 GPU （ cuda.memcpy_htod_async(d_input_1, h_input_1, stream) ），并使用 context.execute 运行推理。最后将结果从 GPU 复制到主机（ cuda.memcpy_dtoh_async(h_output, d_output, stream) ）。</p>

<h1>ONNX 工作流语义分割实例</h1>

<p>在本文 <a href="https://developer.nvidia.com/blog/int8-inference-autonomous-vehicles-tensorrt/">基于 TensorRT 3 的自主车辆快速 INT8 推理</a> 中，作者介绍了一个语义分割模型的 UFF 工作流过程。</p>

<p>在本文中，您将使用类似的网络来运行 ONNX 工作流来进行语义分段。该网络由一个基于 VGG16 的编码器和三个使用反褶积层实现的上采样层组成。网络在 <a href="https://www.cityscapes-dataset.com/">城市景观数据集</a> 上经过大约 40000 次迭代训练</p>

<p>有多种方法可以将 TensorFlow 模型转换为 ONNX 文件。一种方法是 ResNet50 部分中解释的方法。 Keras 也有自己的 Keras 到 ONNX 文件转换器。有时， TensorFlow -to-ONNX 不支持某些层，但 Keras-to-ONNX 转换器支持这些层。根据 Keras 框架和使用的层类型，您可能需要在转换器之间进行选择。</p>

<p>在下面的代码示例中，使用 Keras-to-ONNX 转换器将 Keras 模型直接转换为 ONNX 。下载预先训练的语义分段文件 <a href="https://developer.download.nvidia.com/devblogs/semantic_segmentation.hdf5">semantic_segmentation.hdf5</a> 。</p>

<pre><code>import keras
import tensorflow as tf
from keras2onnx import convert_keras

def keras_to_onnx(model, output_filename):
   onnx = convert_keras(model, output_filename)
   with open(output_filename, "wb") as f:
       f.write(onnx.SerializeToString())

semantic_model = keras.models.load_model('/path/to/semantic_segmentation.hdf5')
keras_to_onnx(semantic_model, 'semantic_segmentation.onnx') 
</code></pre>

<p>图 3 显示了网络的体系结构。</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071503.png"></center>


<p>图 3 。基于 VGG16 的语义分割模型。</p>

<p>与前面的示例一样，使用下面的代码示例创建语义分段引擎。</p>

<pre><code>import engine as eng
from onnx import ModelProto
import tensorrt as trt

engine_name = 'semantic.plan'
onnx_path = "semantic.onnx"
batch_size = 1

model = ModelProto()
with open(onnx_path, "rb") as f:
  model.ParseFromString(f.read())

d0 = model.graph.input[0].type.tensor_type.shape.dim[1].dim_value
d1 = model.graph.input[0].type.tensor_type.shape.dim[2].dim_value
d2 = model.graph.input[0].type.tensor_type.shape.dim[3].dim_value
shape = [batch_size , d0, d1 ,d2]
engine = eng.build_engine(onnx_path, shape= shape)
eng.save_engine(engine, engine_name) 
</code></pre>

<p>要测试模型的输出，请使用 <a href="https://www.cityscapes-dataset.com/">城市景观数据集</a> 。要使用城市景观，必须具有以下功能： sub_mean_chw 和 color_map 。这些函数也用于 post ， <a href="https://developer.nvidia.com/blog/int8-inference-autonomous-vehicles-tensorrt/">基于 TensorRT 3 的自主车辆快速 INT8 推理</a>.</p>

<p>在下面的代码示例中， sub_mean_chw 用于从图像中减去平均值作为预处理步骤， color_map 是从类 ID 到颜色的映射。后者用于可视化。</p>

<pre><code>import numpy as np
from PIL import Image
import tensorrt as trt
import labels  # from cityscapes evaluation script
import skimage.transform

TRT_LOGGER = trt.Logger(trt.Logger.WARNING)
trt_runtime = trt.Runtime(TRT_LOGGER)

MEAN = (71.60167789, 82.09696889, 72.30508881)
CLASSES = 20
HEIGHT = 512
WIDTH = 1024

def sub_mean_chw(data):
   data = data.transpose((1, 2, 0))  # CHW -&gt; HWC
   data -= np.array(MEAN)  # Broadcast subtract
   data = data.transpose((2, 0, 1))  # HWC -&gt; CHW
   return data

def rescale_image(image, output_shape, order=1):
   image = skimage.transform.resize(image, output_shape,
               order=order, preserve_range=True, mode='reflect')
   return image

def color_map(output):
   output = output.reshape(CLASSES, HEIGHT, WIDTH)
   out_col = np.zeros(shape=(HEIGHT, WIDTH), dtype=(np.uint8, 3))
   for x in range(WIDTH):
       for y in range(HEIGHT):

           if (np.argmax(output[:, y, x] )== 19):
               out_col[y,x] = (0, 0, 0)
           else:
               out_col[y, x] = labels.id2label[labels.trainId2label[np.argmax(output[:, y, x])].id].color
   return out_col  
</code></pre>

<p>下面的代码示例是上一个示例的其余代码。必须先运行上一个块，因为需要定义的函数。使用这个例子比较 Keras 模型和 TensorRT 引擎 semantic . plan 文件的输出，然后可视化这两个输出。根据需要替换占位符 /path/to/semantic_segmentation.hdf5 和 input_file_path 。</p>

<pre><code>import engine as eng
import inference as inf
import keras
import tensorrt as trt

input_file_path = ‘munster_000172_000019_leftImg8bit.png’
onnx_file = "semantic.onnx"
serialized_plan_fp32 = "semantic.plan"
HEIGHT = 512
WIDTH = 1024

image = np.asarray(Image.open(input_file_path))
img = rescale_image(image, (512, 1024),order=1)
im = np.array(img, dtype=np.float32, order='C')
im = im.transpose((2, 0, 1))
im = sub_mean_chw(im)

engine = eng.load_engine(trt_runtime, serialized_plan_fp32)
h_input, d_input, h_output, d_output, stream = inf.allocate_buffers(engine, 1, trt.float32)
out = inf.do_inference(engine, im, h_input, d_input, h_output, d_output, stream, 1, HEIGHT, WIDTH)
out = color_map(out)

colorImage_trt = Image.fromarray(out.astype(np.uint8))
colorImage_trt.save(“trt_output.png”)

semantic_model = keras.models.load_model('/path/to/semantic_segmentation.hdf5')
out_keras= semantic_model.predict(im.reshape(-1, 3, HEIGHT, WIDTH))

out_keras = color_map(out_keras)
colorImage_k = Image.fromarray(out_keras.astype(np.uint8))
colorImage_k.save(“keras_output.png”)
</code></pre>

<p>图 4 显示了实际图像和实际情况，以及 Keras 的输出与 TensorRT 引擎的输出的对比。如您所见， TensorRT 发动机的输出与 Keras 的类似。</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071504.png"></center>


<p>图 4a 原始图像 .</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071505.png"></center>


<p>图 4b 地面真相标签。</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071506.png"></center>


<p>图 4c . TensorRT 的输出。</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071507.png"></center>


<p>图 4d : Keras 的输出。</p>

<h1>在其他网络上试试</h1>

<p>现在您可以在其他网络上尝试 ONNX 工作流。有关分段网络的好例子的更多信息，请参阅 GitHub 上的 <a href="https://github.com/qubvel/segmentation_models">具有预训练主干的分割模型</a> 。</p>

<p>作为一个例子，我们用一个 ONNX 网络来说明如何使用。本例中的网络是来自 <a href="https://github.com/qubvel/segmentation_models">segmentation_models</a> 库的 U-Net 。在这里，我们只加载模型，而没有对其进行训练。您可能需要在首选数据集上训练这些模型。</p>

<p>关于这些网络的一个重要点是，当您加载这些网络时，它们的输入层大小如下所示：（ None ， None ， None ， 3 ）。要创建一个 TensorRT 引擎，您需要一个输入大小已知的 ONNX 文件。在将此模型转换为 ONNX 之前，请通过为其输入指定大小来更改网络，然后将其转换为 ONNX 格式。</p>

<p>例如，从这个库（ segmentation _ models ）加载 U-Net 网络并为其输入指定大小（ 244 、 244 、 3 ）。在为推理创建了 TensorRT 引擎之后，做一个与语义分段类似的转换。根据应用程序和数据集的不同，可能需要使用不同的颜色映射。</p>

<pre><code>import segmentation_models as sm
import keras
from keras2onnx import convert_keras
from engine import *
onnx_path = 'unet.onnx'
engine_name = 'unet.plan'
batch_size = 1
CHANNEL = 3
HEIGHT = 224
WIDTH = 224

model = sm.Unet()
model._layers[0].batch_input_shape = (None, 224,224,3)
model = keras.models.clone_model(model)

onx = convert_keras(model, onnx_path)
with open(onnx_path, "wb") as f:
  f.write(onx.SerializeToString())

shape = [batch_size , HEIGHT, WIDTH, CHANNEL]
engine = build_engine(onnx_path, shape= shape)
save_engine(engine, engine_name)
</code></pre>

<p>我们之前提到的另一种下载方式是从 vz6 下载。它有一个预先训练模型的检查点列表。例如，您可以在 TensorFlow 中搜索 UNet ，然后转到 <a href="https://ngc.nvidia.com/catalog/models/nvidia:unetmedtf_fp32">Download</a> 页面以获取最新的检查点。</p>

<h1>总结</h1>

<p>在这篇文章中，我们解释了如何使用 TensorFlow-to-ONNX-to-TensorRT 工作流来部署深度学习应用程序，并给出了几个示例。第一个例子是 ResNet-50 上的 ONNX- TensorRT ，第二个例子是在 Cityscapes 数据集上训练的基于 <a href="https://developer.nvidia.com/deep-learning-performance-training-inference">英伟达数据中心深度学习产品性能</a> 的语义分割。在文章的最后，我们演示了如何在其他网络上应用这个工作流。</p>

<hr />

<h1>关于作者</h1>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071508.jpeg"></center>


<p>关于 Houman Abbasian</p>

<p>Houman 是 NVIDIA 的高级深度学习软件工程师。他一直致力于开发和生产 NVIDIA 在自动驾驶车辆中的深度学习解决方案，提高 DNN 的推理速度、精度和功耗，并实施和试验改进 NVIDIA 汽车 DNN 的新思想。他在渥太华大学获得计算机科学博士学位，专注于机器学习。</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071509.jpeg"></center>


<p>于德成简介</p>

<p>于德成是 NVIDIA 自主驾驶组高级深度学习软件工程师，从事自驾领域的各种感知任务的神经结构搜索和 DNN 模型训练、压缩和部署，包括目标检测、分割、路径轨迹生成等。他于 2016 年获得卡内基梅隆大学机器人学硕士学位。</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024071510.jpeg"></center>


<p>关于乔希·帕克</p>

<p>Josh Park 是 NVIDIA 的汽车解决方案架构师经理。到目前为止，他一直在研究使用 DL 框架的深度学习解决方案，例如在 multi- GPUs /多节点服务器和嵌入式系统上的 TensorFlow 。此外，他一直在评估和改进各种 GPUs + x86 _ 64 / aarch64 的训练和推理性能。他在韩国大学获得理学学士和硕士学位，并在德克萨斯农工大学获得计算机科学博士学位。</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[【DataFun】因果推断与复杂AB测试]]></title>
    <link href="https://ibillxia.github.io/blog/2024/07/09/causal-inference-complex-ab-testing/"/>
    <updated>2024-07-09T15:51:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2024/07/09/causal-inference-complex-ab-testing</id>
    <content type="html"><![CDATA[<h2>24年7月5-6北京站</h2>

<h2><strong>eBay 基于Uplift Model的优惠券分发优化</strong></h2>

<p><a href="https://drive.weixin.qq.com/s?k=AJEAIQdfAAoRew3eteAZgAQAbMAKM#/preview?fileId=s.1970325010981265.7204251960EC_f.720425750F67V">https://drive.weixin.qq.com/s?k=AJEAIQdfAAoRew3eteAZgAQAbMAKM#/preview?fileId=s.1970325010981265.7204251960EC_f.720425750F67V</a></p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070901.png"></center>




<!--more-->




<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070902.png"></center>




<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070903.png"></center>




<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070904.png"></center>




<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070905.png"></center>


<h2><strong>字节 推荐场景下的复杂AB实验设计</strong></h2>

<p><a href="https://drive.weixin.qq.com/s?k=AJEAIQdfAAo5EtQJwpAZgAQAbMAKM">DataFunCon2024-推荐场景的复杂AB实验设计.pdf</a>（抢先版）</p>

<p><a href="https://drive.weixin.qq.com/s?k=AJEAIQdfAAosvCrHVWAZgAQAbMAKM">刘小可-抖音.pdf</a>（正式版）</p>

<p>⼀、双边效应导致的策略效果评估有偏问题</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070906.png"></center>


<p>⼆、⽹络效应导致的策略效果评估有偏问题</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070907.png"></center>




<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070908.png"></center>




<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070909.png"></center>




<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070910.png"></center>




<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070911.png"></center>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[PyTorch+CUDA+显卡算力对应关系]]></title>
    <link href="https://ibillxia.github.io/blog/2024/07/04/pytorch-cuda-gpu-compatibility/"/>
    <updated>2024-07-04T17:06:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2024/07/04/pytorch-cuda-gpu-compatibility</id>
    <content type="html"><![CDATA[<h1>Pytorch 各个GPU版本CUDA和cuDNN对应版本</h1>

<p><a href="https://blog.csdn.net/weixin_45508265/article/details/122006134">https://blog.csdn.net/weixin_45508265/article/details/122006134</a></p>

<p><a href="https://pytorch.org/get-started/previous-versions/">https://pytorch.org/get-started/previous-versions/</a></p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070401.png"></center>




<!--more-->


<h1>pytorch 中注意cuda版本和gpu算力匹配</h1>

<p><a href="https://blog.csdn.net/m0_46483236/article/details/124112298">https://blog.csdn.net/m0_46483236/article/details/124112298</a></p>

<p><a href="https://zhuanlan.zhihu.com/p/427395039">https://zhuanlan.zhihu.com/p/427395039</a></p>

<p><a href="https://docs.nvidia.com/cuda/ampere-compatibility-guide/index.html#verifying-ampere-compatibility-using-cuda-11-0">https://docs.nvidia.com/cuda/ampere-compatibility-guide/index.html#verifying-ampere-compatibility-using-cuda-11-0</a></p>

<p>查看GPU算力：<a href="https://developer.nvidia.com/cuda-gpus">https://developer.nvidia.com/cuda-gpus</a></p>

<p><strong>只有cuda 11版本支持当前GPU 8.6算力</strong></p>

<p>安装cuda 11.1覆盖cuda 10.2。</p>

<p>pip3 install torch==1.9.0+cu111 torchvision==0.10.0+cu111 torchaudio==0.9.0 -f <a href="https://download.pytorch.org/whl/torch_stable.html">https://download.pytorch.org/whl/torch_stable.html</a></p>

<p>查看cuda+torch版本及算力（<a href="https://developer.aliyun.com/article/1374303">https://developer.aliyun.com/article/1374303</a>）</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070402.png"></center>


<h1>nvcc和nvidia-smi显示的版本不一致？</h1>

<p><a href="https://www.jianshu.com/p/eb5335708f2a">https://www.jianshu.com/p/eb5335708f2a</a></p>

<p><strong>nvcc 属于CUDA的编译器，将程序编译成可执行的二进制文件。</strong></p>

<p><strong>nvidia-smi 全称是 NVIDIA System Management Interface ，是一种命令行实用工具，旨在帮助管理和监控NVIDIA GPU设备。 </strong></p>

<p>CUDA有 runtime api 和 driver api，两者都有对应的CUDA版本， nvcc --version 显示的就是前者对应的CUDA版本，而 nvidia-smi显示的是后者对应的CUDA版本。</p>

<p>用于支持driver api的必要文件由 GPU driver installer 安装，nvidia-smi就属于这一类API；而用于支持runtime api的必要文件是由 CUDA Toolkit installer 安装的。nvcc是与CUDA Toolkit一起安装的CUDA compiler-driver tool，它只知道它自身构建时的CUDA runtime版本，并不知道安装了什么版本的GPU driver，甚至不知道是否安装了GPU driver。 CUDA Toolkit Installer通常会集成了GPU driver Installer，如果你的CUDA均通过CUDA Tooklkit Installer来安装，那么runtime api 和 driver api的版本应该是一致的，也就是说， nvcc --version 和 nvidia-smi 显示的版本应该一样。否则，你可能使用了单独的GPU driver installer来安装GPU dirver，这样就会导致 nvidia-smi 和 nvcc --version 显示的版本不一致了。</p>

<p><strong>通常，driver api的版本能向下兼容runtime api的版本，即 nvidia-smi 显示的版本大于nvcc --version 的版本通常不会出现大问题。</strong></p>

<p><strong>多版本CUDA切换</strong></p>

<p><a href="https://baijiahao.baidu.com/s?id=1663920145053509733&amp;wfr=spider&amp;for=pc&amp;qq-pf-to=pcqq.c2c">https://baijiahao.baidu.com/s?id=1663920145053509733&amp;wfr=spider&amp;for=pc&amp;qq-pf-to=pcqq.c2c</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[AI绘画原理解析：从CLIP、DALLE到Stable Diffusion]]></title>
    <link href="https://ibillxia.github.io/blog/2024/07/02/ai-painting-clip-dalle-stable-diffusion/"/>
    <updated>2024-07-02T15:16:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2024/07/02/ai-painting-clip-dalle-stable-diffusion</id>
    <content type="html"><![CDATA[<p>原文链接：<a href="https://blog.csdn.net/v_JULY_v/article/details/131205615">https://blog.csdn.net/v_JULY_v/article/details/131205615</a></p>

<h1>前言</h1>

<p>关于Stable Diffusion，可以先看下这篇<a href="https://jalammar.github.io/illustrated-stable-diffusion/">图解Stable Diffusion</a>的文章(此篇文章也是本文的参考之一)</p>

<ol>
<li>23年3月中旬，当OpenAI宣称GPT4具备了CV多模态的能力之后，让我对AI绘画和CV多模态有了更强的动力去研究探索，并把背后的技术细节写出来</li>
</ol>


<p>其实当时就想写了，但当时因为写各种开源平替模型的原理、部署、微调去了，所以一直没来得及写，包括之前计划的100篇论文也因此耽搁</p>

<p>4.23，我所讲的ChatGPT原理课开课之后，终于有时间开写这篇多模态博客，然想写清楚stable diffusion和midjourney背后的技术细节，不得不先从扩散模型开始，于此便有了上一篇<a href="https://blog.csdn.net/v_JULY_v/article/details/130361959">《图像生成发展起源：从VAE、扩散模型DDPM、DETR到ViT、Swin transformer》</a>「且如果你此前不了解何谓扩散模型、何谓DDPM，务必先看该文，不然没法看懂本文」</p>

<p>援引上一篇文章的这段话“AI绘画随着去年stable diffusion和Midjourney的推出，使得文生图火爆异常，各种游戏的角色设计、网上店铺的商品/页面设计都用上了AI绘画这样的工具，更有不少朋友利用AI绘画取得了不少的创收，省时省力还能赚钱，真香”，此外，包括我司LLM项目团队开发的AIGC模特生成系统也用到了这方面的技术：基于SD二次开发“</p>

<p>沿着上文之后，本文将写清楚下面表格中带下划线的模型</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070201.png"></center>




<!--more-->


<p>且过程中会顺带介绍MiniGPT-4、VisualGPT到HuggingGPT、AutoGPT这几个模型</p>

<h1>第一部分 从CLIP到BLIP1、BLIP2</h1>

<h2>1.1 CLIP：基于对比文本-图像对的预训练方法</h2>

<h3>1.1.1 CLIP的原理结构</h3>

<p>我第一次见识到CLIP这个论文的时候(对应论文为：Learning Transferable Visual Models From Natural Language Supervision，作者为Alec Radford等人)，当时的第一反应是，特么也太强悍了..</p>

<p>CLIP由OpenAI在2021年1月发布</p>

<ol>
<li><p>通过超大规模模型预训练提取视觉特征，进行图片和文本之间的对比学习 (简单粗暴理解就是发微博/朋友圈时，人喜欢发一段文字然后再配一张或几张图，CLIP便是学习这种对应关系)</p></li>
<li><p>且预训练好之后不微调直接推理 (即zero-shot，用见过的图片特征去判断没见过的图片的类别，而不用下游任务训练集进行微调)</p></li>
</ol>


<p>使得在ImageNet数据集上，CLIP模型在不使用ImageNet数据集的任何一张图片进行训练的的情况下，最终模型精度能跟一个有监督的训练好的ResNet-50打成平手 (在ImageNet上zero-shot精度为76.2%，这在之前一度被认为是不可能的)  </p>

<p>为了训练CLIP，OpenAI从互联网收集了共4个亿的文本-图像对，论文称之为WIT(Web Image Text，WIT质量很高，而且清理的非常好，其规模相当于JFT-300M，这也是CLIP如此强大的原因之一，后续在WIT上还孕育出了DALL-E模型)</p>

<p>其训练过程如下图所示：</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070202.png"></center>


<ol>
<li>如下图的第一步所示，CLIP的输入是一对对配对好的的图片-文本对（比如输入是一张狗的图片，对应文本也表示这是一只狗），这些文本和图片分别通过Text Encoder和Image Encoder输出对应的特征。然后在这些输出的文字特征和图片特征上进行对比学习</li>
</ol>


<p>假如模型输入的是n对图片-文本对，那么这n对互相配对的图像–文本对是正样本(上图输出特征矩阵对角线上标识蓝色的部位)，其它n2−n对样本都是负样本，这样模型的训练过程就是最大化n个正样本的相似度，同时最小化n2−n个负样本的相似度</p>

<p>→  Text Encoder可以采用NLP中常用的text transformer模型</p>

<p>→  而Image Encoder可以采用常用CNN模型或者vision transformer等模型</p>

<p>相似度是计算文本特征和图像特征的余弦相似性cosine similarity</p>

<p>之后，CLIP可以直接实现zero-shot的图像分类，即不需要任何训练和微调，其实现zero-shot分类只需要简单的两步，如下第2、3点所示</p>

<ol>
<li><p>根据任务的分类标签构建每个类别的描述文本：A photo of {label}，然后将这些文本送入Text Encoder得到对应的文本特征，如果类别数目为n，那么将得到n个文本特征</p></li>
<li><p>将要预测的图像送入Image Encoder得到图像特征，然后与n个文本特征计算缩放的余弦相似度(和训练过程保持一致)，然后选择相似度最大的文本对应的类别作为图像分类预测结果</p></li>
</ol>


<p>进一步地，可以将这些相似度看成logits，送入softmax后可以到每个类别的预测概率</p>

<p>以下是对应的伪代码</p>

<pre><code># image_encoder - ResNet or Vision Transformer
# text_encoder - CBOW or Text Transformer
# I[n, h, w, c] - 输入图片维度
# T[n, l] - 输入文本维度，l表示序列长度
# W_i[d_i, d_e] - learned proj of image to embed
# W_t[d_t, d_e] - learned proj of text to embed
# t - learned temperature parameter
#  分别提取图像特征和文本特征
I_f = image_encoder(I) #[n, d_i]
T_f = text_encoder(T) #[n, d_t]
# 对两个特征进行线性投射，得到相同维度的特征d_e，并进行l2归一化，保持数据尺度的一致性
# 多模态embedding [n, d_e]
I_e = l2_normalize(np.dot(I_f, W_i), axis=1)
T_e = l2_normalize(np.dot(T_f, W_t), axis=1)
# 计算缩放的余弦相似度：[n, n]
logits = np.dot(I_e, T_e.T) * np.exp(t)
# symmetric loss function
labels = np.arange(n) #  对角线元素的labels
loss_i = cross_entropy_loss(logits, labels, axis=0) # image loss
loss_t = cross_entropy_loss(logits, labels, axis=1) # text loss
loss = (loss_i + loss_t)/2 # 对称式的目标函数
</code></pre>

<p>2021年10月，Accomplice发布的disco diffusion，便是第一个结合CLIP模型和diffusion模型的AI开源绘画工具，其内核便是采用的CLIP引导扩散模型(CLIP-Guided diffusion model)</p>

<p>且后续有很多基于CLIP的一系列改进模型，比如Lseg、GroupViT、ViLD、GLIP</p>

<h3>1.1.2 CLIP匹配的一个示例</h3>

<p>如果让你把下图左侧三张图和右侧三句话配对，你可以轻松完成这个连线。但对 AI 来说，图片就是一系列像素点，文本就是一串字符，要完成这个工作可不简单。</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070203.png"></center>


<p>原因在于</p>

<ol>
<li>这需要 AI 在海量「文本-图片」数据上学习图片和文本的匹配。图中绿色方块是「图片潜在空间」的 N 张图片，紫色方块是「文本潜在空间」的 N 句描述语</li>
</ol>


<p>CLIP会努力将对应的 I1与T1(蓝色方块)匹配，而不是 I1与T2(灰色方块)匹配</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070204.png"></center>


<ol>
<li>当 AI 能成功完成这个连线，也就意味着 AI 建立了「文字潜在空间」到「图片潜在空间」的对应关系，这样才能通过文字控制图片的去噪过程，实现通过文字描述得到图像的生成</li>
</ol>


<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070205.png"></center>


<h2>1.2 从BLIP1、BLIP2到miniGPT4</h2>

<h3>1.2.1 BLIP1：ViT + BERT ——通过encoder-decoder统一理解与生成任务</h3>

<p>随着AI的迅速发展，多模态日渐成为一种趋势，而「Vision-Language Pre-training (VLP) + Fine-tuning => Zero Shot / Few Shot」的模式是快速解决多下游任务的一个好的模式，VLP 是这个模式的开端，所以对于 VLP 的相关研究也很多。BLIP 是一个新的 VLP 架构，可以灵活、快速的应用到下游任务，如：图像-文本检索、图像翻译、以及 VQA 等</p>

<p>简单来讲，BLIP的主要特点是结合了encoder和decoder，形成了统一的理解和生成多模态模型。再利用BLIP进行后续工作的时候，既可以使用其理解的能力(encoder)，又可以利用其生成的能力(decoder)，拓展了多模态模型的应用</p>

<h4>1.2.1.1 BLIP的模型结构</h4>

<p>CLIP 采用了 image-encoder (ViT / ResNet) &amp; text-encoder (transformer)，然后直接拿 图片特征 和 文本特征 做余弦相似度对比，得到结果，而BLIP 的做法要复杂挺多</p>

<p>如下图所示，为了预训练一个同时具有理解和生成能力的统一模型，BLIP模型主要由4个部分组成，从左至右分别是</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070206.jpeg"></center>


<ul>
<li>上图第1部分：视觉编码器Image Encoder(ViT)——提取图片特征</li>
</ul>


<p>视觉编码器本质就是 ViT 的架构：将输入图像分割成一个个的 Patch 并将它们编码为一系列 Image Embedding，并使用额外的 [CLS] token 来表示全局的图像特征</p>

<ul>
<li>上图第2部分：文本编码器Text Encoder(BERT)——提取文本特征</li>
</ul>


<p>文本编码器就是 BERT 的架构，其中 [CLS] token 附加到文本输入的开头以总结句子，作用是提取文本特征与第1部分的图像特征做对比学习</p>

<p>在这个过程中会训练一个对比学习目标函数 (Image-Text Contrastive Loss, ITC)</p>

<p>ITC 作用于第1部分的视觉编码器(ViT)和第2部分的文本编码器(BERT)，目标是对齐视觉和文本的特征空间，方法是使得正样本图文对的相似性更大，负样本图文对的相似性更低，在 ALBEF 里面也有使用到。作者在这里依然使用了 ALBEF 中的动量编码器，它的目的是产生一些伪标签，辅助模型的训练</p>

<p>为方便对比，把BLIP的模型结构图再贴一遍</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070207.jpeg"></center>


<ul>
<li>上图第3部分：视觉文本编码器Image-grounded Text Encoder(变种 BERT)——BERT中插入交叉注意层，从而针对图片特征和文本特征做二分类</li>
</ul>


<p>视觉文本编码器的具体做法是在文本编码器比如BERT的每个transformer  block的自注意(Bi Self-Att)层和前馈网络(Feed Forward)之间额外插入一个交叉注意(Cross-Attention)，以引入视觉特征，作用是根据 ViT 给的图片特征和文本输入做二分类，所以使用的是编码器，且注意力部分是双向的 Self-Attention，且添加一个额外的 [Encode] token，作为图像文本的联合表征</p>

<p>在这个过程中则训练一个图文匹配目标函数 (Image-Text Matching Loss, ITM)</p>

<p>ITM 作用于第1部分的视觉编码器和第3部分的视觉文本编码器，是一个二分类任务，目标是学习图像文本的联合表征，使用一个分类头来预测 image-text pair 的 正匹配 还是 负匹配，目的是学习 image-text 的多模态表示，调整视觉和语言之间的细粒度对齐，作者在这里依然使用了 ALBEF 中的 hard negative mining 技术</p>

<ul>
<li>上图第4部分：视觉文本解码器Image-grounded Text Decoder(变种 BERT)——根据图片特征和文本特征做文本生成</li>
</ul>


<p>视觉文本解码器使用 Cross-Attention，作用是根据 ViT 给的图片特征和文本输入做文本生成的任务，所以使用的是解码器，且将 上图第3部分的 Image-grounded Text Encoder 结构中的 Bi Self-Att 替换为 Causal Self-Att，目标是预测下一个 token，且添加一个额外的 [Decode] token 和结束 token，作为生成结果的起点和终点</p>

<p>一个需要注意的点是：相同颜色的部分是参数共享的，即视觉文本编码器和视觉文本解码器共享除 Self-Attention 层之外的所有参数。每个 image-text 在输入时，image 部分只需要过一个 ViT 模型，text 部分需要过3次文本模型</p>

<p>过程中训练一个语言模型目标函数 (Language Modeling Loss, LM)</p>

<p>毕竟由于BLIP 包含解码器，用于生成任务。既然有这个任务需求，那就意味着需要一个针对于生成任务的语言模型目标函数，LM 作用于第1部分的视觉编码器和第4部分的视觉文本解码器，目标是根据给定的图像以自回归方式来生成关于文本的描述。与 VLP 中广泛使用的 MLM 损失(完形填空)相比，LM 使模型能够将视觉信息转换为连贯的字幕</p>

<h4>1.2.1.2 BLIP的字幕与过滤器方法CapFiltg</h4>

<p>上述整个过程中，有一个不可忽略的问题，即高质量的人工注释图像-文本对(Ih,Th)(例如，COCO) 因为成本高昂所以数量不多</p>

<ul>
<li><p>CLIP 的数据来源于 Web 上爬来的 图像-文本对(Iw,Tw)，所以数据集很容易扩充的很大，而且采用 对比学习的方式，基本属于自监督了，不太需要做数据标注；</p></li>
<li><p>BLIP 改进了 CLIP 直接从 Web 取数据 噪声大 的缺点，提出了 Captioning and Filtering (CapFilt) 模块，这个模块就是用来 减小噪声、丰富数据的，主要包括两个模块：即字幕与过滤器方法CapFilt (Captioning and Filtering)</p></li>
</ul>


<p>如下图所示</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070208.png"></center>


<p>CapFilt 方法包含两个模块：</p>

<ol>
<li><p>字幕器 Captioner：相当于给一张网络图片，生成字幕。它是一个视觉文本解码器(对应于上述BLIP模型结构的第4部分)，在 COCO数据集上使用 LM 目标函数微调，对给定图像的文本进行解码，从而实现给定网络图片Iw，Captioner 生成字幕Ts的效果</p></li>
<li><p>过滤器 Filter：过滤掉噪声图文对image-text pair，它是一个视觉文本编码器(对应于上述BLIP模型结构的第3部分)，看文本是否与图像匹配，在 COCO 数据集上使用 ITC 和 ITM 目标函数微调</p></li>
</ol>


<p>Filter 删除原始 Web 文本Tw和合成文本 Ts中的嘈杂文本，如果 ITM 头将其预测为与图像不匹配，则认为文本有噪声</p>

<p>最后，将过滤后的图像-文本对与人工注释对相结合，形成一个新的数据集，作者用它来预训练一个新的模型</p>

<p>下图展示了被过滤器接受和拒绝的文本可视化(绿色 文本是被 filter 认可的，而 红色 文本是被 filter 拒绝的)</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070209.png"></center>


<h3>1.2.2 BLIP2：CLIP ViT-G/14 + Q-Former + FLAN-T5</h3>

<p>下图是BLIP-2的模型结构 (论文地址：BLIP-2: Bootstrapping Language-Image Pre-training with Frozen Image Encoders and Large Language Models)，其中视觉侧和文本侧分别使用预训练的CLIP ViT-G/14模型和FLAN-T5模型，仅中间的起桥接作用的Q-Former参与训练，训练需要的成本和数据量进一步降低，BLIP-2的训练数据量仅129M，16卡A100训练9天</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070210.png"></center>


<ul>
<li><p>后来的LLAVA工作更是将这一思路简化到极致，仅通过一个projection layer将CLIP ViT-L/14和Vicuna语言模型缝合在一起，训练数据仅用了595K图文对以及158K指令微调数据</p></li>
<li><p>miniGPT4则是在复用BLIP-2的vision encoder + Q-Former的基础上，通过一层project layer缝合了Vicuna语言模型，训练数据仅用了5M的图文对数据+3.5K的指令微调数据</p></li>
</ul>


<h3>1.2.3 MiniGPT4：基于LLaMA微调的Vicuna + BLIP2 + 线性投影层</h3>

<p>MiniGPT-4具有许多类似于GPT-4所展示的功能，如详细的图像描述生成和从手写草稿创建网站，以及根据给定图像编写灵感的故事和诗歌，为图像中显示的问题提供解决方案，比如教用户如何根据食物照片烹饪等</p>

<p>模型结构</p>

<p>miniGPT4的模型架构由一个语言模型拼接一个视觉模型，最后加一个线性投影层来对齐，具体而言</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070211.png"></center>


<ul>
<li><p>它先是使用基于LLaMA微调的小羊驼Vicuna，作为语言解码器</p></li>
<li><p>在视觉感知方面，采用了与BLIP-2相同的预训练视觉组件(该组件由EVA-CLIP[13]的ViT- G/14和Q-Former组成)</p></li>
<li><p>再之后，增加了一个单一的投影层，将编码的视觉特征与语言模型小羊驼对齐，并冻结所有其他视觉和语言组件</p></li>
</ul>


<p>模型训练：预训练(500万图像文本对)-微调</p>

<p>训练上，还是经典的预训练-微调模式</p>

<ol>
<li><p>在整个预训练过程中，无论是预训练的视觉编码器还是LLM都保持冻结状态，只有线性投影层被预训练。具体是使用Conceptual Caption、SBU和LAION的组合数据集来训练我们的模型，历经2万个训练步骤，批大小为256，覆盖了大约500万对图像-文本，整个过程花费大约10小时，且使用的4个A100 (80GB) gpu</p></li>
<li><p>然而，简单地将视觉特征与LLM对齐不足以训练出像聊天机器人那样具有视觉会话能力的高性能模型，并且原始图像-文本对背后的噪声可能导致语言输出不连贯。因此，我们收集了另外3500个高质量对齐的图像-文本对，用设计好的会话模板进一步微调模型(只需要400个训练步骤，批量大小为12，使用单个A100 GPU最终7分钟即可完成)，以提高生成语言的自然度及其可用性</p></li>
</ol>


<h1>第二部分 从DALLE到DALLE 2、DALLE 3</h1>

<h2>2.1 DALL-E：Zero-Shot Text-to-Image Generation</h2>

<p>有趣的是，DALL-E和CLIP一样，也是21年年初发布的，对应论文为《Zero-Shot Text-to-Image Generation》，其参数大小有着12B，其数据集是2.5 亿个图像文本对</p>

<ol>
<li><p>通过上一篇文章可知，VQ-VAE的生成模式是pixcl-CNN +codebook，其中pixcl-CNN就是一个自回归模型</p></li>
<li><p>OpenAI 将pixcl-CNN换成GPT，再加上那会多模态相关工作的火热进展，可以考虑使用文本引导图像生成，所以就有了DALL·E</p></li>
</ol>


<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070212.png"></center>


<p>DALL·E和VQ-VAE-2一样，也是一个两阶段模型：</p>

<ul>
<li>Stage1：Learning the Visual Codebook</li>
</ul>


<p>先是输入：一对图像-文本对(训练时)，之后编码特征，具体编码时涉及到两个步骤</p>

<p>首先，文本经过BPE编码得到256维的特征ft</p>

<p>其次，256×256的图像经过VQ-VAE(将训练好的VQ-VAE的codebook直接拿来用)，得到32×32的图片特征fq</p>

<p>We train a discrete variational autoencoder (dVAE) to compress each 256×256 RGB image into a 32 × 32 grid of image tokens</p>

<ul>
<li>Stage2：Learning the Prior</li>
</ul>


<p>重构原图</p>

<p>将fq拉直为1024维的tokens，然后连上256维的文本特征ft，这样就得到了1280维的token序列，然后直接送入GPT(masked decoder)重构原图</p>

<p>推理时，输入文本经过编码得到文本特征，再将文本通过GPT利用自回归的方式生成图片，生成的多张图片会通过CLIP模型和输入的文本进行相似度计算，然后调出最相似(描述最贴切)的图像</p>

<h2>2.2 DALLE 2：Hierarchical Text-Conditional Image Generation with CLIP Latents</h2>

<p>对于DALL·E2而言，基本就是整合了CLIP和基于扩散模型的GLIDE，而后者则采用了两阶段的训练方式：文本 → 文本特征 → 图片特征 → 图片</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070213.png"></center>


<ol>
<li>CLIP训练过程：学习文字与图片的对应关系</li>
</ol>


<p>如上图所示，CLIP的输入是一对对配对好的的图片-文本对(根据对应文本一条狗，去匹配一条狗的图片)，这些文本和图片分别通过Text Encoder和Image Encoder输出对应的特征，然后在这些输出的文字特征和图片特征上进行对比学习</p>

<ol>
<li>DALL·E2：prior + decoder</li>
</ol>


<p>上面的CLIP训练好之后，就将其冻住了，不再参与任何训练和微调，DALL·E2训练时，输入也是文本-图像对，下面就是DALL·E2的两阶段训练：</p>

<p>→ 阶段一 prior的训练：根据文本特征(即CLIP text encoder编码后得到的文本特征)，预测图像特征(CLIP image encoder编码后得到的图片特征)</p>

<p>换言之，prior模型的输入就是上面CLIP编码的文本特征，然后利用文本特征预测图片特征(说明白点，即图中右侧下半部分预测的图片特征的ground truth，就是图中右侧上半部分经过CLIP编码的图片特征)，就完成了prior的训练</p>

<p>推理时，文本还是通过CLIP text encoder得到文本特征，然后根据训练好的prior得到类似CLIP生成的图片特征，此时图片特征应该训练的非常好，不仅可以用来生成图像，而且和文本联系的非常紧(包含丰富的语义信息)</p>

<p>→阶段二 decoder生成图：常规的扩散模型解码器，解码生成图像</p>

<p>这里的decoder就是升级版的GLIDE(GLIDE基于扩散模型)，所以说DALL·E2 = CLIP + GLIDE</p>

<h2>2.3 DALLE 3：Improving Image Generation with Better Captions</h2>

<p>我司LLM项目团队于23年11月份在给一些B端客户做文生图的应用时，对比了各种同类工具，发现DALLE 3确实强(其对应的论文为《Improving Image Generation with Better Captions》，作者为James Betker等人)</p>

<p>加之也要在七月官网的论文100课上讲DALLE三代的三篇论文，故接下来，咱们结合DALLE 3和相关paper好好看下DALLE 3的训练细节</p>

<h3>2.3.1 为提高文本图像配对数据集的质量：基于谷歌的CoCa​微调出图像字幕生成器</h3>

<p>目前文生图模型的一个很大的问题是模型的文本理解能力，这个文本理解能力指的是生成的图像是否能和文本保持一致，也就是论文里面所说的prompt following能力</p>

<p>如论文中所说</p>

<ul>
<li>论文提出来字幕改进(caption improvement)的方法</li>
</ul>


<p>毕竟现在text-to-image模型现存的一个基本问题便是：这些模型训练所用的文本-图像配对数据的质量较差</p>

<p>We hypothesize that a fundamental issue with existing text-to-image models is the poor quality of the text and image pairing of the datasets they were trained on</p>

<ul>
<li>故论文提出可以通过改进「文本-图像配对数据集中的针对图像的文本描述的质量」来解决这个问题(We propose to address this by generating improved captions for the images in our dataset)</li>
</ul>


<p>为实现这个目标，首先训练一个强大的图像字幕生成器(image captioner)，以生成详细、准确的图像描述，然后我们便可以把这个图像字幕生成器应用到已有的文本-图像配对数据集中，以为各个图像生成更详细、准确的图像描述或字幕，最后在改进的数据集上训练text-to-image模型</p>

<p>We do this by first learning a robust image captioner which produces detailed, accurate descriptions of images. We then apply this captioner to our dataset to produce more detailed captions. We finally train text-to-image models on our improved dataset.</p>

<p>总之，对于稍微复杂的文本，目前的文生图模型生成的图像往往会容易忽略部分文本描述，甚至无法生成文本所描述的图像。这个问题主要还是由于训练数据集本身所造成的，更具体的是说是图像caption不够准确</p>

<ol>
<li><p>一方面，图像常规的文本描述往往过于简单(比如COCO数据集)，它们大部分只描述图像中的主体而忽略图像中其它的很多信息，比如背景，物体的位置和数量，图像中的文字等</p></li>
<li><p>另外一方面，目前训练文生图的图像文本对数据集(比如LAION数据集)都是从网页上爬取的，图像的文本描述其实就是alt-text，但是这种文本描述很多是一些不太相关的东西，比如广告。训练数据的caption不行，训练的模型也就自然而然无法充分学习到文本和图像的对应关系，那么prompt following能力必然存在问题</p></li>
</ol>


<h4>2.3.1.1 什么是谷歌的CoCa</h4>

<p>OpenAI最终基于谷歌的CoCa来训练这个image captioner来合成图像的caption，CoCa构建在encoder-decoder的基础上</p>

<ul>
<li><p>其中，Image Encoder和Text Decoder均采用Transformer模型</p></li>
<li><p>其中，Image Encoder的参数量为1B，而Text Decoder的参数量达到1.1B，这样整个CoCa模型的参数量为2.1B(对于图像，其输入大小为288×288，而patch size为18x18，这样总共有256个image tokens。同时，这里也设计了两个更小模型：CoCa-Base和CoCa-Large)</p></li>
</ul>


<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070214.png"></center>


<p>不过这里将text decoder均分成两个部分：</p>

<ol>
<li><p>一个单模态解码器unimodal text decoder</p></li>
<li><p>一个多模态解码器multimodal text decoder</p></li>
</ol>


<p>然后增加一个cls token在文本的最后(CoCa相比CLIP额外增加了一个Multimodel Text Encoder来生成caption，如此，它训练的损失包含了CLIP的对比损失和captioing的交叉熵损失，所以CoCa不仅可以像CLIP那样进行多模态检索，也可以用于caption生成)</p>

<p>具体而言，如下图的左半部分所示</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070215.png"></center>


<ol>
<li>unimodal text decoder不参与对图像特征的cross-attention(We omit cross-attention in unimodal decoder layers to encode text-only representations，相当于把 Transformer Decoder 中的cross attention去掉，只保留 masked self-attention 和 FFNN)</li>
</ol>


<p>→  这样cls token经过unimodal text decoder之后就能够得到整个句子的全局特征</p>

<p>→  同时采用attention pooling对image encoder得到特征提取图像的全局特征(这里的attention pooling其实就是一个multi-head attention，只不过key和value是image encoder得到的特征，而query是预先定义的一个可训练的embedding，由于我们只需要提取一个全局特征，所以只需要定义一个query就好了)</p>

<p>两个全局特征就可以实现图像-文本的对比学习「相当于image encoder和unimodal text decoder的两个[CLS]向量作为图片和文本的表示，进行batch 内对比学习，同 CLIP」</p>

<ol>
<li>multimodal text decoder将用来执行生成任务，这里也通过一个attention pooling对image encoder得到的特征进行提取，不过这里query数量定义为256，这样attention pooling可以得到256个特征，它作为multimodal text decoder的cross-attention的输入(cascademultimodal decoder layers cross-attending to image encoder outputs to learn multimodal image-textrepresentations)</li>
</ol>


<p>相当于就是正常的Transformer Decoder，包含masked self-attention、cross-attention、FFNN，用于融合图片和文本信息，实现双模态，最后做文本生成</p>

<ol>
<li>所以，CoCa将同时执行对比学习和文本生成任务，它的训练损失也包括两个部分：CoCa=λCon⋅Con+λCap⋅Cap</li>
</ol>


<p>顺带说一句，这个CoCa和如下图所示的ALBEF(这是其论文)还挺像的，所以有人评论它两的关系就像BERT和RoBERTa(可以简单理解为是对BERT的精调)</p>

<ol>
<li><p>但CoCa 与 ALBEF 最大的不同在于：CoCa 右侧处理文本和进行多模态融合的网络是一个 decoder，而非ALBEF那里是 encoder</p></li>
<li><p>再说一句，其实下图的下半部分不就是类似一个CLIP么？^_^</p></li>
</ol>


<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070216.png"></center>


<h4>2.1.1.2 分别通过短caption、长caption微调预训练好的image captioner</h4>

<p>为了提升模型生成caption的质量，OpenAI对预训练好的image captioner进行了进一步微调，这个微调包括两个不同的方案，两个方案构建的微调数据集不同</p>

<ul>
<li><p>第一个方案的微调数据集是只描述图像主体的短caption(类似COCO风格的caption)</p></li>
<li><p>而第二个方案的微调数据集是详细描述图像内容的长caption</p></li>
</ul>


<p>相应地，两个微调模型分别可以生成：短caption(short synthetic captions，简称SSC)、长caption(descriptive synthetic captions，简称DSC)</p>

<p>下图展示了三个样例图像各自的原始caption(ground-truth)，以及生成的短caption、长caption</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070217.png"></center>


<ul>
<li><p>每个样例图像的原始caption是从网页上得到的alt-text，其质量较差</p></li>
<li><p>每个样例图像的合成的短caption简洁地描述了图像的主体内容</p></li>
<li><p>每个样例图像的合成的长caption详细描述了图像的很多内容，细节比较丰富</p></li>
</ul>


<h4>2.1.1.3 为提高合成caption对文生图模型的性能：采用描述详细的长caption，训练的混合比例高达95%</h4>

<p>接下来要解决两个问题</p>

<ol>
<li><p>通过实验来分析合成caption对文生图模型性能的影响</p></li>
<li><p>另外一点是探讨训练过程中合成caption和原始caption的最佳混合比例</p></li>
</ol>


<p>这里之所以要混合合成caption和原始caption(opted to blend synthetic captions with ground truth captions)，主要是为了防止模型过拟合到合成caption的某些范式</p>

<p>比如最常见的例子是合成的caption往往以"a"和"an"开头，解决这个问题最好的方法便是：通过接近the style and formatting that humans might use，以regularize our inputs to a distribution of text</p>

<p>所以，我们在训练过程中，在合成caption中混入原始caption(human-written text)，相当于一种模型正则化</p>

<p>首先是合成caption对模型性能的影响，这里共训练了三个模型，它们的差异是采用不同类型的caption，分别是</p>

<ol>
<li><p>只用原始caption</p></li>
<li><p>5%的原始caption + 95%的合成短caption</p></li>
<li><p>5%的原始caption + 95%的合成长caption</p></li>
</ol>


<p>通过上一节对DALLE 2的分析，我们得知最后的文生图decoder模型是基于扩散模型的</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070218.png"></center>


<p>而DALLE 3的论文中训练的文生图是latent diffusion模型</p>

<ul>
<li><p>其VAE和SD一样都是8x下采样</p></li>
<li><p>而text encoder采用T5-XXL，之所以用T5-XXL，可能主要有两个原因，一方面T5-XXL可以编码更长的文本，另外一方面是T5-XXL的文本编码能力也更强</p></li>
<li><p>这里训练的图像尺寸为256x256(这只是实验，所以低分辨率训练就足够了)，采用batch size为2048共训练50W步，这相当于采样了1B样本</p></li>
</ul>


<p>论文中并没有说明UNet模型的具体架构，只是说它包含3个stages，应该和SDXL类似(SDXL包含3个stage，只下采样了2次，第一个stage是纯卷积，而后面两个stages包含attention)</p>

<p>这里只是想验证模型的prompt following能力，所以采用了CLIP score来评价模型，这里的CLIP score是基于以下两者的相似度，即</p>

<ol>
<li><p>CLIP ViT-B/32来计算生成图像的image embedding即zi</p></li>
<li><p>和text prompt对应的text embedding即zt的余弦相似度(这里是生成了50000个图像并取平均值，并乘以100)</p></li>
</ol>


<p>C(zi,zt)=zi⋅zt‖zi‖‖zt‖</p>

<p>下图展示了采用三种caption训练的模型在CLIP score上的差异：</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070219.png"></center>


<ul>
<li><p>左图在计算CLIP score时，text采用原始caption(ground truth caption)，从整体上来看，无论是采用合成的长caption还是短caption，其CLIP score比只采用原始caption要好一点，但是波动比较大</p></li>
<li><p>右图计算CLIP score时，text采用合成的长caption，这里就可以明显看到：合成长caption > 合成短caption > 原始caption，而且CLIP score要比左图要高很多</p></li>
</ul>


<p>这说明采用长caption来计算CLIP score是比较合理的，因为图像的image embedding信息很大，而短文本信息少，所以两者的相似度就会低一些，而且还可能存在一定的波动</p>

<p>总之，从上面的实验来看，采用合成的长caption对模型的prompt following能力是有比较大的提升的。</p>

<p>接下来的问题就是通过实验来找到最佳的数据混合比例。所以又增加了混合比例为65%、80%、90%的实验，下图展示了不同混合比例训练出来的模型其CLIP score的差异，可以看到采用95%的合成caption训练的模型在效果上要明显高于采用更低比例的caption训练的模型。</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070220.png"></center>


<p>所以最终的结论是采用合成的caption对模型提升帮助比较大，而且要采用描述详细的长caption，训练的混合比例高达95%，这也是后面DALL-E 3的数据训练策略。</p>

<p>不过采用95%的合成长caption来训练，得到的模型也会“过拟合”到长caption上，如果采用常规的短caption来生成图像，效果可能就会变差</p>

<p>为了解决这个问题，OpenAI采用GPT-4来“upsample”用户的caption，下面展示了如何用GPT-4来进行这个优化，不论用户输入什么样的caption，经过GPT-4优化后就得到了长caption：</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070221.png"></center>


<p>下图展示了三个具体的例子，可以看到使用优化后的长caption(图中第二排)其生成图像效果要优于原来的短caption(图中第一排)：</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070222.png"></center>


<p>所以，DALL-E 3接入ChatGPT其实是不得已而为之的事情，因为这样才能保证DALL-E 3的输入不偏离训练的分布</p>

<h3>2.3.2 训练细节总结：原始caption和合成长caption混合训练 + T5 + latent decoder</h3>

<p>DALLE 3的论文提到，DALL-E 3的具体实现有两个关键点：</p>

<ol>
<li><p>首先，DALL-E 3也采用95%的合成长caption和5%的原始caption混合训练，这也应该是DALL-E 3性能提升的关键</p></li>
<li><p>另外，DALL-E 3的模型是上述实验中所采用的模型的一个更大版本(基于T5-XXL的latent diffusion模型)，同时加上其它的改进(但论文中并没有说是具体的哪些改进，只说DALL-E 3也不应简单地归功于在合成caption的数据集上训练，所以DALL-E 3的其它改进应该也会比较重要)</p></li>
</ol>


<p>目前DALL-E 3生成的图像的分辨率都是在1024x1024以上，所以DALL-E 3的模型应该类似于SDXL，采用递进式的训练策略(256 -> 512 -> 1024)，而且最后也是采用了多尺度训练策略来使模型能够输出各种长宽比的图像</p>

<p>另外如小小将所说，附录里面给出了一个细节是</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070223.png"></center>


<ul>
<li>DALL-E 3额外训练了一个latent decoder来提升图像的细节，特别是文字和人脸方面，这个应该是为了解决VAE所产生的图像畸变。这里采用的扩散模型是基于DDPM中的架构，所以是pixel diffusion</li>
</ul>


<p>从直观上来看，这个latent decoder就是替换原始的VAE decoder，因此这个扩散模型的condition是VAE的latent</p>

<p>但是具体是怎么嵌入到扩散模型的UNet中，这里没有说明，最简单的方式应该是通过上采样或者一个可学习的网络将latent转变为和噪音图像一样的维度，然后与噪音图像拼接在一起</p>

<ul>
<li>同时为了加速，基于Consistency Models中提出的蒸馏策略将去噪步数降低为2步，所以推理是相当高效的</li>
</ul>


<p>This diffusion decoder is a convolutional U-Net identical to the one described in Ho et al. (2020). Once trained, we used the consistency distillation process described in Song et al. (2023) to bring it down to two denoising steps</p>

<h3>2.3.3 DALL-E 3的评测结果</h3>

<p>对于DALL-E 3的评测，论文是选取了DALL-E 2和SDXL(加上refiner模块)来进行对比。模型评测包括自动评测和人工评测。</p>

<h4>2.3.3.1 针对DALLE 3的自动评测</h4>

<p>自动评测主要有3个指标</p>

<ol>
<li>首先是计算CLIP score，评测数据集是从COCO 2014数据集中选择4096个captions</li>
</ol>


<p>这里的评测数据集是Imagen中所提出的DrawBench评测集(共包括200个包含不同类型的prompts)</p>

<ol>
<li>然后是采用GPT-4V来进行评测</li>
</ol>


<p>这里的评测是将生成的图像和对应的text输入到GPT-4V，然后让模型判断生成的图像是否和text一致，如果一致就输出正确，否则就输出不正确</p>

<ol>
<li>最后是采用T2I-CompBench来评测</li>
</ol>


<p>这个评测集包含6000个组合类型的text prompts，它包括多个方面，这里只选择color binding、shape binding和texture binding三个方面进行评测，评测是通过BLIP-VQA model来得到评分</p>

<p>下表给出了DALL-E 3与其它模型的对比结果，可以看到DALL-E 3还是明显优于DALL-E 2和SDXL。不过自动评测所选择的三个指标都是评测模型的prompt following能力，并不涉及到图像质量</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070224.png"></center>


<h4>2.3.3.2 针对DALLE 3的人工评测</h4>

<p>人工评测主要包括三个方面</p>

<ol>
<li><p>第一个是prompt following，给出两张不同的模型生成的图像，让人来选择哪个图像和文本更一致</p></li>
<li><p>第二个是style，这里不给文本，只给两张图像，让人选择更喜欢的那个图像</p></li>
<li><p>第三个是coherence，这里也不给文本，让人从两张图像中选择包含更多真实物体的图像</p></li>
</ol>


<p>可以看到后面的两个方面其实就是评测模型生成图像的质量，对于prompt following和style，这里使用的评测集是DALL-E 3 Eval，它共包含170个captions，是从用户应用场景收集得到的。而coherence方面的评测集是从COCO数据集抽样的250个captions，因为coherence是评测真实性，所以采用更偏真实场景的COCO数据集</p>

<p>另外，之前DrawBench评测集是采用GPT-4V来自动评测的，但是GPT-4V也会犯错，比如在计数方面，所以这里也额外增加了DrawBench评测集的人工评测。</p>

<p>所有人工评测的对比结果如表所示，可以看到DALL-E 3具有明显优势：</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070225.png"></center>


<p>这里的ELO score是GLIDE中所提出的来计算胜率的指标：</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070226.png"></center>


<p>尽管DALL-E 3在prompt following方面有很大的提升，但是它依然有一定的局限性</p>

<ol>
<li><p>首先是模型在空间位置关系上还是会比较容易出错，当prompt包含一些位置关系描述如"to the left of"，模型生成的图像并不一定会符合这样的位置关系，这主要是因为合成的caption在这方面也并不可靠</p></li>
<li><p>另外就是文字生成能力，虽然DALL-E 3在生成文字方面很强，但是还是会出现多词或者少词的情况，一个可能的原因T5-XXL的tokenizer并不是字符级的</p></li>
<li><p>还有一个比较大的问题是合成的caption会幻想图像中的重要细节，比如给一幅植物的绘图，image captioner可能会虚构出一个植物并体现在合成的caption上。这就意味着模型可能会学到错误的绑定关系，也导致模型在生成特定种类的东西并不可靠</p></li>
<li><p>最后一点是模型的安全性和偏见，这个是所有大模型所面临的问题，也是由于训练集所导致的，对于这点，DALL·E 3 system card有更多的评测</p></li>
</ol>


<h1>第三部分 通俗理解stable diffusion：文本到图像的潜在扩散模型(改进版DDPM)</h1>

<h2>3.1 SD与DDPM的对比</h2>

<p>我们在上篇博客《AI绘画能力的起源：从VAE、扩散模型DDPM、DETR到ViT/MAE/Swin transformer》中，已经详细介绍了扩散模型DDPM，为方便对后续内容的理解，特再回顾下DDPM相关的内容(至于详细的请看上篇博客)：</p>

<p>前向过程加噪</p>

<p>xt=α¯t‾‾√x0+1−α¯t‾‾‾‾‾‾√ε</p>

<p>逆向过程去噪，由于反向扩散过程不可直接计算，因此训练神经网络εθ来近似它，训练目标(损失函数)如下</p>

<p>Lsimple =��t,x0,ε[‖ε−εθ(xt,t)‖2]</p>

<p>所以，DDPM的关键是训练噪声估计模型，使其预测的噪声 ϵ^ 与真实用于破坏的噪声 相近，具体怎么个训练过程呢？</p>

<ol>
<li><p>比如可以选择选择一个训练图像，例如海边沙滩的照片，然后生成随机噪声图像，通过将此噪声图像添加到一定数量的步骤来损坏训练图像</p></li>
<li><p>然后通过一个噪声估计器U-Net 预测添加的噪声，使其与实际噪声做差异对比，从而建立损失函数做反向传播，最后更新噪声估计器的参数</p></li>
</ol>


<p>说白了，就是通过告诉U-Net 我们在每一步添加了多少噪声，从而手把手一步步教U-Net 怎么去估计噪声</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070227.png"></center>


<ol>
<li>U-Net训练好了之后(意味着其对噪声的估计比较准确了)，我们便可以基于训练好的 U-Net 来生成图像</li>
</ol>


<p>原理很简单，通过噪声估计器预测出噪声后，不模糊的图片便减去每一步添加的噪声以清晰化 不就ok了么</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070228.png"></center>


<p>而如果用一句话介绍Stable Diffusion(SD对应的论文为《High-Resolution Image Synthesis with Latent Diffusion Models》，由慕尼黑大学和runway于2021年年底共同提交，其一作为Robin Rombach)，可以理解为：改进版的DDPM，Stable Diffusion原来的名字叫“Latent Diffusion Model”(LDM)，很明显就是扩散过程发生隐空间中(即latent space，下文或其他地方也有说潜在空间或压缩空间之类的，都一个意思)，其实就是对图片做了压缩</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070229.png"></center>


<p>总之，Stable Diffusion会先训练一个自编码器，来学习将图像压缩成低维表示</p>

<ul>
<li><p>通过训练好的编码器E，可以将原始大小的图像压缩成低维的latent data（图像压缩）</p></li>
<li><p>通过训练好的解码器D，可以将latent data还原为原始大小的图像</p></li>
</ul>


<p>在将图像压缩成latent data后，便可以在latent space中完成扩散过程，对比下和Diffusion扩散过程的区别，如下图所示：</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070230.png"></center>


<p>可以看到Diffusion扩散模型就是在原图 x 上进行的操作，而Stale Diffusion是在压缩后的图像 z 上进行操作</p>

<h2>3.2 从模型结构和源码彻底理解Stable Diffusion</h2>

<h3>3.2.1 SD的模型结构：autoencoder + CLIP text encoder + UNet</h3>

<p>stable diffusion和上面DALL E2的原理其实差不多，具体而言，会先后经历以下几个步骤「左边红色是像素空间 pixel space(含编码器ε和解码器D)、中间绿色区域是潜在空间latent space、右边灰色的是条件condition」</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070231.png"></center>


<ol>
<li>将图像从像素空间(Pixel Space)压缩到潜在空间(Latent Space)</li>
</ol>


<p>图像编码器  可以对图像x进行压缩，可以理解为它能忽略图片中的高频信息，只保留重要的深层特征，从而将x从(3, 515, 512)的像素空间压缩到一个维度为(4, 64, 64)的潜在空间</p>

<p>这个就是所谓的感知压缩（Perceptual Compression），即“将高维特征压缩到低维，接着在低维空间上进行操作”的方法具有普适性，可以很容易的推广到文本、音频、视频等不同模态的数据上</p>

<ol>
<li>针对潜在空间的图片做扩散(Diffusion Process)：添加噪声</li>
</ol>


<p>对潜在空间Latent Space中的图片添加噪声，进行扩散过程，将其结果作为 U-Net 的输入</p>

<ol>
<li>根据用户的输入text/prompt 获取去噪条件(Conditioning)</li>
</ol>


<p>text encoder提取输入text/prompt的text embeddings(维度为77✖️768，意味着77个token嵌入向量，其中每个向量包含768个维度)</p>

<p>然后通过cross attention方式送入噪声估计模型UNet中作为去噪条件condition(当然了，去噪时可以灵活地以文本、图像和其他形式为条件，比如以文本为条件即text2img、以图像为条件即 img2img)</p>

<ol>
<li>噪声估计器U-Net 预测噪声，然后依据「去噪条件」去噪：生成图片的潜在表示</li>
</ol>


<p>首先，噪声估计器 U-Net 将潜在噪声图像和文本提示作为输入，并预测噪声「该过程也在潜在空间(4x64x64 张量)中」</p>

<p>之后，基于上面第3步的去噪条件对图像通过 噪声估计器U-NET进行去噪(Denoising)，以获得生成图片的潜在表示</p>

<p>具体而言，可以分为以下4步骤</p>

<p>a) 事实上，扩散模型可以理解为一个时序去噪自编码器</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070232.png"></center>


<p>，我们需要训练使得预测的噪声与真实噪声相近，则目标函数为：</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070233.png"></center>


<p>b) 而在 Latent Diffusion Models 中，引入了预训练的感知压缩模型，它包含一个编码器和一个解码器，这样就可以在训练时利用 得到 latent representation ，从而让模型在潜在表示空间中学习，其目标函数为：</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070234.png"></center>


<p>c ) 对于条件生成任务，我们将拓展为：，这样就可以通过来控制图片合成的过程，通过在 U-Net 上增加 cross-attention 机制来实现</p>

<p>而为了能够从不同的模态预处理，论文引入了一个领域专用编码器，它用来将映射成一个中间表示</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070235.png"></center>


<p>，这样我们就可以很方便的引入各种形式的条件，例如文本、类别、layout等</p>

<p>d) 最终模型可通过一个 cross-attention 将去噪条件引导融入到 UNet 中(作为query向量，作为key和value向量)，cross-attention 表示为：</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070236.png"></center>


<p>其中</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070237.png"></center>


<p>是 UNet 的一个中间表征</p>

<p>最终，目标函数可以写为：</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070238.png"></center>


<p>总之，加噪后的隐式表达zt和文本语义表征通过Attention的方式输入到U-Net，用以更好的学习文本和图像的匹配关系，这里文本的语义向量就是条件(用户的输入text或prompt)，控制图像生成往我们想要的方向发展，通过U-Net来预测每一步要减少的噪声</p>

<p>然后计算网络输出噪声和真实噪声的差距，最小化损失来反向传播更新参数，注意整个训练阶段VAE是冻结的 也如七月官网的「大模型线上营」中的一学员战士长所说，这里面有两个东西，一个是传统的diffuion，是通过预测噪声来还原成图片，另一个是unet里嵌入的注意力模块，用来控制你图片还原的方向</p>

<ol>
<li>潜在空间转换成最终图像</li>
</ol>


<p>图像解码器负责将去噪后的 latent 图像从潜在空间恢复到原始像素空间来生成最终图x̃ 「各维度为：(3,512,512)，即（红/绿/蓝, 宽, 高)」</p>

<p>下图亦可辅助理解</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070239.png"></center>


<h3>3.2.2 从源码层面理解SD</h3>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024070240.png"></center>


<p>// 待更</p>

<h2>3.3 七月AIGC模特生成平台：基于SD的微调与二次开发</h2>

<p>详见七月官网右上角的体验入口，或七月的SD课程或AIGC模特生成课程</p>

<h1>第四部分 从SDXL Turbo、LCM到stable diffusion3</h1>

<p>为避免本文太长，故特把SDXL Turbo、LCM、SD3相关的内容放到新一篇文章中，详见《文生图的最新进展：从LCM、SDXL到与sora同架构的Stable Diffusion3》</p>

<h1>参考文献与推荐阅读</h1>

<ol>
<li><p>Learning Transferable Visual Models From Natural Language Supervision CLIP原始论文</p></li>
<li><p>​​​​​CLIP 论文逐段精读，这是针对该视频解读的笔记之一：CLIP和改进工作串讲</p></li>
<li><p>BLIP: Bootstrapping Language-Image Pre-training for Unified Vision-Language Understanding and Generation BLIP原始论文</p></li>
<li><p>多模态超详细解读 (六)：BLIP：统一理解和生成的自举多模态模型，极智AI | 多模态新姿势 详解 BLIP 算法实现</p></li>
<li><p>理解DALL·E 2， Stable Diffusion和 Midjourney工作原理</p></li>
<li><p>读完 DALL-E 论文，我们发现大型数据集也有平替版</p></li>
<li><p>Hierarchical Text-Conditional Image Generation with CLIP Latents （DALL E2原始论文）</p></li>
<li><p>DALL·E 2（内含扩散模型介绍），这是针对该视频解读的笔记</p></li>
<li><p>DALL-E 3技术报告阅读笔记</p></li>
<li><p>OpenAI一夜颠覆AI绘画！DALL·E 3+ChatGPT强强联合，画面直接细节爆炸</p></li>
<li><p>CoCa：对比+生成式任务构建“全能型多模态模型”，这是其论文地址</p></li>
<li><p>关于CoCa的另外几篇文章：CoCa: 在图生文过程中加入对比学习、如何评价谷歌的 CoCa，在 ImageNet 上达到新的 SOTA：91.0%？</p></li>
<li><p>浅析多模态大模型的前世今生</p></li>
<li><p>SD的奠基论文《High-Resolution Image Synthesis with Latent Diffusion Models》，由慕尼黑大学和runway于2021年年底共同提交</p></li>
<li><p>The Illustrated Stable Diffusion，这是图解SD的翻译版</p></li>
<li><p>Stable Diffusion Clearly Explained! How does Stable Diffusion paint an AI artwork? Understanding the tech behind the rise of AI-generated art.</p></li>
<li><p>Getting Started With Stable Diffusion</p></li>
<li><p>知乎上关于SD原理的几篇文章：Stable Diffusion 的技术原理是什么？、人工智能Ai画画——stable diffusion 原理和使用方法详解、十分钟读懂Stable Diffusion</p></li>
<li><p>Memory-efficient array redistribution through portable collective communication</p></li>
<li><p>stable diffusion 原理介绍 - AI绘画每日一帖</p></li>
<li><p>How does Stable Diffusion work?</p></li>
<li><p>Stable Diffusion 原理介绍与源码分析（一）</p></li>
<li><p>保姆级讲解 Stable Diffusion</p></li>
<li><p>李宏毅生成式AI模型diffusion model/stable diffusion概念讲解</p></li>
<li><p>速度惊人！手机跑Stable Diffusion，12秒出图，谷歌加速扩散模型破记录</p></li>
<li><p>SDXL Turbo、LCM相继发布，AI画图进入实时生成时代：字打多快，出图就有多快</p></li>
<li><p>文生图10倍速，视频实时渲染！清华发布LCM：兼容全部SD大模型、LoRA、插件等</p></li>
<li><p>七月在线：AIGC下SD/MDJ的原理与实战课 [深度探究AI绘画/多模态]</p></li>
</ol>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[多模态大语言模型调研]]></title>
    <link href="https://ibillxia.github.io/blog/2024/06/27/multimodal-llm-survey/"/>
    <updated>2024-06-27T10:53:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2024/06/27/multimodal-llm-survey</id>
    <content type="html"><![CDATA[<p>原文链接：<a href="https://zhuanlan.zhihu.com/p/689385182">https://zhuanlan.zhihu.com/p/689385182</a></p>

<p>本文是关于论文《MM-LLMs: Recent Advances in MultiModal Large Language Models》的简要介绍。大型语言模型沿着多模态方向发展成为目前越来越受关注的研究领域，这篇论文从方法角度整理了2022年到2024年2月的经典多模态大语言模型，并从技术角度给出了一些前瞻思路。本文主要按照作者提供的框架和案例进行介绍。</p>

<p>有关本专栏的更多内容，请参考大语言模型文献调研专栏目录</p>

<h1>1. 文章简介</h1>

<h2>1.1 基本信息</h2>

<p>题目：MM-LLMs: Recent Advances in MultiModal Large Language Models</p>

<p>论文：https://arxiv.org/pdf/2401.13601.pdf?trk=public_post_comment-text</p>

<p>项目主页：https://mm-llms.github.io/</p>

<p>论文引用：</p>

<pre><code>@article{zhang2024mm,
  title={Mm-llms: Recent advances in multimodal large language models},
  author={Zhang, Duzhen and Yu, Yahan and Li, Chenxing and Dong, Jiahua and Su, Dan and Chu, Chenhui and Yu, Dong},
  journal={arXiv preprint arXiv:2401.13601},
  year={2024}
}
</code></pre>



<!--more-->


<h2>1.2 多模态大语言模型简介</h2>

<p>GPT诞生以来，一向以强大的自然语言处理能力而著称，人们试着将大型语言模型（Large Language Models，LLM）的强大推理和生成能力在除文本以外的模态数据上应用起来。例如图像、视频、音频、3D点云等。得益于各个模态的数据都已经有各自的高质量的编码器和生成器，再加上LLM的加持，可以实现很多有趣的任务，多模态的大语言模型（Multi-modal Large Language Models， MM-LLMs）由此而来。</p>

<p>多模态大语言模型的关键是如何将各个模态的模型（例如图像编码器，视频生成器等）与大语言模型结合起来，因为不同的模态是原生的不兼容的。这就涉及到多模态领域中的一个概念：对齐（Alignment）。这就对MM-LLMs框架提出了要求，一方面，各个模态的编码器、生成器所使用的编码，要和大语言模型中所使用的编码（也就是token）进行对齐，另一方面，在人机交互时，需要模型的功能能够对齐用户的意图，而非答非所问。</p>

<h2>1.3 多模态大语言模型的简要历史</h2>

<p>自从GPT-4和Gemini诞生后，多模态大语言模型吸引众多研究者加入；早期框架还是致力于用LLM辅助特定模态的理解，例如LLaVA、BLIP-2、Video-ChatGPT等；后续陆陆续续有研究关注多模态生成任务，例如MiniGPT-5、SpeechGPT、AudioPaLM等；近期最新成果关注和人类类似的从任意到任意（Any-to-Any）模态的交互，例如AudioGPT、NExt-GPT等。很多公司和机构高也在布局大语言模型以服务于特定的业务需要。</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024062701.png"></center>


<h1>2. 多模态大语言模型的整体架构</h1>

<p>目前的多模态大语言模型采取并联式的框架，以NExtGPT为例，多模态数据送到各自模态的编码器后得到各自模态的编码，这些编码通过各自模态的输入转换器（Input Projection）后得到对齐到大语言模型的文本编码，经过大语言模型处理后的输出，经过限定模态的输出转换器（Output Projection）以及生成器得到多模态的数据。通常的，Input Projection和Output Projection是需要训练的，这些模块占整个框架的参数量的比例很小，通常在2%左右。其他模块多是现成的经过大量数据训练过的，可以固定参数，也可以跟随Projection模块一起微调。</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024062702.png"></center>


<h2>2.1 各个模态的编码器</h2>

<p>各个模态的编码器用于将特定模态的数据转换成编码，现在已经有很多成熟的编码器可以解决这些问题，除了文本模态之外：</p>

<ul>
<li><p>视觉编码器：例如CLIP ViT、SAM HQ、DiNoV2等；</p></li>
<li><p>语音编码器：例如CFormer、Whisper、CLAP等；</p></li>
<li><p>3D编码器：例如ULIP2；</p></li>
<li><p>联合编码器：各种模态均可处理，例如ImageBind；</p></li>
</ul>


<h2>2.2 输入转换器</h2>

<p>用于模态对齐，将多模态数据编码对齐到文本空间，也是多模态融合的关键，这一模块是需要训练的。通常的转换器可以采取的形式有：</p>

<ul>
<li><p>一个简单的线性层；</p></li>
<li><p>多层感知机</p></li>
<li><p>其他类型，例如Cross-Attention、Q-former等</p></li>
</ul>


<h2>2.3 大型语言模型底座</h2>

<p>这一部分可以独自的进行文本到文本的转换，当有其他模态的数据加入时，大型语言模型会将这些模态的数据也认定为一种文本，输出的模态也是以文本的形式，交给后续解码器处理。常见的用于多模态的大语言模型有ChatGLM、PaLM、Vicuna等。</p>

<h2>2.4 输出转换器</h2>

<p>用于将大语言模型的输出转换成多模态生成器空间中所使用的输入编码。与输入转换器形态类似。</p>

<h2>2.5 多模态生成器</h2>

<p>针对不同模态，目前已经有比较成熟的生成解决方案，可以直接拿来使用。例如</p>

<ul>
<li><p>图像生成：Stable Diffusion</p></li>
<li><p>视频生成：ZeroScope</p></li>
<li><p>语音生成：AudioLDM-2</p></li>
</ul>


<h2>2.6 多模态大语言模型的训练框架</h2>

<p>根据训练数据类型的不同，目前的多模态大语言模型通常采用两种训练框架，MMPT和MMIT。前者使用X-文本对形式的输入数据，这里X就是各种模态的输入。例如图像-文本、视频-文本、语音-文本；MMIT使用指令形式的数据，将有监督微调（SFT）和强化学习结合起来。</p>

<h1>3. 目前经典的多模态大语言模型的及其评测</h1>

<h2>3.1 当前经典的大语言模型</h2>

<p>分类。作者根据设计思路将多模态大语言模型按照功能和设计进行分类。</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024062703.png"></center>




<center><img src="https://ibillxia.github.io/images/2024/IMAG2024062704.png"></center>


<p>目前的大语言模型表现出以下趋势：</p>

<ol>
<li><p>从多模态理解到多模态生成，如MiniGPT-4=>MiniGPT-5=>NExtGPT；</p></li>
<li><p>更好的对齐人的意图以及更好的交互，如BLIP-2=>InstructBLIP=>DRESS；</p></li>
<li><p>扩展更多的模态类型；</p></li>
<li><p>使用更高质量的多模态数据；</p></li>
<li><p>更高效的网络结构；</p></li>
</ol>


<h2>3.2 目前SOTA多模态大语言模型及其在特定领域的性能</h2>

<p>作者以视觉-语言任务为例，提供了多模态大语言模型在18个benchmark上的性能：</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024062705.png"></center>


<p>并且有一些发现：</p>

<ol>
<li><p>更高的视觉分辨率可以获得更好的更细节的多模态性能；</p></li>
<li><p>高质量的有监督微调数据集可以促进特定任务中的多模态性能；</p></li>
<li><p>在LLM上使用PEFT可以促进编码对齐</p></li>
<li><p>交叉使用图像和文本比使用图像-文本对效果更好</p></li>
<li><p>使用多模态的有监督微调可以改善特定多模态任务中的性能，但是文本-文本单模态任务中性能下降</p></li>
</ol>


<h1>4. 后续方向</h1>

<p>作者提供了后续多模态大模型发展的一些方向：</p>

<ol>
<li><p>更加强大的模型结构，可以引入网页、热力图或图表之类的更多模态来增加模型对更多模态的处理能力，也可以使用更高质量的多模态数据来改善模型性能，或者得到更强大的各个模态的生成模型；</p></li>
<li><p>更多有挑战的Benchmark，例如像MathVista一样衡量视觉任务中的推理能力，或者像MMMU这样的通用智能多学科专家系统；</p></li>
<li><p>轻量化部署，目前已经有一些工作在做，例如Mobile VLM、TinyGPT-V或Mobile-Agent，提高推理效率；</p></li>
<li><p>嵌入式智能：使得MM-LLMs可以应对真实世界并与之交互，例如PaLM-E或者EmbodiedGPT；</p></li>
<li><p>持续学习：根据当前使用环境进行持续参数更新；</p></li>
<li><p>缓解幻觉现象：改善GPT一本正经的胡说八道的问题；</p></li>
</ol>


<h1>附录：论文的思维导图（大图，请在新标签页打开查阅）</h1>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024062706.png"></center>


<p>有关本专栏的更多内容，请参考<a href="https://zhuanlan.zhihu.com/p/690590730">大语言模型文献调研专栏</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[为什么大型语言模型都在使用SwiGLU作为激活函数]]></title>
    <link href="https://ibillxia.github.io/blog/2024/06/21/swiglu-activation-in-llm/"/>
    <updated>2024-06-21T11:46:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2024/06/21/swiglu-activation-in-llm</id>
    <content type="html"><![CDATA[<p>原文链接：<a href="https://zhuanlan.zhihu.com/p/691227850">https://zhuanlan.zhihu.com/p/691227850</a></p>

<p>如果你一直在关注大型语言模型的架构，你可能会在最新的模型和研究论文中看到“SwiGLU”这个词。SwiGLU可以说是在大语言模型中最常用到的激活函数，我们本篇文章就来对他进行详细的介绍。SwiGLU其实是2020年谷歌提出的激活函数，它结合了SWISH和GLU两者的特点。</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024062101.jpg"></center>


<p>我们一个一个来介绍：</p>

<!--more-->


<h1>Swish</h1>

<p>Swish是一个非线性激活函数，定义如下:</p>

<pre><code>Swish(x)= x*sigmoid(ßx)
</code></pre>

<p>函数的曲线：</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024062102.jpg"></center>


<p>其中，ß 为可学习参数。Swish可以比ReLU激活函数更好，因为它在0附近提供了更平滑的转换，这可以带来更好的优化。</p>

<h1>Gated Linear Unit</h1>

<p>GLU（Gated Linear Unit）定义为两个线性变换的分量积，其中一个线性变换由sigmoid激活。</p>

<pre><code>GLU(x)=sigmoid(W1x+b)⊗(Vx+c)
</code></pre>



<center><img src="https://ibillxia.github.io/images/2024/IMAG2024062103.jpg"></center>


<p>GLU可以有效地捕获序列中的远程依赖关系，同时避免与lstm和gru等其他门控机制相关的一些梯度消失问题。</p>

<h1>SwiGLU</h1>

<p>上面我们已经说到SwiGLU是两者的结合。它是一个GLU，但不是将sigmoid作为激活函数，而是使用ß=1的swish，因此我们最终得到以下公式:</p>

<pre><code>SwiGLU(x)=Swish(W1x+b)⊗(Vx+c)
</code></pre>

<p>我们用SwiGLU函数构造一个前馈网络</p>

<pre><code>FFNSwiGLU(x)=(Swish1(xW)⊗xV)W2
</code></pre>

<h1>Pytorch的简单实现</h1>

<p>如果上面的数学原理看着比较麻烦枯燥难懂，我们下面直接使用代码解释。</p>

<pre><code>class SwiGLU(nn.Module): 

    def __init__(self, w1, w2, w3) -&gt; None: 
        super().__init__() 
        self.w1 = w1 
        self.w2 = w2 
        self.w3 = w3 

    def forward(self, x): 
        x1 = F.linear(x, self.w1.weight) 
        x2 = F.linear(x, self.w2.weight) 
        hidden = F.silu(x1) * x2 
        return F.linear(hidden, self.w3.weight)
</code></pre>

<p>我们代码使用的F.silu函数与ß=1时的swish相同的，所以就直接拿来使用了。</p>

<p>代码可以看到，我们的激活函数中也有3个权重是可以训练的，这就是来自于GLU公式里的参数。</p>

<h1>SwiGLU的效果对比</h1>

<p>SwiGLU与其他GLU变体进行比较，我们可以看到SwiGLU在两种预训练期间都表现得更好。</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024062104.jpg"></center>


<p>下游任务</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024062105.jpg"></center>


<p>效果表现得最好，所以现在的llm，如LLAMA, OLMO和PALM都在其实现中采用SwiGLU。但是为什么SwiGLU比其他的好呢？</p>

<p>论文中只给了测试结果而且并没有说明原因，而是说：</p>

<blockquote><p>We offer no explanation as to why these architectures seem to work; we  attribute their success, as all else, to divine benevolence.</p></blockquote>

<p>作者说炼丹成功了。</p>

<p>但是现在已经是2024年了我们可以强行的解释一波：</p>

<p>1、Swish对于负值的响应相对较小克服了 ReLU 某些神经元上输出始终为零的缺点</p>

<p>2、GLU 的门控特性，这意味着它可以根据输入的情况决定哪些信息应该通过、哪些信息应该被过滤。这种机制可以使网络更有效地学习到有用的表示，有助于提高模型的泛化能力。在大语言模型中，这对于处理长序列、长距离依赖的文本特别有用。</p>

<p>3、SwiGLU 中的参数 W1,W2,W3,b1,b2,b3W1,W2,W3,b1,b2,b3 可以通过训练学习，使得模型可以根据不同任务和数据集动态调整这些参数，增强了模型的灵活性和适应性。</p>

<p>4、计算效率相比某些较复杂的激活函数（如 GELU）更高，同时仍能保持较好的性能。这对于大规模语言模型的训练和推理是很重要的考量因素。</p>

<p>选择 SwiGLU  作为大语言模型的激活函数，主要是因为它综合了非线性能力、门控特性、梯度稳定性和可学习参数等方面的优势。在处理语言模型中复杂的语义关系、长依赖问题、以及保持训练稳定性和计算效率方面，SwiGLU 表现出色，因此被广泛采用。</p>

<p>论文地址: <a href="https://arxiv.org/abs/2002.05202">https://arxiv.org/abs/2002.05202</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[工业界推荐系统技术要点总结]]></title>
    <link href="https://ibillxia.github.io/blog/2024/05/26/recommendation-system-industry-summary/"/>
    <updated>2024-05-26T11:25:00+08:00</updated>
    <id>https://ibillxia.github.io/blog/2024/05/26/recommendation-system-industry-summary</id>
    <content type="html"><![CDATA[<p>原文链接：<a href="https://zhuanlan.zhihu.com/p/689894486">https://zhuanlan.zhihu.com/p/689894486</a></p>

<p>工业界的推荐系统技术要点有很多，想从事推荐系统相关方向的同学都建议刷一刷。</p>

<h1>多目标排序模型</h1>

<p>回顾一下推荐系统的链路</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052601.jpg"></center>




<!--more-->


<p>常见的交互指标</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052602.jpg"></center>


<p>排序模型做的事情</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052603.jpg"></center>


<p>多目标模型就是要预测多个目标</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052604.jpg"></center>


<p>预测概率和实际是否交互求交叉熵损失</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052605.jpg"></center>


<p>训练时通常会遇到类别不平衡问题，可以考虑做采样</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052606.jpg"></center>


<p>采样可能导致预估点击率偏高</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052607.jpg"></center>


<p>可以通过校准公式进行校准</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052608.jpg"></center>


<p>Multi-gate Mixture-of-Experts</p>

<p>几个专家就是放几个神经网络</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052609.jpg"></center>


<p>进一步考虑对多个神经网络的输出进行加权</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052610.jpg"></center>




<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052611.jpg"></center>


<p>可能会出现极化的现象</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052612.jpg"></center>


<p>可以通过dropout的方式来解决极化</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052613.jpg"></center>


<p>多目标有多个预估分数就可以有不同融合方式</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052614.jpg"></center>




<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052615.jpg"></center>




<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052616.jpg"></center>




<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052617.jpg"></center>


<p>视频播放建模</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052618.jpg"></center>


<p>播放时长建模</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052619.jpg"></center>




<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052620.jpg"></center>


<p>视频完播用回归或分类都可以</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052621.jpg"></center>




<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052622.jpg"></center>


<p>完播率通常和视频时长有关，不能直接把预估的完播率⽤到融分公式</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052623.jpg"></center>


<p>通常做个调整再用到融分公式</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052624.jpg"></center>


<h1>排序模型的特征</h1>

<p>用户画像</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052625.jpg"></center>


<p>物品画像</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052626.jpg"></center>


<p>用户统计特征</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052627.jpg"></center>


<p>笔记统计特征</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052628.jpg"></center>


<p>场景特征</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052629.jpg"></center>


<p>特征处理</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052630.jpg"></center>


<p>特征覆盖率的概念</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052631.jpg"></center>


<p>数据服务分三步</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052632.jpg"></center>




<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052633.jpg"></center>




<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052634.jpg"></center>


<h1>粗排</h1>

<p>精排模型的线上推理代价大</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052635.jpg"></center>


<p>双塔模型牺牲准确性换计算量</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052636.jpg"></center>


<p>粗排的三塔模型</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052637.jpg"></center>




<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052638.jpg"></center>


<p>三塔模型的推理</p>

<center><img src="https://ibillxia.github.io/images/2024/IMAG2024052639.jpg"></center>

]]></content>
  </entry>
  
</feed>
